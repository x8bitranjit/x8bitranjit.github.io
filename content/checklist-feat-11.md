# 11. Approval & Workflow — Checklist

Feature-area security **test cases** for “11. Approval & Workflow” — functionality-driven, impact-first with severity + CVSS.

*24 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| WF-001 | Self-approval / maker=checker separation-of-duties bypass | Business Logic (WSTG-BUSL-07) | Approval action where the requester can also be the approver | 1. Create a request (maker) as user A<br>2. Attempt to approve the same request as A (or as an account you control on both roles)<br>3. Confirm the workflow lets the same identity make + approve<br>4. Complete the privileged action with no independent review | Maker and checker must be distinct identities; server enforces segregation of duties | <code>POST /requests/123/approve  (Authorization: same user who created 123)</code> | Separation-of-duties bypass -&gt; self-approve privileged/financial actions | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-863; CWE-841; [IDOR kit](#/checklist/idor); PortSwigger Business logic; OWASP WSTG-BUSL-07 |
| WF-002 | Approve another user's request (BOLA on approval object) | Broken Access Control | Approval endpoint — request/approval object identifier | 1. As low-priv A, enumerate approval/request IDs<br>2. Approve/reject a request that belongs to another tenant/user or a higher approval tier<br>3. Two-account proof: A actions B's pending item<br>4. Confirm the state changed | Approval endpoints authorize the object AND the approver's role/tier per request | <code>POST /approvals/{other_users_request_id}/approve  (A token)</code> | BOLA on approvals -&gt; approve/deny others' requests -&gt; unauthorized state change | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP WSTG-ATHZ-04 |
| WF-003 | Skip approval step / forced workflow-state transition | Business Logic (WSTG-BUSL-06) | Workflow status field / state-transition endpoint | 1. Map the workflow states (draft-&gt;pending-&gt;approved-&gt;executed)<br>2. Try to jump straight to 'approved'/'executed' via a status param or a later-step endpoint<br>3. Force-browse the execute endpoint while status is still pending<br>4. Confirm the guarded step ran without approval | State transitions validated server-side; each step re-checks the prior state + authorization | <code>PATCH /requests/123 {"status":"approved"}  &#124;  POST /requests/123/execute (while pending)</code> | Workflow-state bypass -&gt; execute privileged action with no approval | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-840; [IDOR kit](#/checklist/idor); PortSwigger Business logic; OWASP WSTG-BUSL-06 |
| WF-004 | Approval threshold bypass via transaction splitting | Business Logic (WSTG-BUSL-01) | Amount/quantity field vs approval-required threshold | 1. Find the value that triggers required approval (e.g. &gt; $10,000)<br>2. Split the action into multiple sub-threshold requests that each auto-approve<br>3. Or set an amount just under the limit then top-up post-approval<br>4. Confirm the aggregate exceeds the control with no approval | Thresholds evaluated on aggregate/velocity, not per-request; post-approval edits re-trigger review | <code>3 x POST /transfer {"amount":9999}  (each below the $10k approval gate)</code> | Threshold split -&gt; move value above the approval limit unreviewed -&gt; financial | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-840; PortSwigger Business logic; OWASP WSTG-BUSL-01 |
| WF-005 | TOCTOU: execute action before/while approval is pending (race) | Race Condition | Gap between approval decision and action execution | 1. Submit a request needing approval<br>2. Fire the execute call concurrently with (or just before) the approval commit<br>3. Use parallel/single-packet requests to win the race<br>4. Confirm the action ran without a committed approval | Execution is gated on a committed approval inside one atomic transaction | <code>parallel: POST /requests/123/execute  x N  during the approval window</code> | Approval TOCTOU -&gt; execute unapproved action -&gt; control bypass / financial | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder, Burp | CWE-367; CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle 'Smashing the State Machine' |
| WF-006 | Approve-then-modify (edit request after approval) | Business Logic (WSTG-BUSL-07) | Request object mutability after the approval decision | 1. Submit a benign request and get it approved<br>2. Modify the sensitive fields (amount/recipient/scope) after approval, before execution<br>3. Confirm the executed action uses the modified values, not the approved ones<br>4. Prove the approver reviewed different content | Approved payload is frozen/signed; any edit invalidates the approval | <code>PATCH /requests/123 {"recipient":"attacker"}  (status already 'approved')</code> | Approve-then-modify TOCTOU -&gt; execute attacker-chosen action under a stale approval | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-367; CWE-841; [Race Condition kit](#/checklist/racecondition); PortSwigger Business logic |
| WF-007 | CSRF / replay on the approve action | CSRF (WSTG-SESS-05) | Approve/reject endpoint under cookie auth | 1. Capture an approve request; check for anti-CSRF token / custom header<br>2. Build a cross-site auto-submit (or replay a captured approval)<br>3. Fire at a logged-in approver<br>4. Confirm the approval executed cross-site / on replay | State-changing approvals require anti-CSRF token + are single-use (nonce) | <code>&lt;form action=/approvals/123/approve method=POST&gt;...&lt;/form&gt; auto-submit at an approver</code> | Approval CSRF/replay -&gt; forge or repeat approvals in the approver's session | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF; OWASP WSTG-SESS-05 |
| WF-008 | Second-order BOLA: re-authorization dropped at later workflow steps | Broken Access Control | Multi-step workflow where step 1 authorizes but later steps don't re-check | 1. Start a workflow that authorizes the object at step 1<br>2. Confirm steps 2/3 (approve/execute) don't re-validate object ownership/tier<br>3. Swap in another tenant's object ID at a later step (A token)<br>4. Confirm cross-object action | Every workflow step independently re-authorizes the object and the actor | <code>POST /wf/step1 {obj:A} -&gt; POST /wf/step3/execute {obj:B_id}  (A token)</code> | Second-order BOLA in workflow -&gt; cross-user/tenant privileged action | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-639; [IDOR kit](#/checklist/idor); REST_API TESTING_GUIDE (multi-step BOLA) |
| WF-009 | Fail-open approval (approval-service error defaults to approved) | Business Logic (WSTG-BUSL-06) | Approval decision under an error/timeout of the approval service (A10:2025 / CWE-636) | 1. Force the approval/policy service to error or time out (malformed input, dependency down)<br>2. Observe whether the workflow defaults to APPROVED (fail-open) or blocks (fail-closed)<br>3. Confirm the guarded action proceeds on error<br>4. Report the fail-open decision | Approval fails CLOSED on any error; no auto-approve on exception/timeout | <code>make the policy call throw -&gt; request auto-approves</code> | Fail-open approval -&gt; unreviewed privileged action on service error (A10:2025) | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-636; [IDOR kit](#/checklist/idor); OWASP Top 10:2025 A10; PortSwigger Business logic |
| WF-010 | Multi-level approval collapse (one approval satisfies all tiers) | Broken Access Control | Multi-tier approval chain (L1/L2/L3) | 1. Map the required approval tiers<br>2. Provide one approval and check whether it auto-satisfies higher tiers<br>3. Or reuse the same approver identity across tiers meant to be distinct<br>4. Confirm the action executes with fewer distinct approvals than required | Each tier requires a distinct, authorized approver; server counts distinct identities | <code>L1 approve as A -&gt; action executes though L2/L3 never approved</code> | Approval-chain collapse -&gt; privileged action with insufficient authorization | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-863; [IDOR kit](#/checklist/idor); PortSwigger Business logic; OWASP WSTG-BUSL-07 |
| WF-011 | Delegation / proxy-approval privilege abuse | Broken Access Control | Approval-delegation feature (delegate approve rights) | 1. Delegate approval authority to an account you control (or to yourself)<br>2. Check whether delegation is validated against role/scope and time-boxed<br>3. Use the delegated right to approve out-of-scope requests<br>4. Confirm privilege gained via delegation | Delegation is authorized, scoped, time-boxed, and audited; no self-delegation of higher rights | <code>POST /approvals/delegate {"to":"attacker","scope":"&#42;"}</code> | Delegation abuse -&gt; acquire approval authority beyond role | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-863; [IDOR kit](#/checklist/idor); OWASP WSTG-ATHZ-02 |
| WF-012 | Audit-trail / notification gap on approval actions | Logging (WSTG-CONF-09) | Audit log &amp; notifications for approve/reject/execute events | 1. Perform approve/reject/execute actions<br>2. Check whether each is logged with actor, object, before/after, timestamp<br>3. Check whether the request owner/approvers are notified<br>4. Confirm gaps that let approval abuse go undetected | All approval events are tamper-evident logged + stakeholders notified | <code>approve then verify no audit entry / no notification to the maker</code> | Missing approval audit/alerts -&gt; approval abuse is undetectable (A09:2025) | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Burp, log review | CWE-778; OWASP Top 10:2025 A09 (Logging &amp; Alerting Failures); OWASP WSTG-CONF-09 |
| WF-013 | Stored XSS via approval comment / rejection-reason field | Cross-Site Scripting (WSTG-INPV-02) | Approval comment/reason field rendered in the approver/requester dashboard | 1. Submit a request with an XSS payload in a free-text field (title/reason/comment)<br>2. Confirm it renders unescaped in the approver's queue or the audit view<br>3. Fire in the approver's authenticated context<br>4. Chain to approve-as-approver / token theft | All workflow free-text is context-encoded on output | <code>reason=&lt;img src=x onerror=fetch('/approvals/123/approve',{method:'POST'})&gt;</code> | Stored XSS in approval UI -&gt; auto-approve or token theft in approver session | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; OWASP WSTG-INPV-02 |
| WF-014 | IDOR on approval history / audit-trail read | Information Disclosure (WSTG-ATHZ-04) | Approval history / audit-log endpoint | 1. Access your approval history endpoint; note the ID scheme<br>2. Enumerate/swap IDs to read other users'/tenants' approval records<br>3. Confirm exposure of amounts, approvers, business data<br>4. Quantify scope | Approval/audit reads authorized per record + tenant | <code>GET /approvals/history?id={other}   or   GET /audit/approval/{id}</code> | IDOR on approval audit -&gt; cross-user/tenant disclosure of sensitive workflow data | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Burp Suite | CWE-639; [IDOR kit](#/checklist/idor); OWASP WSTG-ATHZ-04 |
| WF-015 | Bulk / mass-approve abuse | Business Logic (WSTG-BUSL-06) | Bulk-approve endpoint / select-all action | 1. Find a bulk-approve action<br>2. Submit IDs beyond your scope (other tenants/tiers) in the batch<br>3. Check whether per-item authorization is applied in bulk<br>4. Confirm mass unauthorized approval | Bulk operations authorize every item individually server-side | <code>POST /approvals/bulk {"ids":[all_pending_including_others],"action":"approve"}</code> | Bulk-approve authz gap -&gt; mass unauthorized state change | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-639; CWE-840; [IDOR kit](#/checklist/idor); OWASP WSTG-BUSL-06 |
| WF-016 | Approval webhook / callback forgery (spoof 'approved' event) | Business Logic (WSTG-BUSL-07) | External approval callback / webhook that flips state | 1. Find a webhook/callback that marks a request approved (e-sign, external approver, IdP)<br>2. Check signature/HMAC verification and source validation<br>3. Forge/replay an 'approved' event<br>4. Confirm the request flips to approved | Callbacks are signed (HMAC), source-verified, single-use, and idempotent | <code>POST /webhooks/approval {"request":123,"status":"approved"}  (no/invalid signature)</code> | Forged approval callback -&gt; approve without a real approver | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-345; CWE-347; [SSRF kit](#/checklist/ssrf); OWASP WSTG-BUSL-07 |
| WF-017 | Auto-approve on SLA / timeout abuse | Business Logic (WSTG-BUSL-06) | Escalation/timeout policy that auto-approves un-actioned requests | 1. Determine whether un-actioned requests auto-approve after a timeout/SLA<br>2. Submit a request and simply wait / suppress approver notification<br>3. Confirm it auto-approves without human review<br>4. Combine with notification suppression to hide it | Timeouts escalate to a human or fail-closed; never silent auto-approve of privileged actions | <code>submit request -&gt; suppress notification -&gt; wait for SLA auto-approve</code> | SLA auto-approve -&gt; privileged action granted with zero review | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-636; CWE-840; [IDOR kit](#/checklist/idor); OWASP Top 10:2025 A10; WSTG-BUSL-06 |
| WF-018 | Predictable / reusable email approval link | Broken Authentication (WSTG-ATHN-01) | Email 'Approve' magic-link token | 1. Capture an email approve/reject link<br>2. Check token entropy, single-use, expiry, and account binding<br>3. Predict/replay the token, or use it after logout / from another account<br>4. Confirm approval without the approver | Approval links are high-entropy, single-use, short-lived, and bound to the approver session | <code>GET /approve?token=&lt;predictable-or-replayed&gt;</code> | Weak approval link -&gt; approve as the approver without authentication | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-640; CWE-330; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-01 |
| WF-019 | Reviewer / approver self-assignment tampering | Broken Access Control (WSTG-ATHZ-02) | Reviewer-assignment field on the request | 1. Check whether the requester can set/influence who approves<br>2. Assign yourself (or a colluding account) as the approver<br>3. Approve your own request via the assigned approver<br>4. Confirm SoD defeated | Approver assignment is policy-driven server-side; requester cannot select their approver | <code>POST /requests {"amount":9000,"approver":"attacker_account"}</code> | Approver self-assignment -&gt; route approval to a controlled account -&gt; SoD bypass | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-863; [IDOR kit](#/checklist/idor); OWASP WSTG-ATHZ-02 |
| WF-020 | 'approval_required' / conditional-gate flag tampering | Business Logic (WSTG-BUSL-07) | Client-supplied flag that decides whether approval is needed | 1. Inspect the create/submit request for a flag like approval_required / needs_review / auto_approve<br>2. Flip it to skip the approval gate<br>3. Confirm the action executes without entering the workflow<br>4. Prove the gate is client-controlled | Whether approval is required is decided server-side by policy, never by a client field | <code>POST /requests {"amount":50000,"approval_required":false}</code> | Conditional-gate tampering -&gt; skip approval entirely | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-602; CWE-840; [IDOR kit](#/checklist/idor); OWASP WSTG-BUSL-07 |
| WF-021 | Change / deployment approval (CAB) bypass | Broken Access Control (WSTG-ATHZ-04) | CI/CD or change-management approval gate | 1. Identify the deploy/change gate requiring approval (CAB/change ticket)<br>2. Trigger the deploy/apply endpoint directly, or mark the change approved via API<br>3. Check whether the pipeline enforces the gate server-side<br>4. Confirm an unapproved change ships | Deploy/apply is gated on a verified approval in the pipeline; no direct-trigger bypass | <code>POST /deploy {"change":123,"env":"prod"}  (bypassing the CAB gate)</code> | Change-approval bypass -&gt; unreviewed production change (supply-chain/integrity, A08) | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite, curl | CWE-840; [Dependency Confusion kit](#/checklist/depconfusion); OWASP Top 10:2025 A08; WSTG-ATHZ-04 |
| WF-022 | Access-request (JML) approval self-grant | Broken Access Control (WSTG-ATHZ-02) | Joiner-Mover-Leaver access-request approval | 1. Request elevated access/role/group for yourself<br>2. Attempt to approve or auto-grant it (self-approve, or exploit a missing approver check)<br>3. Confirm the entitlement is granted<br>4. Use the new access | Entitlement grants require an independent approver; no self-grant path | <code>POST /access-requests {"role":"admin"} -&gt; POST /access-requests/{id}/approve (self)</code> | Access-request self-grant -&gt; privilege escalation to elevated role | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite | CWE-863; [IDOR kit](#/checklist/idor); OWASP WSTG-ATHZ-02 |
| WF-023 | Break-glass / emergency-override abuse | Business Logic (WSTG-BUSL-07) | Emergency-approval / break-glass bypass path | 1. Locate any emergency/break-glass path that skips normal approval<br>2. Check whether invoking it is itself authorized, logged, and time-boxed<br>3. Invoke it to bypass the workflow<br>4. Confirm privileged action with no approval + weak accountability | Break-glass is tightly authorized, alarmed, fully logged, and auto-expiring | <code>POST /requests/123/emergency-approve {"reason":"x"}  as a normal user</code> | Break-glass abuse -&gt; bypass approval controls under an emergency pretext | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-863; CWE-840; [IDOR kit](#/checklist/idor); OWASP WSTG-BUSL-07 |
| WF-024 | Notification suppression / recall to hide approval abuse | Logging &amp; Monitoring (WSTG-CONF-09) | Notification settings / message-recall around approval events | 1. Perform an approval action, then suppress or recall the resulting notifications to stakeholders<br>2. Check whether owners/approvers can be kept unaware<br>3. Confirm the abuse is concealable<br>4. Tie to missing tamper-evident audit | Approval notifications are non-suppressible + audit is tamper-evident | <code>PATCH /notifications {"approval_events":"off"} then approve silently</code> | Notification suppression -&gt; approval abuse goes unnoticed (A09:2025) | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Burp Suite | CWE-778; OWASP Top 10:2025 A09; WSTG-CONF-09 |
