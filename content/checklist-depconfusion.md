# Dependency Confusion — Checklist

Expert per-attack **test-case matrix** for Dependency Confusion — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*11 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| DEPCONF-001 | Harvest internal package names | Recon | manifests (package.json/requirements.txt/composer.json/pom.xml/&#42;.csproj), JS bundles/source maps, committed .npmrc/pip.conf | 1. Collect manifests exposed on the web / public repos / wayback.<br>2. Extract names from JS bundles / source maps (@scope/..., require(...)).<br>3. Find committed registry configs (.npmrc/pip.conf/nuget.config) PROVING private packages exist.<br>4. Classify names: scoped-unreserved / org-specific unscoped / internal - exclude well-known public packages. | A candidate list of internal package names (and proof a private store exists). | <code>grep '@acme/' package.json ; .npmrc: @acme:registry=https://npm.internal.acme.com/</code> | You can't claim a name you've never seen; a committed .npmrc is a jackpot. | Info | 0.0 (N/A) | gau, GitHub dorks, jsluice | CWE-829; OWASP: Software Supply Chain Security |
| DEPCONF-002 | SBOM / component inventory &amp; n-day reachability mapping (A03:2025) | Recon | Client + server component fingerprinting for the software supply chain | 1. Build a component inventory (SBOM): server headers, framework tells, JS lib versions (retire.js), leaked package manifests (.npmrc/package-lock/pip.conf)<br>2. Map each component+version to known CVEs<br>3. Verify in-context reachability (don't just report a version banner)<br>4. Route reachable RCE classes to the right kit (Log4Shell-&gt;JNDI, gadgets-&gt;Deserialization, front-end libs-&gt;XSS/PP) | SBOM maintained; components patched/pinned with integrity/provenance; unused deps removed | <code>retire.js on all JS  &#124;  grep package-lock.json for versions  &#124;  map to CVE -&gt; reachability proof</code> | Outdated/vulnerable component inventory -&gt; ready-made n-day exploit (often RCE) | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | retire.js, Syft/Grype (SBOM), OWASP Dependency-Check, Recon kit | CWE-1104; CWE-1395; OWASP Top 10:2025 A03 (Software Supply Chain Failures) |
| DEPCONF-003 | Claimability check (public-registry 404 = unclaimed) | Claimability | Each candidate name on npm/PyPI/RubyGems/NuGet | 1. Public-registry lookup: 404 = UNCLAIMED = you can claim it (read-only, safe at scale).<br>2. Check npm SCOPE reservation (@acme unreserved -&gt; ALL its scoped names confusable).<br>3. Confirm the target's resolver can reach the public registry (config evidence). | A name returns 404 on the public registry (claimable), ideally an unreserved scope. | <code>curl -o/dev/null -w '%{http_code}' https://registry.npmjs.org/@acme/config -&gt; 404</code> | Most of the bug's value is confirmed here read-only; an unreserved scope multiplies it. | Info | 0.0 (N/A) | poc/claimable_check.py, curl | CWE-829; HackTricks: Dependency Confusion |
| DEPCONF-004 | Resolution — will the public copy win? | Resolution | The target's install tooling / registry policy | 1. Identify why the public copy would win: highest-version-wins / public-primary / pip --extra-index-url (highest across stores) / Artifactory-Nexus virtual-repo merge preferring remote.<br>2. Note lockfile/npm ci/--require-hashes status (pinned deps resist; unpinned/transitive still exposed). | A resolution path is identified where a high public version is preferred. | <code>publish version 99.99.99 -&gt; highest-version-wins picks yours ; pip --extra-index-url</code> | Claimable != exploitable; the tooling must actually choose your copy. | Info | 0.0 (N/A) | config review | CWE-829; Alex Birsan: Dependency Confusion (2021) |
| DEPCONF-005 | Install-hook mechanics, lockfile &amp; transitive-dep exposure | Resolution &amp; Mechanics | Per-ecosystem install hooks; the CI install command (npm install vs npm ci); the FULL (transitive) dependency tree | 1. Know WHERE the RCE fires (the auto-run install hook): npm preinstall/install/postinstall/prepare ; pip setup.py / sdist build ; gem extconf.rb ; composer post-install-cmd ; NuGet MSBuild targets.<br>2. Lockfile nuance: a pinned lockfile WITH integrity hashes + &#96;npm ci&#96; / &#96;pip install --require-hashes&#96; BLOCKS confusion for pinned deps - BUT CI that runs &#96;npm install&#96; (not &#96;npm ci&#96;), or resolves a NEW/unpinned dep, stays exposed.<br>3. TRANSITIVE internal deps count: a public package that depends on an internal name -&gt; confusion fires even when the TOP-LEVEL deps are pinned. Map the FULL dep tree, not just direct deps. | A concrete install-hook code-exec path that survives (or routes around) lockfile pinning via npm install / a new or transitive unpinned dep. | <code>package.json preinstall hook ; CI runs 'npm install' not 'npm ci' ; pinned top-level + unpinned transitive @acme/&#42; dep</code> | Pinpoints the code-exec path and the lockfile/transitive gaps that keep 'pinned' builds exploitable. | Info | 0.0 (N/A) | manifest/lockfile review, npm/pip | CWE-829; CWE-494; Alex Birsan: Dependency Confusion (2021) |
| DEPCONF-006 | Safe proof — benign beacon package + callback | Impact — Proof | An authorized name in YOUR scope only | 1. Generate a BENIGN beacon package (install hook: preinstall/setup.py) that does exactly ONE thing: a DNS/HTTP callback to your OOB with a token + hostname. Nothing else.<br>2. Version pinned HIGH (99.99.99); publish for a name in YOUR scope only.<br>3. Callback received from the TARGET's build/CI/dev egress carrying your token = the proof. UNPUBLISH/yank immediately; correlate the source to the target (CI/corp ASN), not your machine/a scanner. | A callback arrives from the target's build carrying your unique token. | <code>package.json preinstall: node beacon.js -&gt; DNS $token.$COLLAB from target CI</code> | RCE in the target's CI/CD build pipeline - Critical. | Critical | 10.0 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H) | poc/benign_callback_pkg.py, interactsh | CWE-829; CWE-494; Alex Birsan: Dependency Confusion (2021) |
| DEPCONF-007 | Repo-jacking / namespace reuse (freed GitHub user, Go/import URLs) | Supply-Chain Variants | go.mod / import / homepage URLs referencing a DELETED or RENAMED GitHub user-org; abandoned npm/PyPI maintainer accounts; expired domains | 1. Namespaced ecosystems (Go/Cargo) name by full path (github.com/user/pkg) so classic confusion doesn't apply -&gt; attack the FREED name instead.<br>2. Find a dependency importing a GitHub user/org that now 404s (deleted/renamed): go.mod &#96;require github.com/olduser/pkg&#96;, a broken import URL, a dead redirect.<br>3. Re-register the freed username/org, host the module at the exact old path, add a BENIGN beacon -&gt; the target's &#96;go get&#96;/build pulls yours -&gt; supply-chain RCE.<br>4. Also: abandoned npm/PyPI maintainer accounts, expired domains used as module homepages. | A freed username/org is registrable and the target's build would pull the attacker-controlled module. | <code>go.mod: github.com/deleted-user/lib -&gt; register deleted-user -&gt; serve the module (benign beacon)</code> | Supply-chain RCE via repo-jacking (high-value, easily missed on namespaced ecosystems) - Critical. | Critical | 10.0 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H) | manual, GitHub, confused | CWE-829; CWE-494; HackTricks: Dependency Confusion; broken-link hijacking |
| DEPCONF-008 | Typosquatting / combosquatting (human-error supply chain) | Supply-Chain Variants | Public-registry names that are typo / look-alike neighbours of the org's real dependencies | 1. Unlike DC (which tricks the RESOLVER), typosquatting tricks the HUMAN: publish a package named like a common misspelling of a real dep (reqiests/requsts vs requests; crossenv vs cross-env) so a mistyped install pulls yours.<br>2. Combosquatting: separator/hyphen/underscore variants (acme-utils vs acme_utils vs acmeutils).<br>3. Benign proof only; report proactively as a supply-chain hygiene risk (flag the org's deps that have obvious squat neighbours). | A typo / look-alike name is registrable and a mistyped install would pull it (benign proof). | <code>publish 'reqiests' (typo of requests) / 'acme_utils' (combo of acme-utils) with a benign beacon</code> | Human-typo supply-chain RCE (lower likelihood than resolver confusion) - Medium hygiene risk. | Medium | 6.8 (CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:U/C:H/I:H/A:N) | manual, registry search | CWE-829; HackTricks: Dependency Confusion; typosquatting research |
| DEPCONF-009 | Impact — RCE in CI/CD (described, not exfiltrated) | Impact | A confirmed build-time callback | 1. Establish the callback = RCE in CI/CD (or a dev machine).<br>2. DESCRIBE (do not exfiltrate) the reachable secrets (cloud/signing/CI keys, source) and downstream propagation to customers.<br>3. Never actually take the secrets. | The callback is shown to run inside the build pipeline holding keys/source. | <code>callback from CI runner -&gt; describe reachable AWS/signing keys (not taken)</code> | Critical CI/CD RCE with downstream supply-chain reach - Critical. | Critical | 10.0 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H) | manual | CWE-829; Alex Birsan: Dependency Confusion (2021) |
| DEPCONF-010 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: an internal name REFERENCED but already public / unclaimable; you PUBLISHED but got NO callback (no proof of a pull); a callback from YOUR machine / a scanner (not the target's build egress); everything lockfile-pinned with hashes + npm ci (no unpinned/transitive path); a full-path Go/Cargo namespaced module (that's repo-jacking - file as such).<br>2. REQUIRE: claimable (public 404) + a token-correlated callback from the target's build. | Only claimable + target-build-callback candidates survive. | <code>name already public = FP ; no callback = lead not finding ; own-machine callback = FP</code> | Protects credibility; a lead (could claim) is not the finding (their build ran it). | Info | 0.0 (N/A) | manual | CWE-829; Alex Birsan: Dependency Confusion (2021) |
| DEPCONF-011 | Client-facing impact &amp; responsible-disclosure package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with impact: RCE in the build pipeline (cloud/signing keys + customer shipment).<br>2. Provide the claimability proof (public 404), the token-correlated callback from the target's build, and the publish-&gt;unpublish window.<br>3. Set CVSS (9-10 CI RCE) + CWE-829 (+427/494). Remediation: reserve the name/scope on the public registry, pin + hash all deps (npm ci/--require-hashes), scope-lock the private registry, verify source of every package.<br>4. Benign beacon only, UNPUBLISHED, reported fast; de-dupe to one resolution root cause (e.g. unreserved scope), list the confusable names. | A reproducible, correctly-rated, responsibly-disclosed PoC with clear remediation. | <code>PoC: public 404 + target-build callback (token) + unpublish window + CVSS 9-10 + CWE-829 + reserve-the-name.</code> | Converts the callback into a defensible Critical CI/CD-RCE report. | Info | 0.0 (N/A) | CVSS calculator, DEPENDENCY_CONFUSION_REPORT_TEMPLATE.md | CWE-829; CWE-494; FIRST CVSS v3.1; OWASP: Software Supply Chain Security  &#124;  TOP REFERENCES: Alex Birsan 'Dependency Confusion' (2021); Assetnote supply-chain research; PortSwigger; OWASP supply-chain |
