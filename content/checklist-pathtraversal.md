# Path / Directory Traversal — Checklist

Expert per-attack **test-case matrix** for Path / Directory Traversal — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*13 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| PT-001 | Find READ/serve, static, archive &amp; write sinks | Recon | download/export/attachment/view/avatar/PDF/backup; /static /assets; ZIP-import/restore; upload dest/save/log path | 1. Find READ/serve sinks (download/export/attachment/view/preview/avatar/PDF/backup).<br>2. Static routing (/static,/assets,/files,/media) for server-normalization; archive-extract sinks (ZIP import/restore/theme install) = Zip-Slip (top priority); upload filename/dest and save/export/log-path sinks.<br>3. Discover hidden file/path/name/dest/save params (Arjun); note absolute-path leaks in errors. | A list of read/serve/static/archive/write path sinks and any leaked base dir. | <code>?file= /download /static/&lt;x&gt; ZIP import ; upload filename ; error leaks base dir + OS</code> | Archive-extract (Zip-Slip) and static-normalization sinks are the highest-value. | Info | 0.0 (N/A) | Burp, Arjun, Param Miner | CWE-22; OWASP Testing Guide: Path Traversal (WSTG-ATHZ-01) |
| PT-002 | Baseline &amp; classify the sink (send ../ raw) | Baseline | Each path sink | 1. Confirm the value IS the path (same-dir control vs traversal changes the response).<br>2. Send ../ RAW (curl --path-as-is / Burp), not collapsed client-side.<br>3. Classify: READ/serve / WRITE / INCLUDE-EXECUTE (-&gt; LFI kit) / absolute-path accepted (skip depth). | The sink is classified and ../ confirmed to be sent raw. | <code>curl --path-as-is $URL/download?file=../../../../etc/passwd ; classify READ vs WRITE</code> | An include/execute sink is an LFI/RFI report, not this - classify first. | Info | 0.0 (N/A) | curl --path-as-is, Burp | CWE-22; PortSwigger Web Security Academy: File path traversal |
| PT-003 | Read traversal — escape the base dir | Read Traversal | READ/serve sinks | 1. Varying-depth ../, over-traverse, absolute path (/etc/passwd - CWE-36), ....// (strip-reform), Windows ..\.<br>2. file:///etc/passwd.<br>3. Confirm out-of-dir contents returned. | A file outside the base dir is served. | <code>../../../../etc/passwd ; /etc/passwd ; ....//....//etc/passwd ; ..\..\windows\win.ini</code> | Out-of-dir read (Medium until escalated to secrets/cross-user). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | curl --path-as-is | CWE-22; CWE-36; PortSwigger Web Security Academy: File path traversal |
| PT-004 | Encoding &amp; filter bypass | Read Traversal | Sinks filtering ../ | 1. ..%2f (url), %252e%252e%252f (double - beats decode-once/WAF), ..%c0%af (overlong UTF-8), unicode fullwidth %uff0e, ..%00 (legacy null).<br>2. ..;/ (Java/Tomcat segment), ....// (strip-and-reform).<br>3. Find the one that lands. | Traversal succeeds despite ../ filtering. | <code>..%252f..%252f..%252fetc%252fpasswd ; %uff0e%uff0e%u2215etc%u2215passwd</code> | Bypasses naive traversal filters. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Intruder | CWE-22; HackTricks: Path Traversal |
| PT-005 | Prefix / suffix / allowlist bypass | Read Traversal | Sinks with base-dir prefix / forced suffix / allowed name | 1. Forced base dir prepended -&gt; just climb out.<br>2. Forced suffix appended -&gt; ../../etc/passwd%00.png (legacy) / target a file already ending in the suffix (../../../var/log/app.log if .log).<br>3. Allowlist 'must contain allowed name' -&gt; allowed.txt/../../../../etc/passwd. | The prefix/suffix/allowlist is satisfied and traversal still escapes. | <code>/base/../../../../etc/passwd ; allowed.txt/../../../etc/passwd ; passwd%00.png</code> | Bypasses prefix/suffix/allowlist controls. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Repeater | CWE-22; CWE-23; HackTricks: Path Traversal |
| PT-006 | Server / framework normalization | Server Normalization | /static, /assets, proxies (nginx/Tomcat/IIS) | 1. nginx alias off-by-slash: /static../../etc/passwd (location missing trailing slash).<br>2. Tomcat/Java semicolon: /app/..;/..;/WEB-INF/web.xml.<br>3. Encoded slash not decoded by proxy but by app: /api/%2e%2e%2f%2e%2e%2fWEB-INF/web.xml. Also /static../.git/config, /static../.env. | A server/proxy normalization flaw escapes the served dir. | <code>/static../../../etc/passwd (nginx alias) ; /..;/..;/WEB-INF/web.xml (Tomcat)</code> | Source/config disclosure via infra normalization - High. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp | CWE-22; HackTricks: Path Traversal |
| PT-007 | Language foot-gun — absolute path | Read Traversal | Python os.path.join / .NET Path.Combine / Java sinks | 1. A plain ABSOLUTE path often wins: os.path.join(base, '/etc/passwd') discards base in Python; Path.Combine likewise in .NET.<br>2. No ../ needed.<br>3. Test /etc/passwd and C:\Windows\win.ini directly. | An absolute path bypasses the base-dir join entirely. | <code>file=/etc/passwd (Python os.path.join discards base) ; file=C:\Windows\win.ini (.NET)</code> | Trivial traversal via language path-join semantics - Medium/High. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | curl --path-as-is | CWE-22; CWE-36; HackTricks: Path Traversal |
| PT-008 | READ -&gt; secrets / source disclosure | Impact — Read | Confirmed out-of-dir read | 1. Climb from /etc/passwd to value: .env, config, keys, cloud-creds, source, /proc/self/environ, k8s token.<br>2. Windows: web.config, appsettings.json, .aspx source via ::$DATA.<br>3. Pivot the creds (SSRF/JWT); redact. | Secrets/source/cloud-creds are read from outside the base dir. | <code>.env ; ~/.aws/credentials ; /static../.git/config ; web.config</code> | Secret/source disclosure -&gt; further compromise. High/Critical. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:L) | curl | CWE-22; CWE-73; HackTricks: Path Traversal |
| PT-009 | READ -&gt; other users' / cross-tenant files | Impact — Read | Per-user/tenant file-serving sinks | 1. Traverse/swap to another (your OWN second) account's files / session-token files.<br>2. Confirm PII / session-token read -&gt; ATO.<br>3. Own second account as victim. | Another user's/tenant's private file is read. | <code>download?file=../&lt;victim_id&gt;/statement.pdf ; ../sessions/sess_&lt;victim&gt;</code> | Cross-user PII / session-token read -&gt; ATO. High/Critical. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | CWE-22; CWE-639; HackTricks: Path Traversal |
| PT-010 | WRITE -&gt; Zip-Slip archive extraction | Impact — Write | Archive-extract sinks (ZIP/TAR import/restore/theme install) | 1. Craft an entry path that traverses out: ../../../../var/www/html/poc.php.<br>2. Extraction writes the benign marker OUTSIDE the extraction dir (show the path).<br>3. Webshell-in-webroot escalation described (FileUpload kit for executability). | Extraction writes a file outside the intended directory via a traversal entry. | <code>zip entry: ../../../../var/www/html/poc.php (benign marker) -&gt; lands in webroot</code> | Out-of-dir write -&gt; webshell/RCE. Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/make_zipslip.py | CWE-22; CWE-434; HackTricks: Path Traversal |
| PT-011 | WRITE -&gt; upload-path / save / export | Impact — Write | upload filename/dest, save/export/log path sinks | 1. ../ in the filename/dest writes to webroot / overwrites a served file.<br>2. save/export overwrite (benign proof) of ~/.ssh/authorized_keys / cron / config location -&gt; RCE/persistence described.<br>3. Benign markers to safe paths only; never overwrite real files. | A write escapes the intended dir to a webroot/system path. | <code>filename=../../../../var/www/html/x.php ; export path=../../../home/user/.ssh/authorized_keys</code> | Out-of-dir write -&gt; RCE/persistence. Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp, FileUpload kit | CWE-22; CWE-59; HackTricks: Path Traversal |
| PT-012 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: ../ changed the response but stayed INSIDE the base dir (no escape); /etc/passwd read reported as Critical (it's Medium - climb to secrets); ../ collapsed client-side (use --path-as-is); 'Zip-Slip' with no proof a file landed OUTSIDE; a write only inside your own upload dir; reading a file the app is supposed to serve; an include/execute sink (-&gt; LFI/RFI).<br>2. REQUIRE: out-of-dir escape + impact. | Only genuine out-of-dir escape with impact survives. | <code>in-base-dir change = not traversal ; passwd-only = Medium ; client-collapsed ../ = invalid</code> | Protects credibility; traversal is dense with in-base-dir / passwd-only false positives. | Info | 0.0 (N/A) | manual | CWE-22; PortSwigger Web Security Academy: File path traversal |
| PT-013 | Client-facing impact &amp; SAFE PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with impact (out-of-dir write-&gt;RCE &gt; secret/cross-user read).<br>2. Provide the raw-../ request, the out-of-dir file contents (redacted) or the written marker's path, and the sink type.<br>3. Set CVSS 3.1 + CWE-22 (+36/23/434/59/73 by variant). Remediation: canonicalize and confine to a base dir (realpath prefix check), map user input to fixed identifiers, validate archive entry paths, never build paths by concatenation.<br>4. Read only enough (redact), write only benign markers to safe paths, no real file overwritten; de-dupe (read vs write separate; include/execute -&gt; LFI). | A reproducible, correctly-rated PoC with clear remediation. | <code>PoC: raw-../ request + out-of-dir contents/marker path + CVSS + CWE-22 + remediation.</code> | Converts the escape+impact into a defensible High/Critical report. | Info | 0.0 (N/A) | CVSS calculator, PATH_TRAVERSAL_REPORT_TEMPLATE.md | CWE-22; CWE-36; FIRST CVSS v3.1; OWASP Testing Guide: Path Traversal (WSTG-ATHZ-01)  &#124;  TOP REFERENCES: Orange Tsai reverse-proxy/path-confusion research (BlackHat); PortSwigger Academy; PayloadsAllTheThings; HackTricks; Snyk Zip Slip |
