# Recon — Checklist

Expert per-attack **test-case matrix** for Recon — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*16 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| RECON-001 | Scope &amp; select the target | Scope &amp; Select | The program policy | 1. Read policy; write in_scope.txt / out_scope.txt.<br>2. Confirm wildcard? acquisitions? ASN? mobile/API in scope? Note banned issue types + rate/automation rules + safe-harbor.<br>3. Pick a worthwhile target (broad scope + real backend + auth/APIs) - a bug on an out-of-scope host pays $0. | A clear in/out-of-scope list and a target worth the time. | <code>in_scope: &#42;.$D, acquisitions ; out_scope: blog.$D ; automation allowed: Y</code> | A brilliant bug out of scope pays nothing and can get you banned - scope first. | Info | 0.0 (N/A) | program policy | CWE-200; OWASP Testing Guide: Information Gathering (WSTG-INFO) |
| RECON-002 | Org expansion (ASN / acquisitions / favicon pivots) | Go Wide — Assets | The organization's wider footprint | 1. Expand the org: ASN, reverse-whois, acquisitions, favicon/analytics-ID pivots -&gt; extra root domains.<br>2. Acquisitions/ASN are the least-hunted surface.<br>3. Feed new roots into subdomain enumeration. | Additional root domains and IP ranges owned by the org. | <code>amass intel -asn &lt;ASN&gt; ; reverse-whois ; favicon hash pivot (Shodan)</code> | Org-expansion surface is where your edge over the crowd comes from (least hunted). | Info | 0.0 (N/A) | amass intel, Shodan, whoisxml | CWE-200; HackTricks: External Recon Methodology |
| RECON-003 | Passive subdomain enumeration | Go Wide — Assets | All roots | 1. Passive subs: subfinder + amass + crt.sh + chaos (+ GitHub subs), API keys configured.<br>2. Grep the passive list for admin/internal/staging/dev/uat/vpn/jenkins/jira/git/api.<br>3. Include HISTORICAL/CT hosts (dead ones are prime takeover candidates). | A broad passive subdomain list with interesting names flagged. | <code>subfinder -d $D -all ; crt.sh %.$D ; grep -E 'admin&#124;dev&#124;staging&#124;api'</code> | Coverage of forgotten hosts is the cure for duplicate reports. | Info | 0.0 (N/A) | subfinder, amass, crt.sh, chaos | CWE-200; HackTricks: External Recon Methodology |
| RECON-004 | Active subs + permutations | Go Wide — Assets | Resolved subdomains | 1. Resolve -&gt; DNS brute -&gt; PERMUTATIONS (the high-yield, under-used pass).<br>2. TLS-SAN harvest, recursive enum, internal hostnames from JS/CT, alt-TLD/region.<br>3. Merge -&gt; subs_all.txt (the complete asset list). | A complete asset list including permutation-discovered hosts. | <code>puredns bruteforce ; gotator/altdns permutations ; merge -&gt; subs_all.txt</code> | Permutations + org-expansion are precisely where the uncontested surface is. | Info | 0.0 (N/A) | puredns, gotator, dnsx | CWE-200; HackTricks: External Recon Methodology |
| RECON-005 | Resolve &amp; probe (sort by interesting) | Resolve &amp; Probe | subs_all.txt | 1. httpx probe all subs (status/title/tech/cname/ip), multi-port.<br>2. Build live.txt; SORT BY INTERESTING (auth/api/admin/dev/dashboards), not alphabetically.<br>3. Flag 401/403 auth walls (bypass candidates) and 5xx/redirects. | A map of live, identified services sorted by interest. | <code>httpx -l subs_all.txt -status-code -title -tech-detect -cname -ip</code> | Your manual time is finite - it belongs on the hosts with a real backend. | Info | 0.0 (N/A) | httpx | CWE-200; PortSwigger / Bug bounty recon methodology (Tomnomnom, Jason Haddix) |
| RECON-006 | Ports + origin-IP hunt | Resolve &amp; Probe | Live hosts; WAF/CDN-fronted hosts | 1. naabu top-1000; probe open ports for HTTP; note data-store/dashboard ports.<br>2. Origin-IP hunt where WAF/CDN present (historical DNS / Shodan cert / favicon) -&gt; bypass the WAF later.<br>3. vhost fuzz on shared IPs for no-DNS sites. | Open ports mapped and origin IPs found behind the WAF. | <code>naabu -top-ports 1000 ; Shodan ssl.cert.subject.cn:$D ; favicon hash</code> | The origin-IP trick lets your payloads bypass the WAF later. | Info | 0.0 (N/A) | naabu, Shodan, ffuf (vhost) | CWE-200; HackTricks: External Recon Methodology |
| RECON-007 | URL history + gf routing | Go Deep | Interesting hosts only | 1. gau/wayback/katana -&gt; urls_all.txt; gf-route to xss/ssrf/redirect/idor/sqli/lfi.<br>2. Extract forgotten sensitive files + ALL historical param names.<br>3. Go deep on the ≈50 hosts that matter, never all 5,000. | A routed URL/param corpus tagged by candidate bug class. | <code>gau $D &#124; gf ssrf ; gf redirect ; extract historical params</code> | Routing 'I found 3,000 endpoints' into '40 IDOR/SSRF candidates' is the point of recon. | Info | 0.0 (N/A) | gau, katana, gf | CWE-200; PortSwigger / Bug bounty recon methodology (Tomnomnom, Jason Haddix) |
| RECON-008 | Content discovery + direct sensitive-file probes | Go Deep | Interesting hosts | 1. ffuf dirs/files (context-matched list); recurse into found dirs.<br>2. Probe direct: /.git /.env /actuator /swagger /graphql /server-status /.DS_Store.<br>3. 403/401 bypass attempts on interesting protected paths. | Hidden dirs/files and exposed sensitive endpoints found. | <code>ffuf -w list -u https://host/FUZZ ; probe /.git /.env /actuator</code> | Exposed /.git or /.env is often a direct Critical - probe every interesting host. | Info | 0.0 (N/A) | ffuf, feroxbuster | CWE-200; HackTricks: External Recon Methodology |
| RECON-009 | Parameter discovery (arjun) | Go Deep | Interesting endpoints | 1. arjun per endpoint; test high-value names (redirect/url/id/debug/admin/file).<br>2. Feed hidden params into the relevant attack kits (SSRF/IDOR/LFI/OpenRedirect).<br>3. Cross-ref historical param names from URL history. | Hidden parameters discovered per endpoint. | <code>arjun -u https://host/api/x ; test ?debug= ?admin= ?url= ?file=</code> | Hidden params are the injection points other hunters never find. | Info | 0.0 (N/A) | arjun, param-miner | CWE-200; HackTricks: External Recon Methodology |
| RECON-010 | JS analysis + source maps | Go Deep | Collected JS bundles | 1. Collect bundles; extract endpoints + secrets (jsluice/trufflehog).<br>2. Probe .js.map; reconstruct source where exposed.<br>3. Hand findings to the JS-Files kit for validation. | Endpoints/secrets/source recovered from JS (routed to the JS-Files kit). | <code>jsluice urls/secrets ; try main.js.map -&gt; reconstruct source</code> | JS bundles are the app's blueprint; source maps rebuild the whole thing. | Info | 0.0 (N/A) | jsluice, trufflehog, LinkFinder | CWE-200; CWE-540; HackTricks: External Recon Methodology |
| RECON-011 | API discovery (swagger / GraphQL introspection) | Go Deep | API hosts | 1. Find swagger/openapi; test OLD API versions; GraphQL introspection (or clairvoyance).<br>2. Enumerate operations, params, and auth requirements.<br>3. Route to the API/GraphQL/IDOR kits. | The API surface (incl. old versions and GraphQL schema) is mapped. | <code>curl /swagger.json ; /v1/ vs /v2/ ; GraphQL introspection query</code> | Old API versions and GraphQL introspection expose un-hunted surface. | Info | 0.0 (N/A) | kiterunner, clairvoyance | CWE-200; HackTricks: External Recon Methodology |
| RECON-012 | Subdomain takeover sweep | High-Value (run early) | The full subdomain list | 1. nuclei/subzy across the full list; check cookie scope -&gt; ATO escalation.<br>2. A takeover can be a Critical in your first hour.<br>3. Hand confirmed danglers to the Subdomain-Takeover kit (claim + trust chain). | Dangling/claimable subdomains identified for the takeover kit. | <code>subzy run --targets subs_all.txt ; nuclei -tags takeover</code> | A fast, clean, low-dupe Critical before you even open the main app. | Info | 0.0 (N/A) | subzy, nuclei, Subdomain-Takeover kit | CWE-200; CWE-350; HackTricks: External Recon Methodology |
| RECON-013 | Secrets hunt (GitHub dorks / trufflehog / verify) | High-Value (run early) | Org GitHub, commit history, .tfstate | 1. GitHub dorks + trufflehog on org &amp; commit HISTORY; hunt .tfstate.<br>2. VERIFY every secret before believing (most matches are dead keys).<br>3. Route live secrets to the JS-Files/cloud kits. | Verified, live secrets from public repos/history. | <code>trufflehog github --org=&lt;org&gt; ; GitHub dork 'org:acme AKIA' ; verify with sts</code> | A verified leaked key is a first-hour Critical; verify before believing. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | trufflehog, gitleaks, GitHub dorks | CWE-200; CWE-798; HackTricks: External Recon Methodology |
| RECON-014 | Cloud buckets + exposed git/env/backups | High-Value (run early) | Cloud storage, /.git, /.env, backup files | 1. cloud_enum/s3scanner; test read (and benign write) on buckets.<br>2. nuclei exposures; git-dumper on any /.git.<br>3. Prove with a benign marker you clean up - never touch real data. | Readable/writable buckets and dumped git/env exposures. | <code>s3scanner ; git-dumper http://host/.git out/ ; nuclei -tags exposure</code> | Open buckets and exposed .git/.env are direct Criticals - benign proof only. | High | 7.5 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) | cloud_enum, s3scanner, git-dumper | CWE-200; HackTricks: External Recon Methodology |
| RECON-015 | CORS / cookie-trust map + mobile/3rd-party | High-Value (run early) | Reflected-origin endpoints; APKs; Jira/Confluence/status pages | 1. Reflected-origin CORS test; note .$D cookie/CORS trust (subdomain trust map).<br>2. Mobile deep-mine: decompile APK/IPA (jadx/apktool) -&gt; endpoints/keys/buckets/Firebase RTDB.<br>3. Internet-wide dorking (Shodan/Censys cert-CN + favicon) -&gt; CDN origin IP / exposed panels; Postman/Pastebin/Docker Hub. | A trust map and mobile/3rd-party surface routed to the relevant kits. | <code>curl -H 'Origin: evil.com' ; jadx APK -&gt; endpoints/keys ; Shodan favicon hash</code> | The subdomain trust map + mobile endpoints feed the CORS/SSRF/JWT kits. | Info | 0.0 (N/A) | jadx, apktool, Shodan, Corsy | CWE-200; CWE-942; HackTricks: External Recon Methodology |
| RECON-016 | Route -&gt; impact (the whole point) + discipline | Route -&gt; Impact | Every recon finding | 1. Convert each host/endpoint into 'test bug class X here because Y', ranked by impact-per-hour.<br>2. Consciously SKIP the time-wasters: info-disclosure noise, dead static hosts, auto-closed issue types.<br>3. Feed the routed surface into the per-attack kits (SSRF/IDOR/XSS/SQLi/...). | A prioritized, routed attack plan mapping surface to bug classes. | <code>these 40 endpoints -&gt; IDOR ; these headers -&gt; Host-header ; skip the marketing site</code> | Recon is worthless until each host becomes a routed, ranked test - this is the payoff. | Info | 0.0 (N/A) | manual, gf | CWE-200; PortSwigger / Bug bounty recon methodology (Tomnomnom, Jason Haddix)  &#124;  TOP REFERENCES: Jason Haddix 'The Bug Hunter's Methodology'; ProjectDiscovery toolchain; Tomnomnom; NahamSec; OWASP Amass |
