# 19. Mobile & Device — Checklist

Feature-area security **test cases** for “19. Mobile & Device” — functionality-driven, impact-first with severity + CVSS.

*30 test cases · spreadsheet grid — scroll sideways for all 12 columns.*

| Operation | Test Case Name | Test Category | Where to Test / Injection Point | Test Steps | Expected Result | Payload Example | Impact | Severity | CVSS | Tools | References |
|---|---|---|---|---|---|---|---|---|---|---|---|
| MOB-001 | Mobile API BOLA/BFLA (proxy the app, swap identifiers) | Broken Access Control | Mobile backend API — object IDs &amp; privileged methods behind the app | 1. Proxy the mobile app (Burp + device CA) to see the raw API<br>2. Swap object IDs / call admin-only methods as a normal user (two-account proof)<br>3. The app UI hiding a function != server enforcing it<br>4. Confirm cross-user data / privileged action | Mobile backend authorizes every object &amp; function server-side (UI is not a control) | <code>GET /api/v1/users/{other_id}  from the intercepted mobile session</code> | Mobile backend BOLA/BFLA -&gt; cross-user data / privilege escalation | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite, mitmproxy, Frida | CWE-639; CWE-284; -&gt;[IDOR / BOLA checklist](#/checklist/idor); OWASP API Top 10 (BOLA/BFLA); OWASP MASVS/MASTG |
| MOB-002 | Hardcoded secrets / API keys in the mobile binary | Cryptography | Decompiled APK/IPA — strings, resources, config | 1. Pull the APK/IPA; decompile (jadx/apktool / class-dump)<br>2. Grep for API keys, secrets, endpoints, cloud creds, signing keys<br>3. Validate a found secret against the live API<br>4. Confirm it grants access / cost abuse | No secrets in the client; keys are per-user, scoped, server-brokered, revocable | <code>jadx -&gt; grep -Ri 'api_key&#124;secret&#124;AKIA&#124;bearer' ;  test key against API</code> | Hardcoded secret -&gt; direct API/cloud access or cost abuse | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | jadx, apktool, MobSF, trufflehog | CWE-798; -&gt;[JavaScript Files checklist](#/checklist/jsfiles); OWASP MASVS/MASTG (MASTG-CODE) |
| MOB-003 | Certificate pinning bypass (intercept the mobile API) | Cryptography | TLS pinning between app and backend | 1. Attempt intercept; if pinned, bypass with objection/Frida (unpinning) or patch the APK<br>2. Re-intercept the now-visible API traffic<br>3. Confirm you can read/modify requests<br>4. Proceed to server-side testing of the exposed API | Pinning present AND server-side controls independent of the client (pinning is not authz) | <code>objection -g &lt;app&gt; explore -&gt; android sslpinning disable ;  frida CodeShare unpin</code> | Pinning bypass -&gt; full visibility/tamper of mobile API -&gt; enables all backend testing | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | objection, Frida, apktool | CWE-295; OWASP MASVS/MASTG (MASTG-NETWORK); PortSwigger mobile |
| MOB-004 | Insecure local storage of tokens / PII on device | Cryptography | App sandbox — SharedPreferences/plist/SQLite/Keychain/Keystore | 1. Inspect app storage (adb / iMazing / objection) after login<br>2. Look for session tokens, passwords, PII stored in plaintext or world-readable<br>3. Check Keychain/Keystore usage vs plain files<br>4. Confirm a stolen-device or backup extraction yields creds | Secrets in Keychain/Keystore (hardware-backed); no plaintext tokens/PII at rest | <code>adb backup / objection -&gt; ls sandbox -&gt; read shared_prefs/&#42;.xml, &#42;.sqlite</code> | Insecure at-rest storage -&gt; token/PII theft on device access or backup -&gt; ATO | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | objection, adb, iMazing, MobSF | CWE-922; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP MASVS/MASTG (MASTG-STORAGE) |
| MOB-005 | Deep link / custom URL scheme hijacking | Broken Access Control | App-registered URL scheme / Android App Link / iOS Universal Link | 1. Enumerate registered schemes/links (manifest / Info.plist)<br>2. Check whether another app can claim the scheme (unverified) to intercept links<br>3. Send crafted deep links to trigger privileged in-app actions<br>4. Confirm hijack or unauthorized action | App Links/Universal Links verified (assetlinks/AASA); no sensitive action on unauthenticated deep link | <code>adb shell am start -a android.intent.action.VIEW -d 'app://transfer?to=attacker&amp;amt=1000'</code> | Deep-link hijack/injection -&gt; intercept tokens or trigger privileged actions | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | adb, Frida | CWE-939; CWE-926; OWASP MASVS/MASTG (MASTG-PLATFORM); Android App Links docs |
| MOB-006 | WebView JS-bridge / native-method exposure | Injection | WebView addJavascriptInterface / message handlers loading remote content | 1. Find WebViews that expose native methods to JS (addJavascriptInterface / WKScriptMessageHandler)<br>2. Check whether remote/untrusted content can call those bridges<br>3. Inject JS (via MITM/open redirect/loaded URL) to invoke native functions<br>4. Confirm file access / native action / RCE-ish reach | Bridges exposed only to trusted origins; no untrusted content in privileged WebViews; JS disabled where unneeded | <code>window.AndroidBridge.readFile('/data/data/app/...')  from injected JS</code> | WebView bridge abuse -&gt; native method invocation / local file theft | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Frida, Burp, jadx | CWE-749; CWE-79; -&gt;[XSS checklist](#/checklist/xss); OWASP MASVS/MASTG (MASTG-PLATFORM) |
| MOB-007 | Root / jailbreak detection bypass | Security Misconfiguration | Client-side root/JB detection guarding sensitive flows | 1. Trigger the root/JB check; note where it gates functionality<br>2. Bypass with objection/Frida (root-detection disable) or Magisk hide<br>3. Confirm the app runs its sensitive flow on a rooted device<br>4. Note that detection is defense-in-depth, not a server control | Detection is one signal only; server never relies on client integrity for security decisions | <code>objection -&gt; android root disable ;  frida root-detection-bypass</code> | Root/JB bypass -&gt; run instrumentation/tamper; server must not trust client integrity | Low | 3.1 (CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:L/I:N/A:N) | objection, Frida, Magisk | CWE-693; OWASP MASVS/MASTG (MASTG-RESILIENCE) |
| MOB-008 | Device binding / device-ID spoofing (session portability) | Broken Authentication | Device-bound session / trusted-device feature | 1. Capture the device-identifier used to bind a session/trusted-device<br>2. Replay a victim session or spoof the device-ID from another device<br>3. Check whether the server ties the token to a verifiable device factor<br>4. Confirm session portability / trusted-device bypass | Sessions bound to a verifiable, non-spoofable device factor; re-auth on new device | <code>replay session token + spoofed X-Device-Id on attacker device</code> | Device-binding bypass -&gt; port a victim session to attacker device -&gt; ATO | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp, Frida | CWE-287; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP MASVS/MASTG (MASTG-AUTH) |
| MOB-009 | Biometric auth bypass (client-side trust) | Broken Authentication | Biometric gate protecting login/transactions | 1. Check whether biometric success is decided client-side (a boolean) vs a server-verified crypto object<br>2. Hook the biometric callback (Frida) to force success<br>3. Bypass without the biometric; reach the protected flow<br>4. Confirm no server-side cryptographic binding | Biometric unlocks a Keystore/SE crypto operation the server verifies; not a client boolean | <code>frida hook onAuthenticationSucceeded -&gt; force callback</code> | Biometric bypass -&gt; reach protected transactions without the factor | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Frida, objection | CWE-287; OWASP MASVS/MASTG (MASTG-AUTH) |
| MOB-010 | Cleartext traffic / weak transport (ATS/cleartextTrafficPermitted) | Cryptography | Network config — HTTP endpoints, weak TLS, disabled ATS | 1. Inspect network_security_config / ATS settings and observed traffic<br>2. Find any HTTP (cleartext) endpoints or downgrade paths<br>3. MITM the cleartext channel on a hostile network<br>4. Confirm token/PII interception | All traffic HTTPS with modern TLS; cleartext disabled; no user-added-CA trust in prod | <code>grep cleartextTrafficPermitted=true ;  MITM the http:// endpoint</code> | Cleartext/weak transport -&gt; on-path token/PII interception -&gt; ATO | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | mitmproxy, Burp, MobSF | CWE-319; OWASP MASVS/MASTG (MASTG-NETWORK) |
| MOB-011 | Client-side control bypass (mobile-only validation) on the backend | Business Logic | Validation/limits enforced only in the app, not server-side | 1. Identify limits/validation the app enforces (price, quantity, role, feature flags)<br>2. Replay the API directly (proxy) without the client checks<br>3. Submit values the UI would block<br>4. Confirm the server accepts them | All validation/limits/authorization enforced server-side; client is convenience only | <code>POST /api/order {"price":0,"qty":-1}  directly, bypassing app validation</code> | Client-side-only controls -&gt; business-logic/price/role bypass via direct API | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp Suite | CWE-602; -&gt;[IDOR / BOLA checklist](#/checklist/idor); OWASP MASVS/MASTG (MASTG-CODE); OWASP WSTG-BUSL |
| MOB-012 | SMS/OTP 2FA interception &amp; mobile push abuse | Broken Authentication | SMS OTP / push-based MFA in mobile flows | 1. Test OTP delivery: SIM-swap exposure, OTP in notifications/logs, autofill leaks<br>2. Check OTP rate-limit / reuse / predictability<br>3. Abuse push MFA (fatigue) or device-token spoofing<br>4. Confirm 2FA bypass path | OTPs short-lived/single-use/rate-limited; push uses number-matching; no OTP in logs | <code>brute OTP (no limit)  &#124;  read OTP from notification  &#124;  MFA push fatigue</code> | Mobile 2FA interception/fatigue -&gt; MFA bypass -&gt; ATO | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Burp, Frida | CWE-287; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP MASVS/MASTG (MASTG-AUTH) |
| MOB-013 | Exported Android component abuse (activity/service/receiver) | Broken Access Control | Exported activities/services/broadcast receivers (AndroidManifest) | 1. Parse the manifest for exported=true components (or implicit intent-filters)<br>2. Invoke them from a malicious app / adb with crafted intents<br>3. Reach protected screens/actions or leak data without auth<br>4. Confirm unauthorized action | Components not exported unless required; exported ones enforce permissions + validate callers | <code>adb shell am start -n com.app/.AdminActivity --es token x   &#124;   am broadcast -a com.app.SECRET</code> | Exported-component abuse -&gt; unauthorized in-app action / data access | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | adb, Drozer, jadx | CWE-926; CWE-284; OWASP MASVS/MASTG (MASTG-PLATFORM) |
| MOB-014 | Intent injection / redirection | Injection | Components that forward/execute attacker-controlled intents | 1. Find a component that reads an Intent extra and re-dispatches it (startActivity/serialized intent)<br>2. Inject a nested/redirected intent to reach an unexported/privileged component<br>3. Confirm the redirect grants access it shouldn't<br>4. Prove privileged reach | Intents validated; no forwarding of untrusted intents to privileged components | <code>Intent extra 'forward_intent' -&gt; nested intent to com.app/.InternalActivity</code> | Intent injection -&gt; reach privileged/internal components (confused deputy) | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Drozer, jadx, Frida | CWE-926; OWASP MASVS/MASTG (MASTG-PLATFORM) |
| MOB-015 | Insecure content provider (SQLi / path traversal) | Injection | Exported ContentProvider query/openFile | 1. Enumerate exported content providers<br>2. Query with injection in the selection/URI; try ../ in openFile paths<br>3. Confirm SQLi in the provider or arbitrary file read via the provider<br>4. Extract data | Providers not exported unless needed; parameterized queries; no path traversal in openFile | <code>content://com.app.provider/users' OR '1'='1   &#124;   content://com.app.provider/../../databases/app.db</code> | Insecure provider -&gt; SQLi/arbitrary file read of app data | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | Drozer, adb | CWE-89; CWE-22; -&gt;[SQL Injection checklist](#/checklist/sqli); OWASP MASVS/MASTG (MASTG-PLATFORM) |
| MOB-016 | PendingIntent hijacking | Broken Access Control | Mutable/implicit PendingIntent handed to another app | 1. Find a mutable or implicit PendingIntent the app shares<br>2. Intercept and fill/redirect it to act with the app's identity/permissions<br>3. Confirm you can perform an action as the app<br>4. Prove privilege reuse | PendingIntents are immutable + explicit; least privilege | <code>capture mutable PendingIntent -&gt; fill with attacker action, executed with app identity</code> | PendingIntent hijack -&gt; perform actions with the app's identity/permissions | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Frida, jadx | CWE-926; OWASP MASVS/MASTG (MASTG-PLATFORM) |
| MOB-017 | Task hijacking (StrandHogg) / overlay | Broken Access Control | taskAffinity / recent-tasks + overlay | 1. Check taskAffinity/launchMode config enabling task-hijacking (StrandHogg 1/2)<br>2. Deploy a malicious app that inserts its activity into the target's task<br>3. Present a spoofed login/consent overlay<br>4. Capture credentials / approve actions | Harden taskAffinity/launchMode; detect overlays; FLAG_SECURE on sensitive screens | <code>malicious app with matching taskAffinity overlays a fake login on the target</code> | Task hijack/overlay -&gt; credential capture / action spoofing | High | 8.2 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:L/A:N) | apktool, custom PoC app | CWE-1021; OWASP MASVS/MASTG (MASTG-PLATFORM) |
| MOB-018 | Tapjacking (touch overlay) | Client-Side | Sensitive buttons without touch-filtering | 1. Check whether sensitive actions filter obscured touches (filterTouchesWhenObscured)<br>2. Overlay a transparent window over the target's confirm buttons<br>3. Trick the user into approving hidden actions<br>4. Confirm the action fires | Sensitive controls set filterTouchesWhenObscured / detect overlays | <code>SYSTEM_ALERT_WINDOW overlay over a 'Confirm transfer' button</code> | Tapjacking -&gt; user unknowingly authorizes sensitive actions | Medium | 6.1 (CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N) | custom overlay PoC | CWE-1021; OWASP MASVS/MASTG (MASTG-PLATFORM) |
| MOB-019 | Android backup / allowBackup data extraction | Cryptography | allowBackup=true / cloud backup of app data | 1. Check allowBackup in the manifest<br>2. adb backup the app; unpack the .ab<br>3. Look for tokens/PII/DB in the backup<br>4. Confirm at-rest data is extractable via backup | allowBackup=false for sensitive apps; no secrets in backed-up storage | <code>adb backup -f app.ab com.app ; abe unpack ; grep -Ri token</code> | Backup extraction -&gt; offline theft of tokens/PII -&gt; ATO | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | adb, android-backup-extractor | CWE-922; -&gt;[Account Takeover checklist](#/checklist/ato); OWASP MASVS/MASTG (MASTG-STORAGE) |
| MOB-020 | Clipboard / pasteboard sensitive-data leakage | Information Disclosure | Clipboard use for secrets (OTP/token/password) | 1. Perform flows that copy secrets to the clipboard (OTP autofill, copy token)<br>2. Read the clipboard from a background app<br>3. Confirm another app can harvest the secret<br>4. Note universal-pasteboard sync (iOS) | No secrets on the clipboard; sensitive fields flagged; auto-clear | <code>background app reads ClipboardManager after an OTP copy</code> | Clipboard leakage -&gt; secret theft by any installed app | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Frida, custom app | CWE-200; OWASP MASVS/MASTG (MASTG-STORAGE) |
| MOB-021 | Screenshot / screen-recording leak (missing FLAG_SECURE) | Information Disclosure | Sensitive screens captured in recents / screenshots | 1. Open a sensitive screen (payment/token/PII)<br>2. Trigger the app-switcher snapshot or a screenshot/recording<br>3. Confirm sensitive data is captured to recents/gallery<br>4. Note FLAG_SECURE / hidden-preview missing | FLAG_SECURE on sensitive screens; blank the app-switcher preview | <code>open card details -&gt; recents snapshot retains full PAN</code> | Screenshot/recents leak -&gt; sensitive data exposed on-device / in screenshots | Low | 3.1 (CVSS:3.1/AV:N/AC:H/PR:L/UI:N/S:U/C:L/I:N/A:N) | manual, adb | CWE-200; OWASP MASVS/MASTG (MASTG-STORAGE) |
| MOB-022 | Insecure logging (secrets in logcat / crash logs) | Information Disclosure | App/system logs &amp; crash reporting | 1. Exercise auth/payment flows while capturing logcat<br>2. Grep for tokens/passwords/PII/PAN in logs and crash reports<br>3. Confirm secrets are logged (readable pre-Android-M or via crash SDK)<br>4. Note third-party crash-SDK exfil | No secrets in logs; production logging minimized; crash reports scrubbed | <code>adb logcat &#124; grep -Ei 'token&#124;password&#124;authorization&#124;card'</code> | Sensitive logging -&gt; secret disclosure via logs / crash-reporting SDKs | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | adb, MobSF | CWE-532; OWASP MASVS/MASTG (MASTG-STORAGE) |
| MOB-023 | In-app purchase / receipt-validation bypass | Business Logic | Client-side purchase/receipt verification | 1. Complete or simulate a purchase; capture the receipt-validation call<br>2. Forge/replay a receipt, or unlock the entitlement client-side<br>3. Check whether the server validates the receipt with the store<br>4. Confirm free entitlement | Receipts validated server-to-store; entitlements server-granted; no client unlock | <code>replay/forge store receipt -&gt; POST /iap/validate {"receipt":"forged"} -&gt; premium unlocked</code> | IAP/receipt bypass -&gt; unlock paid features for free (financial) | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Burp, Frida | CWE-602; -&gt;[IDOR / BOLA checklist](#/checklist/idor); OWASP MASVS/MASTG (MASTG-CODE) |
| MOB-024 | Third-party SDK / analytics data leakage | Information Disclosure | Embedded SDKs (analytics/ads/attribution) | 1. Inventory embedded SDKs; observe their outbound traffic<br>2. Check what PII/identifiers/tokens they transmit and to where<br>3. Confirm over-collection or plaintext transmission of sensitive data<br>4. Assess third-party exposure | SDKs vetted, minimized, and sending no sensitive data; encrypted transport | <code>MITM SDK endpoints -&gt; observe device-id/email/token exfil</code> | SDK over-collection -&gt; user data leaked to third parties | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | MobSF, mitmproxy | CWE-359; OWASP MASVS/MASTG (MASTG-CODE) |
| MOB-025 | Debuggable build / exposed debug endpoints | Security Misconfiguration | android:debuggable / dev menus / debug APIs | 1. Check android:debuggable and for hidden dev/QA menus or debug backend endpoints<br>2. Attach a debugger / run-as to inspect memory &amp; data<br>3. Reach debug-only APIs that bypass controls<br>4. Confirm elevated access | Production builds non-debuggable; no dev menus / debug endpoints shipped | <code>adb shell run-as com.app ;  hit /api/debug/impersonate</code> | Debuggable build/debug endpoints -&gt; memory/data access &amp; control bypass | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | adb, jadx, Frida | CWE-489; OWASP MASVS/MASTG (MASTG-CODE) |
| MOB-026 | Firebase / cloud backend misconfiguration (open datastore) | Broken Access Control | Firebase/Firestore/S3/RTDB referenced by the app | 1. Extract backend URLs/keys from the app (Firebase config, bucket names)<br>2. Query the datastore unauthenticated (.json append, public bucket)<br>3. Confirm open read/write to other users' data<br>4. Quantify exposure | Backend rules enforce per-user auth; no public read/write; buckets locked down | <code>GET https://&lt;proj&gt;.firebaseio.com/users.json   &#124;   aws s3 ls s3://&lt;bucket&gt; --no-sign-request</code> | Open cloud backend -&gt; mass cross-user data read/write | Critical | 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) | Firebase tools, awscli, MobSF | CWE-284; -&gt;[IDOR / BOLA checklist](#/checklist/idor); OWASP MASVS/MASTG (MASTG-NETWORK) |
| MOB-027 | Runtime memory secret extraction | Cryptography | Process memory holding tokens/keys | 1. Attach Frida/dump process memory after login<br>2. Search memory for tokens/keys/PINs<br>3. Confirm secrets persist unprotected in memory<br>4. Note absence of zeroization | Secrets minimized in memory + zeroized after use; hardware-backed keys never exported | <code>frida-dump / objection memory search 'Bearer '</code> | Memory secret extraction -&gt; token/key theft on a compromised/rooted device | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | Frida, objection | CWE-316; OWASP MASVS/MASTG (MASTG-STORAGE) |
| MOB-028 | Anti-tamper / repackaging &amp; integrity-check bypass | Security Misconfiguration | App signature/integrity checks | 1. Decompile, patch (remove a check / change logic), repackage, re-sign<br>2. Check whether the app/server detects modification (signature/attestation)<br>3. Run the tampered app against production<br>4. Confirm no server-side attestation (Play Integrity/DeviceCheck) | Server verifies app integrity (Play Integrity/App Attest); tampered clients rejected | <code>apktool d -&gt; patch smali -&gt; apktool b -&gt; sign -&gt; run against prod</code> | Repackaging bypass -&gt; run a tampered client; server must attest integrity | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | apktool, jadx, Frida | CWE-693; OWASP MASVS/MASTG (MASTG-RESILIENCE) |
| MOB-029 | Offline / local DB tampering &amp; sync abuse | Business Logic | Local SQLite/Realm cache synced to server | 1. Modify the local DB/cache (balances, entitlements, flags) on a rooted device<br>2. Trigger sync<br>3. Check whether the server trusts client-side state on sync<br>4. Confirm tampered state persists server-side | Server is source of truth; sync validates + reconciles; no trust of client state | <code>sqlite3 app.db 'UPDATE wallet SET balance=999999' -&gt; sync</code> | Local-state trust on sync -&gt; tamper balances/entitlements server-side | High | 8.1 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) | adb, sqlite3, objection | CWE-602; -&gt;[IDOR / BOLA checklist](#/checklist/idor); OWASP MASVS/MASTG (MASTG-STORAGE) |
| MOB-030 | Geolocation spoofing for location-gated features | Business Logic | Client-provided location for geo-gated logic | 1. Identify features gated on device location (pricing, availability, compliance, check-in)<br>2. Spoof GPS or send a forged location to the API<br>3. Confirm the server trusts client location<br>4. Bypass the geo-restriction | Location verified server-side (IP/geo-IP corroboration); client GPS not solely trusted | <code>mock-location app / POST /api {"lat":..,"lng":..} forged</code> | Geo-spoofing -&gt; bypass location-based pricing/availability/compliance | Medium | 6.5 (CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N) | mock-location, Burp | CWE-602; OWASP MASVS/MASTG (MASTG-CODE) |
